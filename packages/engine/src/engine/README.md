# Data Engine package
Status: LOCKED (conceptual).
Purpose: Deterministic synthetic ledger.
Owns: CLI, registry shim, core, validation, scenario runner, layers
Boundaries: runtime services
When we unlock this: enforce L0/L1/L2/L3 split
