# Control Effectiveness Note

The preferred posture keeps the same governed fraud window and improves fraud-truth yield from `12.06%` to `12.37%`, a gain of `0.313 pp`.
It also reduces selected-flow burden from `4,018,508` to `3,507,008` and total case-event burden from `9,580,900` to `8,395,051`.

That means the preferred posture remains defendable as a bounded first-line control improvement rather than only a narrower rule.