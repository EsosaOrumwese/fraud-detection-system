# Workload Prioritisation Note

The preferred posture reduces selected flows from `4,018,508` to `3,507,008`, a reduction of `511,500` or `12.73%` versus baseline.
It also reduces total downstream case-event volume from `9,580,900` to `8,395,051`, a reduction of `1,185,849` or `12.38%`.

That means the preferred posture reads as a lighter operational workload, not only a tighter rule.