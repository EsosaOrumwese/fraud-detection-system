# Container images
Status: LOCKED (conceptual).
Purpose: Build/runtime images for components.
Owns: base, health/readiness, labels
Boundaries: no secrets; no app config
When we unlock this: pick minimal base; run non-root
