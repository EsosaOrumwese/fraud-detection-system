# dev_min Terraform roots

Canonical stack roots:
- `core/` (persistent)
- `confluent/` (managed messaging substrate)
- `demo/` (ephemeral)

These paths are pinned by the migration handles registry and M2 plan.
