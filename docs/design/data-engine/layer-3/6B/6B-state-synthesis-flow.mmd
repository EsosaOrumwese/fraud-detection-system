flowchart TD
  subgraph gate["S0 - sealed behavioural universe"]
    G0["Arrivals, entity graph, static posture, and behaviour configs are proven and frozen"]
    G1["6B may read only the inventory S0 legalises"]
  end

  subgraph scaffold["S1 + S2 - behavioural scaffolding"]
    S1["S1 says who is acting and which arrivals belong together"]
    S2["S2 turns that scaffolding into the legitimate business-flow baseline"]
  end

  subgraph corruption["S3 - fraud and abuse overlay"]
    F1["S3 adds campaign provenance and corrupts baseline behaviour without mutating the baseline itself"]
  end

  subgraph interpretation["S4 - truth and bank-view interpretation"]
    L1["S4 says what actually happened, what the bank saw, and how cases unfold"]
  end

  subgraph closure["S5 - publish legitimacy"]
    C1["S5 revalidates the whole behavioural chain under critical fail-closed rules"]
    C2["Only the 6B HashGate makes labelled behaviour safe to read downstream"]
  end

  G0 --> G1 --> S1 --> S2 --> F1 --> L1 --> C1 --> C2
