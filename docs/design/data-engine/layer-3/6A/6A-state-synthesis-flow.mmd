flowchart TD
  subgraph gate["S0 - sealed Layer-3 entity world"]
    G0["Upstream Layer-1 and Layer-2 worlds are proven and frozen"]
    G1["6A may read only the inventory S0 legalises"]
  end

  subgraph entity["S1 + S2 + S3 - entity and credential world"]
    E1["S1 fixes who exists and how parties are segmented"]
    E2["S2 fixes which accounts and products exist and who owns them"]
    E3["S3 fixes which payment credentials exist and how they attach to the account world"]
  end

  subgraph network["S4 - static graph realism"]
    N1["S4 fixes devices, IPs, and the static interaction graph over the closed entity world"]
  end

  subgraph closure["S5 - posture and publish legitimacy"]
    C1["S5 assigns static fraud posture without simulating behaviour"]
    C2["S5 publishes the 6A HashGate that 6B must trust"]
  end

  G0 --> G1 --> E1 --> E2 --> E3 --> N1 --> C1 --> C2
