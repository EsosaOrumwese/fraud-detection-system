$schema: "https://json-schema.org/draft/2020-12/schema"
$comment: |
  Template schema pack for Layer-2 Segment 5A.
  Keep the structure aligned with Layer-1 packs: define one object per dataset under $defs.
$defs:
  placeholder_dataset:
    type: array
    description: "Placeholder dataset schema for Segment 5A."
    items:
      type: object
      additionalProperties: false
      required: [placeholder_field]
      properties:
        placeholder_field:
          type: string
          description: "Replace with real fields when Segment 5A is implemented."
