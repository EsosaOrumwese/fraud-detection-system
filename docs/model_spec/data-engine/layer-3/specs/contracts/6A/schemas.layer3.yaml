$schema: "https://json-schema.org/draft/2020-12/schema"
$comment: |
  Shared Layer-3 schema pack template. Add dataset definitions under $defs when Layer-3 segments are confirmed.
$defs:
  placeholder_shared_dataset:
    type: object
    additionalProperties: false
    required: [placeholder_field]
    properties:
      placeholder_field:
        type: string
        description: "Replace with Layer-3 shared fields when available."
