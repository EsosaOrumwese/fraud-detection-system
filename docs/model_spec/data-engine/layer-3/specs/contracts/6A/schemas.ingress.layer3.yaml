$schema: "https://json-schema.org/draft/2020-12/schema"
$comment: |
  Layer-3 ingress schema template. Mirror Layer-1 ingress packs when defining actual payloads.
$defs:
  placeholder_ingress_record:
    type: object
    additionalProperties: false
    required: [placeholder_field]
    properties:
      placeholder_field:
        type: string
        description: "Replace with real ingress fields for Layer-3."
