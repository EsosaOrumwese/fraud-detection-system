# Archive Contracts (v0)

- `archive_event_record_v0.schema.yaml`

Defines immutable archive record payload written by the archive-writer corridor, including origin-offset provenance and canonical run pins.
