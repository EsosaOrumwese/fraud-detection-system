# Execution Plan - Health Data Pipelines And Multimodal Integration Slice

As of `2026-04-14`

Purpose:
- turn the chosen `Imperial College London` `job 16 A` slice into a concrete bounded execution order tied to existing governed multi-source platform outputs and repeatable build patterns already present in the repo
- keep the work pipeline-first, linkage-first, and multimodal-integration-first rather than drifting into generic research-assistant language, fake cloud-estate ownership, or premature `AI`-support rhetoric
- prove one bounded pipeline-ready base, one cleaning-and-linkage surface, one multimodal-style integration output, and one validity-and-reliability note from the same controlled multi-source lane
- keep execution memory-safe by reusing compact governed artefacts before considering any wider raw query footprint

Execution substrate:
- governed local artefacts already available in `artefacts/analytics_slices/`
- compact multi-source integration and fit-for-purpose outputs already proven in earlier NHS and scientist slices
- compact repeatable build and release-safe patterns already proven across the platform
- compact new outputs to be written under `artefacts/analytics_slices/data_scientist/imperial_college_london/01_health_data_pipelines_and_multimodal_integration/`

Primary rule:
- confirm the inherited multi-source and repeatable-build patterns first
- identify the smallest truthful pipeline-and-linkage question second
- define one pipeline-ready base and one cleaning-and-linkage surface third
- materialise one multimodal-style integration output fourth
- write the validity-and-reliability and downstream-usability notes only after the pipeline and integration surfaces are explicit
- package the release checks and caveats only after the analytical surfaces are explicit
- never reopen broad raw detailed surfaces if the pipeline question can be answered from compact governed outputs
- if a figure is used later, it must materially clarify the source-to-linkage-to-integration story rather than duplicate the analytical surfaces

---

## 1. Confirmed Working Assumptions

This plan is built around the following assumptions that must be tested immediately:
- the repo already contains enough compact governed multi-source evidence to support one bounded pipeline-and-integration analogue
- the first pass can stay on source-role framing, cleaning, linkage, and integrated readiness without needing stronger cloud or model-development language
- one explicit pipeline-and-linkage question can be fixed clearly enough to support a real multimodal-style integration reading
- one compact validity-and-reliability layer can be stated once the pipeline and integration surfaces are explicit
- the strongest truthful end state is likely to be research-ready multimodal preparation rather than a stronger platform-ownership claim

Candidate first-pass reusable foundations:
- Guy's `job 14 C` multi-source research-performance trend pack
- South Tyneside `F` information-systems development and optimisation pack
- `JPMorganChase C` implementation-ready and handoff pack
- other compact governed audit, release-safe, and source-role outputs where honest

Working assumption about the pipeline question:
- the slice should stay on one governed multi-source lane
- the lane should support one clear cleaning, linkage, and integration story honestly
- the resulting outputs should help a research team understand how heterogeneous source families can be prepared for later longitudinal or model-support work

Important warning:
- these assumptions must be tested rather than carried forward casually
- if the inherited lane does not support a clean pipeline-and-integration pack, narrow the claim to linkage and research-readiness support rather than inventing a stronger platform or cloud story

## 2. Pipeline-And-Linkage-First Posture

This slice must not begin as another generic reporting pack and it must not become a fake digital-health platform note.

The correct posture is:
- confirm the inherited multi-source and repeatable-build packs first
- define one bounded pipeline-and-linkage question second
- materialise one pipeline-ready base third
- materialise one cleaning-and-linkage surface fourth
- materialise one multimodal-style integration output fifth
- write one validity-and-reliability and downstream-usability note sixth
- package the release checks and caveats seventh
- use Python only after the inherited compact outputs have already bounded the pipeline question

This matters because the `Imperial` responsibility is about enabling multimodal health-data use, not just saying the right research or cloud words.

## 2A. Memory And Query Discipline

This slice must be executed with explicit memory discipline.

The correct posture is:
- do not pull broad raw surfaces into pandas
- do not reopen wide raw profiling if the pipeline question can be answered from existing governed packs
- do not assume multimodal language justifies a broader platform or cloud reconstruction

The correct query discipline is:
- start with compact inherited fact packs, integrated outputs, delivery-safe outputs, and release checks
- use inherited compact outputs first
- if a probe is needed, filter the period and fields at scan time
- materialise compact pipeline, linkage, and integration outputs only after the analytical question is fixed
- let Python read only those compact outputs, not broad raw source families

The default execution ceiling for this slice is:
- inherited multi-source and delivery-safe packs first
- one compact pipeline-ready base second
- one compact cleaning-and-linkage output third
- one compact multimodal-style integration output fourth
- one compact validity-and-reliability note fifth
- one release-check surface sixth

If a query starts behaving like a whole translational platform rebuild rather than a bounded research-ready proof, stop and narrow it before proceeding.

## 3. First-Pass Objective

The first-pass objective is:

`build one health-data-pipeline and multimodal-integration pack with one pipeline-ready base, one cleaning-and-linkage output, one multimodal-style integration output, and one validity-and-reliability note`

The proof object is:
- `health_data_pipelines_and_multimodal_integration_v1`

The first-pass output family is:
- one pipeline-ready base
- one cleaning-and-linkage output
- one multimodal-style integration output
- one validity-and-reliability note
- one release-check output
- one scope note
- one caveats note

## 4. Pipeline Gate

Before building any `job 16 A` output, run a bounded pipeline gate.

The first profiling pass must answer:
- do the inherited multi-source lanes already contain a clear controlled grain that can carry pipeline-and-linkage support honestly?
- can the same controlled base support both linkage explanation and multimodal-style integration interpretation?
- can one compact support surface distinguish pipeline work from the later longitudinal-analysis and cloud slices?
- does the resulting pack remain a pipeline-and-integration proof rather than a platform-ownership story?
- does the resulting pack stay compact enough to act as a bounded proof object?

Required checks:
- continuity of the inherited source-role grain
- continuity of the release-safe and repeatable-build posture
- continuity of the fit-for-use and bounded downstream-usability logic
- ability to state one bounded validity-and-reliability consequence from the same pack

Those checks should be implemented as:
- aggregate-only or compact-output probes
- inherited compact outputs first
- no broad row materialisation into memory

Decision rule:
- if the pipeline lane remains explicit and compact, proceed into pipeline, linkage, and integration materialisation
- if the question is too broad or ambiguous, narrow the slice before proceeding

## 5. Candidate Analytical Base

The first-pass analytical base should stay bounded and explicit.

Each component should have one stated purpose:
- inherited multi-source integration pack
  - define the controlled heterogeneous-source context
- inherited repeatable-build and handoff packs
  - define the delivery-safe and rerunnable posture
- pipeline-ready base layer
  - define the integrated grain and source-role frame
- cleaning-and-linkage layer
  - define how the sources are brought together
- multimodal-style integration layer
  - define why the integrated base reads like heterogeneous health-style preparation rather than a generic merge
- validity-and-reliability layer
  - define why the integrated base is fit for later research-style use

If inherited outputs do not support one of those components cleanly, adapt by narrowing the claim rather than stretching earlier slices past their truth boundary.

## 6. Planned Transformation Chain

The transformation chain should remain explicit and staged:

1. inherited multi-source and delivery-safe confirmation plus pipeline profiling
2. pipeline-ready base materialisation
3. cleaning-and-linkage output materialisation
4. multimodal-style integration output materialisation
5. validity-and-reliability and downstream-usability note
6. release-check and caveat packaging

The transformations should not be hidden inside vague pipeline language.

## 7. Bounded Build Order

### Step 1. Confirm inherited multi-source and repeatable-build packs

Build small profile checks only for:
- the inherited multi-source integration outputs
- the inherited fit-for-purpose and release-safe outputs
- compact delivery and handoff notes
- relevant release checks from the inherited packs

Goal:
- define the smallest truthful pipeline-and-linkage question
- prove that the same governed lane can support a bounded pipeline-support slice without reopening broad scope

### Step 2. Define the pipeline-ready base

Create one bounded output, for example:
- `pipeline_ready_base_v1`

This output should:
- state the integrated grain being handled
- state the controlled source-role context
- remain compact enough to act as the centre of the pipeline proof

### Step 3. Define the cleaning-and-linkage output

Create one bounded output, for example:
- `cleaning_linkage_output_v1`

This output should:
- summarise the cleaning and linkage stages explicitly
- show how the pack remains tied to the same governed analytical lane
- remain clearly below platform or cloud-estate ownership

### Step 4. Define the multimodal-style integration output

Create one bounded output, for example:
- `multimodal_integration_output_v1`

This output should:
- state the heterogeneous-source integration reading explicitly
- show what source-role diversity the integrated base preserves
- remain clearly tied to the inherited governed multi-source logic

### Step 5. Write the validity-and-reliability note

Write one bounded note, for example:
- `validity_reliability_note_v1.md`

This note should:
- explain what the pipeline, linkage, and integration readings imply for research-style downstream use
- connect the usability story back to the same governed lane
- stay below platform, cloud, or whole-programme rhetoric

### Step 6. Package the release checks and caveats

Create one bounded output, for example:
- `health_data_pipeline_release_checks_v1`

Write:
- the health-data-pipeline scope note
- the pipeline-ready base note
- the cleaning-and-linkage note
- the multimodal-integration note
- the validity-and-reliability note
- the caveats note
- the regeneration README

Only after the analytical surfaces are real.

## 8. Pipeline And Integration Boundary Strategy

The pipeline and multimodal-integration story has to stay honest.

The first-pass strategy should answer:
- what exactly is becoming more pipeline-shaped than the already completed slices?
- how does the cleaning-and-linkage surface reveal engineering and preparation work rather than generic analysis?
- how does the multimodal-style integration output connect back to the same governed multi-source lane?
- why does the slice stop at bounded research-ready preparation rather than stronger cloud or platform ownership?

Allowed posture:
- one bounded pipeline-ready base
- one compact cleaning-and-linkage output
- one compact multimodal-style integration output
- one compact validity-and-reliability note
- one repeatable release-check pack

Disallowed posture:
- claiming live `Google Cloud` estate ownership
- asserting whole-platform or translational-programme ownership
- importing generic data-engineering language as fake proof
