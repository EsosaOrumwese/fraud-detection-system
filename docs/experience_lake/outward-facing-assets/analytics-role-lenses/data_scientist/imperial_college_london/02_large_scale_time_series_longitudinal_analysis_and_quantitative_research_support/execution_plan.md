# Execution Plan - Large Scale Time Series Longitudinal Analysis And Quantitative Research Support Slice

As of `2026-04-14`

Purpose:
- turn the chosen `Imperial College London` `job 16 B + C` slice into a concrete bounded execution order tied to the completed `job 16 A` pack and other compact governed time-aware analytical artefacts already present in the repo
- keep the work time-series-first, longitudinal-first, and quantitative-support-first rather than drifting into generic research reporting, fake programme ownership, or premature `AI`-support rhetoric
- prove one bounded time-aware analytical base, one longitudinal-style surface, one quantitative research-support output, and one fit-for-use implication note from the same controlled integrated lane
- keep execution memory-safe by reusing compact governed artefacts before considering any wider raw query footprint

Execution substrate:
- the completed `job 16 A` health-data-pipeline and multimodal-integration pack
- governed local artefacts already available in `artefacts/analytics_slices/`
- compact multi-source trend, fit-for-purpose, and evaluation outputs already proven in earlier slices
- compact new outputs to be written under `artefacts/analytics_slices/data_scientist/imperial_college_london/02_large_scale_time_series_longitudinal_analysis_and_quantitative_research_support/`

Primary rule:
- confirm the completed `job 16 A` pack and inherited time-aware analytical patterns first
- identify the smallest truthful longitudinal and repeated-state question second
- define one time-aware analytical base and one longitudinal-style surface third
- materialise one quantitative research-support output fourth
- write the bounded implication, fit-for-use, and release-safe longitudinal notes only after the analytical surfaces are explicit
- package the release checks and caveats only after the analytical surfaces are explicit
- never reopen broad raw detailed surfaces if the longitudinal question can be answered from compact governed outputs
- if a figure is used later, it must materially clarify the trajectory-to-implication story rather than duplicate the analytical surfaces

---

## 1. Confirmed Working Assumptions

This plan is built around the following assumptions that must be tested immediately:
- the completed `job 16 A` pack already contains enough compact governed evidence to support one bounded longitudinal and quantitative-support analogue
- the first pass can stay on repeated-state, trajectory, and quantitative interpretation without needing stronger cloud or model-development language
- one explicit time-aware question can be fixed clearly enough to support a real longitudinal reading
- one compact fit-for-use and bounded-implication layer can be stated once the longitudinal and quantitative surfaces are explicit
- the strongest truthful end state is likely to be research-ready longitudinal support rather than a stronger programme or modelling claim

Candidate first-pass reusable foundations:
- the completed `job 16 A` pipeline-and-integration pack
- Guy's `job 14 C` multi-source trend-and-implication pack
- Cambridge `01` mixed-method and evaluation pack
- other compact governed release-safe and fit-for-purpose outputs where honest

Working assumption about the longitudinal question:
- the slice should stay on one governed integrated lane
- the lane should support one clear time-aware and repeated-state story honestly
- the resulting outputs should help a research team understand how the integrated base can support trajectory-style and quantitative interpretation over time

Important warning:
- these assumptions must be tested rather than carried forward casually
- if the inherited lane does not support a clean longitudinal pack, narrow the claim to time-aware and fit-for-use support rather than inventing a stronger programme or model story

## 2. Time-Series-And-Longitudinal-First Posture

This slice must not begin as another generic multi-source report and it must not become a fake translational programme note.

The correct posture is:
- confirm the completed `job 16 A` pack and inherited time-aware analytical packs first
- define one bounded time-aware and repeated-state question second
- materialise one time-aware analytical base third
- materialise one longitudinal-style surface fourth
- materialise one quantitative research-support output fifth
- write one bounded implication, fit-for-use, and release-safe longitudinal note sixth
- package the release checks and caveats seventh
- use Python only after the inherited compact outputs have already bounded the longitudinal question

This matters because the `Imperial` responsibility is about using the integrated base analytically over time, not just restating that the sources were linked.

## 2A. Memory And Query Discipline

This slice must be executed with explicit memory discipline.

The correct posture is:
- do not pull broad raw surfaces into pandas
- do not reopen wide raw profiling if the longitudinal question can be answered from existing governed packs
- do not assume time-series language justifies a broader research-programme reconstruction

The correct query discipline is:
- start with compact inherited fact packs, integrated outputs, trend-safe outputs, evaluation outputs, and release checks
- use inherited compact outputs first
- if a probe is needed, filter the period and fields at scan time
- materialise compact time-aware, longitudinal, and quantitative outputs only after the analytical question is fixed
- let Python read only those compact outputs, not broad raw source families

The default execution ceiling for this slice is:
- completed `job 16 A` and inherited analytical packs first
- one compact time-aware base second
- one compact longitudinal-style output third
- one compact quantitative-support output fourth
- one compact fit-for-use implication note fifth
- one release-check surface sixth

If a query starts behaving like a whole translational research rebuild rather than a bounded longitudinal proof, stop and narrow it before proceeding.

## 3. First-Pass Objective

The first-pass objective is:

`build one large-scale time-series longitudinal-analysis and quantitative-support pack with one time-aware analytical base, one longitudinal-style output, one quantitative-support output, and one fit-for-use implication note`

The proof object is:
- `large_scale_time_series_longitudinal_analysis_and_quantitative_support_v1`

The first-pass output family is:
- one time-aware analytical base
- one longitudinal-style output
- one quantitative research-support output
- one fit-for-use implication note
- one release-check output
- one scope note
- one caveats note

## 4. Longitudinal Gate

Before building any `job 16 B + C` output, run a bounded longitudinal gate.

The first profiling pass must answer:
- does the completed `job 16 A` lane already contain a clear controlled grain that can carry time-aware support honestly?
- can the same controlled base support both trajectory-style interpretation and bounded quantitative research support?
- can one compact support surface distinguish longitudinal work from the earlier pipeline-and-linkage slice?
- does the resulting pack remain a longitudinal and quantitative-support proof rather than a programme or modelling story?
- does the resulting pack stay compact enough to act as a bounded proof object?

Required checks:
- continuity of the inherited integrated grain
- continuity of the release-safe and validity posture
- continuity of the fit-for-use and bounded downstream-support logic
- ability to state one bounded implication consequence from the same pack

Those checks should be implemented as:
- aggregate-only or compact-output probes
- inherited compact outputs first
- no broad row materialisation into memory

Decision rule:
- if the longitudinal lane remains explicit and compact, proceed into time-aware, longitudinal, and quantitative-support materialisation
- if the question is too broad or ambiguous, narrow the slice before proceeding

## 5. Candidate Analytical Base

The first-pass analytical base should stay bounded and explicit.

Each component should have one stated purpose:
- completed `job 16 A` integration pack
  - define the controlled integrated source context
- inherited trend and fit-for-purpose packs
  - define the time-aware and implication-safe posture
- time-aware analytical base layer
  - define the repeated-state and temporal frame
- longitudinal-style layer
  - define how change, persistence, or repeated state is read over time
- quantitative-support layer
  - define what measurable research-style evidence the same lane now supports
- fit-for-use implication layer
  - define why the bounded longitudinal reading is usable for downstream research support

If inherited outputs do not support one of those components cleanly, adapt by narrowing the claim rather than stretching earlier slices past their truth boundary.

## 6. Planned Transformation Chain

The transformation chain should remain explicit and staged:

1. completed `job 16 A` and inherited time-aware analytical confirmation plus longitudinal profiling
2. time-aware analytical base materialisation
3. longitudinal-style output materialisation
4. quantitative research-support output materialisation
5. bounded implication, fit-for-use, and release-safe longitudinal note
6. release-check and caveat packaging

The transformations should not be hidden inside vague longitudinal language.

## 7. Bounded Build Order

### Step 1. Confirm the completed `job 16 A` pack and inherited time-aware analytical packs

Build small profile checks only for:
- the completed pipeline-ready base and multimodal-integration outputs
- inherited trend-and-implication outputs
- inherited fit-for-purpose and evaluation notes
- relevant release checks from the inherited packs

Goal:
- define the smallest truthful time-aware and repeated-state question
- prove that the same governed lane can support a bounded longitudinal slice without reopening broad scope

### Step 2. Define the time-aware analytical base

Create one bounded output, for example:
- `time_aware_analytical_base_v1`

This output should:
- state the integrated and repeated-state grain being handled
- state the controlled source-role context
- remain compact enough to act as the centre of the longitudinal proof

### Step 3. Define the longitudinal-style output

Create one bounded output, for example:
- `longitudinal_surface_output_v1`

This output should:
- summarise the trajectory or repeated-state stages explicitly
- show how the pack remains tied to the same governed analytical lane
- remain clearly below programme or modelling ownership

### Step 4. Define the quantitative research-support output

Create one bounded output, for example:
- `quantitative_research_support_output_v1`

This output should:
- state the quantitative-support reading explicitly
- show what bounded research-style evidence the lane now provides
- remain clearly tied to the inherited governed multi-source logic

### Step 5. Write the fit-for-use implication note

Write one bounded note, for example:
- `fit_for_use_implication_note_v1.md`

This note should:
- explain what the time-aware, longitudinal, and quantitative readings imply for bounded translational research support
- connect the usability story back to the same governed lane
- stay below programme, cloud, or model-development rhetoric

### Step 6. Package the release checks and caveats

Create one bounded output, for example:
- `longitudinal_analysis_release_checks_v1`

Write:
- the longitudinal-analysis scope note
- the time-aware base note
- the longitudinal-surface note
- the quantitative research-support note
- the fit-for-use implication note
- the caveats note
- the regeneration README

Only after the analytical surfaces are real.

## 8. Longitudinal And Quantitative Boundary Strategy

The longitudinal and quantitative-support story has to stay honest.

The first-pass strategy should answer:
- what exactly is becoming more longitudinal-shaped than the completed `job 16 A` slice?
- how does the longitudinal surface reveal repeated-state and trajectory reading rather than generic trend reporting?
- how does the quantitative-support output connect back to the same governed integrated lane?
- why does the slice stop at bounded research-ready longitudinal support rather than stronger programme or `AI` ownership?

Allowed posture:
- one bounded time-aware analytical base
- one compact longitudinal-style output
- one compact quantitative research-support output
- one compact fit-for-use implication note
- one repeatable release-check pack

Disallowed posture:
- claiming live longitudinal programme ownership
- asserting full multimodal modelling-estate ownership
- importing generic `AI` or translational-programme language as fake proof
