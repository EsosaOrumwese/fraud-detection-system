# Execution Plan - Research System User Support, Training, And Documentation Slice

As of `2026-04-14`

Purpose:
- turn the chosen Guy's and St Thomas' `job 14 E` slice into a concrete execution order tied to the governed research-performance, quality-and-audit, and integrated-trend packs already available from `job 14 A + B`, `D`, and `C`
- keep the work advisory-first, training-first, and documentation-first rather than drifting into vague stakeholder-support rhetoric, fake helpdesk ownership, or synthetic enterprise training authority
- prove one bounded frontline-advisory output, one user-support summary, one user-training output, one procedural-documentation output, and one data-consistency support note from the same controlled research-performance lane
- keep execution memory-safe by reusing compact `job 14` outputs before considering any wider raw query footprint

Execution substrate:
- governed local artefacts already available in `artefacts/analytics_slices/`
- the completed `job 14 A + B` pack as the primary research-system and reporting base
- the completed `job 14 D` pack as the quality, audit, and data-consistency control layer
- the completed `job 14 C` pack as the fit-for-purpose and bounded-implication context layer
- compact new outputs to be written under `artefacts/analytics_slices/data_analyst/guys_and_st_thomas_nhs_foundation_trust_rd_data_analyst/04_research_system_user_support_training_and_documentation/`

Primary rule:
- confirm the inherited reporting, audit, and trend packs first
- identify the smallest truthful user-support and enablement question second
- define one frontline-advisory posture and one user-support summary third
- materialise one user-training output and one procedural-documentation output fourth
- write the data-consistency support note and enablement-value note only after the support and training surfaces are explicit
- package the release checks and caveats only after the support, training, and documentation outputs are explicit
- never reopen broad raw detailed surfaces if the support-and-documentation question can be answered from compact governed outputs
- if a figure is used later, it must materially clarify the support or training story rather than duplicate the enablement outputs

---

## 1. Confirmed Working Assumptions

This plan is built around the following assumptions that must be tested immediately:
- the completed `job 14 A + B`, `D`, and `C` packs already contain enough compact evidence to support one bounded user-support and enablement analogue
- the first pass can stay on frontline advisory help, training, and documentation without needing any broader service-function framing
- one explicit support question can be fixed clearly enough to support an advisory-contact claim
- one compact training and documentation layer can be stated once the advisory surface is explicit
- the strongest truthful end state is likely to be user enablement and data-consistency guidance rather than a stronger helpdesk or enterprise training claim

Candidate first-pass reusable foundations:
- `job 14 A + B` research-performance pack
- `job 14 D` quality-and-audit pack
- `job 14 C` integrated trend-and-fit-for-purpose pack

Working assumption about the support question:
- the slice should stay on the same governed research-reporting lane
- the lane should support one clear user-support story around correct system use, data consistency, and procedural guidance
- the resulting outputs should help a research-system user understand what to do, how to use the outputs correctly, and where consistency checks matter

Important warning:
- these assumptions must be tested rather than carried forward casually
- if the inherited lane does not support a clean support-and-enablement pack, narrow the claim to advisory support and documentation rather than inventing a stronger training-function story

## 2. Advisory-And-Training-First Posture

This slice must not begin as another reporting pack and it must not become a fake helpdesk or training-function note.

The correct posture is:
- confirm the inherited reporting, audit, and trend packs first
- define one bounded user-support question second
- materialise one frontline-advisory output third
- materialise one user-support summary fourth
- materialise one user-training output fifth
- materialise one procedural-documentation output sixth
- write one data-consistency support note seventh
- write one enablement-value note eighth
- package the release checks and caveats ninth
- use Python only after the inherited compact outputs have already bounded the support-and-enablement question

This matters because the Guy's and St Thomas' responsibility is about advising users, training them, maintaining guidance, and helping them handle data consistently. The execution should therefore read like one controlled enablement pack rather than broad support-function rhetoric.

## 2A. Memory And Query Discipline

This slice must be executed with explicit memory discipline.

The correct posture is:
- do not pull broad raw surfaces into pandas
- do not reopen wide raw profiling if the support-and-documentation question can be answered from the existing research-reporting, audit, and trend packs
- do not assume training language justifies a broader systems-support reconstruction

The correct query discipline is:
- start with the `job 14 A + B`, `D`, and `C` fact packs, prepared reporting base, mandatory summary, dashboard output, audit output, integrated trend output, implication output, fit-for-purpose note, and release checks
- use inherited compact outputs first
- if a probe is needed, filter the period and fields at scan time
- materialise compact advisory, training, and documentation outputs only after the support question is fixed
- let Python read only those compact outputs, not broad raw source families

The default execution ceiling for this slice is:
- inherited reporting, audit, and trend packs first
- one compact advisory output second
- one compact user-support summary third
- one compact user-training output fourth
- one compact procedural-documentation output fifth
- one compact data-consistency support note sixth
- one release-check surface seventh

If a query starts behaving like a Trust-wide research-systems service rebuild rather than a bounded user-support proof, stop and narrow it before proceeding.

## 3. First-Pass Objective

The first-pass objective is:

`build one research-system user-support and enablement pack with one frontline-advisory output, one user-support summary, one user-training output, one procedural-documentation output, and one data-consistency support note`

The proof object is:
- `research_system_user_support_training_and_documentation_v1`

The first-pass output family is:
- one advisory-support output
- one user-support summary
- one user-training output
- one procedural-documentation output
- one data-consistency support note
- one release-check output
- one enablement-value note
- one scope note
- one caveats note

## 4. User-Support Gate

Before building any `job 14 E` output, run a bounded user-support gate.

The first profiling pass must answer:
- does the inherited research-reporting lane already contain a clear support-and-guidance story?
- can the same controlled base support both user advice and training/documentation interpretation?
- can one compact support surface distinguish frontline guidance from broader service-function ownership?
- does the resulting pack remain a user-enablement proof rather than a helpdesk-function story?
- does the resulting pack stay compact enough to act as a bounded proof object?

Required checks:
- continuity of the inherited reporting grain
- continuity of the shared reporting cohort
- continuity of the inherited audit, trend, and fit-for-purpose posture
- ability to state one bounded data-consistency consequence from the same pack

Those checks should be implemented as:
- aggregate-only or compact-output probes
- inherited compact outputs first
- no broad row materialisation into memory

Decision rule:
- if the research-reporting lane remains explicit and compact, proceed into support, training, and documentation materialisation
- if the question is too broad or ambiguous, narrow the slice before proceeding

## 5. Candidate Analytical Base

The first-pass analytical base should stay bounded and explicit.

Each component should have one stated purpose:
- inherited prepared research-performance base
  - define the system-and-reporting context users need help with
- inherited mandatory-return-style summary
  - define the accountable reporting posture users must understand
- inherited audit-and-consistency output
  - define the data-consistency and release-safe guidance conditions
- inherited integrated trend and fit-for-purpose outputs
  - define what guidance should still remain attached to use decisions
- advisory-support layer
  - define the frontline issue-handling posture
- training layer
  - define how users should be taught to use the lane correctly
- documentation layer
  - define the repeatable procedural steps and maintained guidance

If inherited outputs do not support one of those components cleanly, adapt by narrowing the claim rather than stretching earlier slices past their truth boundary.

## 6. Planned Transformation Chain

The transformation chain should remain explicit and staged:

1. inherited reporting, audit, and trend-pack confirmation and support profiling
2. advisory-support output materialisation
3. user-support summary materialisation
4. user-training output materialisation
5. procedural-documentation output materialisation
6. data-consistency support note
7. enablement-value note
8. release-check and caveat packaging

The transformations should not be hidden inside vague user-support language.

## 7. Bounded Build Order

### Step 1. Confirm the inherited reporting, audit, and trend packs

Build small profile checks only for:
- the prepared research-performance base
- the mandatory-return-style summary
- the dashboard output
- the research-data quality summary
- the audit-and-consistency output
- the integrated trend output
- the fit-for-purpose note
- the release checks

Goal:
- define the smallest truthful support-and-enablement question
- prove that the same research-reporting lane can support a bounded advisory-and-training slice without reopening broad scope

### Step 2. Define the advisory-support output

Create one bounded output, for example:
- `advisory_support_output_v1`

This output should:
- state the user issue or advisory need being handled
- state the correct controlled guidance
- remain compact enough to act as the centre of the support proof

### Step 3. Define the user-support summary

Create one bounded output, for example:
- `user_support_summary_v1`

This output should:
- summarise the support themes that matter
- show how support remains tied to the same governed research lane
- remain clearly below helpdesk or service-function ownership

### Step 4. Define the user-training output

Create one bounded output, for example:
- `user_training_output_v1`

This output should:
- state the explicit training points
- show what users need to understand to use the lane correctly
- remain clearly tied to the inherited research-performance and consistency logic

### Step 5. Define the procedural-documentation output

Create one bounded output, for example:
- `procedural_documentation_output_v1`

This output should:
- state the operating steps or guidance rules explicitly
- support repeatable and controlled use
- remain proportional to the evidence

### Step 6. Write the data-consistency support note

Write one bounded note, for example:
- `data_consistency_support_note_v1.md`

This note should:
- explain how users should handle data-consistency and analytical-use issues
- connect the support story back to the same audit and fit-for-purpose lane
- stay below governance-office or service-desk rhetoric

### Step 7. Package the enablement-value note, release checks, and caveats

Create one bounded output, for example:
- `research_system_user_support_release_checks_v1`

Write:
- the user-support scope note
- the advisory-support note
- the user-training note
- the procedural-documentation note
- the data-consistency support note
- the enablement-value note
- the caveats note
- the regeneration README

Only after the support, training, and documentation surfaces are real.

## 8. Support-And-Training Boundary Strategy

The support story has to stay honest.

The first-pass support strategy should answer:
- what exactly is being supported?
- how does the support surface reveal frontline advisory posture rather than broader service ownership?
- how do the training and documentation outputs connect back to the same governed research-reporting lane?
- why does the slice stop at bounded user enablement rather than stronger helpdesk or enterprise training ownership?

Allowed posture:
- one bounded advisory-support story
- one compact user-support summary
- one compact training surface
- one compact documentation surface
- one data-consistency support note
- one repeatable release-check pack

Disallowed posture:
- claiming a Trust-wide research-systems helpdesk
- asserting an enterprise training function or learning-management estate
- importing generic stakeholder-support language as fake proof
