# Execution Report - Research Management System Support And Research Performance Reporting Slice

As of `2026-04-14`

Purpose:
- record what was actually executed for the Guy's and St Thomas' `R&D Data Analyst` slice around specialist research-management-system support, research-performance reporting, mandatory-return-style delivery, and dashboard-backed `R&D` visibility
- preserve the truth boundary between one bounded research-performance reporting pack and any wider claim about live `EDGE` ownership, `CPMS` integration ownership, direct national-submission authority, or whole-Trust `R&D` data-estate control
- package the saved facts, prepared research-performance base, mandatory-return-style summary, dashboard-style output, and claim-ready evidence into one outward-facing report

Truth boundary:
- this execution was completed by building directly on compact inherited reporting-product, framework, and control outputs already available in the repo
- the slice did not reopen a broad raw research-detail analytical base
- the slice did not claim live `EDGE` administration, `CPMS` data-flow ownership, or a live national-submission estate
- the slice did not claim the full Guy's and St Thomas' `R&D` reporting or governance function
- the slice stayed limited to one bounded aligned reporting view:
  - `Mar 2026`
- the honest platform analogue here is:
  - `1` prepared research-performance base
  - `1` mandatory-return-style summary
  - `1` dashboard-style management-information output
  - `1` `R&D` reporting-cycle note
  - `1` release-safe research-performance reporting pack
- the slice therefore supports a truthful claim about system-backed research-performance reporting support and accountable reporting delivery
- it does not support a claim that a live Trust research-management-system estate or national-reporting authority has already been proven

---

## 1. Executive Answer

The slice asked:

`can one governed specialist-system-backed reporting lane be packaged into a bounded research-performance surface that supports both internal programme management and mandatory external-style reporting?`

The bounded answer is:
- one aligned reporting window was fixed:
  - `Mar 2026`
- one prepared research-performance base output was produced:
  - `1`
- one mandatory-return-style summary output was produced:
  - `1`
- one dashboard-style management-information output was produced:
  - `1`
- one `R&D` reporting-cycle reading was produced:
  - `1`
- the same shared reporting cohort remained explicit:
  - `50_plus`
- that cohort remained supported by:
  - `3` source streams
- the pack retained:
  - `3` reporting views
- the mandatory-reporting side retained:
  - `3` measure families
- the dashboard side retained:
  - `3` bounded metrics
- the current case-pressure gap remained:
  - `+1.33 pp`
- the current truth-quality gap remained:
  - `-1.99 pp`
- the carried control gap remained:
  - `+4.80 pp`
- the research-performance pack passed:
  - `8/8` release checks
- regeneration takes about:
  - `0.17` seconds

That means this slice did not just restate an inherited BI or KPI pack. It prepared and repackaged the same governed reporting lane into one research-performance chain that can act as a reusable `R&D` reporting base, a mandatory-return-style surface, and a bounded dashboard-backed management-information layer.

## 2. Slice Summary

The slice executed was:

`one research-management-system support and research-performance reporting pack with one prepared reporting base, one mandatory-return-style summary, one dashboard-style management-information output, and one R&D reporting-cycle reading`

This was chosen because it allowed a direct response to the Guy's and St Thomas' requirement around:
- supporting and improving the `EDGE` research-management-system environment
- using and maintaining databases that back research-metric reporting
- supporting the reporting and presentation of all `R&D` metrics
- submitting mandatory performance metrics to national bodies
- using a dashboard-backed reporting structure for `R&D` visibility

The main delivered outputs were:
- one prepared research-performance base output
- one mandatory-return-style summary output
- one dashboard-style management-information output
- one research-reporting release-check output
- one compact fact pack
- one research-performance scope note
- one prepared-research-performance-base note
- one mandatory-return note
- one dashboard note
- one `R&D` reporting-cycle note
- one caveats note
- one regeneration README

## 3. How This Maps To The Slice Plan

The execution stayed aligned to the approved Guy's and St Thomas' `job 14 A + B` slice rather than drifting into either generic reporting or a fake research-system story.

The delivered scope maps back to the planned lens responsibilities as follows:
- `04 - Analytics Engineering and Analytical Data Product`: one explicit prepared research-performance base and one specialist-system-backed reporting posture
- `02 - BI, Insight, and Reporting`: one mandatory-return-style summary and one dashboard-style management-information output
- `03 - Data Quality, Governance, and Analytics`: one explicit controlled-release posture and one inherited control-family status
- `09 - Analytical Delivery and Operating Discipline`: light support only, through one recurring `R&D` reporting-cycle reading and controlled rerun posture

The report therefore needs to be read as proof of bounded research-management-system support and research-performance reporting delivery, not as proof that the whole Guy's `R&D` system estate is already in place.

## 4. Execution Posture

The execution followed the intended prepared-base-first and mandatory-return-first posture rather than either dashboard-only reporting or inflated system language.

The working discipline was:
- start from compact inherited outputs first
- define one bounded research-performance reporting question:
  - how the current governed reporting lane can support both internal programme-management use and mandatory external-style reporting
- fix the prepared research-performance base before writing mandatory-return or dashboard language
- keep the specialist-system side on bounded support posture rather than live administration
- keep the mandatory side on controlled reporting posture rather than submission authority
- write the `R&D` reporting-cycle note only after the reporting surfaces were explicit
- use Python only after the inherited output set had already reduced the slice to compact reporting tables

This matters for the truth of the slice because the Guy's requirement is about running a controlled research-performance reporting environment, and the execution should therefore read like one compact accountable reporting pack rather than broad research-systems rhetoric.

## 5. Bounded Build That Was Actually Executed

### 5.1 Prepared research-performance base

The first step was to confirm that the inherited reporting-product, framework, and control packs could act as the base for one research-performance reporting lane.

Observed prepared-base facts:

| Measure | Value |
| --- | ---: |
| Prepared research-performance outputs produced | 1 |
| Reporting grain | `research_metric_programme_reporting_pack` |
| Shared reporting cohort | `50_plus` |
| Retained source streams | 3 |
| Retained reporting views | 3 |
| Current case-pressure gap | +1.33 pp |
| Current truth-quality gap | -1.99 pp |

Prepared-base reading:
- the prepared base combines reusable reporting structure, management-information readability, and controlled cross-source confirmation into one compact research-performance source suitable for recurring `R&D` reporting use

Meaning:
- the preparation step is clear enough to support a database-backed research-reporting claim
- the slice only counted because the resulting surface became more reusable than separate inherited reporting views

### 5.2 Mandatory-return-style summary

The slice then materialised one accountable external-style reporting surface from the same governed base.

Observed mandatory-reporting facts:

| Measure | Value |
| --- | ---: |
| Mandatory-return-style outputs produced | 1 |
| Reporting cycle shape | `department_of_health_nihr_style_bundle_analogue` |
| Measure families retained | 3 |
| Change-tracking KPIs retained | 2 |
| Current control gap | +4.80 pp |

Mandatory-return-style reading:
- the summary keeps measure coverage, change tracking, and controlled release posture explicit, which is the right bounded analogue for `R&D` performance returns without implying live submission authority

This matters because the Guy's requirement is not only local reporting. It also needs a mandatory-return-shaped output from the same controlled reporting base.

### 5.3 Dashboard-style management-information output

The slice then materialised one dashboard-style internal reporting surface from the same pack.

Observed dashboard facts:

| Measure | Value |
| --- | ---: |
| Dashboard-style outputs produced | 1 |
| Dashboard metrics retained | 3 |
| Shared reporting cohort | `50_plus` |
| Retained source streams | 3 |

Dashboard contents:
- one cross-source confirmation metric
- one reporting-pressure gap metric
- one quality-consistency gap metric

This matters because the Guy's requirement is not satisfied by saying “we can produce reports.” The slice needed one explicit `R&D` visibility surface from the same prepared reporting base.

### 5.4 R&D reporting-cycle note

The slice then translated the same reporting pack into one bounded `R&D` reporting-cycle note.

Observed interpretation:
- the same prepared base now supports both accountable external-style reporting and recurring internal management visibility
- the retained reporting cohort `50_plus` remains cross-source confirmed at `3` streams
- the management-information side still reads as controlled rather than self-service because release posture and reconciliation remain explicit

Boundary:
- this is a recurring reporting-cycle readability layer
- it is not a live `EDGE` support desk claim
- it is not a direct national-reporting authority claim
- it is not a whole Trust `R&D` data-estate claim

### 5.5 Release and rerun posture

The research-performance pack then ran a compact release-check layer to prove that the inherited surfaces and new outputs remained controlled.

Observed release results:

| Check | Result |
| --- | ---: |
| prepared research-performance base output present | pass |
| mandatory-return-style summary output present | pass |
| dashboard-style output contains three metrics | pass |
| aligned reporting window stays consistent | pass |
| South Tyneside reporting pack remains green | pass |
| Money and Pensions framework pack remains green | pass |
| Welsh control pack remains green | pass |
| language stays below `EDGE` or submission ownership | pass |

Release verdict:
- `8/8` checks passed

This is enough for the slice because the requirement is not “prove a live research-management estate.” It is “support a specialist research-performance environment and deliver controlled reporting from it.”

## 6. What Was Actually Added Beyond Earlier Reporting Slices

This slice was not meant to repeat South Tyneside, Money and Pensions, or Welsh reporting work directly. It was meant to turn those earlier governed surfaces into one research-performance and accountable-reporting pack.

What was inherited directly:
- the prepared reporting-product posture
- the management-information readability layer
- the framework-style metric logic
- the controlled release posture

What was added for Guy's and St Thomas' `job 14 A + B`:
- one explicit prepared research-performance base
- one mandatory-return-style summary
- one dashboard-style management-information output
- one `R&D` reporting-cycle note
- one explicit control against fake `EDGE`, `CPMS`, or national-authority language

That is the correct widening:
- not another generic dashboard slice
- not another generic KPI pack
- not a fake `EDGE` or `CPMS` story
- but one bounded research-management-system support and research-performance reporting layer built directly on compact inherited evidence

## 7. Assets Produced

The slice produced the assets that make the research-performance pack credible.

Research-performance assets:
- prepared research-performance base
- mandatory-return-style summary
- dashboard-style management-information output

Control and explanation assets:
- research-reporting release checks
- scope note
- prepared-base note
- mandatory-return note
- dashboard note
- `R&D` reporting-cycle note
- caveats note
- regeneration README
- changelog

This is the key difference between this slice and a vague “I do research reporting” claim:
- the output here is not just a description
- it is one explicit research-performance pack with one bounded question, one prepared base, one mandatory-return-style cut, one dashboard-style cut, and one recurring reporting-cycle reading

## 8. Figures

No figures have been added yet for this slice.

That is intentional at this stage:
- the strongest proof here is the prepared base, the mandatory-return-style summary, and the dashboard-style output
- a figure would only be useful if it materially clarified the research-performance interpretation
- if later review shows that one analytical reporting view would improve understanding, it can be added then

So the current slice should be treated as:
- research-performance reporting pack first
- figure optional

## 9. What This Slice Supports Claiming

This slice supports truthful statements such as:
- supported a specialist research-management data environment through prepared reporting structures
- delivered research-performance reporting for internal and external-style use from one controlled base
- produced mandatory-return-style and dashboard-style `R&D` outputs from the same governed reporting lane
- kept the research-performance pack controlled and rerunnable

The slice does not support claiming that:
- a live `EDGE` estate was operated in this slice
- `CPMS` flow ownership has already been proven
- direct national submission authority has already been proven
- the full Guy's `R&D` data and governance estate has already been owned

## 10. Candidate Resume Claim Surfaces

This section should be read as a direct response to the Guy's and St Thomas' `job 14 A + B` responsibility, not as a generic “I can report” statement.

The requirement asks for someone who can:
- support and improve the research-management-system environment
- use and maintain databases for research-metric reporting
- submit mandatory performance metrics to national bodies
- produce quarterly, monthly, annual, and ad hoc `R&D` performance reporting
- use a dashboard-backed reporting structure for `R&D` visibility

The slice now supports the following claim surfaces.

### 10.1 Flagship claim surface

> Supported a specialist research-management data environment and delivered research-performance reporting, mandatory-return-style outputs, and dashboard-backed R&D visibility, as measured by producing `1` prepared research-performance base, `1` mandatory-return-style summary, `1` dashboard-style management-information output, and `1` R&D reporting-cycle reading over a shared `50_plus` reporting cohort supported by `3` source streams, while passing `8/8` release checks in `0.17` seconds, by turning compact inherited reporting-product, framework, and control outputs into a bounded research-performance pack without overstating live `EDGE`, `CPMS`, or national-submission ownership.

### 10.2 Shorter recruiter-facing claim surface

> Built governed research-performance reporting products from a prepared reporting base, as measured by one mandatory-return-style summary and one dashboard-style management-information surface, by turning controlled reporting logic into reusable R&D reporting outputs for internal and external-style use.

### 10.3 Direct-response claim surface

> Supported research-management-system reporting and mandatory performance outputs, as measured by one prepared-base pack with mandatory-return-style and dashboard-style surfaces on the same governed logic, by producing reusable R&D reporting outputs that are stronger than isolated reports while staying below live system or submission ownership.

## 11. Overall Verdict

The Guy's and St Thomas' `job 14 A + B` slice is now real and reusable.

The strongest truthful reading is:
- one bounded research-management-system support and research-performance reporting analogue
- one prepared-base reuse interpretation
- one mandatory-return and dashboard-backed reporting layer

The correct boundary is:
- not a live `EDGE` claim
- not a `CPMS` ownership claim
- not a national-submission authority claim
- not a whole-Trust `R&D` data-estate claim

Within that boundary, the slice is strong enough to stand as the anchor proof object for the `job 14` lane.
