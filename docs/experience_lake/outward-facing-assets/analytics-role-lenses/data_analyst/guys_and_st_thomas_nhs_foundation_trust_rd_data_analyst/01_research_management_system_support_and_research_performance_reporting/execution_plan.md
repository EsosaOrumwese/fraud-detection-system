# Execution Plan - Research Management System Support And Research Performance Reporting Slice

As of `2026-04-14`

Purpose:
- turn the chosen Guy's and St Thomas' `job 14 A + B` slice into a concrete execution order tied to compact governed outputs and inherited reporting-product patterns already available in the repo
- keep the work prepared-base-first, mandatory-return-first, and research-management-system-support-first rather than drifting into generic dashboard language, fake `EDGE` administration, or a broad Trust `R&D` data-estate story
- prove one prepared research-performance base, one mandatory-return-style summary, one dashboard-style management-information output, and one bounded `R&D` reporting-cycle reading from the same controlled reporting question
- keep execution memory-safe by reusing compact inherited artefacts before considering any wider raw query footprint

Execution substrate:
- governed local artefacts already available in `artefacts/analytics_slices/`
- the completed South Tyneside `01_bi_reporting_product_and_reporting_platform_support` pack where it sharpens prepared-base and trusted-source support posture
- the completed Guy's and St Thomas' `01_workforce_intelligence_edi_dashboards_and_statutory_reporting` pack where it sharpens governed reporting and senior-facing packaging
- the completed Welsh Government `01_validation_reconciliation_and_cyclical_operational_control` pack where it sharpens recurring reporting control
- the completed Money and Pensions Service `05_kpi_and_framework_measurement_support` pack where it sharpens mandatory-return and external-style reporting posture
- compact new outputs to be written under `artefacts/analytics_slices/data_analyst/guys_and_st_thomas_nhs_foundation_trust_rd_data_analyst/01_research_management_system_support_and_research_performance_reporting/`

Primary rule:
- confirm the smallest truthful research-performance reporting question first
- define one prepared reporting base second
- define one mandatory-return-style summary third
- define one dashboard-style management-information surface fourth
- package one `R&D` reporting-cycle reading and one compact release-check layer fifth
- never reopen broad raw detailed surfaces if the research-performance question can be answered from compact governed outputs
- keep the system and mandatory-reporting language below live `EDGE`, `CPMS`, or national-submission ownership
- if a figure is used later, it must be an analytical reporting view rather than a system or workflow schematic

---

## 1. Confirmed Working Assumptions

This plan is built around the following assumptions that must be tested immediately:
- the repo already contains enough compact inherited evidence to support one prepared research-performance base without a fresh large-scale rebuild
- the repo already contains enough governed reporting logic to support one honest mandatory-return-style summary
- one combined reporting pack can support specialist-system-support language without pretending to be a live `EDGE` or Trust `R&D` systems estate
- the strongest truthful end state is likely to be a bounded prepared-base and accountable reporting reading rather than a broader research-system ownership claim

Candidate first-pass reusable foundations:
- South Tyneside `01_bi_reporting_product_and_reporting_platform_support`
- Guy's and St Thomas' `01_workforce_intelligence_edi_dashboards_and_statutory_reporting`
- Welsh Government `01_validation_reconciliation_and_cyclical_operational_control`
- Money and Pensions Service `05_kpi_and_framework_measurement_support`
- compact reporting, shaping, and prepared-base outputs from earlier slices only where they genuinely align to the same bounded reporting question

Working assumption about the reporting question:
- the slice should stay on one bounded research-performance use only
- the prepared base should state how compact inherited inputs are combined or reused into one product-ready `R&D` structure
- the reporting side should show what a mandatory-return-style and management-information view looks like from that same base
- the combined pack should support one `R&D` reporting-cycle interpretation about recurring internal and external-style use

Important warning:
- these assumptions must be tested rather than carried forward casually
- if the inherited preparation logic cannot support a clean research-performance base, narrow the claim rather than inventing synthetic system ownership

## 2. Prepared-Base-And-Mandatory-Return-First Posture

This slice must not begin as another generic dashboard pack and it must not become a fake research-system note.

The correct posture is:
- confirm the bounded research-performance question first
- confirm the compact prepared-base inputs second
- confirm the mandatory-return and management-information structure third
- materialise the prepared research-performance base fourth
- materialise the mandatory-return-style summary fifth
- materialise the dashboard-style or management-information output sixth
- write the `R&D` reporting-cycle note seventh
- use Python only after the inherited compact outputs have already bounded the reporting question

This matters because the Guy's and St Thomas' responsibility is about:
- specialist research-management-system support
- databases that back research reporting
- mandatory research metrics
- internal board-, lead-, and executive-facing performance reporting

The execution should therefore read like one compact research-performance and mandatory-reporting pack rather than like generic BI delivery.

## 2A. Memory And Query Discipline

This slice must be executed with explicit memory discipline.

The correct posture is:
- do not pull broad raw Trust-style or research-detail surfaces into pandas
- do not reopen wide raw profiling if the reporting question can be answered from compact governed outputs and selected preparation-ready artefacts
- do not assume research-system language justifies synthetic `EDGE` or `CPMS` recreation

The correct query discipline is:
- start with compact inherited outputs first
- use prepared-base, reporting-ready, and control summaries before any new materialisation
- use product-like or governed reporting artefacts only where they sharpen the same bounded research-performance question
- if a probe is needed, filter the period and fields at scan time
- materialise compact reporting outputs only after the bounded question is fixed
- let Python read only those compact outputs, not broad raw source families

The default execution ceiling for this slice is:
- one compact prepared reporting base
- one compact mandatory-return-style summary
- one compact dashboard-style or management-information output
- one compact `R&D` reporting-cycle note
- one compact release-check output

If a query starts behaving like a full research-management-system rebuild rather than a bounded reporting proof, stop and narrow it before proceeding.

## 3. First-Pass Objective

The first-pass objective is:

`build one research-management-system support and research-performance reporting pack over a bounded reporting question, with one prepared reporting base, one mandatory-return-style summary, one dashboard-style management-information output, and one R&D reporting-cycle reading`

The proof object is:
- `research_management_system_support_and_research_performance_reporting_v1`

The first-pass output family is:
- one prepared reporting-base output
- one mandatory-return-style summary output
- one dashboard-style or management-information output
- one `R&D` reporting-cycle note
- one release-check or repeatability-check output
- one research-performance scope note
- one caveats note

## 4. Reporting Scope Gate

Before building either the prepared reporting base or the mandatory-return-style summary, run a bounded scope gate.

The first profiling pass must answer:
- which compact inherited outputs best represent the prepared-base inputs?
- what is the single bounded research-performance reporting question for the slice?
- can the inherited inputs be combined honestly into one mandatory-return-ready structure?
- does the resulting pack remain compact enough to act as a bounded proof object?
- can one `R&D` reporting-cycle note be stated honestly without pretending to improve a live Trust research-system estate?

Required checks:
- continuity of the prepared-base inputs
- continuity of the bounded reporting question
- ability to define one compact mandatory-return and management-information structure
- ability to produce one recurring reporting-cycle reading without opening a second unrelated analytical question

Those checks should be implemented as:
- aggregate-only or compact-output probes
- inherited compact outputs and selected preparation-ready artefacts where available
- no broad row materialisation into memory

Decision rule:
- if the inherited inputs are explicit enough, proceed with the bounded research-performance pack
- if not, add one compact normalisation layer rather than broadening the query footprint

## 5. Candidate Analytical Base

The first-pass analytical base should stay bounded and explicit.

Each component should have one stated purpose:
- prepared reporting base
  - define the reusable research-performance combined structure
- mandatory-return-style summary
  - define the accountable external-style reporting surface produced from that base
- dashboard-style or management-information output
  - define the internal reporting and visibility surface from the same base
- `R&D` reporting-cycle reading
  - define what recurring programme-management use the combined pack supports
- release or repeatability checks
  - define whether the pack is controlled enough to reuse

If inherited outputs do not support one of those components cleanly, adapt by narrowing the claim rather than stretching earlier slices past their truth boundary.

## 6. Planned Transformation Chain

The transformation chain should remain explicit and staged:

1. bounded-question confirmation and prepared-base profiling
2. prepared reporting base materialisation
3. mandatory-return-style summary materialisation
4. dashboard-style or management-information output materialisation
5. `R&D` reporting-cycle note packaging
6. release-check and caveat packaging

The transformations should not be hidden inside vague `R&D` or systems language.

## 7. Bounded Build Order

### Step 1. Confirm the bounded research-performance question

Build small profile checks only for:
- compact reporting-ready, control-ready, and prepared-base artefacts that can define the retained reporting fields
- compact dashboard or management-information outputs that can define the visible reporting side

Goal:
- define the smallest truthful research-performance reporting question
- prove that the same bounded question can support both internal programme-management reporting and mandatory external-style reporting without reopening broad scope

### Step 2. Materialise the prepared reporting base

Create one bounded output, for example:
- `prepared_research_performance_base_v1`

This output should:
- show how inherited compact inputs are combined or normalised
- keep the reporting question explicit
- remain compact enough to act as the preparation anchor of the pack

### Step 3. Materialise the mandatory-return-style summary

Create one bounded output, for example:
- `mandatory_research_reporting_summary_v1`

This output should:
- show the accountable external-style surface produced from the prepared base
- keep the mandatory-reporting contribution explicit and bounded
- avoid pretending the surface is a live national-submission estate

### Step 4. Materialise the dashboard-style or management-information output

Create one bounded output, for example:
- `research_performance_dashboard_output_v1`

This output should:
- show the internal reporting and visibility surface produced from the same base
- keep the management-information contribution explicit and usable
- avoid pretending the surface is a live self-service or enterprise reporting estate

### Step 5. Package the R&D reporting-cycle note

Create one bounded note, for example:
- `research_reporting_cycle_note_v1`

This note should:
- state what recurring programme-management and accountability use the pack supports
- stay below live system ownership or national-reporting authority
- remain explicit about preparation and reuse limits

### Step 6. Package the checks and caveats

Create one bounded output, for example:
- `research_performance_release_checks_v1`

Write:
- the research-performance scope note
- the prepared-reporting-base note
- the mandatory-return note
- the dashboard or management-information note
- the `R&D` reporting-cycle note
- the caveats and regeneration note

Only after the prepared base, mandatory-return, and management-information surfaces are real.

## 8. Workflow-Boundary Strategy

The system-support side has to stay honest.

The first-pass strategy should answer:
- what exactly counts as the prepared research-performance base?
- why is it acceptable as a specialist-system-support analogue?
- what does it add to the mandatory-return and dashboard side?
- why does that still stop short of a live `EDGE`, `CPMS`, or whole-Trust `R&D` estate claim?

Allowed posture:
- prepared-base reuse
- bounded system-backed reporting support
- compact management-information and mandatory-return readiness tied to the same reporting question

Disallowed posture:
- claiming direct live `EDGE` ownership where the slice only uses bounded preparation logic
- claiming direct national-submission authority where the slice only proves a mandatory-return-style surface
- treating the dashboard output as proof of a full Trust `R&D` performance estate

The workflow should therefore close on:
- one bounded research-performance reporting pack
- one explicit mandatory-return-style surface
- one explicit dashboard-style internal reporting surface
- one explicit `R&D` reporting-cycle reading
- one explicit caveat about system and national-reporting boundaries
