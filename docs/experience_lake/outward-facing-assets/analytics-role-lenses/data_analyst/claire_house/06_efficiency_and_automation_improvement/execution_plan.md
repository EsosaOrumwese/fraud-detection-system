# Execution Plan - Efficiency And Automation Improvement Slice

As of `2026-04-04`

Purpose:
- turn the chosen Claire House `3.F` slice into a concrete execution order tied to the governed Claire House analytical lane already available in the repo
- keep the work workflow-first, automation-first, and repeatability-first rather than drifting into another maintained-layer slice or vague process-improvement language
- prove one repeated manual reporting or data-handling step can be reduced through a reusable audience-pack regeneration path built on compact existing outputs
- keep execution memory-safe by reusing Claire House `3.B`, `3.C`, and `3.D` outputs before considering any wider raw query footprint

Execution substrate:
- governed local artefacts already available in `artefacts/analytics_slices/`
- the Claire House `3.B` reporting pack
- the Claire House `3.C` leadership and oversight pack
- the Claire House `3.D` maintained analytical layer and regeneration notes
- compact workflow-improvement outputs to be written under `artefacts/analytics_slices/data_analyst/claire_house/06_efficiency_and_automation_improvement/`

Primary rule:
- confirm the exact repeated workflow burden first
- define the reusable regeneration path second
- materialise one compact workflow-automation summary, one repeatability-check pack, and one supported-output summary from that same bounded path
- package the workflow-friction note and automation-improvement note only after the improved path is real
- never reopen broad raw detailed surfaces if the workflow question can be answered from the inherited compact Claire House outputs
- if a figure is used later, it must be an analytical plot that clarifies the workflow benefit rather than a process diagram

---

## 1. Confirmed Working Assumptions

This plan is built around the following assumptions that must be tested immediately:
- the repo already contains enough governed Claire House outputs to support one honest workflow-automation slice without a fresh analytical rebuild
- the real repeated burden is not raw KPI logic itself, because Claire House `3.D` already centralised that
- the repeated burden is more likely the regeneration and packaging of downstream audience-ready outputs from the same governed lane
- one reusable regeneration path can be materialised cleanly enough to count as bounded automation improvement

Candidate first-pass reusable foundations:
- Claire House `02_reporting_dashboards_and_visualisation`
- Claire House `03_senior_leadership_and_external_reporting`
- Claire House `04_data_infrastructure_models_and_process_improvement`

Working assumption about the bounded automation question:
- the slice should focus on one repeated audience-pack regeneration step
- the output should show that the same governed lane can now be rerun and packaged with less repeated manual handling
- the slice should prove workflow automation and repeatability, not a new analytical discovery

Important warning:
- these assumptions must be tested rather than carried forward casually
- if the repeated burden is not strong enough in the inherited lane, narrow the automation claim rather than widening the raw scope

## 2. Workflow-First Posture

This slice must not begin as another reporting or maintained-layer exercise.

The correct posture is:
- confirm the inherited governed lane first
- identify the repeated manual or repeated rebuild step second
- define the reusable regeneration path third
- build the automation summary fourth
- build the repeatability checks fifth
- write the workflow-friction and automation-improvement notes sixth
- use Python only after the inherited compact outputs have already bounded the workflow question

This matters because the Claire House responsibility here is about efficiency and automation improvement, and the execution should read like real workflow improvement rather than relabelled analytical control work.

## 2A. Memory And Query Discipline

This slice must be executed with explicit memory discipline.

The correct posture is:
- do not pull broad raw surfaces into pandas
- do not rerun wide historical profiling if the workflow question can be answered from Claire House `3.B`, `3.C`, and `3.D` outputs plus one compact automation layer
- do not assume “automation” justifies reopening broad raw analytical scope

The correct query discipline is:
- start with compact workflow profiling before any new materialisation
- use inherited compact outputs and existing builders first
- if a probe is needed, filter window and fields at scan time
- materialise compact workflow outputs only after the regeneration path is clear
- let Python read only those compact outputs, not raw detailed source families

The default execution ceiling for this slice is:
- inherited reporting and maintained-layer outputs first
- one compact workflow-automation summary second
- one compact repeatability-check pack third
- one compact supported-output summary fourth

If a query starts behaving like a fresh analytical rebuild rather than a workflow-improvement proof, stop and narrow it before proceeding.

## 3. First-Pass Objective

The first-pass objective is:

`build one reusable audience-pack regeneration path over the Claire House governed lane, then package the resulting repeatability and maintenance benefit into one bounded workflow-automation proof`

The proof object is:
- `efficiency_and_automation_improvement_v1`

The first-pass output family is:
- one workflow-friction note
- one automation-improvement note
- one regeneration-path note
- one workflow-automation summary
- one workflow-repeatability check output
- one supported-output summary
- one caveat and regeneration pack

## 4. Early Scope Gate

Before building either the automation summary or the repeatability checks, run a bounded workflow-scope gate.

The first profiling pass must answer:
- which exact Claire House `3.B`, `3.C`, and `3.D` outputs define the current repeated workflow?
- what part of that workflow still depends on repeated manual assembly or repeated builder invocation?
- what compact reusable regeneration path can replace or reduce that repeat burden?
- what downstream outputs should be counted as supported by the improved path?
- what do the row counts, builder inputs, and note coverage say about the safe scope of the slice before any automation claim is issued?

Required checks:
- continuity of the maintained analytical layer
- continuity of the downstream audience-pack outputs
- continuity of the fields needed for regenerated output packaging
- continuity of the fields needed for workflow-repeatability checks
- ability to state one clear maintenance or rerun benefit from the improved path

Those checks should be implemented as:
- aggregate-only or compact-output probes
- inherited compact outputs where available
- no broad row materialisation into memory

Decision rule:
- if the inherited lane is strong enough, proceed with the bounded automation pack
- if not, add one compact automation layer rather than broadening the query footprint

## 5. Candidate Workflow Base

The first-pass workflow base should stay bounded and explicit.

Each component should have one stated purpose:
- maintained analytical layer
  - define the governed reusable facts already available
- audience-pack regeneration layer
  - define how those facts are assembled into downstream outputs
- automation summary layer
  - define what repeated manual effort has been reduced
- repeatability-check layer
  - define how the improved path is verified
- supported-output layer
  - define which outputs benefit from the improved path

If inherited outputs do not support one of those components cleanly, adapt by narrowing the workflow claim rather than stretching earlier slices past their truth boundary.

## 6. Planned Transformation Chain

The transformation chain should remain explicit and staged:

1. inherited workflow confirmation and friction-scope checks
2. reusable regeneration-path materialisation
3. workflow-automation summary materialisation
4. repeatability-check materialisation
5. supported-output and note packaging

The transformations should not be hidden inside manual copy-paste or ad hoc notebook edits.

## 7. Bounded Build Order

### Step 1. Confirm the inherited workflow and friction point

Build small profile checks only for:
- the maintained analytical layer needed for downstream regeneration
- the leadership and oversight outputs already using the governed lane
- any existing regeneration notes that show current rerun posture
- the fields needed to compare supported outputs before and after the improved path

Goal:
- define the workflow-automation slice honestly
- prove that one repeated manual or repeated rebuild step can be reduced from inherited compact Claire House outputs

### Step 2. Materialise the reusable regeneration path

Create one bounded path, for example:
- `workflow_regeneration_path_v1`

This path should:
- use compact governed inputs only
- regenerate the chosen downstream audience-pack outputs or their required summaries
- remain small enough to count as a bounded automation improvement rather than a platform build

### Step 3. Materialise the workflow-automation summary

Create one bounded output, for example:
- `workflow_automation_summary_v1`

This output should:
- state what repeated step existed
- state what is now automated or semi-automated
- state the rerun and maintenance benefit
- remain compact enough to act as the proof surface rather than a workflow dashboard

### Step 4. Materialise the repeatability checks

Create one bounded output, for example:
- `workflow_repeatability_checks_v1`

This output should:
- confirm the regenerated outputs remain aligned to the governed lane
- confirm the reusable path can be rerun consistently
- confirm the slice remains tied to compact inherited outputs

### Step 5. Materialise the supported-output summary

Create one bounded output, for example:
- `workflow_supported_outputs_v1`

This output should:
- state which downstream packs or summaries are supported by the improved path
- keep the proof anchored in actual benefited outputs rather than abstract workflow claims

## 8. Workflow Benefit Strategy

The automation pack must remain small in conceptual scope but strong in workflow truth.

The first-pass benefit strategy should answer:
- what repeated step existed before?
- what is now reduced or automated?
- what maintenance or rerun improvement does that create?
- which downstream outputs benefit from the improved path?

Decision rule:
- every retained field must earn its place in either:
  - workflow-friction statement
  - automation summary
  - repeatability check
  - supported-output proof
- if a field does not support one of those purposes, drop it

## 9. Documentation And Control Strategy

The slice is not complete unless another analyst could rerun the improved path responsibly.

The documentation and control pack should answer:
- what the repeated workflow burden was
- what is now automated or semi-automated
- how the path is rerun
- what outputs it supports
- what checks confirm the path still works
- what the slice does and does not automate

Create:
- `workflow_friction_note_v1.md`
- `automation_improvement_note_v1.md`
- `regeneration_path_note_v1.md`
- `workflow_caveats_v1.md`
- `README_workflow_automation_regeneration.md`
- `CHANGELOG_workflow_automation.md`

## 10. Reproducibility Strategy

This slice is not complete unless the regeneration path and its checks can be rerun consistently.

The first-pass reproducibility pack should include:
- versioned regeneration logic
- versioned workflow-repeatability checks
- compact supported-output summary
- regeneration README
- execution fact pack

## 11. Definition Of Done For Execution

Execution is only complete when all of the following are true:
- one repeated manual or repeated rebuild step has been identified explicitly
- one reusable regeneration path has been materialised
- one automation summary states the workflow benefit clearly
- one repeatability-check pack confirms the regenerated outputs remain governed
- one supported-output summary ties the improvement to real downstream outputs
- the slice remains clearly distinct from Claire House `3.D`
- the whole pack remains compact and rerunnable from inherited governed outputs

## 12. Immediate Next Step

Immediate next step after approving this execution plan:
- run the workflow-scope gate and decide whether the Claire House `3.B`, `3.C`, and `3.D` outputs support one honest reusable audience-pack regeneration path without reopening any broad raw analytical rebuild
