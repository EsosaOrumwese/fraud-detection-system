# Execution Plan - Anomaly Resolution And Trusted Output Control Slice

As of `2026-04-08`

Purpose:
- turn the chosen South Tyneside and Sunderland NHS Foundation Trust `C + D` slice into a concrete execution order tied to the governed BI reporting-product pack already available from South Tyneside `A + B`
- keep the work anomaly-investigation-first, robustness-check-first, corrective-action-third, and trusted-output-fourth rather than drifting into broad governance rhetoric, fake warehouse-administration ownership, or synthetic whole-Trust data-quality language
- prove one bounded anomaly summary, one robustness-check output, one corrective-action output, and one trusted-output note from the same controlled BI lane
- keep execution memory-safe by reusing compact South Tyneside `A + B` outputs before considering any wider raw query footprint

Execution substrate:
- governed local artefacts already available in `artefacts/analytics_slices/`
- the South Tyneside `01_bi_reporting_product_and_reporting_platform_support` pack as the primary BI trusted-output base
- compact inherited control-oriented outputs already proven in earlier role lanes where they genuinely sharpen anomaly and trust posture
- compact new South Tyneside outputs to be written under `artefacts/analytics_slices/data_analyst/south_tyneside_and_sunderland_nhs_foundation_trust/02_anomaly_resolution_and_trusted_output_control/`

Primary rule:
- confirm the inherited South Tyneside `A + B` pack first
- identify the smallest truthful anomaly-and-trust question second
- define one explicit anomaly class and one robustness posture third
- materialise one anomaly summary fourth
- materialise one robustness-check output fifth
- materialise one corrective-action output sixth
- write one trusted-output note seventh
- package the release checks and caveats only after the anomaly, robustness, and corrective surfaces are explicit
- never reopen broad raw detailed surfaces if the anomaly-and-trust question can be answered from compact governed outputs
- keep the governance language below live governance-office or warehouse-administration ownership
- if a figure is used later, it must materially clarify the anomaly or trusted-output story rather than duplicate the robustness surface

---

## 1. Confirmed Working Assumptions

This plan is built around the following assumptions that must be tested immediately:
- the South Tyneside `A + B` pack is already strong enough to support one anomaly-resolution and trusted-output analogue
- the first pass can stay on anomaly detection, robustness testing, corrective action, and release-safe trust without needing any stronger warehouse or governance-estate simulation
- one explicit anomaly or discrepancy class can be fixed clearly enough to support a BI output-validity claim
- one bounded corrective-action direction can be stated once the anomaly and robustness surfaces are explicit
- the strongest truthful end state is likely to be trusted-output control rather than a stronger governance-office ownership claim

Candidate first-pass reusable foundation:
- South Tyneside `01_bi_reporting_product_and_reporting_platform_support`

Working assumption about the anomaly-and-trust question:
- the slice should stay on the same governed BI reporting-product lane
- the lane should support one explicit anomaly or output-validity issue
- the trusted-source and release-safe posture should be visible enough to act as a bounded control layer
- the resulting outputs should help a BI stakeholder understand what is wrong, why it matters, and what should be corrected before reuse

Important warning:
- these assumptions must be tested rather than carried forward casually
- if the inherited lane does not support a clean anomaly-and-control pack, narrow the claim to release-safe robustness control rather than inventing a stronger governance story

## 2. Anomaly-And-Trust-First Posture

This slice must not begin as another reporting pack and it must not become a fake governance-office note.

The correct posture is:
- confirm the inherited South Tyneside `A + B` pack first
- define one anomaly-detection or output-validity question second
- materialise one anomaly summary third
- materialise one robustness-check output fourth
- materialise one corrective-action output fifth
- write one trusted-output note sixth
- package the release checks and caveats seventh
- use Python only after the inherited compact outputs have already bounded the anomaly-and-trust question

This matters because the South Tyneside responsibility is about investigating anomalies, testing whether analyses are robust, and applying corrective action to keep outputs trusted, and the execution should therefore read like one bounded anomaly-and-control pack rather than broad governance rhetoric.

## 2A. Memory And Query Discipline

This slice must be executed with explicit memory discipline.

The correct posture is:
- do not pull broad raw surfaces into pandas
- do not reopen wide raw profiling if the anomaly-and-trust question can be answered from the existing South Tyneside `A + B` pack
- do not assume governance language justifies a broader warehouse or system reconstruction

The correct query discipline is:
- start with the South Tyneside `A + B` fact pack, prepared reporting base, management-information output, reporting-platform-support output, and release checks
- use inherited compact outputs first
- if a probe is needed, filter the period and fields at scan time
- materialise compact anomaly, robustness, corrective, and release outputs only after the anomaly-and-trust question is fixed
- let Python read only those compact outputs, not broad raw source families

The default execution ceiling for this slice is:
- inherited South Tyneside `A + B` pack first
- one compact anomaly summary second
- one compact robustness-check output third
- one compact corrective-action output fourth
- one trusted-output note fifth
- one release-check surface sixth

If a query starts behaving like whole-Trust governance simulation rather than a bounded BI trust proof, stop and narrow it before proceeding.

## 3. First-Pass Objective

The first-pass objective is:

`build one anomaly-resolution and trusted-output-control pack with one explicit anomaly class, one robustness-check surface, one corrective-action direction, and one release-safe trusted-output reading`

The proof object is:
- `anomaly_resolution_and_trusted_output_control_v1`

The first-pass output family is:
- one anomaly summary
- one robustness-check output
- one corrective-action output
- one release-check output
- one trusted-output note
- one scope note
- one caveats note

## 4. Anomaly-And-Trust Gate

Before building any South Tyneside `C + D` output, run a bounded anomaly-and-trust gate.

The first profiling pass must answer:
- does the inherited South Tyneside `A + B` pack already contain a clear anomaly or output-validity risk pattern?
- can the same controlled base support both robustness reading and trusted-output control?
- can one compact robustness surface distinguish anomaly conditions from routine BI reporting?
- does the resulting pack remain an anomaly-and-control proof rather than a broader governance-function story?
- does the resulting pack stay compact enough to act as a bounded proof object?

Required checks:
- continuity of the inherited BI reporting-product grain
- continuity of the shared focus or key metric posture
- continuity of the inherited trusted-source and release-safe reporting posture
- ability to state one bounded corrective consequence from the same pack

Those checks should be implemented as:
- aggregate-only or compact-output probes
- inherited compact outputs first
- no broad row materialisation into memory

Decision rule:
- if the BI reporting pack remains explicit and compact, proceed into anomaly, robustness, and corrective materialisation
- if the question is too broad or ambiguous, narrow the slice before proceeding

## 5. Candidate Analytical Base

The first-pass analytical base should stay bounded and explicit.

Each component should have one stated purpose:
- inherited prepared reporting base
  - define the recurring BI reporting structure
- inherited management-information output
  - define the service-facing BI surface under test
- inherited reporting-platform-support output
  - define the trusted-source support posture
- anomaly layer
  - define what is wrong and where
- robustness layer
  - define whether the current output should still be trusted
- corrective layer
  - define what should change before wider reuse

If inherited outputs do not support one of those components cleanly, adapt by narrowing the claim rather than stretching earlier slices past their truth boundary.

## 6. Planned Transformation Chain

The transformation chain should remain explicit and staged:

1. inherited BI-pack confirmation and anomaly profiling
2. anomaly summary materialisation
3. robustness-check materialisation
4. corrective-action output materialisation
5. trusted-output note
6. release-check and caveat packaging

The transformations should not be hidden inside vague governance language.

## 7. Bounded Build Order

### Step 1. Confirm the inherited South Tyneside `A + B` pack

Build small profile checks only for:
- the prepared reporting base
- the management-information output
- the reporting-platform-support output
- the release checks

Goal:
- define the smallest truthful anomaly-and-trust question
- prove that the same BI reporting pack can support a bounded anomaly slice without reopening broad scope

### Step 2. Define the anomaly summary

Create one bounded output, for example:
- `anomaly_summary_v1`

This output should:
- state the retained BI reporting grain
- state the anomaly or output-validity condition that matters
- remain compact enough to act as the centre of the trust proof

### Step 3. Define the robustness-check output

Create one bounded output, for example:
- `robustness_check_output_v1`

This output should:
- state how the current BI output is being tested
- show whether the anomaly is bounded or trusted for reuse
- remain clearly tied to the inherited BI reporting lane

### Step 4. Define the corrective-action output

Create one bounded output, for example:
- `corrective_action_output_v1`

This output should:
- show what corrective or preventative direction follows
- expose what should change before wider reuse
- stay proportional to the evidence

### Step 5. Write the trusted-output note

Write one bounded note, for example:
- `trusted_output_note_v1.md`

This note should:
- explain what should still be trusted
- explain what should be corrected before wider reuse
- stay below full governance-office or warehouse-administration rhetoric

### Step 6. Package the release checks and caveats

Create one bounded output, for example:
- `anomaly_output_release_checks_v1`

Write:
- the anomaly-resolution scope note
- the anomaly-summary note
- the robustness-check note
- the corrective-action note
- the trusted-output note
- the caveats note
- the regeneration README

Only after the anomaly, robustness, and corrective surfaces are real.

## 8. Trust Strategy

The trusted-output story has to stay honest.

The first-pass trust strategy should answer:
- what exactly is being tested for trust?
- how does the robustness surface reveal whether the output is safe to reuse?
- how does the release-safe posture connect back to the same BI lane?
- why does the slice stop at anomaly resolution and trusted-output control rather than stronger governance ownership?

Allowed posture:
- one bounded anomaly class
- one compact robustness surface
- one corrective-action direction
- one repeatable release-check pack

Disallowed posture:
- claiming whole-Trust governance leadership
- asserting full warehouse-administration ownership
- importing generic governance language as fake proof

Decision rule:
- every retained element must earn its place in either:
  - anomaly detection
  - robustness testing
  - corrective action
  - trusted-output control
- if an element does not support one of those purposes, drop it

## 9. Corrective-Action Strategy

This is what makes the slice useful rather than merely descriptive.

The first-pass corrective layer should answer:
- what should change?
- what is the likely reason for the anomaly?
- what should be protected before wider reuse?
- what can a BI stakeholder now rely on more safely?

Required posture:
- use the inherited BI reporting pack to support one explicit corrective reading
- use the robustness surface to make that reading clearer or safer
- keep the consequence proportional to the evidence

Disallowed posture:
- claiming whole-governance ownership
- claiming wider warehouse authority
- claiming organisation-wide anomaly consequences not evidenced by the pack

## 10. Planned Deliverables

Anomaly-and-control assets:
- one anomaly summary
- one robustness-check output
- one corrective-action output
- one trusted-output note

Support assets:
- one scope note
- one caveats note

Controls:
- one release-check output
- one regeneration README
- one changelog

## 11. Suggested Artefact Names

Recommended names:
- `anomaly_summary_v1.parquet`
- `robustness_check_output_v1.parquet`
- `corrective_action_output_v1.parquet`
- `anomaly_output_release_checks_v1.parquet`
- `anomaly_resolution_scope_note_v1.md`
- `anomaly_summary_note_v1.md`
- `robustness_check_note_v1.md`
- `corrective_action_note_v1.md`
- `trusted_output_note_v1.md`
- `anomaly_control_caveats_v1.md`
- `README_anomaly_control_regeneration.md`
- `CHANGELOG_anomaly_control.md`
- `execution_fact_pack.json`

## 12. Definition Of Green Before Reporting

Do not move to the execution report until the following are true:
- one explicit anomaly class is fixed
- one robustness-check surface is fixed
- one corrective-action output exists
- one trusted-output note exists
- one release-check surface exists
- the pack stays clearly distinct from BI product delivery work
- the pack stays clearly bounded below whole-Trust governance ownership
- the whole pack stays compact and rerunnable from governed outputs

## 13. Failure Conditions

Stop and narrow the slice if:
- the anomaly class cannot be stated without broad ambiguity
- the robustness surface remains mostly routine BI reporting with no real trust meaning
- the evidence would force fake governance-office language
- the trusted-output note drifts into consequences the pack cannot support
- the execution starts reopening broad raw scope without materially strengthening the anomaly-and-control proof

## 14. Immediate Next-Step Order

1. Run the inherited anomaly-and-trust gate over South Tyneside `A + B`.
2. Define one compact anomaly summary from that pack.
3. Materialise the robustness-check output and the corrective-action output.
4. Write the trusted-output note.
5. Package the caveats, changelog, and regeneration note.
