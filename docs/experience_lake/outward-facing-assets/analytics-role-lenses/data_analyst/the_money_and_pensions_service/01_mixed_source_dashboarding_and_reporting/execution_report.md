# Execution Report - Mixed-Source Dashboarding And Reporting Slice

As of `2026-04-04`

Purpose:
- record what was actually executed for the Money and Pensions Service `Data Insight Analyst` slice around mixed-source dashboarding and reporting
- preserve the truth boundary between one bounded integrated reporting pack and any wider claim about literal `Qualtrics`, `Decision Focus`, or full `CX&Q` platform ownership
- package the saved facts, integrated dashboard summary, supporting detail output, release checks, and claim-ready evidence into one outward-facing report

Truth boundary:
- this execution was completed by building directly on compact governed outputs already produced in earlier HUC, Claire House, and Hertfordshire slices
- the slice did not rebuild a broad raw customer-experience reporting estate
- the slice did not claim live `Qualtrics`, `Decision Focus`, or source-tool ownership
- the slice stayed limited to one bounded aligned reporting view:
  - `Mar 2026`
  - with one supporting current operational snapshot week:
    - `2026-03-23`
- the honest platform analogue here is:
  - `3` governed evidence streams
  - `1` common reporting grain
  - `1` dashboard-style summary
  - `1` supporting report-style detail output
  - `1` bounded audience-use note
- the slice therefore supports a truthful claim about building dashboards and reports from multiple evidence streams
- it does not support a claim that the full Money and Pensions Service `CX&Q` reporting estate or measurement-tool stack has already been implemented

---

## 1. Executive Answer

The slice asked:

`can several governed evidence surfaces be turned into one coherent dashboard-and-reporting pack that is more useful than any single-source view on its own?`

The bounded answer is:
- one aligned reporting window was fixed:
  - `Mar 2026`
- one current operational snapshot week was fixed:
  - `2026-03-23`
- `3` governed evidence streams were combined:
  - HUC current operational segment surface
  - Claire House reporting-supporting detail surface
  - Hertfordshire current shortfall surface
- one common reporting grain was fixed:
  - `amount_band`
- that grain carried `4` values across the integrated pack:
  - `<10`
  - `10-25`
  - `25-50`
  - `50+`
- one dashboard-style summary was produced:
  - `1`
- one supporting report-style detail output was produced from the same integrated logic:
  - `1`
- one explicit shared focus band was confirmed across all `3` streams:
  - `50_plus`
- that shared focus carried:
  - `10.78%` current case-open rate from HUC
  - `10.82%` reporting case-open rate from Claire House
  - `10.82%` current case-open rate from Hertfordshire
  - `-1.99 pp` truth-quality gap to peers in the Hertfordshire focus reading
- the integrated pack passed `6/6` release checks
- regeneration takes about `0.18` seconds because the slice reuses compact inherited outputs rather than reopening broad raw analytical scope

That means this slice did not just combine files for the sake of saying “multiple sources.” It turned several controlled surfaces into one integrated reporting pack with a shared grain, one clear cross-source attention point, and a summary-plus-detail structure that remains governed and rerunnable.

## 2. Slice Summary

The slice executed was:

`one integrated mixed-source reporting pack with one dashboard-style summary, one supporting detail output, and one bounded audience-use note`

This was chosen because it allowed a direct response to the Money and Pensions Service requirement:
- work across systems to transform multiple data sources into meaningful reporting and dashboards
- build dashboards and reports that do not depend on a single source alone
- provide regular reporting for different stakeholder audiences
- translate fragmented evidence into one coherent decision-support view

The main delivered outputs were:
- one mixed-source reporting base
- one dashboard-style summary output
- one supporting detail output
- one release-check output
- one mixed-source reporting scope note
- one dashboard summary note
- one supporting detail note
- one audience-use note
- one caveats note
- one regeneration README
- one compact fact pack

## 3. How This Maps To The Slice Plan

The execution stayed aligned to the approved Money and Pensions Service `3.B` slice rather than drifting into governance ownership, mixed-method analysis, or root-cause investigation.

The delivered scope maps back to the planned lens responsibilities as follows:
- `02 - BI, Insight, and Reporting Analytics`: one integrated dashboard-style summary, one supporting detail output, one bounded reporting product structure
- `03 - Data Quality, Governance, and Trusted Stewardship`: one explicit shared reporting grain, one auditable cross-source combination, and one release-check layer over the integrated pack
- `08 - Stakeholder Translation, Communication, and Influence`: one audience-use note and one concise summary reading rather than a disconnected source-by-source dump
- `05 - Business Analysis, Change, and Decision Support`: light support only, through one bounded note about what the pack is useful for

The report therefore needs to be read as proof of mixed-source dashboarding and reporting from controlled analytical surfaces, not as proof that the whole Money and Pensions Service customer-experience reporting estate is already in place.

## 4. Execution Posture

The execution followed the intended mixed-source-first and reporting-product-first posture rather than forcing a synthetic data-collection or mixed-method slice too early.

The working discipline was:
- confirm the inherited compact evidence surfaces first
- identify one common reporting grain before shaping any summary output
- keep the first pass on structured and quantitative controlled surfaces only
- derive the dashboard summary and the supporting detail output from the same integrated logic
- keep all heavy work inside compact inherited outputs plus one new compact integrated layer
- use Python only after the candidate surfaces had already been reduced to compact reporting-ready tables

This matters for the truth of the slice because the Money and Pensions Service responsibility is about building meaningful reporting and dashboards from fragmented evidence, and the execution should reflect controlled synthesis rather than broad rebuilding.

## 5. Bounded Build That Was Actually Executed

### 5.1 Inherited surface confirmation

The first step was to confirm that the repo already contained compact governed surfaces strong enough to support one honest mixed-source reporting pack.

Observed reusable foundations:

| Input | Shape |
| --- | ---: |
| HUC current segment surface | `4` band rows |
| Claire House supporting detail cut | `4` band rows |
| Hertfordshire current focus surface | `1` focus row |

Meaning:
- the integrated pack could be built from already-governed compact outputs
- the slice did not need a fresh raw rebuild
- the shared analytical question could therefore be answered through controlled repackaging rather than wide reprocessing

That is an important result in its own right:
- the mixed-source reporting slice can be built as a bounded extension of earlier analytical work rather than as a redundant heavy rerun

### 5.2 Common reporting grain

The integrated pack then fixed one common reporting grain that all retained surfaces could support honestly.

Observed grain:

| Grain | Values |
| --- | --- |
| `amount_band` | `<10`, `10-25`, `25-50`, `50+` |

This matters because the Money and Pensions Service requirement is not satisfied by “several sources exist.” The slice needed one defensible shared grain that could support both the dashboard summary and the supporting detail output without forcing incompatible logic together.

### 5.3 Dashboard-style summary

The dashboard summary was intentionally kept compact.

Observed summary facts:

| Measure | Value |
| --- | ---: |
| Evidence streams combined | 3 |
| Common reporting grain | `amount_band` |
| Grain values carried | 4 |
| Dashboard outputs produced | 1 |
| Supporting detail outputs produced | 1 |
| Shared focus band | `50+` |
| Streams confirming that focus | 3 |

This proves that the pack supports a top-level integrated reading from several controlled evidence streams rather than from a single source alone.

### 5.4 Supporting detail output

The supporting detail output was not a second unrelated report. It was a detail cut generated from the same integrated logic as the dashboard summary.

Observed supporting detail facts:

| Band | Stream Coverage | Attention Confirmation Count | Cross-Source Reading |
| --- | ---: | ---: | --- |
| `<10` | 2 | 0 | background reporting band |
| `10-25` | 2 | 0 | background reporting band |
| `25-50` | 2 | 0 | background reporting band |
| `50+` | 3 | 3 | shared attention point |

Meaning:
- the supporting detail cut deepens the same integrated story as the summary output
- the pack therefore answers both:
  - the dashboard question:
    - what is the cross-source top-level picture?
  - and the reporting follow-up question:
    - which band remains the clearest shared attention point once the streams are aligned?

### 5.5 Shared focus reading

The slice then verified whether one attention point remained visible across all retained evidence streams.

Observed shared focus facts:

| Source | `50+` Case-Open Reading |
| --- | ---: |
| HUC current operational surface | 10.78% |
| Claire House reporting detail surface | 10.82% |
| Hertfordshire shortfall surface | 10.82% |

Additional supporting fact:
- Hertfordshire current truth-quality gap to peers for the same focus band was `-1.99 pp`

Reading:
- the integrated pack does not just place several source rows side by side
- it identifies one shared attention point that is materially reinforced across all three surfaces
- the rest of the bands act as reporting context rather than competing focal points

This is the central mixed-source proof of the slice.

### 5.6 Release and rerun posture

The pack then ran a compact release-check layer to prove that the integrated outputs remained controlled.

Observed release results:

| Check | Result |
| --- | ---: |
| HUC band surface covers all expected bands | pass |
| Claire reporting detail surface covers all expected bands | pass |
| Hertfordshire current focus surface is explicit | pass |
| Integrated reporting grain remains complete | pass |
| Shared focus band confirmed across streams | pass |
| Inherited source surfaces remain green | pass |

Release verdict:
- `6/6` checks passed

This is enough for the slice because the requirement is not “prove a whole customer-experience data platform.” It is “build meaningful reporting and dashboards from multiple systems or evidence sources in a controlled and reusable way.”

## 6. What Was Actually Added Beyond Earlier Slices

This slice was not meant to win by repeating earlier reporting or performance slices independently. It was meant to prove a new combined reporting product built on top of them.

What was inherited directly:
- HUC multi-source operational band surface
- Claire House controlled reporting detail surface
- Hertfordshire current focus shortfall surface

What was added for Money and Pensions Service `3.B`:
- one explicit integrated reporting base
- one dashboard-style summary over that integrated base
- one supporting detail output from the same logic
- one audience-use note for bounded multi-audience value
- one release-check layer for the mixed-source pack

That is the correct widening:
- not a fake source-tool rebuild
- not mere renaming of earlier reporting work
- but an integrated dashboard-and-reporting layer built directly from several controlled evidence streams

## 7. Assets Produced

The slice produced the assets that make the mixed-source reporting pack credible.

Reporting assets:
- mixed-source reporting base
- dashboard-style summary output
- supporting detail output

Control assets:
- release checks
- audience-use note
- caveats note
- regeneration README

This is the key difference between this slice and a vague “I built dashboards from multiple sources” claim:
- the output here is not just an assertion
- it is one explicit integrated reporting pack with one common grain, one shared attention point, and one controlled summary-plus-detail structure

## 8. Figures

No figures have been added yet for this slice.

That is intentional at this stage:
- the strongest proof here is the integrated reporting base, the summary output, the supporting detail output, and the release-check layer
- a figure would only be useful if it sharpened the cross-source contribution materially
- if later review shows that one analytical plot would improve understanding, it can be added then

So the current slice should be treated as:
- mixed-source reporting pack first
- figure optional

## 9. What This Slice Supports Claiming

This slice supports truthful statements such as:
- built dashboards and reports from multiple evidence streams
- translated fragmented governed evidence into one coherent reporting product
- delivered one summary-plus-detail pack from a shared reporting grain
- kept the integrated reporting layer controlled through release checks and regeneration notes

The slice does not support claiming that:
- the full Money and Pensions Service customer-experience measurement platform has already been implemented
- literal `Qualtrics` and `Decision Focus` reporting tooling has already been deployed on the platform
- mixed-method or root-cause analysis has already been fully proven by this slice alone
- all `CX&Q` reporting needs are already covered

## 10. Candidate Resume Claim Surfaces

This section should be read as a direct response to the Money and Pensions Service `3.B` responsibility, not as a generic “I can make dashboards” statement.

The requirement asks for someone who can:
- build dashboards and reports from multiple systems or evidence streams
- translate fragmented evidence into coherent reporting and visuals
- provide regular reporting outputs for different audiences

The claim therefore needs to answer back in evidence form:
- I built one coherent reporting pack from several governed evidence streams
- I kept summary and supporting detail outputs on the same combined logic
- I made the cross-source attention point explicit and rerunnable

### 10.1 Flagship `X by Y by Z` claim

> Built dashboards and reports from multiple evidence streams, as measured by combining `3` governed source surfaces into `1` common `amount_band` reporting grain with `4` aligned grain values, producing `1` dashboard-style summary and `1` supporting detail output, and passing `6/6` release checks in `0.18` seconds, by turning inherited operational, reporting, and shortfall surfaces into one coherent mixed-source pack that made `50_plus` the shared attention point across all three streams.

### 10.2 Shorter recruiter-facing version

> Built mixed-source dashboards and reports, as measured by `3` aligned evidence streams, `1` integrated summary-plus-detail pack, and `6/6` release checks, by combining controlled reporting surfaces into one coherent cross-source view.

### 10.3 Closer direct-response version

> Produced dashboards and reports from multiple systems or evidence sources, as measured by one common reporting grain, one dashboard-style summary, and one supporting detail output on the same governed combined logic, by integrating several controlled analytical surfaces into a single reporting pack.
