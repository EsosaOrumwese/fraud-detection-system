# Execution Plan - Structured And Unstructured Evidence Analysis Slice

As of `2026-04-04`

Purpose:
- turn the chosen Money and Pensions Service `3.D` slice into a concrete execution order tied to the governed reporting lane already available in the repo
- keep the work mixed-type-first, interpretation-first, and boundary-safe rather than drifting into fake `NLP`, broad qualitative-research rhetoric, or a repeat of the mixed-source reporting slice
- prove one governed structured evidence output, one bounded narrative-style evidence output, and one combined insight output from the same analytical question
- keep execution memory-safe by reusing the existing Money and Pensions Service `3.B` and `3.A` outputs before considering any wider raw query footprint

Execution substrate:
- governed local artefacts already available in `artefacts/analytics_slices/`
- the Money and Pensions Service `01_mixed_source_dashboarding_and_reporting` pack as the structured starting point
- the Money and Pensions Service `02_data_governance_and_output_stewardship` pack as the control starting point
- inherited issue, finding, recommendation, or action-language artefacts from earlier slices only where they genuinely strengthen the same focus question
- compact new mixed-type outputs to be written under `artefacts/analytics_slices/data_analyst/the_money_and_pensions_service/03_structured_and_unstructured_evidence_analysis/`

Primary rule:
- confirm the governed structured lane first
- define the bounded narrative-style analogue second
- materialise the structured evidence output third
- materialise the narrative-style evidence output fourth
- materialise the combined insight output fifth
- package the caveat and interpretation notes only after the two evidence surfaces are explicit
- never reopen broad raw detailed surfaces if the mixed-type question can be answered from the existing governed pack and compact inherited note-like artefacts
- if figures are used later, they must be analytical plots rather than diagrams, word-clouds, or decorative qualitative visuals

---

## 1. Confirmed Working Assumptions

This plan is built around the following assumptions that must be tested immediately:
- the repo already contains enough governed structured output to support one honest mixed-type slice without a fresh analytical rebuild
- the strongest platform analogue for the unstructured side is a bounded narrative-style surface built from issue-like, finding-like, recommendation-like, or action-language artefacts already present on the platform
- one combined insight output can show a clearer pattern or priority than the structured lane alone
- one bounded caveat note can keep the narrative analogue honest without collapsing the slice

Candidate first-pass reusable foundations:
- Money and Pensions Service `01_mixed_source_dashboarding_and_reporting`
- Money and Pensions Service `02_data_governance_and_output_stewardship`
- HUC `04_issue_to_action_briefing`
- Hertfordshire `04_service_improvement_support_from_performance_information`
- Claire House `05_data_quality_audit_and_governance_support`

Working assumption about the mixed-type question:
- the slice should focus on one shared analytical priority only
- the structured lane should state the numeric or governed reporting picture
- the narrative-style surface should add coded explanatory or issue-language context
- the slice should prove structured-plus-narrative analysis, not literal free-text mining

Important warning:
- these assumptions must be tested rather than carried forward casually
- if the inherited note-like artefacts cannot support a clean narrative-style surface, narrow the claim rather than inventing synthetic text evidence

## 2. Mixed-Type-First Posture

This slice must not begin as another dashboard slice and it must not drift into a fake text-analytics demo.

The correct posture is:
- confirm the inherited governed structured lane first
- identify the one shared focus question second
- identify a bounded narrative-style evidence surface third
- build the structured evidence output fourth
- build the narrative-style evidence output fifth
- build the combined insight output sixth
- write the caveat and interpretation notes seventh
- use Python only after the inherited compact outputs have already bounded the mixed-type question

This matters because the Money and Pensions Service responsibility is about analysing both structured and unstructured-style evidence, and the execution should read like combined interpretation rather than either pure reporting or artificial text mining.

## 2A. Memory And Query Discipline

This slice must be executed with explicit memory discipline.

The correct posture is:
- do not pull broad raw surfaces into pandas
- do not reopen wide raw profiling if the mixed-type question can be answered from the existing governed pack and compact note-like artefacts
- do not assume “unstructured” justifies synthetic free-text generation or new raw-text processing

The correct query discipline is:
- start with the Money and Pensions Service governed outputs before any new materialisation
- use inherited summary, detail, requirements, and control outputs first
- if a probe is needed, filter the window and fields at scan time
- materialise compact structured and narrative-style outputs only after the shared focus question is fixed
- let Python read only those compact outputs and selected note-like artefacts, not raw detailed source families

The default execution ceiling for this slice is:
- inherited governed structured lane first
- one compact structured evidence output second
- one compact narrative-style evidence output third
- one compact combined insight output fourth
- one compact note set fifth

If a query starts behaving like a full qualitative or text-analytics rebuild rather than a bounded mixed-type proof, stop and narrow it before proceeding.

## 3. First-Pass Objective

The first-pass objective is:

`build one structured-plus-narrative analytical pack over the governed reporting lane, with one structured evidence output, one bounded narrative-style evidence output, and one combined insight output on the same focus question`

The proof object is:
- `structured_and_unstructured_evidence_analysis_v1`

The first-pass output family is:
- one structured evidence summary
- one narrative-style evidence summary
- one combined insight output
- one mixed-type release-check or repeatability-check output
- one mixed-type analysis scope note
- one structured evidence note
- one narrative evidence note
- one combined insight note

## 4. Early Scope Gate

Before building either the structured evidence output or the narrative-style output, run a bounded scope gate.

The first profiling pass must answer:
- which governed Money and Pensions Service output best represents the structured side?
- what is the single shared focus question for the slice?
- which inherited note-like artefact best represents the narrative-style side?
- can both evidence surfaces be aligned honestly without pretending they are from the same literal source family?
- do the row counts, fields, and current notes show that the slice can remain compact?

Required checks:
- continuity of the governed structured lane
- continuity of the shared focus signal
- ability to define one compact narrative-style evidence surface
- ability to produce one combined reading without diverging into a second separate analytical question

Those checks should be implemented as:
- aggregate-only or compact-output probes
- inherited compact outputs and selected note-like artefacts where available
- no broad row materialisation into memory

Decision rule:
- if the inherited structured and note-like surfaces are explicit enough, proceed with the bounded mixed-type pack
- if not, add one compact coded narrative layer rather than broadening the query footprint

## 5. Candidate Analytical Base

The first-pass analytical base should stay bounded and explicit.

Each component should have one stated purpose:
- governed structured output
  - define what the numeric lane shows
- bounded narrative-style surface
  - define what issue-like or action-language evidence says about the same focus question
- combined insight layer
  - define what becomes clearer when both are read together
- release or repeatability checks
  - define whether the mixed-type pack is controlled enough to reuse

If inherited outputs do not support one of those components cleanly, adapt by narrowing the mixed-type claim rather than stretching earlier slices past their truth boundary.

## 6. Planned Transformation Chain

The transformation chain should remain explicit and staged:

1. governed-lane confirmation and focus-question profiling
2. narrative-style surface selection and boundary profiling
3. structured evidence output materialisation
4. narrative-style evidence output materialisation
5. combined insight output materialisation
6. note and caveat packaging

The transformations should not be hidden inside vague mixed-method language or manual copy-paste.

## 7. Bounded Build Order

### Step 1. Confirm the governed structured lane

Build small profile checks only for:
- the mixed-source reporting base
- the governed-output summary
- the supporting detail output
- the governance-and-output control surface

Goal:
- define the structured side honestly
- prove that the Money and Pensions Service reporting lane can act as the structured anchor without broad raw rebuild

### Step 2. Select and bound the narrative-style analogue

Build a compact selection pass only for:
- issue-like artefacts
- finding-like artefacts
- recommendation-like artefacts
- action-pathway or support-note artefacts

Goal:
- identify the smallest honest narrative-style surface tied to the same focus question
- avoid pretending the slice is based on a literal text corpus

### Step 3. Materialise the structured evidence output

Create one bounded output, for example:
- `structured_evidence_summary_v1`

This output should:
- show the governed structured reading
- keep the focus question explicit
- remain compact enough to act as one side of the mixed-type pack

### Step 4. Materialise the narrative-style evidence output

Create one bounded output, for example:
- `narrative_evidence_summary_v1`

This output should:
- show the coded issue-like or action-language surface
- keep the narrative contribution explicit and bounded
- avoid pretending the surface is raw free text

### Step 5. Materialise the combined insight output

Create one bounded output, for example:
- `combined_mixed_type_insight_v1`

This output should:
- show what the structured lane says
- show what the narrative-style surface adds
- state one clearer focus pattern or gap that becomes stronger when both are used together

### Step 6. Package the notes and caveats

Write:
- the mixed-type analysis scope note
- the structured evidence note
- the narrative evidence note
- the combined insight note
- the caveats and regeneration note

Only after the structured, narrative-style, and combined outputs are real.

## 8. Narrative-Boundary Strategy

The unstructured analogue has to stay honest.

The first-pass narrative strategy should answer:
- what exactly counts as the narrative-style surface?
- why is it acceptable as an unstructured-style analogue?
- what does it add to the structured lane?
- why does that still stop short of real text mining or full qualitative-research ownership?

Allowed narrative posture:
- issue-like, finding-like, recommendation-like, or action-language analytical surfaces
- compact coded narrative evidence tied to the same focus question

Disallowed narrative posture:
- claiming raw free-text analytics where the slice only uses bounded analytical notes or coded language surfaces

Decision rule:
- every retained narrative element must earn its place in either:
  - combined interpretation
  - explanatory context
  - boundary-safe evidence of mixed-type analysis
- if a narrative element does not support one of those purposes, drop it

## 9. Documentation And Control Strategy

The slice is not complete unless another analyst could understand and review the mixed-type logic responsibly.

The documentation and control pack should answer:
- what the structured side is
- what the narrative-style side is
- what the combined insight adds
- what the slice does and does not prove

Create:
- `mixed_type_analysis_scope_note_v1.md`
- `structured_evidence_note_v1.md`
- `narrative_evidence_note_v1.md`
- `combined_insight_note_v1.md`
- `mixed_type_analysis_caveats_v1.md`
- `README_mixed_type_analysis_regeneration.md`
- `CHANGELOG_mixed_type_analysis.md`

## 10. Reproducibility Strategy

This slice is not complete unless the mixed-type pack and its checks can be rerun consistently.

The first-pass reproducibility pack should include:
- versioned structured evidence logic
- versioned narrative-style evidence logic
- versioned combined insight logic
- compact release or repeatability checks
- regeneration README
- execution fact pack

## 11. Definition Of Done For Execution

Execution is only complete when all of the following are true:
- one governed structured evidence output has been identified explicitly
- one bounded narrative-style evidence output has been stated
- one combined insight output has been materialised clearly
- one release-safe or control output has been materialised
- the slice remains clearly distinct from the Money and Pensions Service `3.B` reporting-product work
- the whole pack remains compact and rerunnable from inherited governed outputs

## 12. Immediate Next Step

Immediate next step after approving this execution plan:
- run the mixed-type scope gate and decide which governed structured surface and bounded narrative-style surface best support one honest combined insight pack without reopening any broad raw analytical rebuild
