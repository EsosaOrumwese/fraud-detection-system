# Logbook
### Date: 26th August 2025
### Project: Fraud Detection System
### Issues Resolved: [SD-02](https://github.com/EsosaOrumwese/fraud-detection-system/issues/25) `in-progress`
### Reference commits: Check commits on this date (if any)

* 12:48pm
  * Changed the mismatchin the S0 expanded text for the `rng_audit_log.jsonl` referenced filename from `audit.jsonl` to `rng_audit_log.jsonl` to match with the dictionary name.

* 2:22pm
  * Included the TTS version for the remaining subsections of state-0

* 5:11pm
  * Edited `schemas.layer1.yaml/#rng.events.hurdle_bernoulli`

* 11:06pm
  * Finalize envelope/payload contract; remove ambiguity in v1.1
  * Summary;
    * Pin `ts_utc` precision in S1.4 semantics to RFC-3339 with Z and exactly 6 fractional digits (microseconds), matching schema and S1.1.
    * Remove duplicated “Deterministic vs stochastic cases (branch-clean)” header.
    * Finalize payload contract in text to match schema:
        * Payload keys are {merchant_id, pi, is_multi, deterministic, u}.
      * `u` is conditional: present in (0,1) when deterministic=false; `u=null` when deterministic=true.
    * Update stochastic JSON example: add `"deterministic": false` and include `u` example; fix commas and comment (“per schema”).
    * Update validator V3 note: remove schema-uncertainty wording; enforce single contract (deterministic ⇒ u:null; stochastic ⇒ 0<u<1).
