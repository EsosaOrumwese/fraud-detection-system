# Logbook
### Date: 27th June 2025
### Project: Fraud Detection System
### Issues Resolved: [SD-01](https://github.com/EsosaOrumwese/fraud-detection-system/issues/24) `in-progress`
### Reference commits: Check commits on this date (if any)

* 5:05pm
  * Resumed working on improving my data generator -> `src/fraud_detection/simulator/generate.py`
  * Added in order of approach, things that need to be fixed in my data gen
  * Switch to new branch `revamp_data_generator` which will be used to hold the history of my fixes for the data-generator. Incase it crashes, I can be able to switch back to my `data-generator` branch which was created for SD-01.

* 5:23pm
  * Began to modularize and decouple for generator.
  * Added code into empty modular scripts but left `generate.py` untouched before commit

* 6:00pm
  * Going to have to remove the poor realism that was baked into my current `generate.py` and shift it to after SD-01. 
    * Its logic was so poor that what it basically was a random number generator. Countries didn't match currency used (at least even if not all, majority should have), longitude, latitude and country didn't match at all, and much more.

* 7:24pm
  * Restarted the refactor because former code really didn't put my `transaction_schema` into consideration.
  * Scrapped former revamped