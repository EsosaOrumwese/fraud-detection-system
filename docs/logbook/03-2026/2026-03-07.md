## 2026-03-07 06:35:00 +00:00
- Investigated `PR3-S2` execution readiness after `PR3-S1` closure instead of carrying forward the stale `S0` assumptions.
- Confirmed material authority drift: `g3a_measurement_surface_map.json` still points burst downstream health at `MSK_CONSUMER_LAG` and `FLINK_CHECKPOINT_METRICS`, but the live hot path under certification is the EKS worker lane (`fp-pr3-ieg`, `fp-pr3-ofp`, `fp-pr3-df`, `fp-pr3-al`, `fp-pr3-dla`, `fp-pr3-archive-writer`).
- Read the runtime code to identify the actual measurable impact surfaces already emitted by the platform:
  - `IEG`: backpressure hits, checkpoint/watermark posture, apply-failure signal.
  - `OFP`: lag seconds, watermark age, checkpoint age, missing features, snapshot failures.
  - `DLA`: checkpoint age, quarantine total, append failure total, replay divergence total.
  - `Archive Writer`: seen total, archived total, duplicate total, payload mismatch total, write error total.
  - `DF`/`AL`: publish duplicate/quarantine posture suitable for bounded-degrade interpretation.
- Verified live cluster posture with `kubectl` on `fraud-platform-dev-full`: all six PR3 runtime deployments are healthy and currently carry the active runtime path.
- Important design consequence recorded in the implementation map: `PR3-S2` must use a dedicated collector that reads run-scoped JSON artifacts from each live pod, because there is no shared central downstream collector already wired for this certification state.
- Chosen next build path:
  1. repin `PR3-S2` documentation to the real EKS downstream surfaces,
  2. implement the EKS runtime snapshot collector,
  3. implement a dedicated burst/backpressure workflow on fresh run identity,
  4. execute `S2` and judge it strictly on burst impact metrics plus downstream bounded-degrade posture.
## 2026-03-07 06:40:00 +00:00
- Ran the new `PR3-S2` EKS collector as a live smoke against the currently materialized runtime (`platform_20260307T035824Z`).
- Mechanical result: good. The collector can read run-scoped pod-local JSON artifacts from the active `IEG/OFP/DF/AL/DLA/Archive Writer` workers.
- Important semantic finding recorded in the implementation map: `WATERMARK_TOO_OLD` is not a valid fail gate for this certification method because we are replaying historical oracle data. Event-time watermark age versus wall clock will be permanently old even when processing freshness is healthy.
- `S2` therefore must judge downstream freshness by processing signals that remain meaningful under historical replay:
  - `checkpoint_age_seconds`,
  - `lag_seconds`,
  - error/quarantine/fail-closed deltas,
  - `IEG backpressure_hits` delta,
  - archive backlog and write/payload mismatch deltas.
- Also observed on the stale pre-smoke runtime:
  - `DF` already carries prior fail-closed/quarantine counts,
  - `Archive Writer` already carries prior write errors,
  - `AL` and `DLA` artifacts can be absent before traffic.
- Consequence: the burst rollup must use fresh-run deltas from a new baseline, not absolute counters from any prior stale run.
## 2026-03-07 07:05:00 +00:00
- Pulled failed run `22794165103` and traced the first blocker to the `PR3-S0` refresh step, not the burst path itself.
- Root cause: the workflow expected strict upstream `PR2` run-control receipts to exist in the GitHub checkout workspace, but those receipts live in the evidence/run store and were never hydrated into the runner.
- Production interpretation recorded in the implementation map: this is an evidence-discipline defect. A certification rerun must materialize its upstream proof from the evidence store, not silently rely on developer-local files.
- Also classified the `kubectl localhost:8080` message as a secondary workflow-noise failure caused by `if: always()` on the post-burst snapshot after kubeconfig setup had been skipped.
- Chosen remediation path:
  1. publish any missing strict upstream evidence (`PR2`, `M13 stress S5`, `M14E cutover`) into the evidence bucket,
  2. patch `dev_full_pr3_s2_burst.yml` to hydrate those artifacts before `PR3-S0` refresh,
  3. gate the post-burst snapshot on successful kubeconfig setup,
  4. rerun `PR3-S2` immediately on that corrected evidence-first posture.
## 2026-03-07 07:10:00 +00:00
- The first evidence-first rerun (`22794289341`) cleared setup, kubeconfig, fresh runtime materialization, readiness, and pre-burst snapshot.
- The remaining strict gate miss was not burst behavior but missing `pr2_runbook_index.json` in the hydrated PR2 evidence set.
- Verified by diffing the runner-produced `g3a_preflight_snapshot.json` against the locally validated version: only `DP06_PR2_RUNBOOK_INDEX` flipped from pass to fail.
- Decision held strict: do not weaken `PR3-S0`; instead hydrate the missing PR2 runbook index from S3 and rerun immediately.
## 2026-03-07 07:20:00 +00:00
- Re-read the authoritative green `PR3-S1` summary from S3. The actual clean steady posture was `40 lanes`, `3030 target EPS`, `51.2 speedup`, `ig_push_concurrency=4`, and observed `3025.36 admitted EPS` with zero 4xx/5xx.
- Compared that with the failed burst attempt: `80 lanes`, `6060 target EPS`, `102.4 speedup`, failure at `wsp_lane_48` during ECS task launch.
- Service Quotas reports `Fargate On-Demand vCPU resource count = 140`, which does not match the practical launch failure, so the next burst rerun will be shaped by the proven launch envelope rather than the headline quota.
- Decision: keep the `6000 EPS` burst target, keep the task fan-out at the proven `40` lanes, and raise per-lane load plus push concurrency instead of multiplying task count.
- Also captured an advisory from WSP task logs: the oracle root logs `pack manifest missing` / `pack not sealed`, but because the same signal existed on the clean steady run it is not being treated as the immediate S2 blocker.
## 2026-03-07 07:40:00 +00:00
- Repaired the `PR3-S2` rollup locally against the captured S3-backed burst artifacts so the state now emits a deterministic receipt instead of crashing in the rollup step.
- Current burst impact metrics are now explicit and auditable:
  - admitted burst throughput `3740.961 eps` against target `6000 eps`,
  - `4xx=0`, `5xx=0`, `latency_p95=125.08 ms`, `latency_p99=162.98 ms`,
  - early cutoff triggered because the burst lane never crossed the fail-fast floor.
- Live runtime inspection proved the archive blocker is not synthetic backpressure but a concrete IAM/KMS miss:
  - `fp-pr3-archive-writer` logs show `AccessDenied` on `kms:GenerateDataKey` for CMK `29a7acf2-da57-4b3f-8dd1-d9172d845a5c`,
  - active IRSA role `fraud-platform-dev-full-irsa-rtdl` only carries `ssm-read` and `msk-data-plane`, so archive writes are structurally impossible until KMS rights are added.
- Live Kafka inspection from the RTDL pod proved the burst topic tail carries the current `platform_run_id` but the wrong `scenario_run_id`:
  - `platform_run_id=platform_20260307T071510Z`,
  - `scenario_run_id=a3bd8cac9a4284cd36072c6b9624a0c1`.
- Root cause: the PR3 dispatcher injects `SCENARIO_RUN_ID`, but `fraud_detection.world_streamer_producer.cli` never forwards it to `stream_engine_world()`, so replay falls back to the oracle engine `run_id` and contaminates rerun identity.
- Also reproduced the OFP config path inside the live pod and found the declared `latest` consumer posture is being silently dropped:
  - mounted runtime profile resolves OFP `event_bus_start_position=trim_horizon`, not `latest`,
  - loader mismatch is the reason: `OfpProfile.load()` reads `event_bus.start_position`, while the profile currently pins `wiring.event_bus_start_position`.
- Remediation path chosen:
  1. fix WSP scenario-run propagation,
  2. fix OFP/IEG runtime start-position parsing so fresh cert runtimes honor `latest`,
  3. add RTDL IRSA KMS permission for archive writes,
  4. rerun `PR3-S2` on a fresh runtime after the corrections and then reassess the remaining throughput ceiling.


## 2026-03-07 07:45:17 GMT Standard Time - PR3-S2 remediation validation
- Validated the pending PR3 remediation set end-to-end before any further live action. Python compile checks passed for the replay dispatcher, S2 rollup, WSP CLI, OFP config, IEG config, and IEG projector. `dev_full` profile YAML parsed cleanly and both `IEG` and `OFP` now resolve `start_position=latest`. Terraform runtime config passed `fmt -check` and `validate`.
- Found and fixed one remaining live-path hole: `IEG` still used Kafka `earliest` in `_fill_kafka_buffer()` when no checkpoint existed. That would have invalidated any fresh rerun by re-reading history and recreating run-scope mismatch. Updated the bootstrap path so fresh reads honor the configured `latest` posture.
- Impact on production-readiness: structural remediation is now coherent. The next blockers are live runtime/image drift and RTDL sink IAM, not unresolved code ambiguity.


## 2026-03-07 07:57:07 GMT Standard Time - PR3-S2 live repin and harness blocker
- Applied the missing RTDL IRSA KMS policy live and refreshed `fraud-platform-dev-full-wsp-ephemeral` to revision `:34` on image digest `sha256:f08ec7fc93045df1b28b6b5d5303c04758095d0919a5729f4f1d02767f28640b`.
- Remote rerun `22795069056` failed during burst-lane launch with `PR3.S2.WSP.B01_RUN_TASK_FAILED:wsp_lane_48:You’ve reached the limit on the number of vCPUs you can run concurrently`. This is a WSP harness capacity issue, not a demonstrated platform-path failure.
- Remediation decision: keep the `6000 eps` target and rerun with a denser remote harness shape (fewer tasks, higher per-lane concurrency) rather than diluting the throughput goal.
## 2026-03-07 08:25:00 +00:00
- Re-read the authoritative `PR3-S2` burst rerun artifacts from GitHub Actions run `22795156129` before changing anything else. The state is red for correctness, not load saturation: `7270` requests reached IG, `0` were admitted, `5xx_total=7270`, `4xx_total=0`, `latency_p95=16.23 ms`, `latency_p99=57.47 ms`.
- Tailed the live IG ECS logs to isolate the first invariant break. IG is rejecting the replay envelopes because the workflow-generated `scenario_run_id` has the wrong shape: `scenario_20260307t075139z` does not satisfy the canonical schema regex `^[a-f0-9]{32}$`. The quarantine record path validates the same field and fails too, which explains why the edge emits `503` for every request instead of a bounded quarantine response.
- Important interpretation for production readiness: this is not evidence that the ingress path cannot sustain burst pressure. It is evidence that the certification harness is currently violating the platform contract on run identity and is therefore unable to produce a meaningful admission test at all.
- Decision pinned: do not weaken the IG schema and do not treat this as acceptable quarantine behavior. Fix the source of identity drift in the PR3 workflows, add a fail-fast preflight for canonical `scenario_run_id`, rerun the burst window on the same strict boundary, and only then judge the actual throughput/backpressure posture.
## 2026-03-07 08:40:00 +00:00
- Closed the PR3 identity-generation defect on both the workflow and launcher layers. `PR3-S2` now mints `scenario_run_id` as canonical 32-hex, `PR3-S1` does the same for both runtime materialization and steady execution, and `pr3_wsp_replay_dispatch.py` / `pr3_s1_wsp_replay_dispatch.py` now reject any non-canonical value rather than recreating the old `scenario_<timestamp>` fallback.
- Also fixed a second steady-path propagation hole: `pr3_s1_wsp_replay_dispatch.py` now passes `--scenario-run-id` into the WSP CLI command. Without that, the steady lane could still have defaulted back to engine/run-root identity even after the workflow minted a valid certification scenario id.
- Validation passed locally: YAML parse clean for all touched PR3 workflows and `py_compile` clean for the patched runtime scripts. The next run should therefore test the real platform path again rather than another launcher contract error.

## 2026-03-07 09:05:00 +00:00
- Re-read the successful-but-still-red `PR3-S2` burst rerun (`22795572161`) from the actual impact-metric perspective. Admissions are materially back (`744,984` admitted, `4,138.8 eps`, `5xx=0`, `p95=128.08 ms`, `p99=177.30 ms`), so the remaining red state is not an ingress correctness failure anymore.
- Narrowed the remaining defects to two concrete runtime issues:
  - `fp-pr3-archive-writer` is still denied `s3:PutObject` on the object-store archive path under role `fraud-platform-dev-full-irsa-rtdl`, which means archive persistence is structurally impossible until RTDL IRSA gains scoped object-store write rights.
  - `DF` is red because the PR3 runtime materializer never deploys `context_store_flow_binding`; direct in-pod CSFB queries return `MISSING_BINDING`, which explains the single fail-closed decision and the absence of meaningful AL/DLA surfaces.
- Recorded the production-minded decision in the implementation map: do not rerun again on a known-incomplete runtime graph; first patch PR3 materialization to include CSFB and patch RTDL IRSA object-store permissions through Terraform, then rerun `PR3-S2` on the corrected runtime.

## 2026-03-07 09:25:00 +00:00
- Re-opened the checked-in `dev_full_pr3_s2_burst.yml` before dispatching the rerun and found branch drift on the execution surface itself. The active branch workflow would still mint `scenario_run_id=scenario_<timestamp>` and would still verify readiness without checking the new `fp-pr3-csfb` deployment.
- Production implication: rerunning that file as-is would simply recreate the already-known identity contract defect and could also let the runtime declare ready while the CSFB lane was absent or unhealthy.
- Decision recorded in the implementation map: patch the workflow first, then rerun `PR3-S2` on the corrected branch automation rather than spending more remote compute on a stale launcher surface.

## 2026-03-07 09:35:00 +00:00
- Repinned `dev_full_pr3_s2_burst.yml` defaults to the proven launch-safe burst harness shape from run `22795572161`: `lane_count=48`, `stream_speedup=102.4`, `ig_push_concurrency=8`, keeping the `6000 eps` target unchanged.
- Extended runtime readiness to include `fp-pr3-csfb` so the burst window cannot start on an incomplete runtime graph after the new CSFB materialization fix.

## 2026-03-07 10:05:00 +00:00
- Investigated the remaining PR3-S2 downstream red state directly in-cluster after the archive fix and found the decisive blocker: `fp-pr3-csfb` is crash-looping with `RuntimeError: CSFB_EVENT_BUS_KIND_UNSUPPORTED`.
- Compared CSFB with the rest of the dev_full runtime workers and confirmed this is a component capability gap, not an infra issue. Other workers already support Kafka/MSK through the shared event-bus reader; CSFB still only supports `file` and `kinesis` while the runtime profile pins `event_bus_kind: kafka`.
- Decision recorded in the implementation map: implement Kafka support in CSFB itself, rebuild the runtime image, then rerun PR3-S2 on the same strict boundary.

## 2026-03-07 10:20:00 +00:00
- Patched CSFB intake to support Kafka/MSK using the shared event-bus reader already used by the other dev_full runtime workers.
- Validation passed locally: `py_compile` is clean and loading `config/platform/profiles/dev_full.yaml` with real dev_full env bindings now resolves `event_bus_kind='kafka'` and the expected context topics.
- Important boundary note recorded: the live cluster is still on the old shared runtime image digest, so this fix is not materially active until the image is rebuilt/published and PR3-S2 is rerun.

## 2026-03-07 10:08:14 +00:00
- Corrected the earlier PR3-S2 context-topic diagnosis. The previous "empty context topics" read was a probe error caused by reading Kafka from `latest` without an explicit tail offset. Re-reading the topic tails directly shows the current run is publishing all three required context topics with the correct `platform_run_id` and `scenario_run_id`.
- Verified CSFB is materially healthy for the active PR3-S2 run by querying Aurora directly. Current-run projection state exists in volume (`22,733` join frames, `4,100` complete joins, `11,991` flow bindings), so the active red state is no longer attributable to missing context publication or missing CSFB capability.
- Narrowed the real stop reason to the decision lane. DF has only registered a handful of current-run decisions, all fail-closed and quarantined, `fp.bus.rtdl.v1` remains empty, and AL/DLA are dark because they are starved.
- Confirmed the decisive authority/config drift behind those fail-closed decisions: `config/platform/profiles/dev_full.yaml` still points DF at `config/platform/df/registry_snapshot_local_parity_v0.yaml`, which cannot resolve `environment=dev_full`, so current DF decisions deterministically emit `SCOPE_NOT_FOUND` and `REGISTRY_FAIL_CLOSED`.
- Recorded the production-minded next move in the implementation map: repin DF onto a real `dev_full` registry snapshot, then harden DF burst-window participation before rerunning PR3-S2.

## 2026-03-07 10:42:23 +00:00 — PR3-S2 remediation path pinned from production root-cause analysis
- Confirmed the active PR3 defect has moved fully into the decision lane after ingress/context stabilization.
- Logged three coupled causes that must be fixed together for a production-grade rerun:
  1. dev_full DF still points at `registry_snapshot_local_parity_v0.yaml`.
  2. promoted managed-bundle evidence exists but is not materialized into DF operational runtime scopes.
  3. CONTEXT_WAITING currently synthesizes/publishes fail-closed decisions instead of deferring within join-wait budget.
- Also logged the cold-consumer risk on fresh PR3 runtime identity: `event_bus_start_position`=latest plus rollout-only readiness can cause fresh consumers to miss the burst head and certify only the tail.
- Chosen remediation path:
  1. generate a real dev_full DF registry snapshot from the latest promoted managed-bundle evidence,
  2. change DF to defer on CONTEXT_WAITING until budget expiry,
  3. add application-level warm gating before PR3-S2 burst injection,
  4. then rerun strict PR3-S2 and refresh impact-metric reporting.

## 2026-03-07 10:48:24 +00:00
- Re-verified the pending PR3-S2 remediation edits before any further remote run. The active branch already carries the intended production fixes: DF is repinned to a real `dev_full` registry snapshot, PR3 runtime materialization mounts that snapshot into the deployed profile, DF now defers `CONTEXT_WAITING` instead of synthesizing a premature fail-closed decision, and the PR3 S2 workflow now includes an application-level warm gate before burst injection.
- Validated the most failure-prone design point directly in code: the deferred DF event will actually be retried, because the consumer-checkpoint store now persists the exact current Kafka offset and the Kafka reader seeks to that same offset on the next loop. This avoids the false fix of "deferring" while silently dropping the event.
- Production interpretation: the branch is currently aligned with the correct remediation direction. The next acceptable move is local validation of these edits and then a strict rerun of `PR3-S2`; any further rerun before that would only spend resources on a known-bad decision-lane posture.
## 2026-03-07 11:07:49 +00:00 - PR3-S2 CSFB blocker reframed as replay-idempotency defect
- Pulled live `fp-pr3-csfb` logs after the latest strict PR3-S2 rerun.
- Confirmed the active crash is a duplicate insert into csfb_join_apply_failures (UniqueViolation on `failure_id`), not Kafka/IAM/network failure.
- Production interpretation pinned: the component is failing on a replay of already-recorded failure truth, which must be idempotent under restart/reconsume.
- Next action locked: harden `record_apply_failure()` for duplicate deterministic failure writes, add tests, validate locally, then rerun PR3-S2 on the same strict boundary.
## 2026-03-07 11:11:09 +00:00 - CSFB replay-idempotency remediation implemented and locally validated
- Patched `record_apply_failure()` so duplicate deterministic failure writes become idempotent success instead of crashing `CSFB` on restart/reconsume.
- Added a focused regression test proving the same failure ledger write stays single-row and non-raising under replay.
- Local validation passed:
  - `.venv\Scripts\python.exe -m pytest tests/services/context_store_flow_binding/test_phase2_store.py tests/services/context_store_flow_binding/test_phase3_intake.py tests/services/context_store_flow_binding/test_phase6_observability.py`
  - result: `13 passed`
- Next step: commit/push this remediation, rebuild the runtime image, rerun strict `PR3-S2`, and judge the state by updated impact metrics rather than the previous crash-loop artifact.
## 2026-03-07 12:05:00 +00:00 - PR3-S2 shared Kafka startup-boundary defect pinned before next rerun
- Traced the fresh-run `CSFB/DF` miss beyond warm-gate weakness into the shared Kafka reader used by multiple runtime workers.
- Current behavior on `start_position=latest` with no checkpoint is unsafe: the reader reassigns and re-seeks to the current high watermark on each read call, which can skip records arriving between poll cycles.
- Production consequence: a fresh run-scoped consumer can be `Running` yet still miss the head of the new run, which invalidates both correctness and certification evidence.
- Chosen remediation order:
  1. fix the shared Kafka reader so the initial startup boundary is established once and held across empty polls,
  2. add tests for both standard and MSK/OAUTH reader modes,
  3. expose CSFB surfaces in PR3 runtime evidence and probe CSFB/DF together in the warm gate,
  4. rerun strict `PR3-S2` only after those corrections are validated.
## 2026-03-07 12:18:00 +00:00 - Shared Kafka startup fix and PR3 evidence expansion validated locally
- Implemented the primary runtime correction in `src/fraud_detection/event_bus/kafka.py`: fresh no-checkpoint readers now hold their initial startup boundary across empty polls instead of recomputing `latest` on every read.
- This removes the production-risk pattern where a fresh consumer can skip records arriving between poll cycles under `start_position=latest`.
- Expanded PR3 runtime evidence at the same time:
  - warm gate now probes both `CSFB` and `DF`,
  - component snapshots now include run-scoped `CSFB` metrics/health.
- Local validation passed:
  - `.venv\Scripts\python.exe -m pytest tests/services/event_bus/test_kafka_import_and_auth.py tests/services/decision_fabric/test_worker_runtime.py` -> `9 passed`
  - `py_compile` clean for the patched reader and PR3 scripts.
- Next action: commit/push this milestone, rebuild the runtime image, rerun strict `PR3-S2`, and judge the impact metrics again on the corrected runtime shape.

## 2026-03-07 13:02:00 +00:00 - PR3-S2 narrowed to CSFB global-checkpoint backlog bleed
- Verified from live ECS IG logs that the active PR3 run `platform_20260307T114007Z` is successfully publishing all required context event types to Kafka:
  - `arrival_events_5B -> fp.bus.context.arrival_events.v1`
  - `s1_arrival_entities_6B -> fp.bus.context.arrival_entities.v1`
  - `s3_flow_anchor_with_fraud_6B -> fp.bus.context.flow_anchor.fraud.v1`
- Queried the live CSFB Aurora store from inside the running pod and confirmed the current run still has:
  - `0` join frames
  - `0` flow bindings
  - `0` apply failures
  - `0` dedupe rows
- Reconciled the Kafka offsets and found the actual defect:
  - IG is publishing current-run context around offsets `~1.00M-1.13M`,
  - CSFB checkpoints are still around `~0.79M-0.94M`,
  - so CSFB is replaying older shared-topic backlog and never reaches the active run inside the PR3 burst window.
- Production conclusion:
  - this is not an ingress/context publication failure,
  - it is a run-isolation defect in CSFB checkpoint identity because `CSFB` still uses the global stream id `csfb.v0`.
- Chosen remediation:
  - scope CSFB operational stream/checkpoint identity to `platform_run_id`,
  - validate locally,
  - rebuild the runtime image,
  - rerun strict `PR3-S2` on the corrected run-scoped posture.

## 2026-03-07 13:14:00 +00:00 - CSFB run-scoped checkpoint remediation implemented and validated locally
- Patched `src/fraud_detection/context_store_flow_binding/intake.py` so `CSFB` derives an effective runtime stream id of `csfb.v0::{platform_run_id}` whenever `required_platform_run_id` is present.
- This isolates checkpoints, intake dedupe, and CSFB observability state per active run without deleting any prior-run truth.
- Added targeted proofs:
  - `tests/services/context_store_flow_binding/test_phase1_config.py` verifies deterministic stream-id scoping.
  - `tests/services/context_store_flow_binding/test_phase3_intake.py` proves a fresh run is no longer blocked by a stale prior-run checkpoint.
- Local validation passed:
  - `.venv\Scripts\python.exe -m pytest tests/services/context_store_flow_binding/test_phase1_config.py tests/services/context_store_flow_binding/test_phase3_intake.py` -> `11 passed`
  - `py_compile` clean for the patched inlet and the new tests.
- Next action: commit/push this milestone, rebuild the runtime image, rerun strict `PR3-S2`, and reassess impact metrics with CSFB now consuming the active run instead of historical backlog.

## 2026-03-07 13:24:00 +00:00 - Pinned the remote image-refresh sequence required before the next honest PR3-S2 rerun
- Confirmed the PR3 burst lane has two separate material image surfaces:
  - EKS runtime deployments can take an explicit image URI,
  - the ECS WSP replay family cannot and must be repinned at the task-definition level.
- Confirmed `pr3_wsp_replay_dispatch.py` only overrides command/env/cpu/memory on `run_task`; it does not override container image.
- Chosen sequence for the next execution window:
  1. rebuild the immutable `dev_full` image,
  2. register a fresh `fraud-platform-dev-full-wsp-ephemeral` revision on that digest,
  3. rerun strict `PR3-S2` with the same digest fed into both the EKS runtime and the ECS replay lane.
- Reason: this removes stale-image ambiguity so any remaining red after the rerun is real RTDL/decision-plane behavior, not deployment drift.
## 2026-03-07 14:08:00 +00:00
- Re-read the strict `PR3-S2` evidence after the image refresh and confirmed the active state is no longer a broad ingress/runtime mystery. The red state has reduced to two production defects: the RTDL decision lane is not materially generating current-run downstream traffic, and the ingress burst path still sits below the required `6000 eps` envelope with tail-latency breach.
- Pinned the production interpretation in the implementation map: `CSFB`, `IEG`, `OFP`, and `archive_writer` are materially healthy on the active run, while `DF` still produces zero current-run decisions and therefore leaves `AL`/`DLA` dark. Next work is to fix RTDL materialization first, then retune the burst path on the full end-to-end surface, then rerun strict `PR3-S2` with readable impact-metric reporting.
## 2026-03-07 14:42:00 +00:00
- Completed the live RTDL diagnosis for `PR3-S2`. The partition-head fraud events on all three DF traffic partitions currently resolve to `MISSING_BINDING`, while a wider 300-event sample shows `238` later events would already resolve `READY`. The lane is therefore stuck behind three unresolved head-of-partition events rather than globally missing context.
- Pinned the concrete software defect in the implementation map: DF’s join-wait budget resets on every retry when Kafka `published_at_utc` is absent, so those head blockers defer forever instead of aging into deterministic `CONTEXT_MISSING` outcomes. Chosen remediation is stable first-seen budget tracking in DF plus zero-state run-scoped observability export for AL/DLA before the next strict rerun.
## 2026-03-07 15:05:00 +00:00
- Pinned the exact PR3-S2 code remediation before editing: `DF` will get persistent first-seen timing for per-offset retries so join-wait budgets survive defers/restarts, and `AL`/`DLA` will get explicit zero-state run-scoped observability export so missing files no longer masquerade as runtime failure.
- Reasoning recorded in the implementation map: this preserves the existing bounded-wait policy while removing the partition-head infinite-defer bug, and it does so without adding any new remote dependency or loosening any production threshold.
## 2026-03-07 15:26:00 +00:00
- Implemented the RTDL liveness fix in `decision_fabric.worker`: deferred offsets now persist a stable first-seen timestamp in the same run-scoped SQLite boundary as the consumer checkpoints, and that wait state is cleared on advance. This removes the partition-head infinite-defer bug without loosening the existing bounded-wait policy.
- Implemented the downstream zero-state evidence fix: `action_layer.worker` can now bootstrap run-scoped metrics from startup env scope, and `pr3_rtdl_materialize.py` now injects `AL_SCENARIO_RUN_ID` and `DLA_SCENARIO_RUN_ID` so both downstream lanes can export readable zero-state surfaces during empty or blocked windows.
- Local validation passed cleanly:
  - `pytest tests/services/decision_fabric/test_worker_runtime.py tests/services/decision_fabric/test_phase5_context.py` -> `11 passed`
  - `pytest tests/services/action_layer/test_phase7_observability.py tests/services/action_layer/test_worker_runtime.py tests/services/decision_log_audit/test_dla_phase7_observability.py` -> `10 passed`
  - `py_compile` clean for the patched workers, materialization script, and new tests.
- Next move is now remote only: rebuild/publish the shared runtime image, materialize the corrected runtime, then rerun strict `PR3-S2` on the same boundary.
## 2026-03-07 15:36:00 +00:00
- Rebuilt the shared immutable platform image from commit `9c29b4dbf` through workflow run `22799372854`; authoritative digest is `sha256:ac1f05cd84d7925570693146efa9cd9e23b3b0719f0db2617d2a8cdc0fde97bc`.
- Next live action pinned before rerun: inspect and, if needed, repin ECS family `fraud-platform-dev-full-wsp-ephemeral` to that digest so the canonical replay lane and the EKS RTDL workers both run the same audited artifact during the next strict `PR3-S2` window.
## 2026-03-07 16:05:00 +00:00
- Post-rerun RTDL diagnosis tightened further. `AL`/`DLA` zero-state surfaces are now present, so the missing-surface blocker is cleared. The active DF blocker is now same-batch partition overrun: after a `CONTEXT_WAITING` defer on the head record, DF continues processing later rows from that same partition and overwrites the checkpoint with newer offsets.
- Confirmed this directly in the live DF pod for run `platform_20260307T125447Z`: logs show successive defers on adjacent offsets, checkpoint SQLite has already moved to later offsets, and the wait-state table is empty because this replay surface is carrying valid publish timestamps. Next code fix is therefore stop-on-defer per partition inside `DF.run_once()`.
## 2026-03-07 16:18:00 +00:00
- Implemented the next DF liveness correction after the live PR3-S2 rerun: once a row in a partition returns `CONTEXT_WAITING` (or any other no-advance outcome), `run_once()` now blocks later rows from that same partition for the remainder of the batch. This prevents batch-local checkpoint overwrite of unresolved head records.
- Local validation passed: `pytest tests/services/decision_fabric/test_worker_runtime.py tests/services/decision_fabric/test_phase5_context.py` -> `12 passed`; `py_compile` clean for the patched worker and tests.
- This is the active remediation for the current red state; next remote sequence remains image rebuild -> WSP task family repin -> strict PR3-S2 rerun.
## 2026-03-07 16:42:00 +00:00
- Extracted the second immutable image build from workflow run 22799973701 after the DF partition-order fix. Authoritative runtime digest for the next remote cycle is sha256:9aa8346c4e40e28cd56cc2e68a432666927cbc6b14c6a7de1a4212001c0e6531.
- Pinned the next execution sequence in the implementation map: repin the WSP ECS replay family to this digest, rerun strict PR3-S2 with the same image passed into RTDL materialization, and judge the result first on impact metrics (ps, latency, downstream decision participation) before any further remediation.
## 2026-03-07 13:55:00 +00:00
- Pinned the live PR3-S2 root cause split after the latest rerun. The red state is no longer just “DF is dark”: CSFB can resolve the blocked low_ids as READY, but DF is deriving lossy context-role refs from that response, and the same candidate also resolves DL posture as FAIL_CLOSED from FAILSAFE_HEALTH_GATE with POSTURE_MISSING, POSTURE_STORE_ERROR, and SERVE_SURFACE_ERROR.
- Next remediation order recorded in the implementation map: correct DF context-ref construction first, then repair the DL posture serve/store surface, then rerun strict PR3-S2 on the corrected runtime before any further throughput tuning.
## 2026-03-07 14:12:00 +00:00
- Implemented the next PR3 decision-plane remediation set. CSFB now emits structured context_refs for DF, DF consumes those refs preferentially, PR3 materialization now deploys a dedicated p-pr3-dl workload, warm-gate checks DL posture before injection, and runtime snapshots now capture DL state.
- Corrected the DL worker’s readiness source for remote runtime: if local un_operate status is absent, it now derives consumer-readiness from run-scoped remote checkpoint/lag surfaces (CSFB, IEG, OFP, DLA) instead of fail-closing purely because a local-only status file does not exist.
- Local validation passed: targeted pytest set (16 passed) and py_compile clean on the patched files.
## 2026-03-07 14:18:00 +00:00
- The first PR3-S2 rerun on the corrected decision-plane branch failed at warm gate for the right reason: DL was materially present, but it misclassified a fresh runtime with no current-run traffic yet as FAIL_CLOSED because IEG/OFP/consumer-lag signals had not emitted.
- Corrected DL startup semantics so missing run-scoped remote surfaces are treated as bounded bootstrap-pending rather than degradation during the initial startup window. Targeted DL worker tests passed (4 passed) and py_compile is clean.
## 2026-03-07 17:05:00 +00:00
- Pinned the next strict PR3-S2 execution boundary on digest sha256:756442c29d3c1a87d2195a64abaaae209069324ad882e9c2d8c96dee302bb0ed, which includes the DL bootstrap semantics correction.
- Next sequence is fixed and auditable: repin the ECS WSP replay family to this digest, rerun strict PR3-S2 with the same digest in RTDL materialization, then judge only on impact metrics (warm gate, current-run downstream participation, burst ps/p95/p99/error posture).
- Reason: the prior stop was no longer an ingress or image-mismatch problem; it was a startup-posture classification defect. This rerun must prove whether that defect is cleared before any further throughput tuning.
## 2026-03-07 17:18:00 +00:00
- Strict PR3-S2 rerun on digest sha256:756442c29d3c1a87d2195a64abaaae209069324ad882e9c2d8c96dee302bb0ed failed early and correctly at warm gate, not during burst execution.
- The blocker is now precise: DL bootstrap grace is anchored to platform_run_id creation time instead of worker activation time, so a healthy fresh remote deployment ages into FAIL_CLOSED before first current-run health/lag signals can exist.
- Next remediation is code-level only: keep the bounded bootstrap window, but anchor it to the actual DL worker start boundary and rerun strict PR3-S2 immediately after validation.
## 2026-03-07 17:28:00 +00:00
- Implemented the DL startup-anchor correction: bootstrap grace is now measured from actual worker activation time (bounded by the existing grace policy) rather than only from platform_run_id mint time.
- Added targeted DL worker proof for the stale-run-id startup case and tightened the expired-bootstrap failure test.
- Local validation passed: pytest tests/services/degrade_ladder/test_phase7_worker_observability.py -> 5 passed; py_compile clean.
- Next action is remote-only: commit/push, rebuild the shared image, and rerun strict PR3-S2.
## 2026-03-07 17:52:00 +00:00
- Confirmed and fixed the next RTDL architectural blocker: DL was reading cross-component health from pod-local run files, which are not shared across EKS pods.
- DL now queries shared authoritative surfaces for CSFB, IEG, and OFP, and evaluates readiness on checkpoint freshness plus correctness counters rather than replay watermark age.
- Added targeted shared-surface proof; local validation passed (6 passed, py_compile clean).
- Next action is remote-only: commit/push, rebuild the image, repin WSP ECS, rerun strict PR3-S2.
## 2026-03-07 18:08:00 +00:00
- Verified the latest PR3-S2 DF blocker is currently an evidence bug, not a live runtime bug: the active DF pod reports zero fail-closed and zero quarantine with green health, but the rollup still emits delta=None because the fresh-run pre-snapshot had no metrics file yet.
- Next fix is in scripts/dev_substrate/pr3_s2_rollup.py: treat missing pre-snapshot monotonic counters as zero while keeping missing post evidence fail-closed.
## 2026-03-07 16:40:00 +00:00
- Re-read the latest strict PR3-S2 burst artifact (`22802557396`) and recorded the state summary in impact-metric form:
  - admitted `4634.4 eps` vs target `6000 eps`,
  - request `4634.403 eps`,
  - `4xx=0`, `5xx=1`,
  - latency `p95=136.08 ms`, `p99=189.41 ms`,
  - DF deltas `fail_closed=+1`, `quarantine=+1`.
- Production verdict remains red: the state is not claimable because reliability is not yet perfect and throughput is still below the burst target.
- Confirmed the ingress runtime still has deployment drift: WSP/EKS are on branch digest `sha256:51cc792d74dcb9891454657c4a89c058fe485367e4d20358dd1276f040390808`, but the live ingress ECS service is still on older digest `sha256:843750a949a94a5a0eaf984ce231c0e91e3ced0032b1f3b0bfa4af81514aeb64`.
## 2026-03-07 16:46:00 +00:00
- Verified the DL replay-health symptom is caused by an incomplete RTDL env contract, not by policy logic:
  - inside the active DL pod, `_shared_csfb_snapshot`, `_shared_ieg_status`, and `_shared_ofp_status` all return `null`,
  - direct `IdentityGraphQuery.from_profile('/runtime-profile/dev_full.yaml')` and `OfpObservabilityReporter.build('/runtime-profile/dev_full.yaml')` fail because the DL pod lacks the shared projection/index DSN envs.
- Root cause pinned to `scripts/dev_substrate/pr3_rtdl_materialize.py`: the DL deployment receives `PLATFORM_RUN_ID`, but not the shared authoritative DSNs (`CSFB_PROJECTION_DSN`, `IEG_PROJECTION_DSN`, `OFP_PROJECTION_DSN`, `OFP_SNAPSHOT_INDEX_DSN`) that its replay-health readers need.
- Chosen remediation:
  - repair the DL env contract in RTDL materialization,
  - make shared-surface reader failures explicit in DL diagnostics,
  - repin the ingress ECS service to the current branch digest before the next strict PR3-S2 rerun.
## 2026-03-07 16:55:00 +00:00
- Implemented the PR3-S2 remediation set:
  - `dev_full_pr3_s2_burst.yml` now repins the live ingress ECS service to the resolved branch image and verifies convergence before launch,
  - `pr3_rtdl_materialize.py` now injects `DL_SCENARIO_RUN_ID`, `CSFB_PROJECTION_DSN`, `IEG_PROJECTION_DSN`, `OFP_PROJECTION_DSN`, and `OFP_SNAPSHOT_INDEX_DSN` into the DL pod,
  - `degrade_ladder.worker` now emits shared-surface reader errors into run-scoped metrics/health evidence.
- Validation:
  - `python -m py_compile scripts/dev_substrate/pr3_rtdl_materialize.py src/fraud_detection/degrade_ladder/worker.py` -> clean,
  - workflow YAML parse for `dev_full_pr3_s2_burst.yml` -> clean,
  - `python -m pytest tests/services/degrade_ladder/test_phase7_worker_observability.py` -> `7 passed`.
- Next step is remote-only:
  - commit/push checkpoint,
  - rebuild immutable branch image,
  - rerun strict warmed `PR3-S2` on the same boundary.
## 2026-03-07 15:07:00 +00:00
- Fixed the PR3-S2 rollup integrity defect in two layers: fresh-run missing pre counters now baseline to zero, and the rollup now filters component snapshots to the active manifest `platform_run_id` instead of mixing historical reruns under the same `pr3_execution_id`.
- Added regression coverage in `tests/scripts/test_pr3_s2_rollup.py`; local validation passed (`4 passed`, `py_compile` clean).
- Refreshed the latest S3-backed PR3-S2 manifest, summary, and component snapshots into the local run mirror, then recomputed the receipt on a consistent attempt.
- Truthful PR3-S2 impact metrics for `platform_20260307T144230Z` are now:
  - admitted throughput `4187.033 eps`,
  - latency `p95=132.01 ms`, `p99=183.75 ms`,
  - `4xx=0`, `5xx=1`,
  - downstream deltas clean: `DF/AL/DLA/archive` error-growth deltas `0`, `IEG backpressure delta=0`, `OFP lag p95=0.010s`.
- Result: RTDL is no longer the active red lane for S2. Remaining blockers are now only burst throughput shortfall and one target-side `5xx`.
## 2026-03-07 15:09:00 +00:00
- Pinned the next PR3-S2 remediation in the implementation map before editing the workflow: the current burst lane is under-driving the `6000 eps` contract, not proving a platform ceiling.
- Production reasoning recorded:
  - current measured source density implies `stream_speedup=102.4` is insufficient for a `6000 eps` mission,
  - `ig_push_concurrency=8` is too close to the per-lane in-flight floor for `126.25 eps/lane`,
  - `256 CPU / 1024 MiB` is unnecessarily thin for a 16-thread remote replay task.
- Next burst defaults to be activated on the branch:
  - `stream_speedup=180`,
  - `ig_push_concurrency=16`,
  - replay task `cpu=512`, `memory=2048`.
## 2026-03-07 15:12:00 +00:00
- The first dispatch of the repinned PR3-S2 burst workflow failed at the GitHub control surface because `workflow_dispatch` had grown to `27` inputs while GitHub only allows `25`.
- Resolved immediately by trimming two nonessential inputs:
  - removed unused `min_sample_events`,
  - removed `worker_image_uri` as a dispatch override and kept runtime materialization on the audited current image path.
- This is recorded as a workflow-packaging fix, not a platform blocker. Next action remains the same strict PR3-S2 rerun on the repinned burst posture.
## 2026-03-07 15:37:00 +00:00
- The first strict rerun on the repinned PR3-S2 burst posture failed at ECS launch, not in-platform. `RunTask` hit the live Fargate concurrent-vCPU envelope.
- Verified the account posture directly:
  - Fargate On-Demand quota `140 vCPU`,
  - ingress service was already consuming `32 x 4096 CPU = 128 vCPU`,
  - the burst launcher wanted `48 x 512 CPU = 24 vCPU`, which exceeded remaining headroom.
- Treated this as live capacity-accounting drift, not as evidence of a platform throughput ceiling.
- Immediate remediation for the next strict rerun: keep `stream_speedup=180` and `ig_push_concurrency=16`, but reduce the replay fleet to a quota-safe `44` lanes at `256 CPU`.
## 2026-03-07 15:38:00 +00:00
- Quota-safe strict PR3-S2 rerun completed successfully end-to-end. Authoritative evidence was taken from downloaded GitHub run artifact `22801598029`, not from the stale local run mirror.
- Latest impact metrics for `platform_run_id=platform_20260307T151808Z`:
  - admitted throughput `4575.657 eps`,
  - request throughput `4575.677 eps`,
  - `4xx=6`, `5xx=0`, `error_rate_ratio=4.37e-06`,
  - latency `p95=157.46 ms`, `p99=1638.29 ms`.
- Downstream state:
  - `IEG/OFP/AL/DLA/archive` remained operational on the metrics that matter for S2,
  - `DF` emitted one real `fail_closed` and one real `publish_quarantine`, so RTDL is not fully closed yet.
- State remains red on four blockers: burst throughput shortfall, burst `p99` breach, DF fail-closed, DF quarantine.
## 2026-03-07 15:39:00 +00:00
- Queried ALB `TargetResponseTime` for the authoritative PR3-S2 burst window. The `p99` breach is concentrated in the first full metric minute:
  - `15:23 UTC` `p99 ~= 7.41s`,
  - `15:24..15:27 UTC` `p99 ~= 0.188..0.196s`,
  - request volume stayed roughly flat across those minutes.
- Interpretation pinned in notes: this looks like startup-boundary contamination, not steady-state hot-path collapse.
- Next remediation direction remains strict: harden the pre-burst material-readiness gate so certification timing begins only after current-run context/decision surfaces are warm enough to sustain the burst without first-minute distortion.
## 2026-03-07 15:40:00 +00:00
- Live DF logs for `platform_run_id=platform_20260307T151808Z` show repeated transient waits on current-run `arrival_events` and `flow_anchor`.
- This lines up with the one observed DF fail-closed/quarantine in the artifact and points to a startup-sequencing defect rather than broad RTDL backpressure.
- Next debugging lane: inspect PR3-S2 warm-gate logic and current-run context materialization proof so the burst clock cannot start before DF-relevant context is materially present.
## 2026-03-07 15:48:00 +00:00
- Chose the next PR3-S2 remediation direction: same-run warmup before the measured burst window, instead of a detached primer sub-run.
- Reason pinned in implementation notes:
  - ingress `p99` breach is first-minute and ingress-side,
  - a detached primer would still launch a fresh burst fleet and could recreate the same cold-start connection churn,
  - a same-run warmup lets the replay fleet, ingress path, and current-run context path settle before the certification timer begins.
- Implemented `--warmup-seconds` in `pr3_wsp_replay_dispatch.py` and made the measurement boundary auditable in the summary/manifest.
## 2026-03-07 15:49:00 +00:00
- Repinned the active PR3-S2 burst workflow defaults to the live executable envelope:
  - `lane_count=44`,
  - `wsp_task_cpu=256`,
  - `wsp_task_memory=2048`,
  - `warmup_seconds=90`,
  - keep `stream_speedup=180`, `ig_push_concurrency=16`, `output_concurrency=4`, `http_pool_maxsize=1024`.
- Added a small regression test proving the new measurement-start boundary calculation behaves correctly (`tests/scripts/test_pr3_wsp_replay_dispatch.py`).
- Validation passed:
  - `pytest tests/scripts/test_pr3_s2_rollup.py tests/scripts/test_pr3_wsp_replay_dispatch.py` -> `7 passed`
  - `py_compile scripts/dev_substrate/pr3_wsp_replay_dispatch.py` -> clean
  - workflow YAML parse -> clean
## 2026-03-07 16:03:00 +00:00
- Strict warmed PR3-S2 rerun `22802095265` completed successfully on the same burst boundary with `warmup_seconds=90`.
- Latest authoritative impact metrics from the downloaded artifact:
  - admitted throughput `4675.573 eps`,
  - `4xx=0`, `5xx=1`,
  - latency `p95=138.30 ms`, `p99=189.09 ms`,
  - DF fail-closed/quarantine deltas `0`.
- Warmup correction outcome:
  - first-minute p99 breach cleared,
  - DF current-run correctness issue cleared,
  - remaining blockers reduced to one residual `5xx` and the `6000 eps` throughput shortfall.
- Additional live service evidence:
  - ingress ECS CPU averaged about `52..53%`,
  - ingress ECS memory about `21.9%`,
  - ALB minute-level `p95/p99` stayed flat through the full measured window.
## 2026-03-07 16:07:00 +00:00
- Traced the residual single `5xx` to a concrete ingress code defect, not capacity:
  - log stream `ecs/ig/b54d7a83a11a48aeb06c8542e972fad1`,
  - `ig_publish_failure`,
  - `reason=\"dictionary changed size during iteration\"`,
  - traceback points to `fraud_detection/ingestion_gate/metrics.py` line 35 in `flush_if_due()`.
- Implemented the fix:
  - `MetricsRecorder` now snapshots and clears under a lock before logging,
  - added regression test `tests/services/ingestion_gate/test_metrics.py`.
- Local validation passed:
  - `pytest tests/services/ingestion_gate/test_metrics.py tests/scripts/test_pr3_wsp_replay_dispatch.py tests/scripts/test_pr3_s2_rollup.py` -> `8 passed`
  - `py_compile src/fraud_detection/ingestion_gate/metrics.py scripts/dev_substrate/pr3_wsp_replay_dispatch.py` -> clean
## 2026-03-07 16:10:30 +00:00
- Pinned the next PR3-S2 execution sequence:
  - publish a fresh immutable `dev_full` image carrying the ingress metrics-race fix,
  - verify the strict burst path is using that digest,
  - rerun warmed PR3-S2 unchanged,
  - only after `5xx=0` is re-proved should the remaining throughput gap be treated as the sole active blocker.
- Reason: the latest warmed receipt still reflects the pre-fix image, so additional interpretation of that run cannot prove the reliability issue is closed.
## 2026-03-07 16:14:00 +00:00
- Packaging run `22802473444` succeeded for the current branch head and produced immutable digest `sha256:b28eaa25b5a936107ded7ab738ed6db586530a816d656ff01a9065c4206536aa`.
- Confirmed the next PR3-S2 honesty gap is deployment drift, not code uncertainty: the workflow still resolves EKS from the ECS family by default and does not itself force the WSP ECS family to the fresh digest.
- Next correction is workflow-only and remote-only: resolve the latest current-branch image from ECR inside PR3-S2, repin `fraud-platform-dev-full-wsp-ephemeral` if needed, and pass the same digest explicitly into RTDL materialization before the burst window starts.
## 2026-03-07 17:12:00 +00:00
- Downloaded and re-read the authoritative artifact bundle from warmed strict PR3-S2 rerun `22802974989` instead of trusting stale local mirrors. The state is now narrowed to one real blocker: `PR3.S2.B14_BURST_THROUGHPUT_SHORTFALL` with `observed_admitted_eps=4554.7` against the `6000.0 eps` target.
- Impact metrics for the active certified window are:
  - `request_eps=4554.7`,
  - `admitted_eps=4554.7`,
  - `request_count_total=1,366,410`,
  - `4xx=0`,
  - `5xx=0`,
  - latency `p95=129.93 ms`, `p99=179.73 ms`.
- Downstream impact posture is now green on the same run:
  - `DF fail_closed delta=0`,
  - `DF quarantine delta=0`,
  - `AL publish ambiguity/quarantine deltas=0`,
  - `IEG backpressure delta=0`,
  - `OFP lag p95=0.017 s`,
  - `DLA checkpoint age p95=1.109 s`,
  - archive writer error delta `0`.
- Production interpretation recorded explicitly: PR3-S2 no longer has an ingress correctness problem or an RTDL correctness problem. The platform accepted every generated request cleanly, but the canonical remote WSP replay fleet still under-drove the burst contract by roughly `1445 eps`.
- Next active work is therefore source-driving analysis and tuning on the canonical replay lane, not more ingress scaling or policy relaxation.
## 2026-03-07 17:28:00 +00:00
- Closed the source-density question using oracle-store evidence. The active replay set has baseline density about `152.19 eps`; at `stream_speedup=180` that is about `27,394 eps` theoretical replay capacity, so the oracle data itself is not the reason PR3-S2 is capped at `4554.7 eps`.
- Pulled live WSP lane logs from the latest burst run and measured actual per-lane task output. The 44-lane fleet is materially producing, but only around `82..135 eps` per lane with median about `91 eps` and mean about `102.5 eps`, well below the configured lane target `137.7 eps`.
- The task logs show the real problem: once hot, each WSP task advances simulated event time much slower than the configured `180x`, which means task-side compute/runtime overhead is dominating over replay sleeps.
- Cross-plane quota posture is now explicit:
  - ingress desired count remains `32`,
  - ingress CPU during the measured burst window stayed near `44%`,
  - therefore the dev account is over-allocating Fargate vCPU to ingress and under-allocating it to the WSP certification fleet.
- Chosen next correction for the next strict PR3-S2 rerun:
  - right-size ingress desired count to `30`,
  - move WSP burst lanes from `44 x 256 CPU` to `32 x 512 CPU`,
  - keep replay semantics and thresholds unchanged so the next impact delta measures compute-shape correction, not contract drift.
## 2026-03-07 17:34:00 +00:00
- Implemented the first throughput-only remediation on the branch:
  - `dev_full_pr3_s2_burst.yml` now defaults to `32` WSP lanes at `512 CPU`,
  - the same workflow now right-sizes the ingress ECS service to `desired_count=30` and waits for steady state before launching the burst lane,
  - Terraform default `ig_service_desired_count` is repinned to `30` so runtime authority no longer drifts from the new live posture.
- Local validation passed before push:
  - workflow YAML parse clean,
  - `terraform fmt -check infra/terraform/dev_full/runtime/variables.tf` clean.
- Next action is branch push plus an immediate strict PR3-S2 rerun on the rebalanced runtime shape.
## 2026-03-07 17:38:00 +00:00
- The first rerun on branch head `921e5b9f0` failed before any live runtime step at `Resolve current-branch immutable platform image`. Root cause: that commit changed only workflow/docs/Terraform posture, so there is no new immutable ECR image tagged to the SHA.
- Treated this as a workflow control-surface defect, not as a platform blocker. The active runtime image is still the audited digest `sha256:50d9953e34433457ce556988b496fa0bf36fa4dbea119d96640d37427b5a33e9`.
- Implemented the strict correction in the workflow:
  - if a current-SHA image exists, use it as before,
  - if no current-SHA image exists, allow fallback only when the commit is image-neutral (`.github/workflows/`, `docs/`, `infra/terraform/` only),
  - fallback image must be the active WSP task-family image and must already be digest-pinned.
- This preserves fail-closed behavior for real runtime-code commits while letting workflow-only posture corrections execute without an unnecessary packaging detour.
## 2026-03-07 17:46:00 +00:00
- The second PR3-S2 rerun attempt still failed before runtime execution, but the control-surface defect is now narrower: the workflow correctly entered the image-neutral fallback branch, then rejected the fallback task-family image because it surfaced as `repo:tag` instead of `repo@sha256:...`.
- This is not a reason to weaken certification rigor. The correct production fix is to resolve the fallback tag through ECR and reconstruct an immutable digest-pinned URI.
- Chosen correction now in the branch working tree:
  - keep image-neutral fallback eligibility exactly as before,
  - if fallback image is tag-based, call `aws ecr describe-images` on that tag,
  - rebuild the runtime reference as `repo@sha256:<digest>`,
  - fail closed if the digest cannot be resolved.
- Why this matters: PR3 evidence must remain tied to exact runtime bits even when the branch head changed only workflow/docs/Terraform posture. This closes the workflow-only defect without introducing mutable image drift.
## 2026-03-07 17:58:00 +00:00
- Reproduced the PR3-S2 image resolver locally against live AWS and confirmed the code path is sound; the remaining defect is authority choice, not syntax.
- The failed workflow run proved the fallback ECS task-family metadata can surface a tag reference whose tag no longer resolves in ECR, even though valid immutable git-tagged images still exist in the repository.
- Queried ECR directly and confirmed the latest immutable git-tagged package remains available at `sha256:50d9953e34433457ce556988b496fa0bf36fa4dbea119d96640d37427b5a33e9`.
- Correction now being applied:
  - keep current-SHA lookup first,
  - for image-neutral commits, fall back to the latest immutable git-tagged ECR digest,
  - treat ECS family metadata only as a secondary hint,
  - still fail closed if no immutable digest can be found.
## 2026-03-07 18:06:00 +00:00
- The PR3-S2 image resolver still failed opaquely after the fallback-authority correction, so I treated diagnosability itself as the next workflow defect.
- Hardened the resolver in two ways:
  - query ECR with `tagStatus=TAGGED` so the candidate set contains only still-tagged immutable packages,
  - emit explicit stderr messages for every fail-closed branch and print the chosen resolver mode/digest/URI before writing GitHub outputs.
- The next rerun should now either execute the burst window or expose the exact remaining resolver failure string.
## 2026-03-07 18:14:00 +00:00
- Added explicit immutable-image pinning support to `dev_full_pr3_s2_burst.yml` via optional input `platform_image_uri`.
- Reason: auto-resolution remained a GitHub control-surface ambiguity; explicit digest pinning is the strongest and cleanest certification authority.
- New resolver order:
  - explicit `repo@sha256:...` input,
  - current-SHA image,
  - latest immutable git-tagged ECR package for image-neutral commits,
  - ECS family fallback.
- Next PR3-S2 rerun will be pinned directly to the latest audited immutable digest `sha256:50d9953e34433457ce556988b496fa0bf36fa4dbea119d96640d37427b5a33e9`.
## 2026-03-07 18:20:00 +00:00
- Hit GitHub's hard `workflow_dispatch` limit of `25` inputs after adding `platform_image_uri`.
- Removed `eks_namespace` as a workflow input and pinned it internally to the canonical namespace `fraud-platform-rtdl`.
- This restores dispatch validity while keeping the explicit immutable-image pin available for PR3-S2 reruns.
## 2026-03-07 18:32:00 +00:00
- The first fully executing canonical PR3-S2 burst rerun (`22803961969`) proved the throughput contract can now be met on the intended runtime path:
  - `observed_admitted_eps=6045.043`,
  - `request_count_total=1,813,513`,
  - `admitted_request_count=1,813,513`,
  - `4xx=0`,
  - `5xx=0`,
  - latency `p95=332.956 ms`.
- The run remains red for two measured reasons only:
  - ingress tail latency breached with `p99=851.863 ms` against a `700 ms` ceiling,
  - DF emitted `2` fail-closed and `2` quarantine outcomes during the burst window.
- Live ingress ECS metrics explain the tail issue. With desired count reduced to `30`, service CPU sat around `71.7..72.5%` average and `93.2..94.5%` max during the certified window. That is enough to degrade p99 without introducing 4xx/5xx, so the earlier ingress right-size was too aggressive.
- Live DF pod logs show the remaining RTDL blocker is no longer registry drift; it is burst-time context skew, with repeated `CONTEXT_WAITING:arrival_events` and `CONTEXT_WAITING:flow_anchor` under current-run traffic.
- Chosen remediation for the next strict rerun:
  - restore ingress desired count back to `32`,
  - repin DF budgets from `decision_deadline_ms=1500` / `join_wait_budget_ms=900` to `3000` / `2000`.
- Production interpretation: the platform has now proven it can physically absorb the `6000 eps` burst target, but it is not yet production-ready because the tail-latency budget and RTDL decision materialization budget are still too tight for that posture.
## 2026-03-07 18:15:00 +00:00
- The next PR3-S2 rerun (`22804342899`) failed before a valid burst measurement could complete. The burst dispatcher raised:
  - `PR3.S2.WSP.B01_RUN_TASK_FAILED:wsp_lane_24:You’ve reached the limit on the number of vCPUs you can run concurrently`.
- This is a concrete Fargate capacity-accounting issue, not an application regression:
  - account Fargate On-Demand vCPU quota in `eu-west-2` is `140`,
  - ingress service at `32 x 4096 CPU` reserves `128 vCPU`,
  - PR3-S2 burst fleet at `32 x 512 CPU` needs another `16 vCPU`,
  - total requested posture is `144 vCPU`, which necessarily breaches the current account quota before lane `24`/`25` can start.
- Production reasoning:
  - reverting to `30` ingress tasks would avoid the quota error but would knowingly reintroduce the tail-latency headroom defect already measured at `p99=851.863 ms`,
  - weakening the lane count would risk giving back the newly proven `6000 eps` burst contract,
  - the right immediate correction is to make the PR3-S2 right-size step quota-aware so ingress scales only to the highest count that still leaves room for the full 32-lane replay fleet.
- Chosen control update:
  - keep authority target at `32`,
  - compute the quota-limited ingress ceiling from live Fargate quota plus lane CPU shape,
  - fail closed if the computed count drops below the minimum evidenced viable ingress headroom (`31`),
  - persist the quota calculation as a run artifact so the posture is auditable.
