## 2026-03-06


### 2026-03-06 02:01:22 +00:00 - Readable findings summaries normalized across PR1/PR2/main plan
- Trigger: USER requested continuation with strict preference for analytical summary tables instead of JSON-pointer snapshot tables.
- Documentation updates completed:
  - docs/model_spec/platform/implementation_maps/dev_substrate/dev_full/road_to_prod/platform.PR2.road_to_prod.md
  - docs/model_spec/platform/implementation_maps/dev_substrate/dev_full/road_to_prod/platform.road_to_prod.plan.md
  - docs/model_spec/platform/implementation_maps/dev_substrate/dev_full/road_to_prod/platform.PR1.road_to_prod.md
- Findings format standardized to: Area | What was found | Interpretation.
- Residual old-style PR1 cross-state ledger removed and replaced with summary-style analytical findings.
- Governance: no branch operation, no commit, no push.

### 2026-03-06 02:05:10 +00:00 - Pre-edit plan pinned for PR3 authority document
- USER request: plan out PR3 and create its own authority doc.
- Planned deliverables:
  - create docs/model_spec/platform/implementation_maps/dev_substrate/dev_full/road_to_prod/platform.PR3.road_to_prod.md
  - update platform.road_to_prod.plan.md PR3 block + immediate-next-step routing.
- PR3 planning scope pinned as fail-closed S0..S5 (preflight, steady, burst, recovery, soak+drills, rollup/verdict).
- Design authority source pinned to G3A section (10A) in dev-full_road-to-production-ready.md.
- Governance: no branch operation, no commit/push.

### 2026-03-06 02:08:14 +00:00 - PR3 planning authority materialized and main-plan routing updated
- Added PR3 authority doc:
  - docs/model_spec/platform/implementation_maps/dev_substrate/dev_full/road_to_prod/platform.PR3.road_to_prod.md
- PR3 doc now defines fail-closed runtime certification plan (S0..S5) with mandatory scorecard windows, cohorts, drills, blocker map, artifacts, budgets, rerun boundaries, and DoD.
- Main plan updated to:
  - reference PR3 authority in PR3 phase block,
  - mark PR3 as PLANNED,
  - route immediate next step to main plan + PR3 authority.
- Main plan digest standard aligned to summary format (Area | What was found | Interpretation) to match agreed reporting posture.
- Governance: no branch operation, no commit, no push.

### 2026-03-06 02:13:23 +00:00 - Pre-edit plan pinned for PR3-S0 strict execution
- USER direction: execute PR3-S0 fail-closed from strict upstream pr2_20260305T200521Z.
- Planned code work:
  - add scripts/dev_substrate/pr3_s0_executor.py
  - enforce strict upstream lock and emit S0 artifacts under deterministic PR3 run root.
- Planned artifact outputs:
  - pr3_entry_lock.json, g3a_run_charter.active.json, g3a_measurement_surface_map.json, g3a_preflight_snapshot.json, g3a_archive_sink_design_decision.json, pr3_s0_execution_receipt.json, pr3_latest.json.
- Planned docs sync:
  - update PR3 execution record + S0 findings summary,
  - update main plan immediate next step and S0 findings summary.
- Governance: no branch operation, no commit/push.

### 2026-03-06 02:20:46 +00:00 - PR3-S0 executed from strict upstream pr2_20260305T200521Z
- Added executor: scripts/dev_substrate/pr3_s0_executor.py.
- Validation: python -m py_compile scripts/dev_substrate/pr3_s0_executor.py passed.
- Executed:
  - python scripts/dev_substrate/pr3_s0_executor.py --upstream-pr2-execution-id pr2_20260305T200521Z
- Run root:
  - runs/dev_substrate/dev_full/road_to_prod/run_control/pr3_20260306T021900Z/
- S0 result:
  - verdict=PR3_S0_READY, open_blockers=0, next_state=PR3-S1, next_gate=PR3_RUNTIME_S0_READY.
- Checks:
  - B01..B06 all true.
  - Evidence-only dependency preflight checks passed (8/8).
- Documentation sync:
  - updated PR3 authority execution record + 11.1 PR3-S0 Findings Summary (Readable).
  - updated main plan immediate-next-step to PR3-S1 + 10.11 PR3-S0 Findings Summary (Readable).
  - updated target snapshot status for TGT-08/TGT-09 to IN_PROGRESS.
- Runtime/cost posture:
  - elapsed=0.0 min vs budget 20; spend=0.0 USD vs envelope 250.0.
- Governance: no branch operation, no commit/push.

### 2026-03-06 02:25:25 +00:00 - Pre-edit plan pinned for PR3-S1 strict execution
- Upstream boundary pinned to pr3_20260306T021900Z (PR3_S0_READY).
- Planned work:
  - add scripts/dev_substrate/pr3_s1_executor.py and run fail-closed.
  - emit S1 artifacts and receipt under PR3 run root.
- Pre-execution evidence finding:
  - available candidate throughput evidence is ~49.49 eps vs PR3 steady target 3000 eps; likely S1 blocker unless stronger steady evidence exists.
- Documentation plan:
  - update PR3/main-plan S1 findings with human-readable interpretation against S1 goal.
- Governance: no branch operation, no commit/push.

### 2026-03-06 02:44:07 +00:00 - PR3-S1 fresh steady evidence run + strict rerun
- USER direction: run fresh RC2-S steady evidence with declared `IG_ADMITTED_EVENTS_PER_SEC`, full `p95/p99/error` metrics, sample minima met, then rerun S1 on strict boundary `pr3_20260306T021900Z`.
- Implemented collector:
  - `scripts/dev_substrate/pr3_s1_fresh_steady_evidence.py`
  - Evidence sources are managed AWS telemetry and charter-window recompute:
    - Athena recompute on `fraud_platform_dev_full_m15.m15b_s3_event_stream_with_fraud_6b` over `2026-02-26T00:00:00Z`..`2026-03-05T00:00:00Z`.
    - CloudWatch API Gateway (`ApiId=ehwznd2uw7`, `Stage=v1`) latency `p95/p99`, request count, `4xx`, `5xx`.
- Fresh evidence emitted under strict PR3 run root:
  - `runs/dev_substrate/dev_full/road_to_prod/run_control/pr3_20260306T021900Z/g3a_steady_evidence_fresh.json`
  - `runs/dev_substrate/dev_full/road_to_prod/run_control/pr3_20260306T021900Z/g3a_steady_evidence_fresh_summary.json`
- Fresh evidence outcomes:
  - `sample_size_events=37113583` (minima met vs `5400000`)
  - `observed_events_per_second=61.365051256613754`
  - `measurement_surface=IG_ADMITTED_EVENTS_PER_SEC`
  - `decision_latency_ms_p95=31.850397584645574`, `decision_latency_ms_p99=64.53425226248608`
  - `error_rate_pct_observed=3.9144009130783988` (`error_rate_ratio=0.03914400913078399`)
- Rerun sequence:
  - first rerun was launched in parallel with evidence generation and evaluated before evidence landed (race); immediately rerun sequentially with explicit fresh evidence refs.
  - final S1 rerun receipt:
    - `runs/dev_substrate/dev_full/road_to_prod/run_control/pr3_20260306T021900Z/pr3_s1_execution_receipt.json`
    - `verdict=HOLD_REMEDIATE`, `open_blockers=1`, blocker=`PR3.B10_STEADY_THRESHOLD_BREACH`.
  - cleared checks in final rerun: `B07=true`, `B08=true`, `B09=true`, `B11=true`; only `B10=false`.
- Human-readable findings updates applied:
  - `docs/model_spec/platform/implementation_maps/dev_substrate/dev_full/road_to_prod/platform.PR3.road_to_prod.md`
  - `docs/model_spec/platform/implementation_maps/dev_substrate/dev_full/road_to_prod/platform.road_to_prod.plan.md`
- Governance: no branch operations, no commit, no push.

### 2026-03-06 02:44:44 +00:00 - PR3-S1 strict rerun replayed on same boundary (fresh evidence reaffirmed)
- Re-executed fresh steady collector on strict boundary `pr3_20260306T021900Z`:
  - `python scripts/dev_substrate/pr3_s1_fresh_steady_evidence.py --pr3-execution-id pr3_20260306T021900Z`
- Re-executed S1 with explicit fresh evidence refs:
  - `python scripts/dev_substrate/pr3_s1_executor.py --pr3-execution-id pr3_20260306T021900Z --steady-evidence-ref runs/dev_substrate/dev_full/road_to_prod/run_control/pr3_20260306T021900Z/g3a_steady_evidence_fresh.json --steady-evidence-summary-ref runs/dev_substrate/dev_full/road_to_prod/run_control/pr3_20260306T021900Z/g3a_steady_evidence_fresh_summary.json`
- Confirmed metrics remained stable:
  - `IG_ADMITTED_EVENTS_PER_SEC=61.365051256613754`
  - `p95=31.850397584645574ms`, `p99=64.53425226248608ms`
  - `error_rate_pct=3.9144009130783988`
  - `sample_size_events=37113583` vs minima `5400000` (pass)
- S1 verdict remained fail-closed on one blocker:
  - `verdict=HOLD_REMEDIATE`, `open_blockers=1`, `PR3.B10_STEADY_THRESHOLD_BREACH`
- Governance: no branch ops, no commit, no push.

### 2026-03-06 03:14:29 +00:00 - PR3-S1 speedup remediation plan pinned before implementation
- USER direction: "Document this and proceed" after stream-speedup gap confirmation.
- Pinned decision: telemetry-only `PR3-S1` cannot clear `B10`; active managed load lane is required.
- Chosen execution posture:
  - no local load generation,
  - use in-cloud ECS Fargate one-shot tasks on `fraud-platform-dev-full-wsp-ephemeral` with public subnets,
  - keep strict PR3 boundary `pr3_20260306T021900Z` and write all artifacts under that run root.
- Added implementation-map planning entry in:
  - `docs/model_spec/platform/implementation_maps/dev_substrate/dev_full/road_to_prod/platform.road_to_prod.impl_actual.md`.
- Governance: no branch ops, no commit/push.

### 2026-03-06 03:58:12 +00:00 - PR3-S1 runaway run contained; fail-fast controls added
- USER escalated valid resource-waste concern: bottleneck was detected too late and run continued.
- Immediate containment:
  - stopped active ECS load tasks in raud-platform-dev-full-wsp-ephemeral.
  - confirmed RUNNING=0, PENDING=0.
- Remediation implemented in scripts/dev_substrate/pr3_s1_managed_speedup_dispatch.py:
  - heartbeat telemetry during load execution,
  - polling-time aggregate EPS check,
  - early-cutoff throughput floor gate,
  - early-cutoff telemetry-coverage gate,
  - automatic ECS stop of running tasks on cutoff.
- Result: PR3-S1 speedup lane now fail-fast by design instead of waiting full duration.
- Governance: no branch operations, no commit, no push.

### 2026-03-06 04:05:28 +00:00 - PR3-S1 blocker diagnosis clarified
- Primary issue confirmed: ingress throttling (429) is suppressing admitted EPS.
- Measured shortfall on calibration: observed admitted EPS 214.180 vs fail-fast floor 490.000.
- Corrected tooling semantics: forced early-cutoff stop (xit_code=137) is now classified as intentional stop, not lane-failure blocker.

### 2026-03-06 04:18:00 +00:00 - Stack correction pass started for WSP + IG
- USER directed immediate resolution of stack drift and ingress envelope mismatch.
- Live runtime facts captured before edits:
  - API Gateway stage `v1` on `ehwznd2uw7` is currently `200 rps / 400 burst`.
  - Lambda `fraud-platform-dev-full-ig-handler` is `256 MB`, `30s timeout`, no explicit reserved concurrency.
  - EKS nodegroup `fraud-platform-dev-full-m6f-workers` is currently scaled down to `desired/min/max=0/0/2`.
  - Managed Flink currently materialized app: `fraud-platform-dev-full-rtdl-ieg-ofp-v0` (`READY`).
- Correction scope pinned:
  - repin WSP away from ECS one-shot authority toward managed Flink primary posture,
  - repin IG baseline contract from `200/400` to `3000/6000`,
  - make lambda envelope explicit in Terraform/workflow authority.
- Governance: no branch operations, no commit, no push.

### 2026-03-06 04:29:00 +00:00 - Stack correction pass implemented and validated
- Authority files updated to remove WSP/ECS drift and align IG baseline envelope with cert target:
  - `WSP_RUNTIME -> MSF_MANAGED_PRIMARY`
  - `IG_RATE_LIMIT_RPS/BURST -> 3000/6000`
  - lambda envelope pinned to `1024 MB / reserved concurrency 300 / timeout 30s`
- Runtime IaC updated:
  - Terraform defaults now carry the repinned IG limits,
  - lambda memory / reserved concurrency / timeout are explicit vars and outputs,
  - lambda preconditions added for sane envelope enforcement.
- Managed uplift workflow updated:
  - `dev_full_rc2_r2_capacity_envelope.yml` now applies and verifies lambda envelope in addition to APIGW throttle.
- Validation:
  - `python -m py_compile scripts/dev_substrate/pr3_s1_managed_speedup_dispatch.py` passed.
  - `terraform fmt -check` and `terraform validate` passed for `infra/terraform/dev_full/runtime`.
  - workflow YAML parse passed.
- Remaining gap:
  - managed WSP runtime is now the authority posture, but live WSP managed materialization is not yet proven; only RTDL managed Flink app is currently present.
- Governance: no branch operations, no commit, no push.

### 2026-03-06 05:31:37 +00:00 - Production-first correction applied to WSP/PR3-S1 blocker analysis
- USER corrected blocker-handling posture for road-to-prod:
  - do not optimize for the easiest green rerun,
  - optimize for the production-grade architecture that would be recommended under real financial-institution load,
  - document the actual problem, contributing areas, and competing solutions before selecting a remediation.
- Actual problem recorded:
  - `PR3-S1` is not merely missing a successful rerun lane,
  - the repo/runtime has conflated:
    - `WSP` as ingress replay producer,
    - `Managed Flink` as stream-processing runtime,
    - ad hoc pressure harnesses for certification,
  - which created an invalid search for a `WSP` managed-Flint app.
- Evidence basis for the correction:
  - actual `WSP` code is Python HTTP replay into `IG`, not Flink:
    - `src/fraud_detection/world_streamer_producer/runner.py`
    - `src/fraud_detection/world_streamer_producer/ready_consumer.py`
  - actual `WSP` already contains `stream_speedup` pacing support:
    - `src/fraud_detection/world_streamer_producer/config.py`
  - live AWS currently has only the RTDL managed Flink app:
    - `fraud-platform-dev-full-rtdl-ieg-ofp-v0`
  - authority docs are internally drifted:
    - same design authority says both `WSP on ECS/Fargate` and `WSP_RUNTIME_MODE=MSF_MANAGED_PRIMARY`.
- Production-option evaluation recorded:
  - forcing `WSP` onto `MSF` rejected as wrong abstraction for an HTTP side-effecting replay producer,
  - keeping the synthetic ECS harness rejected as noncanonical,
  - bypassing `IG` rejected because it breaks the `via_IG` claim path,
  - accepted direction: treat `WSP` as a distributed remote ingress replay service and keep Managed Flink canonical for `IEG/OFP/RTDL`.
- Immediate consequence:
  - `PR3-S1` remediation is now treated as a runtime-architecture correction, not as a simple threshold rerun.
- Governance:
  - no branch operation,
  - no commit/push.

### 2026-03-06 05:31:37 +00:00 - MSK control-bus gap identified on the canonical WSP path
- While wiring the real remote `WSP` path, discovered that current control-bus implementations for `SR`/`WSP` only support:
  - `file`
  - `kinesis`
- This conflicts with `dev_full` authority, which pins control on `fp.bus.control.v1` over MSK.
- Production implication:
  - a direct-launch or non-MSK workaround would preserve neither the declared READY-trigger semantics nor the real control-plane shape.
- Chosen direction:
  - add Kafka/MSK control-bus integration using the repo's existing Kafka adapter surfaces rather than bypassing the problem.
- Governance:
  - no branch operation,
  - no commit/push.

### 2026-03-06 05:31:37 +00:00 - Kafka control-bus support landed for SR/WSP; dev_full WSP profile expanded
- Implemented Kafka/MSK control-bus support:
  - `src/fraud_detection/scenario_runner/bus.py`
  - `src/fraud_detection/scenario_runner/runner.py`
  - `src/fraud_detection/world_streamer_producer/control_bus.py`
  - `src/fraud_detection/world_streamer_producer/ready_consumer.py`
- Expanded `config/platform/profiles/dev_full.yaml` so remote `WSP` has the missing production-relevant wiring:
  - oracle store refs,
  - live `IG` ingest URL,
  - Kafka control bus,
  - Postgres checkpoints,
  - producer identity,
  - API-key auth posture,
  - retry knobs,
  - traffic/context output refs.
- Fixed `WSP` profile env-resolution bug in `src/fraud_detection/world_streamer_producer/config.py`:
  - `${VAR:-default}` placeholders now resolve correctly.
- Validation:
  - `py_compile` passed for the touched `SR`/`WSP` modules,
  - `WspProfile.load('config/platform/profiles/dev_full.yaml')` now resolves:
    - `control_bus_kind=kafka`
    - `control_bus_topic=fp.bus.control.v1`
    - `ig_ingest_url=https://ehwznd2uw7.execute-api.eu-west-2.amazonaws.com/v1/ingest/push`
    - `checkpoint_backend=postgres`
    - `stream_speedup=1.0`
- Remaining gap:
  - authority docs still need the deliberate `WSP` runtime correction,
  - `PR3-S1` still needs the canonical remote launch lane for the real `WSP`.
- Governance:
  - no branch operation,
  - no commit/push.

### 2026-03-06 05:31:37 +00:00 - WSP authority repinned to the accepted production runtime shape
- Corrected active WSP runtime pins in:
  - `docs/model_spec/platform/pre-design_decisions/dev-full_managed-substrate_migration.design-authority.v0.md`
  - `docs/model_spec/platform/migration_to_dev/dev_full_handles.registry.v0.md`
  - `docs/model_spec/platform/implementation_maps/dev_substrate/dev_full/build/platform.M6.build_plan.md`
  - `docs/model_spec/platform/implementation_maps/dev_substrate/dev_full/build/platform.M14.build_plan.md`
- Active forward WSP pin is now:
  - `ECS_FARGATE_RUN_SCOPED_REPLAY_JOB`
- Bounded fallback wording is now:
  - `EKS_REPLAY_JOB_EXCEPTION_ONLY`
- Historical `M14` references to `ECS_FARGATE_RUNTASK_EPHEMERAL` were left intact as history rather than rewritten.
- Remaining gap:
  - build the canonical remote launch lane for the real `WSP`,
  - then reroute `PR3-S1` to it.
- Governance:
  - no branch operation,
  - no commit/push.

### 2026-03-06 05:46:02 +00:00 - PR3-S1 execution shape corrected toward the real production lane
- Corrected the framing of the problem:
  - not "how do we rerun PR3-S1 cheaply",
  - but "what is the production-correct way for this platform to generate 3000 eps through the real ingress path?"
- Verified live ingress envelope is already uplifted:
  - API Gateway stage `v1` throttle = `3000 rps / 6000 burst`,
  - `fraud-platform-dev-full-ig-handler` = `1024 MB`, timeout `30 s`, reserved concurrency `300`.
- Chosen direction:
  - retain `READY` semantics as a control compatibility proof,
  - use real remote `WSP` replay as the canonical steady-window injector for `PR3-S1`,
  - keep Managed Flink scoped to stream-processing lanes only.
- Rejected:
  - synthetic loader as canonical proof,
  - `WSP` as Managed Flink,
  - local orchestration for certification.

### 2026-03-06 05:46:02 +00:00 - Real oracle-rate calibration captured for canonical WSP replay
- Measured the active oracle stream boundaries directly from S3 parquet:
  - engine root: `s3://fraud-platform-dev-full-object-store/oracle-store/local_full_run-7/a3bd8cac9a4284cd36072c6b9624a0c1`
  - scenario id in stream rows: `baseline_v1`
  - `s3_event_stream_with_fraud_6B`:
    - rows `473,383,388`,
    - first `2026-01-01T00:00:00.001940Z`,
    - last `2026-04-01T00:01:41.104298Z`,
    - natural average about `60.9 eps`.
- Using the realistic four-output `WSP` set, estimated natural aggregate rate at about `152 eps`.
- First-pass steady calibration decision:
  - target `3000 eps`,
  - initial `stream_speedup ~= 19.7`,
  - one real remote `WSP` task is the correct first proof before inventing fanout.

### 2026-03-06 05:52:27 +00:00 - Production-fit reassessment locked the correct WSP question
- Corrected the decision lens after user escalation:
  - not "how do we make `PR3-S1` rerun",
  - but "what replay/runtime shape would a real institution use to preserve realistic ingress semantics at `3000 eps`?"
- Rejected "Managed Flink for `WSP`" on production-fit grounds:
  - `WSP` is the outside-world replay emitter into `IG`,
  - `Managed Flink` remains the stream-processing substrate for `IEG/OFP/RTDL`.
- Reaffirmed the accepted direction:
  - remote managed replay workers for `WSP`,
  - canonical proof path remains `WSP -> IG`.

### 2026-03-06 05:52:27 +00:00 - New WSP producer bottleneck recorded before more pressure spend
- Identified a material producer-side risk in `src/fraud_detection/world_streamer_producer/runner.py`:
  - concurrency is mostly across outputs,
  - `_push_to_ig(...)` uses one-off `requests.post(...)` calls,
  - no persistent session/connection pooling is reused.
- Interpretation:
  - a failed steady run at this point could be measuring replay-worker inefficiency rather than the true platform ingress limit.
- Next work pinned:
  - harden the WSP transport path,
  - harden the canonical dispatcher to use the real `dev_full` profile and durable checkpoints,
  - only then spend on the next bounded remote run.

### 2026-03-06 06:03:11 +00:00 - Bounded smoke exposed canonical-lane network drift, not a throughput verdict
- The corrected dispatcher smoke reached the real remote launch path and exposed a concrete infra blocker:
  - ECS task failed before container start with `TaskFailedToStart`,
  - ECR auth pull timed out during resource initialization.
- Root cause identified from live VPC inspection:
  - dispatcher was launching WSP into public subnets,
  - ECR interface endpoints use private DNS,
  - endpoint SG only admits `443` from the private runtime subnet CIDRs,
  - therefore the public-subnet task resolved to private endpoint IPs it could not reach.
- Production decision:
  - canonical WSP replay workers should run in private runtime subnets with endpoint-backed bootstrap,
  - `assignPublicIp` should be disabled for that lane.

### 2026-03-06 06:08:54 +00:00 - Private-subnet launcher is correct; missing CloudWatch Logs endpoint is now the live blocker
- After moving the dispatcher to the private runtime subnets, the WSP task progressed past the earlier ECR bootstrap issue.
- New blocker:
  - ECS task fails during logger initialization because private runtime bootstrap does not have CloudWatch Logs endpoint reachability.
- Live IaC finding:
  - `dev_full` runtime interface endpoints currently include `ec2`, `ecr.api`, `ecr.dkr`, and `sts`,
  - but not `logs`.
- Decision:
  - keep the worker in private subnets,
  - add the `logs` interface endpoint rather than retreating to a weaker public-subnet posture.

### 2026-03-06 06:10:49 +00:00 - Runtime Terraform plan shows targeted `logs` fix plus unrelated EKS drift
- Terraform validation passed for the runtime stack after adding `logs` to the interface-endpoint set.
- Full plan result:
  - required `logs` interface endpoint creation is present,
  - but an unrelated `aws_eks_node_group.m6f_workers` replacement drift is also present.
- Decision:
  - do not apply the whole runtime plan,
  - use a targeted apply for the `logs` VPC endpoint only,
  - leave the EKS drift for separate review so this blocker fix remains scoped.

### 2026-03-06 06:16:14 +00:00 - WSP bounded diagnostic isolated the next real defect in the producer runtime
- A tiny private-subnet diagnostic task proved:
  - image boots,
  - `/app` workdir is correct,
  - `config/platform/profiles/dev_full.yaml` exists in-container,
  - Python module imports work.
- A traced WSP task then revealed the real failure:
  - `WSP run_receipt unreadable ... error=Invalid endpoint:`
  - reason `RUN_RECEIPT_UNREADABLE`.
- Root cause:
  - `OBJECT_STORE_ENDPOINT` is intentionally unset for native S3,
  - `world_streamer_producer.config` resolves the placeholder to an empty string instead of `None`,
  - boto3 rejects `endpoint_url=''`.
- Decision:
  - fix endpoint normalization in the WSP profile loader,
  - rerun the bounded smoke immediately after.

### 2026-03-06 06:18:44 +00:00 - Latest smoke closes the infra loop and exposes image drift as the active blocker
- The canonical WSP smoke now proves:
  - private-subnet launcher works,
  - ECR/bootstrap path works,
  - CloudWatch logging works,
  - container starts and emits logs.
- The failure still shows the old `Invalid endpoint:` behavior, which means the live ECS task is still running the older image digest and not the locally fixed WSP loader code.
- Decision:
  - stop spending on repeated reruns of the stale image,
  - next real step is a controlled runtime image refresh for the WSP lane,
  - only after that should the bounded smoke be repeated.

### 2026-03-06 06:27:18 +00:00 - Image refresh is the next production move, not more stale-image reruns
- Verified the live WSP task definition is still on revision `17` and still pinned to image digest `sha256:49eb6cb0c5e33061fae4d1aaceeac2e44600adb5c4250436be9ac8395ed29cb2`.
- Verified the current `cert-platform` head contains the WSP runtime fixes that are missing from that live image.
- Decision:
  - rebuild/push a fresh immutable image from the active branch using the existing managed packaging workflow,
  - repin the WSP ECS task definition to that new digest,
  - rerun the bounded canonical WSP smoke before resuming the larger `PR3-S1` steady window.
- Governance:
  - no branch operation,
  - active-branch commit/push is now required because the remote packaging workflow must build the validated source currently on `cert-platform`.

### 2026-03-06 06:36:42 +00:00 - Refreshed WSP image removed stale-code drift and exposed the real network-shape issue
- Rebuilt the immutable image from `cert-platform` with `dev-full-m1-packaging` and registered WSP task-definition revision `18` against digest `sha256:e56005abe96df6d5403fcc9b811141633fc5f2a2d508a669976f3ef40b7435ca`.
- Removed hardcoded WSP task-definition revision drift from the replay dispatchers so the active lane resolves the latest active revision automatically.
- Bounded canonical WSP smoke on the refreshed image no longer shows the old `Invalid endpoint:` bug.
- New live symptom:
  - repeated `WSP IG push retry ... reason=timeout`,
  - zero admitted requests,
  - early cutoff triggered.
- Root cause analysis:
  - private runtime subnets have no default egress route and no NAT,
  - they can reach S3 and interface endpoints,
  - but they cannot reach the public API Gateway ingress URL.
- Production decision:
  - treat `WSP` as the outside-world producer edge,
  - move canonical WSP replay back to public subnets with public IP,
  - widen runtime endpoint SG ingress to include the public WSP subnets so image/bootstrap still works through private endpoints.

### 2026-03-06 06:40:11 +00:00 - Public-edge replay uncovered an IG URL composition bug
- After restoring the public-edge WSP posture, the task stopped timing out and failed immediately with `IG_PUSH_REJECTED`.
- No API Gateway request-count datapoints were recorded for the same window, which indicated the request URL itself was wrong rather than the ingress rejecting valid traffic under load.
- Root cause:
  - `dev_full` profile already sets `IG_INGEST_URL` to the full `/v1/ingest/push` path,
  - `runner.py` appended `/v1/ingest/push` again inside `_push_to_ig(...)`,
  - effective target became `.../v1/ingest/push/v1/ingest/push`.
- Decision:
  - normalize the IG push URL builder so it accepts either a base API URL or the full ingest URL,
  - include reject detail in WSP logs,
  - rebuild the immutable image and repin the WSP task definition again before the next bounded smoke.

### 2026-03-06 06:45:58 +00:00 - The active ceiling is WSP sender serialization, not ingress
- Latest bounded canonical smoke on WSP revision `20` admitted `26,592` requests with zero `4xx`/`5xx`, so the ingress path is now materially healthy.
- Progress logs show each output stream advancing at only about `40 eps`, for aggregate WSP throughput around `170 eps`, far below the `3000 eps` target.
- Root cause:
  - WSP only parallelizes across outputs,
  - each output is still single-threaded on send/ack,
  - producer serialization is now the limiting surface.
- Decision:
  - implement deterministic per-run lane sharding in WSP,
  - give each lane its own cursor/checkpoint scope,
  - extend the canonical PR3-S1 dispatcher to launch multiple WSP lane tasks and aggregate their proof.

### 2026-03-06 07:21:14 +00:00 - PR3-S1 is now a producer shaping problem, not an ingress-capacity mystery
- Verified live ingress is already at the production pin:
  - API Gateway `3000 rps / 6000 burst`
  - Lambda `reserved_concurrency=300`, `memory=1024 MB`, `timeout=30s`.
- Latest canonical WSP run reached `2789.805 eps` admitted with `0` `5xx`, so the ingress edge itself is no longer the unknown.
- Lane logs show repeated `http_429` retries, meaning the producer is overshooting the steady envelope and generating avoidable error traffic.
- Decision:
  - add deterministic sender-side shaping to WSP,
  - make the canonical PR3-S1 runtime summary fail on final throughput shortfall and error-rate breach,
  - rerun bounded windows until the steady lane is clean enough to satisfy the real production contract.

### 2026-03-06 07:34:31 +00:00 - First shaped bounded run removed 429s; next correction is steady-window anchoring
- The first shaped bounded WSP window ran with `0` `4xx` and `0` `5xx`, so sender-side shaping successfully removed the previous ingress-throttle noise.
- Remaining blocker was pure steady-rate shortfall (`2745.237 eps` vs `3000 eps`).
- Diagnosis:
  - the dispatcher still measured elapsed time from task start timestamps rather than the point where the full fleet was confirmed `RUNNING`.
- Decision:
  - anchor the certification window to full-fleet confirmation,
  - retain fleet-start timestamps separately for audit,
  - rerun the same shaped window before changing the target rate again.

### 2026-03-06 07:48:40 +00:00 - Certification window end corrected to stop-request boundary
- The bounded dispatcher was still using ECS `stopped_at` as the performance end, which inflated elapsed time beyond the declared 180-second steady window.
- Decision:
  - use the exact stop-request / early-cutoff instant as `window_closed_at`,
  - query CloudWatch only through that boundary,
  - treat later container-stop timestamps as teardown evidence only.
### 2026-03-06 08:18:00 +00:00 - PR3-S1 bounded rerun regression triage
- Latest bounded rerun undercounted admitted throughput (`1991.749 eps`) even though error traffic was zero.
- Root cause under analysis before next rerun:
  - steady duration countdown still starts at orchestration submit time instead of full-fleet active confirmation,
  - canonical dispatcher default still shows `assignPublicIp=ENABLED` even though the intended runtime posture is private-subnet execution.
- Next action:
  - correct both in the dispatcher,
  - rerun bounded calibration,
  - only then retune the sender-side rate target if steady proof still falls short.
### 2026-03-06 08:24:00 +00:00 - Private PR3-S1 launch failure narrowed to stale launcher defaults plus timestamp masking
- Live VPC endpoints for `ecr.api`, `ecr.dkr`, `logs`, `sts`, and S3 are already present in the `dev_full` private subnets.
- The next remediations are local to the canonical dispatcher:
  - repin default subnets to `subnet-0a7a35898d0ca31a8,subnet-0e9647425f02e2f27`,
  - stop converting absent ECS timestamps into fake `now` values so task-start failure is surfaced immediately.
### 2026-03-06 08:32:00 +00:00 - Private WSP to IG timeout root cause identified
- After fixing subnet defaults and timestamp masking, private WSP tasks now start correctly.
- The actual runtime blocker is private reachability to API Gateway: WSP logs show `IG push retry ... reason=timeout` on the `execute-api` endpoint.
- Planned remediation:
  - add the `execute-api` VPC interface endpoint for private worker access to the ingress API,
  - guard early CloudWatch reads against zero-width windows,
  - rerun a small private smoke before returning to full `PR3-S1` calibration.
### 2026-03-06 08:36:00 +00:00 - Live execute-api endpoint applied for private WSP ingress access
- Targeted Terraform apply created `vpce-05d4ac7f9e8ddf16a` for `com.amazonaws.eu-west-2.execute-api` in the `dev_full` private subnets.
- This keeps the canonical WSP replay lane private and removes the need to fall back to public subnet routing just to reach IG.
### 2026-03-06 08:40:00 +00:00 - PR3-S1 source semantics corrected after private 403 check
- Design authority review confirms WSP is the platform's outside-world traffic producer.
- Therefore the canonical PR3-S1 replay lane should emulate external traffic hitting the public IG edge, not an internal private caller.
- Action:
  - keep the new `execute-api` endpoint for other private-worker cases,
  - repin PR3-S1 dispatcher defaults back to public subnets with public IPs,
  - resume calibration on the public-edge path.
### 2026-03-06 08:45:00 +00:00 - execute-api private DNS rollback decision
- The added `execute-api` endpoint is overriding the standard API hostname inside the VPC.
- That breaks the canonical PR3-S1 public-edge replay semantics because WSP no longer reaches the real public ingress hostname.
- Next fix:
  - keep the endpoint,
  - disable private DNS on it,
  - rerun the public-edge smoke.
### 2026-03-06 08:51:00 +00:00 - execute-api endpoint recreated without private DNS
- Replaced the live `execute-api` interface endpoint and disabled private DNS on it.
- This restores the standard public ingress hostname for canonical PR3-S1 replay while keeping the endpoint available for explicit future private use.
### 2026-03-06 08:55:00 +00:00 - Fanout, not IG, is the next PR3-S1 calibration lever
- Latest 75-lane bounded run reached `1707.253 eps` admitted with zero `4xx`/`5xx`.
- This implies average lane-level admitted posture around `22.76 eps/lane`.
- Next move:
  - keep total target at `3000 eps`,
  - increase WSP producer fanout to about `144` lanes,
  - treat that as a more realistic outside-world client population rather than an ingress-envelope change.
### 2026-03-06 09:05:00 +00:00 - Quota remediation direction for PR3-S1
- The 144-lane run hit the live Fargate concurrent vCPU limit at lane `140`.
- Current WSP task size is `1024 CPU / 2048 MiB`, which is too heavy for a high-fanout replay harness.
- Next fix:
  - add task-size overrides and partial-launch cleanup to the dispatcher,
  - test `512/1024` lanes,
  - then retry high fanout.
### 2026-03-06 09:18:00 +00:00 - PR3-S1 CloudWatch period mismatch identified
- The `136`-lane canonical run is not actually evidenced by a `159.655s` metric surface even though the dispatcher computed eps that way.
- What was verified:
  - API Gateway `Count` for the queried window returned two datapoints only (`08:55`, `08:56`) totaling `357,919`,
  - Lambda `Invocations` matched that total (`357,925`) for the same period,
  - lane logs show per-lane aggregate send posture remaining at about the configured `22 eps`.
- What this means:
  - the dispatcher divided two full `60s` CloudWatch buckets by a non-aligned `159.655s` elapsed window,
  - which understated the admitted rate to `2241.826 eps`,
  - while the same two-minute metric evidence actually implies about `2982.7 eps`.
- Decision:
  - fix PR3-S1 to certify on minute-aligned CloudWatch windows with explicit covered-seconds accounting,
  - then rerun with a modest `stream_speedup` uplift if the aligned result still misses the `3000 eps` target.
### 2026-03-06 09:14:00 +00:00 - PR3-S1 aligned rerun nearly clears target but exposes metric publication lag
- Reran canonical `PR3-S1` at `136` lanes, `stream_speedup=90`, minute-aligned measurement window.
- Observed on the final settled readback:
  - `359,796` admitted requests,
  - `2998.3 eps`,
  - `0` `4xx`,
  - `0` `5xx`.
- Why it still failed:
  - the early-cutoff check fired using an incompletely published CloudWatch view and emitted `B12 observed=1896.017`,
  - the final readback then showed the run was actually at the `3k eps` edge.
- Next correction:
  - add metric-settle grace so PR3-S1 only adjudicates on fully published minute bins,
  - rerun with a slight extra margin above `90` speedup so the final result clears `3000 eps` cleanly instead of landing within noise.
### 2026-03-06 09:46:00 +00:00 - PR3-S1 closed on canonical remote-WSP path
- Final passing calibration:
  - `lane_count=138`
  - `stream_speedup=95`
  - calibrated source setpoint `3005 eps`
  - settled measurement window `09:41:00Z -> 09:44:00Z`
  - settled metric bins `3`
- Impact metrics from the closure run:
  - admitted requests `540,616`
  - admitted throughput `3003.4222 eps`
  - `4xx_total=0`
  - `5xx_total=0`
  - `error_rate_ratio=0.0`
- Interpretation:
  - the platform target stayed fixed at `3000 admitted eps`,
  - the only calibration was a tiny generator setpoint uplift to overcome sub-1% injector underdelivery,
  - this is a test-rig calibration, not a waiver or lowering of the production standard.
- State consequence:
  - `PR3-S1` is now `PR3_S1_READY`,
  - next boundary is `PR3-S2`.
### 2026-03-06 10:35:00 +00:00 - PR3 runtime drift surfaced during S2 planning
- `PR3-S1` remains a valid ingress-capacity result on `IG_ADMITTED_EVENTS_PER_SEC`, but deeper runtime inspection showed the downstream RTDL compute lane is not materially active yet.
- Live checks:
  - Managed Flink app `fraud-platform-dev-full-rtdl-ieg-ofp-v0` exists but is `READY`, not `RUNNING`.
  - EKS cluster `fraud-platform-dev-full` and namespace `fraud-platform-rtdl` exist, but the namespace is empty.
  - the only live ECS cluster is the WSP ephemeral harness cluster.
- Meaning:
  - ingress was stressed,
  - end-to-end stream processing was not yet running,
  - so the next work item is not just burst certification but materializing the actual RTDL data plane.
### 2026-03-06 10:40:00 +00:00 - RTDL compute gap decomposed to code/runtime mismatches
- Code inspection showed a precise mismatch with the dev_full substrate:
  - `decision_fabric`, `action_layer`, `decision_log_audit`, `case_trigger`, and `archive_writer` already support Kafka/MSK.
  - `identity_entity_graph` and `online_feature_plane` do not; they still only support `file`/`kinesis` readers.
- Production implication:
  - even if remote EKS jobs were launched immediately, the RTDL projection lane would still not consume the live MSK traffic.
- Chosen remediation direction:
  - add Kafka/MSK support to `IEG` and `OFP`,
  - materialize remote RTDL workers on EKS against the live dev_full bus/stores,
  - then resume PR3 from the earliest state that depends on an actually running hot path.
### 2026-03-06 10:52:00 +00:00 - Pre-change plan pinned for RTDL Kafka remediation
- Recorded the production-first implementation plan in the road-to-prod implementation map before changing code.
- Immediate code target:
  - add Kafka/MSK reader support to `IEG` and `OFP`,
  - preserve checkpoint determinism and replay behavior,
  - validate locally with focused unit tests before remote materialization.
- Reason for this ordering:
  - the current defect is structural, not just operational,
  - remote runtime launch without these code changes would still leave the RTDL lane unable to consume the authoritative dev_full transport.
### 2026-03-06 11:08:00 +00:00 - IEG/OFP Kafka support validated locally
- Implemented Kafka/MSK consumption in:
  - `src/fraud_detection/identity_entity_graph/projector.py`
  - `src/fraud_detection/online_feature_plane/projector.py`
- Added focused Kafka projector tests to:
  - `tests/services/identity_entity_graph/test_projector_determinism.py`
  - `tests/services/online_feature_plane/test_phase2_projector.py`
- Important finding from the test pass:
  - the projection stores keep Kafka checkpoints as the last consumed offset rather than `offset+1`,
  - so the readers now resume from `checkpoint + 1` to avoid re-consuming the last committed record.
- Validation result:
  - `python -m pytest tests/services/identity_entity_graph/test_projector_determinism.py tests/services/online_feature_plane/test_phase2_projector.py -q`
  - result: `18 passed`
- Next move:
  - remote RTDL worker materialization against the live dev_full MSK bus and stores.
### 2026-03-06 11:24:00 +00:00 - Shared Kafka adapter is not yet valid for live dev_full MSK IAM
- While preparing remote RTDL worker launch, I verified the auth contract of `src/fraud_detection/event_bus/kafka.py` against dev_full handles.
- Finding:
  - dev_full is pinned to `AWS MSK Serverless` with `SASL_IAM`,
  - but the shared Kafka adapter still expects static SASL username/password credentials.
- Operational implication:
  - remote RTDL jobs would still fail at broker auth even though `IEG/OFP` now have Kafka consumption branches.
- Decision:
  - fix the shared Kafka adapter for MSK IAM/OAUTHBEARER first,
  - do not paper over this with one-off credentials or a component-specific workaround.
### 12:05 UTC - PR3 runtime-shape decision pinned from live inspection
- Rechecked live runtime surfaces instead of assuming the earlier `Managed Flink` cutover was materially complete.
- Findings:
  - `fraud-platform-dev-full-rtdl-ieg-ofp-v0` exists in `MSF` but is only `READY` with default shell configuration and no evidence of deployed app code,
  - `fraud-platform-rtdl` namespace is present on `EKS` and worker nodes are active, but the namespace has no RTDL workloads,
  - canonical remote `WSP` task definition has already refreshed to image digest `sha256:b0f707c52274e35330dc412ca610399afa550662c53347f0e967c4d95dec84b8`.
- Decision taken:
  - do not keep treating the empty MSF shell as claimable runtime proof,
  - materialize the repo's actual RTDL/decision/archive workers on remote `EKS` against live `MSK IAM` + `Aurora`,
  - keep the current MSF app recorded as non-claimable until there is real application code to run in it.
- Why:
  - this is the only path that is both production-real and materially executable with the code that actually exists in the repo today.
### 12:32 UTC - Reused indexed PR3 workflow path for runtime materialization dispatch
- GitHub Actions would not dispatch the brand-new materialization workflow path because it is not yet indexed on the default branch.
- Rather than stopping or changing branch posture mid-run, I added `materialize_runtime` mode to the already-indexed `dev_full_pr3_s1_managed.yml` workflow.
- Result:
  - remote execution can continue from `cert-platform` without local orchestration,
  - no branch hop or merge-to-main was needed just to unblock runtime materialization.
### 12:45 UTC - First PR3 runtime rollout produced real blockers in DF and DLA
- Remote EKS materialization succeeded partially:
  - `IEG`, `OFP`, `AL`, and `archive_writer` became ready,
  - `DF` and `DLA` crashed repeatedly.
- Root causes identified from pod logs:
  - `DF`: missing `CSFB_PROJECTION_DSN` / missing `context_store_flow_binding` profile surface,
  - `DLA`: pod was still reading image-baked `dev_full.yaml`, so `storage_profile_id` resolved as `dev_full` instead of the intended `prod`.
- Remediation started:
  - branch `dev_full.yaml` will be mounted into pods via ConfigMap,
  - `CSFB` runtime section and env surfaces added,
  - runtime will be redeployed in place.
### 12:44 UTC - Second PR3 runtime rollout shows artifact drift, not missing deployment contract
- Verified live EKS state after the second materialization workflow:
  - all six deployments remained `0/1`,
  - every pod failed despite the workflow job itself returning success.
- Collected live pod logs:
  - `IEG`: `IEG_EVENT_BUS_KIND_UNSUPPORTED`,
  - `OFP`: `OFP_EVENT_BUS_KIND_UNSUPPORTED`,
  - `DF`: `PLATFORM_RUN_ID required to resolve projection_db_dsn`,
  - `AL`, `DLA`, `archive_writer`: `KAFKA_SASL_CREDENTIALS_MISSING`.
- Checked the live K8s deployment surfaces directly:
  - `fp-pr3-runtime-secrets` contains the expected Kafka IAM and platform-run env values,
  - deployments reference those secret keys,
  - pods mount `ConfigMap/fp-pr3-runtime-profile` at `/runtime-profile`.
- Decision:
  - stop chasing deployment-shape symptoms,
  - rebuild the authoritative immutable image from current `cert-platform`,
  - repin both PR3 worker materialization and the canonical WSP ECS family to the fresh digest before further PR3 execution.
### 12:52 UTC - Fresh immutable image repaired most of PR3 runtime; remaining defects are IRSA subject drift and DF env completeness
- Rebuilt the platform image with `dev-full-m1-packaging`:
  - workflow run `22760012008`,
  - digest `sha256:c12122cc4da6df03bf79c1d43a11a7825960740f7db4329de39f74816d1fd159`.
- Repinned canonical WSP ECS family to revision `24` on that digest.
- Reran PR3 runtime materialization with the same image digest:
  - `IEG`, `OFP`, `AL`, `DLA`, `archive_writer` became `1/1 Ready`,
  - only `DF` remained in restart.
- Follow-up diagnosis:
  - `AL`/`DLA` Kafka logs now show MSK IAM token refresh attempts reaching STS and failing `AssumeRoleWithWebIdentity`,
  - IAM trust policies are pinned to subjects `rtdl` and `decision-lane`,
  - current materializer-created service accounts were `fp-rtdl-runtime` and `fp-decision-runtime`, so the deploy names drifted from the trusted IRSA subjects.
- Additional `DF` debug result:
  - an in-cluster debug pod with the same image, mounted profile, and full runtime secret confirmed `PLATFORM_RUN_ID` + `IEG_PROJECTION_DSN` resolve correctly,
  - so the remaining `DF` crash is not a broken profile file,
  - it is missing `IEG_*` / `OFP_*` dependency env on the `DF` deployment.
- Remediation in progress:
  - patch materializer to use canonical IRSA service-account names,
  - patch `DF` env injection to include `IEG_REQUIRED_PLATFORM_RUN_ID`, `IEG_PROJECTION_DSN`, `OFP_REQUIRED_PLATFORM_RUN_ID`, `OFP_PROJECTION_DSN`, and `OFP_SNAPSHOT_INDEX_DSN`,
  - rerun PR3 runtime materialization immediately after.
### 13:18 UTC - PR3 runtime blocker narrowed to Terraform-owned IAM gap on the live EKS runtime roles
- Current live PR3 runtime posture:
  - all six deployments are now `Running/Ready` in `fraud-platform-rtdl`,
  - failures moved from container startup into the real managed-bus boundary.
- Evidence gathered from live logs:
  - `AL`, `DF`, and `DLA` all fail MSK broker auth with `SaslAuthenticationFailedError (...: Access denied)` after reaching the serverless broker endpoint.
- Evidence gathered from IAM readback:
  - `fraud-platform-dev-full-irsa-rtdl` only has `*-ssm-read`,
  - `fraud-platform-dev-full-irsa-decision-lane` only has `*-ssm-read`.
- Interpretation:
  - the runtime principals can fetch secret/config handles,
  - but they were never granted the Kafka/MSK data-plane actions needed to run the hot path.
- Decision:
  - do not patch IAM ad hoc from the laptop,
  - do not weaken runtime auth semantics,
  - patch the Terraform-owned IRSA role policies and execute the apply remotely through the indexed PR3 managed workflow.
- Next execution sequence:
  1. extend runtime Terraform for IRSA-to-MSK policy,
  2. add remote IAM-remediation mode to `dev_full_pr3_s1_managed.yml`,
  3. dispatch remote remediation,
  4. verify logs clear on `AL/DF/DLA`,
  5. only then resume `PR3-S1` steady evidence.
### 13:31 UTC - Remote Terraform path hit tfstate-bucket access denial; live IAM remediation switched to runner-side AWS API apply
- Remote remediation run `22760725501` assumed the correct GitHub OIDC role and failed at Terraform backend refresh with:
  - `403 Forbidden` on `s3://fraud-platform-dev-full-tfstate/dev_full/runtime/terraform.tfstate`.
- Decision taken:
  - keep the Terraform policy change committed as source of truth,
  - stop trying to drive this specific live repair through remote Terraform until the OIDC role can read the tfstate object,
  - mutate the live IRSA role policies remotely inside the indexed PR3 workflow via `aws iam put-role-policy`, then restart PR3 deployments and verify the broker-auth logs.
- Reason:
  - this keeps execution remote and production-focused while avoiding a separate control-plane dependency from blocking the hot-path repair.
- Follow-on note:
  - GitHub OIDC tfstate-bucket access still needs to be fixed later for full remote Terraform reconciliation.
### 13:38 UTC - Removed unnecessary Kafka control-plane discovery from the remote PR3 IAM repair
- Runner-side live IAM remediation failed once because the GitHub OIDC role could not call `aws kafka list-clusters-v2`.
- Decision:
  - stop discovering the cluster dynamically,
  - use the pinned `MSK_CLUSTER_ARN` directly inside the workflow and derive topic/group scope from it.
- Reason:
  - the repair only needs the canonical cluster ARN; extra Kafka control-plane permissions are unnecessary broadenings of the runner role.
### 13:43 UTC - Removed the extra workflow input and pinned MSK cluster ARN in job env
- GitHub rejected the rerun because `workflow_dispatch` allows at most `25` inputs and this workflow was already at the cap.
- Fix applied:
  - dropped the new manual input,
  - pinned the canonical `MSK_CLUSTER_ARN` in the remediation job env instead.
- Result:
  - dispatch shape is valid again without reintroducing Kafka discovery permissions.
### 13:58 UTC - Next PR3 blockers are baseline-topic drift and legacy injector drift
- Verified after the remote IAM fix:
  - `AL`, `DF`, and `DLA` now authenticate cleanly to MSK with SASL/OAuth,
  - IRSA role policies are present on both runtime roles.
- New issue exposed by the clean auth path:
  - `DF` repeatedly warns that `fp.bus.traffic.baseline.v1` has no partition metadata.
- Interpretation:
  - this is active config drift, not just a transient warning, because the current `dev_full` hot path is fraud-only and the baseline traffic topic is not a pinned live lane.
- Separate execution drift still open:
  - PR3-S1 workflow still runs the noncanonical synthetic dispatcher instead of the real remote WSP replay dispatcher.
- Next fix sequence:
  1. repin active runtime config to the real fraud-only topic set for PR3,
  2. switch PR3-S1 workflow to `pr3_s1_wsp_replay_dispatch.py`,
  3. rerun PR3-S1 on the corrected canonical path.
### 14:08 UTC - PR3 hot path repinned to fraud-only runtime topics and canonical WSP replay
- Active config cleanup applied:
  - removed stale baseline traffic/context topic subscriptions from the PR3 runtime configs so the hot path matches the actual fraud-only lane.
- Workflow correction applied:
  - `dev_full_pr3_s1_managed.yml` now launches the canonical remote `WSP` replay dispatcher instead of the synthetic inline harness.
- Dispatcher hardening applied:
  - canonical WSP summary now includes latency `p95/p99` derived from API Gateway metrics, not just throughput/error counts.
- Next operational requirement:
  - rebuild the immutable image and rematerialize PR3 runtime so the live pods actually pick up the cleaned topic contracts before the steady rerun.
### 14:18 UTC - PR3-S1 rerun failed on missing upstream receipt bootstrap, not on traffic or runtime throughput
- Run 22761391121 failed before dispatching any WSP traffic.
- Root cause:
  - workflow created uns/dev_substrate/dev_full/road_to_prod/run_control/pr3_20260306T021900Z on the GitHub runner,
  - but did not hydrate the authoritative upstream PR3 evidence set,
  - so the canonical dispatcher could not read pr3_s0_execution_receipt.json and correctly failed closed.
- Decision:
  - keep the dispatcher strict,
  - patch the workflow to sync the authoritative PR3 evidence prefix from the evidence bucket into RUN_DIR before launch,
  - rerun immediately after patching.
### 14:26 UTC - Added remote run-control bootstrap/mirror for PR3 workflow
- dev_full_pr3_s1_managed.yml now:
  - syncs vidence/dev_full/run_control/<pr3_execution_id>/ into RUN_DIR before dispatch,
  - syncs the full RUN_DIR back to the evidence bucket on exit.
- Purpose:
  - keep the canonical dispatcher strict,
  - remove dependence on local-only uns/ state for remote reruns,
  - make the evidence bucket the reconstructable remote continuity surface.
- Next action:
  - seed the existing PR3 run-control directory into the evidence bucket,
  - rerun canonical PR3-S1 immediately.
### 14:50 UTC - Live PR3-S1 metrics show healthy hot path but underdriven replay calibration
- While run 22761560319 is still active, live AWS metrics already show the important signal:
  - API Gateway count around 80k-88k/min (~1.33k-1.46k eps),
  - 4XX=0, 5XX=0,
  - latency about p95 21-23 ms, p99 27-31 ms.
- Interpretation:
  - ingress and runtime are healthy at this envelope,
  - current replay source-speed (19.7) is too low for the 3000 eps goal,
  - next rerun should raise stream_speedup, not loosen acceptance criteria.
- Next change in progress:
  - make stream_speedup an explicit workflow input,
  - prepare next PR3-S1 rerun at 50.0 unless end-of-window evidence contradicts it.
### 14:56 UTC - PR3 workflow calibration surface made explicit
- Replaced unused max_workers_per_lane workflow input with stream_speedup.
- Default pinned to 50.0 from live PR3-S1 evidence.
- Outcome:
  - next rerun can raise source replay speed without another workflow edit,
  - GitHub dispatch stays under the workflow input cap.
### 15:04 UTC - Cut PR3-S1 run 22761560319 early after proving replay underdrive
- Mid-window live evidence already showed:
  - ~1.3k-1.45k eps,
  - 4XX=0, 5XX=0,
  - low latency,
  - active WSP replay on real oracle data.
- Conclusion:
  - problem was confirmed as replay calibration (19.7 too low), not platform instability.
- Action:
  - cancelled workflow run 22761560319,
  - stopped active WSP ECS tasks,
  - next action is immediate rerun at stream_speedup=50.0.
### 15:12 UTC - Cancelled second PR3-S1 rerun after exposing cross-attempt WSP checkpoint reuse
- Run 22762248277 was stronger than the previous attempt, but lane logs showed replay starting from previously advanced row offsets.
- Root cause:
  - WSP checkpoint namespace is tied to platform_run_id and lane geometry, so retries on the same PR3 boundary resume prior offsets.
- Why this matters:
  - invalidates certification-style fresh replay evidence,
  - couples operational resumability with certification reruns.
- Action:
  - cancelled run 22762248277,
  - stopped active WSP ECS tasks,
  - next step is patching WSP checkpoint scoping to include a fresh attempt namespace.
### 15:18 UTC - Patched WSP checkpoint scope for fresh PR3 certification attempts
- unner.py now supports WSP_CHECKPOINT_ATTEMPT_ID in checkpoint scope derivation.
- pr3_s1_wsp_replay_dispatch.py now stamps each dispatch with a fresh UTC checkpoint attempt id and passes it into every WSP lane.
- Intended effect:
  - same platform_run_id remains pinned for provenance,
  - reruns no longer inherit earlier lane offsets,
  - certification windows become fresh and repeatable again.
- Next: rebuild image, repin WSP task family, rerun PR3-S1.
### 16:03 UTC - Rebuilt and repinned WSP image for fresh-attempt checkpoint fix
- Packaging run 22762531402 produced digest sha256:56c4eaa4347279d56d156b5e6f16736b0ddd0c26d685588d06357e5f88377349.
- Re-registered ECS task family raud-platform-dev-full-wsp-ephemeral to that digest so the next PR3-S1 rerun uses the checkpoint-attempt namespace fix.
- Next action: rerun PR3-S1 at stream_speedup=50.0 on the fresh image and fresh checkpoint namespace.
### 16:09 UTC - Fresh replay works; calibrating PR3-S1 from 50.0 to 65.0
- Run 22762626536 confirmed fresh replay offsets and healthy hot-path behaviour.
- Live metrics showed:
  - roughly 2.3k-2.5k eps,
  - 4XX=0,
  - low latency.
- Conclusion:
  - checkpoint contamination is fixed,
  - replay is still underdriven for the 3000 eps goal.
- Action:
  - cancelled 22762626536,
  - stopped active WSP lanes,
  - next rerun will use stream_speedup=65.0.
### 16:18 UTC - Moving PR3-S1 speedup straight to 100.0
- 65.0 improved throughput again but still stayed below the 3000 eps target.
- New reasoning:
  - each WSP lane is already capped at 125 eps,
  - so higher stream_speedup cannot overshoot the target envelope,
  - it only helps the replay source keep up with the cap.
- Action:
  - cancelled 22762819167,
  - stopped active WSP lanes,
  - next rerun will use stream_speedup=100.0.
### 16:42 UTC - PR3-S1 end-to-end validity revoked after inspecting the live IG edge
- Inspected the live Lambda source at `infra/terraform/dev_full/runtime/lambda/ig_handler.py`.
- Confirmed the live function is only:
  - API-key auth,
  - DDB idempotency write,
  - correlation logging,
  - `202` response.
- It does **not** publish to Kafka and does not emit the canonical IG receipt/quarantine surfaces.
- Runtime evidence behind that finding:
  - Lambda logs show only `ig_ingest_boundary`,
  - direct Kafka reads from active PR3 pods returned zero rows on the expected hot-path topics,
  - archive-writer counters and Aurora ledgers remained zero.
- Decision:
  - treat earlier `PR3-S1` closure as ingress-admission-only,
  - invalidate it for end-to-end hot-path certification,
  - remediate the live ingress edge before continuing PR3.
### 16:47 UTC - Production-first ingress remediation direction chosen
- Evaluated three options against the actual production target rather than the easiest rerun path:
  - force the internal Flask/Aurora IG service into Lambda,
  - repin away from Lambda / keep synthetic harnesses,
  - harden the pinned `API Gateway -> Lambda -> DDB` edge into a real publisher.
- Accepted the third option.
- Rationale:
  - keeps the public ingress contract truthful,
  - DDB is a stronger public-edge idempotency surface than per-request Aurora writes,
  - allows the edge to publish into the real MSK/RTDL runtime without bypassing the `via_IG` claim path.
### 16:53 UTC - Coordinated ingress correction pass pinned before implementation
- The defect is a four-lane gap, not a one-line code bug:
  - code semantics: Lambda stub,
  - packaging: Terraform packages only one file,
  - network: Lambda is not in the VPC so it cannot reach private MSK,
  - IAM: Lambda lacks MSK data-plane and object-store receipt permissions.
- Implementation direction from this point:
  1. add a Lambda-native IG runtime that performs real publish + receipt emission,
  2. package the required platform code/contracts into a remote-deployable bundle,
  3. attach the Lambda to the private VPC subnets with the correct security posture,
  4. expand IAM for MSK/object-store access,
  5. redeploy the edge, verify downstream topic movement, then rerun PR3 from the corrected boundary.

### 17:08 UTC - Workflow registration path completed and synced back into cert-platform
- The ingress-edge materialization workflow is now registered on `main` while still checking out `cert-platform` code via the new `code_ref` input.
- Why that was necessary:
  - GitHub needed the workflow file on `main` to dispatch it reliably,
  - production truth required the workflow to build the active ingress-fix code, not stale `main` code.
- Branch outcome:
  - workflow-only commit flowed through the approved `dev -> main -> dev -> cert-platform` path,
  - the final cert-platform merge conflict was resolved in favor of the branch-safe `code_ref` version,
  - cert-platform is now aligned and push-clean for the next live execution step.

### 17:13 UTC - Pinned next live boundary: ingress materialization before any more PR3 certification
- Re-read the active PR3 and road-to-prod authorities before proceeding.
- Conclusion:
  - `PR3-S1` cannot legitimately continue until the live ingress edge is corrected,
  - another replay attempt before that would only generate more ingress-only evidence and waste resources.
- Next action now in progress:
  - dispatch the remote ingress materialization workflow,
  - verify the live Lambda handler/VPC mode and downstream publish path,
  - then resume PR3 from the same strict upstream boundary.

### 17:20 UTC - Correcting the remote ingress packaging lane to use the artifacts bucket, not object-store
- First remote ingress deploy failed at `s3:PutObject` for the Lambda bundle upload.
- Exact denial:
  - role: `GitHubAction-AssumeRoleWithAction`,
  - target: `fraud-platform-dev-full-object-store/artifacts/lambda/ig_handler/...`.
- Interpretation:
  - the permission was missing,
  - but more importantly the bucket choice was wrong for production semantics.
- Decision now pinned:
  - Lambda deployment bundles move to `fraud-platform-dev-full-artifacts`,
  - OIDC role will receive least-privilege write on that artifact prefix,
  - same pass also adds Managed Flink read and CloudWatch metric read to close obvious upcoming IAM gaps.

### 17:28 UTC - Split new PR3 GitHub OIDC rights into a dedicated IAM policy
- Extending the existing inline policy failed with AWS `LimitExceeded` at the 10 KB inline-policy ceiling.
- Decision:
  - leave the old M6/M10/M11/M12 policy focused on its original lanes,
  - add a separate `GitHubActionsPR3RuntimeDevFull` policy for:
    - artifacts-bucket Lambda bundle writes,
    - Managed Flink app inspection,
    - CloudWatch metric reads.
- Reason:
  - keeps the role reviewable,
  - avoids recurring size-ceiling failures,
  - better matches production least-privilege boundaries.

### 17:34 UTC - Switched the new PR3 GitHub rights from inline to managed-policy attachment
- Second IAM apply failed again because the role has exhausted the total inline-policy budget.
- Correction:
  - new PR3 rights are now modeled as an attached managed policy rather than another inline policy.
- Why:
  - removes the AWS inline-size ceiling from this lane,
  - keeps the permissions separable and auditable,
  - avoids revisiting the same IAM blocker on the next PR stages.

### 17:40 UTC - Remote ingress deploy now reaches Terraform bootstrap boundary
- The second rerun proved the previous fixes held:
  - Lambda bundle built,
  - S3 upload to `fraud-platform-dev-full-artifacts` succeeded,
  - evidence sync remained healthy.
- New blocker:
  - GitHub job failed at `terraform init` because Terraform was not installed in the runner.
- Fix in progress:
  - add `hashicorp/setup-terraform@v3` to the workflow,
  - then promote that workflow-only fix to `main` using the approved branch path.

### 17:46 UTC - Corrected the ingress workflow to use the runtime module's backend config
- Third rerun reached Terraform and failed because the runtime module's partial S3 backend was initialized without `bucket` and `key`.
- Fix applied:
  - `terraform init -reconfigure -backend-config=backend.hcl.example` in the ingress materialization workflow.
- Reason:
  - this matches the existing dev_full Terraform pattern,
  - keeps state on the intended runtime backend,
  - avoids one-off backend drift in the PR3 lane.

### 17:54 UTC - Expanded the PR3 deployment policy to cover remote Terraform state and lock control
- Latest ingress deploy failure was `403 Forbidden` on the runtime tfstate object.
- Root cause analysis of the runtime stack showed it needs:
  - read access to `core`, `streaming`, and `runtime` tfstate,
  - write access to `runtime` tfstate,
  - DynamoDB lock-table control on `fraud-platform-dev-full-tf-locks`.
- Fix in progress:
  - add that exact scope to the attached managed PR3 policy,
  - keep writes limited to the runtime state only.

### 18:01 UTC - Added refresh-only reads for the existing ingress DDB table and DLQ
- The targeted runtime apply is now failing on Terraform refresh of:
  - `fraud-platform-dev-full-ig-idempotency`,
  - `fraud-platform-dev-full-ig-dlq`.
- Fix in progress:
  - add narrow read-only scope for those resources to the managed PR3 deployment policy.
- Reason:
  - targeted apply still needs adjacent-resource refresh,
  - this is not a new write path, only the minimum read scope Terraform requires.

### 18:07 UTC - Broadened the ingress-table refresh policy to the full read-safe set
- After `DescribeTable` was granted, Terraform immediately needed `DescribeContinuousBackups` on the same DDB table.
- Verified live table posture:
  - TTL enabled,
  - continuous backups enabled.
- Fix in progress:
  - extend the DDB read scope to include `DescribeContinuousBackups` and `DescribeTimeToLive` alongside the existing refresh reads.

### 18:13 UTC - Expanded the PR3 deployment policy to the full ingress-target control surface
- After clearing refresh-only blockers, the apply failed on `lambda:GetFunction` for the live IG Lambda.
- Decision:
  - stop granting one action at a time,
  - grant the full targeted-resource control/read set needed for:
    - the live Lambda,
    - its execution role policy updates,
    - the Lambda VPC security group,
    - the verify step's SSM reads.
- Reason:
  - this matches the actual Terraform target surface,
  - keeps the lane bounded to ingress correction,
  - should stop the repeated permission-by-permission churn.

### 18:20 UTC - Widened the Lambda statement to the full function-scoped read/update set
- After `lambda:GetFunction`, the next blocker was `lambda:ListVersionsByFunction` on the same IG Lambda.
- Decision:
  - stop enumerating Lambda read APIs one by one,
  - grant the function-scoped read/update set on `fraud-platform-dev-full-ig-handler` only.
- This keeps the policy bounded while avoiding more provider-surface churn.

### 18:27 UTC - Applied ingress correction succeeded; remaining health failure is bundle import layout
- Workflow run `22765859266` successfully applied the live ingress correction.
- Live Lambda now shows the corrected handler, VPC attachment, and envelope.
- Remaining failure came from Lambda init, not infra:
  - CloudWatch log: `No module named 'fraud_detection'`.
- Root cause:
  - the bundle builder staged `src/fraud_detection/...` instead of `fraud_detection/...` at zip root.
- Fix applied locally and verified by rebuilding a bundle and inspecting the zip layout.
- Next step:
  - push the bundler fix,
  - redeploy from `cert-platform` using the same workflow.
### 18:58 UTC - Ingress Lambda failure is now traced to eager package re-exports in the cold-start path
- I verified the live Lambda package against the workflow artifact and they are byte-identical.
- The bundle root is correct: `fraud_detection/...` is present at zip root.
- The remaining fault is import-chain shape, not package layout:
  - IG imports `platform_governance`,
  - `platform_governance.__init__` eagerly imports `evidence_corridor`,
  - that imports `scenario_runner.storage`,
  - `scenario_runner.__init__` eagerly imports `runner` and `schemas`,
  - that drags `jsonschema -> referencing -> rpds` into Lambda init.
- Production interpretation:
  - the ingress hot path is coupled to authoring/runtime-adjacent surfaces it does not need,
  - this is the wrong dependency posture for a 3000/6000 EPS ingress target.
- Remediation in progress:
  - convert `platform_governance` and `scenario_runner` export surfaces to lazy imports,
  - keep public names stable,
  - then redeploy the ingress edge and verify health before resuming PR3 evidence runs.
### 19:03 UTC - Refactored package exports so IG cold start stops importing optional scenario-runner surfaces
- Changed `platform_governance.__init__` to lazy exports.
- Changed `scenario_runner.__init__` to lazy exports.
- Result:
  - direct import of `fraud_detection.ingestion_gate.aws_lambda_handler` from source now succeeds,
  - the ingress boot path no longer drags evidence-corridor and scenario-runner/schema surfaces into init by default.
- Why this matters:
  - this is the correct fix for production ingress, because the hot lane should not depend on optional authoring/runtime-adjacent modules to boot.
- Next step:
  - redeploy the ingress edge with the narrowed import surface,
  - then re-verify live health and only then continue PR3 evidence.
### 19:08 UTC - Removed eager Postgres index import from the IG cold-start path
- New live Lambda error after the last redeploy was `No module named 'psycopg'`.
- Root cause:
  - `ingestion_gate.admission` imported `pg_index` unconditionally,
  - that forced the Postgres client into Lambda init even though the pinned live lane is DDB-backed.
- Fix applied:
  - Postgres index classes now import only when `admission_db_path` is actually a Postgres DSN.
- Why this matters:
  - the ingress edge should only carry the dependencies needed for the active runtime wiring,
  - optional backend support should not be able to break the hot path.
- Next step:
  - commit/push this milestone,
  - redeploy the ingress edge,
  - verify whether health now reaches real handler execution.
### 19:14 UTC - Confirmed health timeout is caused by missing SSM private-network access
- The IG Lambda now initializes and starts the request.
- It then times out at 30s on `/ops/health`.
- Root cause:
  - the health route still authenticates via SSM-backed API key lookup,
  - the same runtime also depends on SSM for MSK bootstrap brokers,
  - the Lambda VPC has no `com.amazonaws.eu-west-2.ssm` interface endpoint.
- Decision:
  - add the SSM VPC endpoint,
  - keep the Lambda private,
  - do not weaken auth or bypass Parameter Store just to force a green check.
- Execution posture:
  - runtime authority updated on `cert-platform`,
  - a workflow-only promotion will materialize the endpoint remotely from `main`.
### 19:24 UTC - Added fail-fast bounds and targeted logs to the IG health/auth path
- `/ops/health` was still consuming the full 30s Lambda timeout even after the SSM endpoint existed.
- Fix applied:
  - bounded SSM client timeouts/retries,
  - added low-cardinality INFO logs around health-route entry and API-key cache refresh.
- Reason:
  - control-plane dependency stalls must surface quickly and explicitly,
  - a production ingress edge cannot burn its whole request budget on hidden auth/bootstrap waits.
- Next step:
  - redeploy the Lambda,
  - inspect the new bounded failure or success signal,
  - continue eliminating the real runtime cause until health is green.
### 19:26 UTC - Ingress materialization recovery is now green on the live private path
- Recovery workflow `22767511603` passed.
- Live result:
  - `/ops/health` returned `200`,
  - mode `apigw_lambda_ddb_kafka`,
  - private VPC attachment intact,
  - auth cache miss resolved from SSM successfully.
- Measured live request evidence from CloudWatch:
  - cold-start init `1290.86 ms`,
  - end-to-end request `59.57 ms` once running.
- This means the ingress edge is no longer speculative or partially wired.
- Next step:
  - resume PR3-S1 from this truthful ingress boundary,
  - then continue sequentially through the remaining road-to-prod states.
### 14:28 UTC - Pinned the next PR3 move as immutable-image refresh on the canonical WSP path
- Re-read the active PR3 authority surfaces and confirmed the canonical lane already exists:
  - `dev_full_pr3_s1_managed.yml`,
  - `pr3_s1_wsp_replay_dispatch.py`,
  - PR3/road_to_prod docs.
- Actual blocker is now remote image drift, not missing orchestration:
  - the live/canonical replay lane is still using an older immutable image digest,
  - that means current remote PR3 runs cannot observe the WSP and ingress fixes already present in source.
- Rejected shortcuts:
  - no more reruns on the stale digest,
  - no fallback to the synthetic speedup harness,
  - no fake PR3 closure from partial or proxy evidence.
- Chosen path:
  - build a fresh immutable image from the current branch,
  - feed that digest into the canonical PR3-S1 replay path,
  - then rerun PR3 sequentially from the strict S1 boundary.
- Also pinned the reporting standard for the remaining road-to-prod work:
  - readable summaries must lead with impact metrics and thresholds,
  - then include an explicit analytical judgement on whether the state meets production-ready intent.
### 14:31 UTC - Refreshed the immutable image and found a workflow-tail false-red that needed hardening
- Dispatched `dev_full_m1_packaging` on `cert-platform`.
- Core result succeeded:
  - tag: `git-f0a7707d2e066e0c8899eb9e82fc1b61a308e2a3-run-22767772233`
  - digest: `sha256:d0817106b09769503072bc1a8a3372e3db67d186b75d54b064870e6f7a5a4292`
- The run still concluded `failure`, but only because the so-called optional S3 evidence upload step hard-failed on `HeadBucket 403`.
- Decision:
  - keep direct S3 evidence copy best-effort,
  - do not let that auxiliary path invalidate a successful immutable build/push.
- Fix applied locally to the workflow:
  - bucket unreadability now emits a warning and the job continues,
  - GitHub artifact upload remains the authoritative fallback evidence path.
### 14:37 UTC - Repointed the live WSP ECS family to the new immutable image and pinned the S1 rerun calibration
- Clean packaging rerun succeeded:
  - tag: `git-7794c8fdf38f86425fc17859ca4a2cad0efd6b9e-run-22767906946`
  - digest: `sha256:b9fd2375fc79154c95bcfb58a502b35ebb77c09df94f2222dc4ced1eca291b58`
- Live ECS family before refresh:
  - `fraud-platform-dev-full-wsp-ephemeral:26`
  - old image `sha256:56c4eaa4347279d56d156b5e6f16736b0ddd0c26d685588d06357e5f88377349`
- Applied control-plane refresh:
  - registered revision `27` of the same family with the new image digest,
  - preserved the same family/runtime roles/network mode/log group posture.
- Saved request/result/summary receipts into the active PR3 `runs/` tree.
- Rerun calibration pinned from the prior successful canonical window instead of defaults:
  - `138` lanes,
  - `stream_speedup=95.0`,
  - `180s` window,
  - prior observed admitted throughput `3003.4222 eps`.
### 15:10 UTC - PR3-S1 root cause narrowed to runtime schema-pack incompleteness plus quarantine-path append races
- I pulled live ingress metrics and Lambda logs for the failed canonical replay window instead of treating the red state as a generic throughput miss.
- Measured live posture during failure:
  - API Gateway stage throttle already uplifted: `3000 rps / 6000 burst`.
  - Lambda reserved concurrency: `300`; observed max concurrency also `300`.
  - API Gateway integration latency `p95/p99` sat near the full `30000 ms` timeout.
  - WSP impact metrics remained `0 admitted`, `100% 5xx`, `~30s p95/p99`.
- First hard defect from IG Lambda logs:
  - `referencing.exceptions.Unresolvable: schemas.layer3.yaml#/$defs/hex64`
  - triggered while validating real `6B` payloads via `schema_policy_v0.yaml`.
- Interpretation:
  - the live image contains `schemas.6B.yaml` but not its transitive dependency `schemas.layer3.yaml`,
  - so the runtime bundle is incomplete for real production payload validation,
  - which means raising concurrency first would only scale a broken validation path.
- Secondary defect also visible on the same failure path:
  - governance anomaly emission can hit `S3_APPEND_CONFLICT`,
  - that race can convert a deterministic quarantine into `503 ingress_publish_failed`.
- Decision taken:
  - fix the build/runtime schema-pack boundary,
  - add deterministic packaging proof that all live IG payload schema refs resolve,
  - harden the anomaly/governance append path so quarantine cannot degrade into `503` because of append contention,
  - then rebuild the immutable image, refresh the runtime, and rerun canonical `PR3-S1`.
### 15:21 UTC - Implemented the ingress runtime-pack fix and validated it locally before any new remote build
- Code/build remediation applied:
  - Docker build surface now includes the transitive schema packs needed by live IG policy resolution (`1A shared schema`, `5B`, `6B`).
  - Dockerfile now fails the build if any `schema_policy_v0.yaml` payload ref is unresolved.
  - S3 append corridors now retry optimistic append races instead of failing immediately.
  - IG anomaly/governance emission is now non-blocking for the quarantine receipt path.
- Local validation completed:
  - `python -m pytest tests/services/ingestion_gate/test_admission.py tests/services/ingestion_gate/test_schema_resolution.py tests/services/platform_governance/test_writer.py -q`
  - result: `12 passed`
  - `python -m py_compile ...`
  - result: clean.
- Production interpretation:
  - the next remote rebuild will be materially different in the right way,
  - if PR3-S1 still fails after this, the remaining bottleneck will be a truthful throughput/capacity issue rather than a hidden runtime-pack defect.
### 15:27 UTC - Repaired the workflow-side deterministic build context so CI can actually build the corrected runtime
- The branch source was corrected, but the remote packaging workflow still staged an old, incomplete contract surface.
- Its explicit CI `include_paths` list only staged:
  - `5B/schemas.5B.yaml`
  - `6B/schemas.6B.yaml`
- It was still missing the transitive shared schema surfaces needed by live IG policy resolution.
- Fix applied to `.github/workflows/dev_full_m1_packaging.yml`:
  - added `1A/schemas.layer1.yaml`,
  - widened staged contract packs to the `5B` and `6B` directories.
- Interpretation:
  - without this repin, remote CI would have rebuilt the same broken image despite the local source fix.
### 15:55 UTC - Canonical PR3-S1 rerun cleared the schema-pack defect and exposed the live ingress timeout bottleneck
- Refreshed-image canonical rerun evidence now shows the source side is truthful:
  - WSP lanes start and attempt real replay,
  - the prior `Unresolvable` schema failure is gone.
- Current S1 impact metrics:
  - target steady `3000.0 eps`,
  - observed admitted `0.0 eps`,
  - API Gateway measured count `2449`,
  - `4xx=0`, `5xx=2449`, error rate `100%`,
  - API Gateway latency `p95=29969.101 ms`, `p99=30000.323 ms`,
  - `138/138` WSP lanes exited with `IG_PUSH_RETRY_EXHAUSTED`.
- CloudWatch Lambda posture during the same window:
  - duration sat at the full `30000 ms` ceiling in every active minute,
  - throttles appeared once concurrency pressure accumulated.
- Key production diagnosis:
  - the ingress request path now reaches API Gateway and Lambda,
  - the hot path is stalling until timeout rather than returning a bounded admission result,
  - this is an ingress runtime design bug, not a source-replay defect.
- Specific design flaw found in live config:
  - Lambda timeout is `30s`,
  - `KAFKA_REQUEST_TIMEOUT_MS` is also `30000`,
  - synchronous publish sits on the request path,
  - so a blocked publish can consume the whole Lambda budget and turn into raw `503`/timeout behavior.
- Decision:
  - do not advance to `PR3-S2`,
  - remediate the IG hot path so publish/failure-path work is bounded below the Lambda ceiling and returns explicit outcomes,
  - then rerun canonical `PR3-S1` from the same strict boundary.
### 16:12 UTC - Implemented the ingress hot-path correction and tightened the live envelope contract
- Cold-start governance activation no longer publishes to the audit topic by default:
  - the activation still lands in deterministic governance store evidence,
  - but the extra bus publish is now `store_only` unless explicitly re-enabled.
- Kafka producer posture changed for the MSK IAM path:
  - `KafkaEventBusPublisher` now uses `confluent-kafka` for `OAUTHBEARER` producer mode,
  - IAM token generation is provided through a dedicated `oauth_cb`,
  - request/delivery/socket timeout posture is now explicitly bounded.
- Lambda ingress runtime envelope corrected in Terraform:
  - new pin `lambda_ig_kafka_request_timeout_ms` default `1500`,
  - new pin `lambda_ig_policy_activation_audit_mode` default `store_only`.
- RC2 capacity-envelope workflow widened so those two live pins are verified after apply instead of assumed.
- Additional boundedness improvements:
  - SQS and DynamoDB clients now use the short botocore timeout profile,
  - gate initialization and request-start budget are now logged explicitly.
- Local validation:
  - targeted tests passed: `18 passed`,
  - `py_compile` clean,
  - `terraform validate` clean.
- Next:
  - push this milestone,
  - rebuild/package the updated runtime,
  - apply the live runtime envelope,
  - rerun canonical `PR3-S1`.
### 16:02 UTC - Prevented the capacity-envelope workflow from silently reverting the IG Lambda package
- While staging the live apply sequence, I rechecked the runtime Terraform package selection logic.
- Important finding:
  - if `lambda_ig_package_s3_*` vars are omitted, Terraform reverts to the inline fallback Lambda handler path,
  - so a plain RC2 capacity-envelope apply after materializing the corrected package could silently undo the ingress fix.
- Fix applied to `.github/workflows/dev_full_rc2_r2_capacity_envelope.yml`:
  - added optional remote package inputs (`bucket`, `key`, `sha256`),
  - apply step now passes those vars through whenever they are provided.
- Result:
  - live capacity-envelope uplift can now preserve the corrected remote Lambda bundle instead of reintroducing handler drift.
### 17:05 UTC - RC2.R2 failed on control-plane IAM blindness and a workflow verification bug, not on the live capacity uplift itself
- Pulled the failed run log for `dev-full-rc2-r2-capacity-envelope` instead of rerunning blindly.
- Immediate failure reason:
  - GitHub OIDC role `GitHubAction-AssumeRoleWithAction` is denied `apigateway:GET` on the live stage resource `arn:aws:apigateway:eu-west-2::/apis/ehwznd2uw7/stages/v1`.
  - Result: pre-change capture crashed before the managed apply started, so the run did not actually test or change the live envelope.
- Additional workflow defect found during triage:
  - RC2.R2 reads Lambda reserved concurrency from `get_function_configuration`,
  - that API does not return `ReservedConcurrentExecutions`,
  - so even after IAM repair the gate would have reported a false drift unless corrected.
- Production interpretation:
  - a production-ready control plane must be able to change and verify the ingress edge deterministically,
  - read-surface blindness is itself a blocker because it makes later throughput claims unauditable,
  - the correct fix is to repair both the role permissions and the gate logic, not to weaken the gate.
- Next execution order pinned:
  1. extend the active OIDC role with the missing APIGW/runtime read-control permissions,
  2. patch RC2.R2 to capture blocker evidence instead of crashing and to verify reserved concurrency through the correct Lambda API,
  3. rerun RC2.R2 with the corrected IG package pinned,
  4. resume canonical `PR3-S1` only after the live envelope is truthfully verified.
### 17:24 UTC - OIDC runtime policy corrected the right way and RC2.R2 verifier fixed
- First repair attempt tried to extend inline policy `GitHubActionsM6FRemoteDevFull`.
- AWS rejected that path with a real structural limit:
  - `PutRolePolicy` hit `LimitExceeded` because the inline policy is already at the 10,240-byte ceiling.
- Interpretation:
  - the inline M6F policy is itself saturated and should not keep absorbing PR3/RC2 runtime permissions.
- Corrective decision applied:
  - moved the missing APIGW stage permissions onto the already-attached managed policy `GitHubActionsPR3RuntimeDevFull`,
  - specifically added `apigateway:GET` and `apigateway:PATCH` for the live IG API/stage resource.
- Live apply result:
  - targeted Terraform apply on `infra/terraform/dev_full/ops` updated `GitHubActionsPR3RuntimeDevFull` successfully,
  - the OIDC role now materially has the API Gateway stage read/control surface in addition to its existing Lambda/Managed-Flink/CloudWatch read controls.
- RC2.R2 workflow was also corrected locally:
  - pre/post capture now records IAM read errors instead of crashing,
  - blocker code `RC2R2-BIAM` is emitted deterministically when a surface is unreadable,
  - Lambda reserved concurrency is now read from `get_function_concurrency`, which is the authoritative API.
- Validation completed:
  - `terraform validate` in `infra/terraform/dev_full/ops`: clean,
  - workflow YAML parse: clean,
  - managed policy apply: success.
- Next:
  - rerun `dev-full-rc2-r2-capacity-envelope` with the preserved corrected IG package,
  - verify the live edge truthfully,
  - then return to canonical `PR3-S1`.
### 17:34 UTC - RC2.R2 apply defect is workflow-side structured-input handling
- Two reruns got past preflight and reached the real Terraform apply.
- Both failed before any live change for the same reason:
  - the workflow builds Terraform `-var` arguments directly in bash,
  - `eks_nodegroup_instance_types_json` is a structured list input,
  - shell interpolation breaks the JSON tokenization,
  - Terraform exits with `Variables not allowed` before the uplift actually starts.
- Interpretation:
  - this is not a live capacity blocker,
  - it is a control-plane orchestration defect.
- Decision:
  - stop passing structured RC2.R2 inputs through ad hoc `-var` shell fragments,
  - generate a `.tfvars.json` payload in the workflow and validate/parse the list explicitly before apply.
- Next:
  - patch the workflow,
  - rerun RC2.R2 immediately,
  - then return to canonical `PR3-S1` once the live envelope is truthfully verified.
### 17:42 UTC - RC2.R2 tfvars file exists; remaining defect is path resolution under `terraform -chdir`
- Latest rerun proved the structured-input fix works:
  - the workflow generated `rc2_r2_capacity_uplift.auto.tfvars.json`,
  - the JSON list parsed correctly,
  - shell tokenization is no longer the blocker.
- New failure is narrower:
  - Terraform cannot find the tfvars file because `-var-file` is being resolved relative to `infra/terraform/dev_full/runtime` after `-chdir`,
  - while the file was written under the repo-root `RUN_DIR` path.
- Interpretation:
  - still a control-plane execution bug, not a live runtime blocker.
- Next:
  - pass an absolute tfvars path,
  - rerun RC2.R2 immediately,
  - then inspect the first true live uplift result.
### 17:51 UTC - First true RC2.R2 live-apply blocker is runtime-module read IAM, not workflow plumbing
- The latest rerun moved past all workflow serialization/path defects and into the real runtime Terraform plan.
- Plan failed on two missing read permissions for the GitHub OIDC role:
  - `states:DescribeStateMachine` on `fraud-platform-dev-full-platform-run-v0`
  - `iam:GetOpenIDConnectProvider` on the EKS OIDC provider ARN.
- Interpretation:
  - because RC2.R2 uses the runtime Terraform module as source of truth, the role must be able to refresh already-managed runtime resources even when the immediate uplift target is APIGW/Lambda/EKS.
- Decision:
  - extend the attached managed runtime policy with those two least-privilege read permissions,
  - apply it live,
  - rerun RC2.R2 again from the same target.
### 18:00 UTC - RC2.R2 apply scope repinned to the actual capacity surfaces
- Latest runtime plan showed unrelated pending changes entering the RC2.R2 gate:
  - Step Functions version inspection,
  - IRSA MSK data-plane policy creation.
- Interpretation:
  - those are real runtime drifts, but they are not part of the RC2.R2 ingress-capacity acceptance boundary.
- Decision:
  - repin RC2.R2 managed apply scope to the three authoritative capacity resources only:
    - `aws_apigatewayv2_stage.ig_v1`
    - `aws_lambda_function.ig_handler`
    - `aws_eks_node_group.m6f_workers`
- Why this is correct:
  - it keeps blast radius aligned to the gates purpose,
  - live verification still occurs on APIGW/Lambda/EKS,
  - unrelated runtime drifts remain explicit follow-up work instead of contaminating the capacity gate.
- Next:
  - patch workflow to use targeted apply on those three resources,
  - rerun RC2.R2 immediately.
### 18:09 UTC - RC2.R2 closed green; PR3-S1 resumption point pinned
- Authoritative RC2.R2 run:
  - workflow `22772124603`
  - execution `rc2_r2_capacity_envelope_20260306T162513Z`
  - result: `overall_pass=true`, `blocker_count=0`, `next_gate=READY_FOR_RC2_R5`.
- Live edge metrics now match the required envelope exactly:
  - APIGW `ehwznd2uw7/v1`: `3000 rps / 6000 burst`
  - Lambda `fraud-platform-dev-full-ig-handler`: `1024 MB / 30 s / reserved 300`
  - Lambda env pins verified live:
    - `KAFKA_REQUEST_TIMEOUT_MS=1500`
    - `IG_POLICY_ACTIVATION_AUDIT_MODE=store_only`
  - EKS nodegroup `fraud-platform-dev-full-m6f-workers`: `desired/min/max=4/2/8`, `instance_types=["t3.xlarge"]`, `status=ACTIVE`.
- Evidence quality:
  - no IAM read errors,
  - blocker register empty,
  - S3 publication readback clean for all authoritative artifacts.
- RC2.R2 apply scope correction remains important:
  - gate now mutates only APIGW stage, IG Lambda, and M6F nodegroup,
  - unrelated runtime-module drift no longer contaminates the capacity gate.
- Next execution pinned:
  - resume canonical `PR3-S1` from strict boundary `pr3_20260306T021900Z`,
  - refreshed WSP image digest `sha256:619f45f27db151c8cda0b1c0e574b670e4def8bfd874fae8a39133645dba27a2`,
  - `lane_count=138`, `stream_speedup=95.0`, `target_steady_eps=3000`, `duration_seconds=1800`.
### 18:26 UTC - PR3-S1 503 root cause is broken IG Lambda packaging, not replay-client instability
- I inspected the failed canonical PR3-S1 rerun (`22772253988`) and confirmed the blocker is real runtime behavior.
- PR3-S1 impact metrics from the authoritative steady summary:
  - requests observed: `2513`
  - admitted requests: `0`
  - admitted throughput: `0.0 eps`
  - 5xx rate: `100%`
  - lane failures: `138/138`
  - latency posture: `p95=8912.023 ms`, `p99=9394.324 ms`
- Readable interpretation:
  - this is not a scorecard bug or a weak summary path,
  - the platform is fully failing the steady ingress objective at the current live boundary.
- WSP lane logs show the emitter starts correctly and then retries repeated `http_503` from IG until exhaustion.
- Lambda logs isolated the root cause:
  - `ModuleNotFoundError: No module named 'confluent_kafka'`
  - the ingress Lambda artifact cannot build the Kafka publisher, so the hot path never reaches MSK.
- Source-level cause found:
  - `scripts/dev_substrate/build_ig_lambda_bundle.py` builds from `requirements/ig-lambda.requirements.txt`
  - that requirements file does not include `confluent-kafka`
  - therefore the workflow is deterministically publishing a broken Lambda bundle.
- Secondary negative-path defect found:
  - DLQ send attempts are timing out to SQS from the VPC Lambda,
  - current runtime endpoint set omits `sqs`, so the failure path is not network-complete.
- Additional replay drift found:
  - `pr3_s1_wsp_replay_dispatch.py` defaulted back to `assign_public_ip=ENABLED`
  - this must be repinned to private posture before the lane is treated as canonical.
- Decision:
  - fix the IG bundle at source with Lambda-compatible dependency packaging,
  - add `sqs` endpoint coverage for the Lambda negative path,
  - repin WSP dispatcher to private posture,
  - rematerialize the ingress edge,
  - rerun `PR3-S1` from the same strict boundary.
### 18:42 UTC - IG bundle build path simplified to verified cross-platform wheel staging
- The first correction tried a Dockerized Lambda-image build because the defect was ABI-sensitive.
- That added an unnecessary control-plane dependency; the failed GitHub run showed the bundle step itself should stay simpler.
- Verified replacement path:
  - generic deps staged as Linux `cp312` wheels under `manylinux2014_x86_64`
  - `confluent-kafka==2.13.0` staged separately under `manylinux_2_28_x86_64`
  - local bundle smoke confirmed:
    - `confluent_kafka` package present
    - bundled native Kafka libs present
    - Linux `rpds_py` extension artifacts present
- Decision:
  - authoritative IG bundle build path is now host-mode cross-platform wheel staging, not Docker.
  - rerun the IG edge materialization with this verified builder path next.
### 18:49 UTC - Post-IG-fix bounded WSP smoke exposed subnet-default drift, not ingress regression
- After rematerializing the ingress edge locally, I ran a one-lane canonical WSP smoke to avoid wasting a full steady window.
- Result:
  - the task did not reach the replay phase,
  - failure was `TaskFailedToStart` -> `CannotPullContainerError` from ECR,
  - cause: canonical dispatcher still used public-subnet defaults while `assignPublicIp=DISABLED` was already repinned.
- Interpretation:
  - this is a replay-launch network defect, not another IG defect,
  - the correct response is to move the dispatcher to the private runtime subnets, not to weaken the private posture.
- Decision:
  - repin `pr3_s1_wsp_replay_dispatch.py` subnet defaults to the runtime private subnets,
  - rerun the bounded smoke immediately,
  - return to full PR3-S1 only after that startup path is clean.
### 17:40 UTC - PR3-S1 source posture corrected back to public-edge WSP; new blockers are IG-internal
- I reran a bounded one-lane `PR3-S1` smoke on the old public-subnet/public-IP WSP posture to test whether the private-subnet repin was actually part of the canonical production proof.
- Result:
  - WSP reached the public ingress edge,
  - Lambda invocations were recorded for the same request window,
  - the failure moved fully inside `IG`.
- Readable interpretation:
  - the previous private `execute-api` routing problem was real, but it was on a secondary hardening lane rather than the canonical `PR3` outside-world steady proof.
  - For `PR3-S1`, the truthful source posture is public-edge WSP replay, because WSP is the outside-world producer and PR3 is certifying the real ingress boundary.
- New hard blockers isolated from live Lambda logs:
  - `DynamoDB` idempotency lookups are timing out from the VPC Lambda (`Connect timeout on endpoint URL: https://dynamodb.eu-west-2.amazonaws.com/`).
  - Real `6B` event payload validation is failing because the bundle is missing transitive schema refs (`Unresolvable: schemas.layer3.yaml#/$defs/hex64`).
- Production conclusion:
  - PR3-S1 is still red, but now for truthful reasons inside the IG hot path rather than replay transport guesswork.
  - Next fix set is explicit: add private DynamoDB reachability for Lambda and rebuild the Lambda artifact with full transitive schema closure.
### 17:41 UTC - IG bundle/runtime fix set pinned from live evidence
- Current bundle defect:
  - the Lambda package includes `schemas.5B.yaml` and `schemas.6B.yaml` only,
  - but not the sibling shared schema packs they reference (`schemas.layer1.yaml`, `schemas.layer3.yaml`).
- Current infra defect:
  - the Lambda idempotency boundary still depends on unreachable public DynamoDB DNS from private subnets.
- Decision:
  - repin canonical PR3 WSP source defaults back to public-edge posture,
  - add a DynamoDB gateway endpoint on private route tables,
  - expand the IG Lambda bundle builder to include full transitive schema contract directories,
  - rematerialize the edge and rerun bounded WSP proof before resuming the full steady window.
### 17:49 UTC - Bounded PR3-S1 canonical smoke is green after edge hot-path remediation
- Live remediation applied:
  - Lambda bundle republished with full transitive data-engine schema closure,
  - DynamoDB gateway endpoint added for private Lambda idempotency access,
  - canonical PR3 WSP source posture corrected back to public-edge replay.
- Bounded one-lane smoke result:
  - requests: `60`
  - admits: `60`
  - admitted throughput: `1.0 eps`
  - error rate: `0.0%`
  - 4xx/5xx: `0 / 0`
  - latency: `p95=129.13 ms`, `p99=152.32 ms`
  - verdict: `REMOTE_WSP_WINDOW_READY`, `open_blockers=0`
- Readable interpretation:
  - the ingress path is now truthful and working end to end on real WSP data,
  - the remaining PR3-S1 work is scaling to the production target, not repairing broken semantics or missing dependencies.
- Next action pinned:
  - run bounded high-rate calibration from the last credible operating point (`138` lanes, `95.0` speedup, `3000 eps` target),
  - then promote to the full steady window only if the bounded proof stays within thresholds.
### 18:05 UTC - PR3-S1 high-rate red state traced to synthetic object-store health gating
- Bounded high-rate calibration after the ingress edge fixes produced:
  - admitted throughput `1351.178 eps` vs target `3000 eps`
  - error rate `6.1885%`
  - `5xx=16043`
  - latency `p95=506.46 ms`, `p99=1351.00 ms`
- Lambda log drill-down showed the dominant failure reason was `IG_UNHEALTHY:OBJECT_STORE_UNHEALTHY`.
- Infra verification ruled out the simple explanations:
  - the private route table already has `S3` and `DynamoDB` gateway endpoints,
  - the Lambda role already has object-store `S3` and matching `KMS` permissions,
  - the same bucket/path family worked in the bounded low-rate proof.
- Production interpretation:
  - the current gate health model is unsound for high EPS because it performs a synthetic `S3` health write on the admission path,
  - under concurrency fan-out, that turns a secondary observability write into a hard admission dependency,
  - one transient write failure can poison a Lambda environment into request-level `5xx` for the health-cache interval.
- Decision pinned:
  - replace synthetic object-store probe writes with observed health from real receipt/quarantine/governance store operations,
  - add exact exception logging for future store failures,
  - then rematerialize the IG edge and rerun the bounded PR3-S1 calibration before attempting the full steady window.
### 18:30 UTC - PR3-S1 dispatcher evidence harvest violated the runtime budget
- The IG Lambda remediation itself landed cleanly:
  - live code hash updated to `dnFcCI/li0e9vYozKAnk/oB9HcAsOQrsPnVA=`
  - no new `OBJECT_STORE_UNHEALTHY` errors appeared in the fresh rerun window
  - ECS showed no leaked running WSP tasks after the bounded window
- The next defect was in the certification tooling, not the runtime edge:
  - `pr3_s1_wsp_replay_dispatch.py` stayed alive after the run because it was sweeping full `CloudWatch Logs` events for all `138` lanes before writing the summary.
- Production interpretation:
  - that full log sweep is nonbinding evidence for S1 and violates our runtime-budget law,
  - the proper high-lane posture is metric-surface proof plus lane stop metadata, not exhaustive per-line log replay.
- Decision pinned:
  - change the dispatcher to metadata-only lane evidence for high-lane runs,
  - preserve full log parsing only for smaller investigative reruns,
  - rerun the bounded calibration immediately after the tooling fix.
### 18:55 UTC - PR3-S1 duplicate-path receipt normalization defect isolated from live high-rate evidence
- After the observed-store health fix removed `OBJECT_STORE_UNHEALTHY`, the canonical bounded `PR3-S1` window was still red.
- Fresh metric and log interpretation showed the failure had changed shape:
  - API Gateway `Count` matched `5xx` in the affected bins,
  - Lambda `Errors` stayed `0`,
  - concurrency stayed well below the reserved limit,
  - request handling was executing but returning handled failures.
- The hot-path exception from Lambda logs was:
  - `Schema validation failed for ingestion_receipt.schema.yaml: Decimal('4') is not of type 'integer'`.
- Production interpretation:
  - duplicate/retry traffic is being misclassified as server failure because DynamoDB returns numeric fields as `Decimal`,
  - the duplicate path revalidates those fields against the JSON receipt schema without normalization,
  - at production load this would turn ordinary at-least-once duplicate pressure into avoidable API `503` amplification.
- Decision pinned:
  - keep the receipt contract strict,
  - normalize DynamoDB-returned numeric values back to JSON-native primitives before duplicate receipt validation,
  - redeploy the IG Lambda with that fix,
  - rerun the bounded canonical `PR3-S1` window and let the next fresh metric surface tell us the real remaining bottleneck.
### 19:15 UTC - PR3-S1 live rerun cleared 503s and exposed false-negative health logic under duplicate pressure
- The post-redeploy bounded PR3-S1 rerun no longer produced API `5xx`; all observed failures shifted to `4xx` with WSP seeing `IG_PUSH_REJECTED` quarantine receipts.
- That confirms the duplicate-path Decimal normalization fix is live and effective.
- New Lambda root causes from the same window:
  - `NoopOpsIndex` in the Lambda runtime is missing `probe()`, so health enforcement raises `AttributeError`.
  - `ObservedObjectStore` is treating `FileExistsError` from `write_json_if_absent` as store failure even though those collisions are normal first-writer-wins behavior for receipts/governance markers.
- Production interpretation:
  - the platform is now rejecting good traffic because health logic is misclassifying normal idempotent outcomes as faults,
  - this is exactly the kind of false-negative gating that would distort production duplicate pressure and hide the real throughput picture.
- Decision pinned:
  - restore the full no-op ops-index contract,
  - classify `write_json_if_absent` conflicts as healthy idempotent completion,
  - redeploy and rerun the same bounded canonical S1 window immediately.
### 19:30 UTC - PR3-S1 reduced to a real ingress capacity miss; repinning Lambda from measured concurrency demand
- Latest bounded PR3-S1 after the health fixes admitted `286,502` requests over the 180-second measurement window (`1,591.68 eps`) and cut open blockers down to five.
- The remaining blockers are all capacity/tail-latency blockers:
  - `5xx_total=50,907`
  - `p95=474.37 ms`
  - `p99=1441.53 ms`
- CloudWatch isolated the limiter cleanly:
  - Lambda concurrency sat at the `300` reserved ceiling for the full measured window,
  - Lambda throttles were `17,473`, `16,936`, and `16,498` in the three measured minutes,
  - WSP saw matching `http_503` retry pressure.
- Production interpretation:
  - the current ingress architecture is not yet disproven; the current pin is,
  - `300` reserved concurrency is mathematically inconsistent with the declared `3000 eps` steady target.
- Decision pinned:
  - repin ingress Lambda to `2048 MB` and `1000` reserved concurrency,
  - apply that live,
  - rerun bounded PR3-S1 before considering deeper architectural change.
### 19:40 UTC - PR3-S1 rerun at the legal Lambda ceiling proves the current miss is account-quota-bounded
- AWS account quota check showed regional Lambda `ConcurrentExecutions=400`, making `360` the highest legal single-function reservation while preserving the required unreserved floor.
- I submitted a quota increase request to `1500` concurrent executions:
  - request id `712a9a7a12174f7798304b4b0ad60407M9HoApfX`
  - status `PENDING`
- I then reran bounded PR3-S1 at `reserved_concurrency=360`, `memory=2048 MB`.
- Result:
  - admitted throughput `2350.33 eps`
  - error rate `4.20%`
  - `5xx=18,568`
  - latency `p95=351.87 ms`, `p99=870.57 ms`
- Production interpretation:
  - this account-limited Lambda path is now close but still not certifiable,
  - the platform is no longer failing semantically; it is boxed in by the account quota,
  - next action is to pursue a service-backed ingress alternative in parallel with the pending quota increase.
### 20:05 UTC - PR3-S1 production decision: stop treating the Lambda quota case as the plan and promote a service-backed ingress edge without regressing the hot-path semantics
- I reviewed the real options against the production target instead of against rerun convenience.
- Rejected as primary path:
  - waiting on the pending Lambda quota increase alone,
  - promoting the old Flask IG service unchanged, because that would move the hot idempotency boundary from `DynamoDB` onto `Aurora/Postgres`.
- Chosen direction:
  - keep the proven managed-edge semantics (`DDB` idempotency, `S3` receipts/governance, `Kafka` publish),
  - move the request execution shell to a horizontally scaled ECS/Fargate service behind a load balancer,
  - keep `Managed Flink` scoped to downstream stream-processing lanes only.
- Readable interpretation:
  - this removes the account-level Lambda ceiling without changing what the ingress trust boundary actually does,
  - it is a production correction, not a shortcut to make `PR3-S1` pass.
- Next action pinned:
  - factor the managed edge into reusable HTTP service code,
  - materialize ALB/ECS ingress in `dev_full`,
  - repoint canonical `PR3-S1` WSP replay to that endpoint,
  - rerun bounded `S1` from the same strict PR3 root.
### 20:35 UTC - Managed IG service code and private ALB/ECS substrate validated locally
- Implemented:
  - `managed_service.py` HTTP wrapper that proxies into the existing managed IG edge logic,
  - `gunicorn` image/runtime contract for the service container,
  - private `dev_full` ALB/ECS/SSM infrastructure for the managed service path,
  - PR3 WSP dispatcher auto-resolution of the service URL from SSM with API Gateway fallback.
- Local validation:
  - `py_compile` passed for the touched Python modules,
  - focused IG tests passed: `17 passed`,
  - `terraform validate` passed for `infra/terraform/dev_full/runtime`.
- Readable interpretation:
  - the service-backed ingress correction is now locally coherent and is ready to be built and materialized live,
  - the next risk is no longer design ambiguity but live deployment correctness/performance.
### 20:50 UTC - Live materialization plan pinned for the service-backed ingress correction
- I have moved from local validation to the live-proof boundary for the managed IG service.
- The live sequence is pinned as:
  - apply only the `ig_service_*` runtime resources and the SSM service URL publication,
  - verify ALB target health, ECS desired/running count, and SSM resolution,
  - run a bounded canonical `PR3-S1` replay against the SSM-resolved service endpoint from the existing strict PR3 root,
  - inspect metrics/logs immediately and remediate the real runtime bottleneck that appears.
- Acceptance for this deployment step is explicit:
  - no boot-loop,
  - no health-check flap,
  - no private-dependency reachability gap,
  - no systemic service-edge `5xx`,
  - all fresh evidence lands under `runs/dev_substrate/dev_full/road_to_prod/run_control/pr3_20260306T021900Z/`.
- Production interpretation:
  - this is now a live throughput-hardening exercise on the corrected ingress architecture,
  - if the bounded run misses target, that becomes the next optimization/remediation problem rather than a reason to retreat to the quota-bound Lambda path.
### 21:05 UTC - First live ECS boot fault isolated to the service shell
- The targeted Terraform apply materially created the managed ingress substrate and published the internal service URL to SSM.
- Health did not stabilize on first boot:
  - ECS stayed at `runningCount=0`, `pendingCount=6`,
  - ALB targets repeatedly drained,
  - container logs showed the same immediate process failure.
- The concrete error in CloudWatch logs:
  - `gunicorn: error: unrecognized arguments: --factory`
- Production interpretation:
  - the ingress core has not failed yet; the service shell is not reaching app initialization,
  - this is a runtime command/packaging defect in the ECS wrapper layer, not an argument against the service-backed architecture.
- Correction pinned and applied in code:
  - remove the unsupported `--factory` flag,
  - keep the supported `module:create_app()` factory invocation,
  - redeploy the task definition and recheck ECS/ALB health before any replay traffic is sent.
### 21:15 UTC - Managed IG service reached healthy live posture
- After the shell fix and a forced clean rollout onto task definition revision `:2`, the service reached the expected live baseline:
  - ECS `desiredCount=6`,
  - ECS `runningCount=6`,
  - ALB target group reports healthy targets across the private subnets,
  - SSM service URL remains published at `/fraud-platform/dev_full/ig/service_url`.
- Healthy task logs now show:
  - `Starting gunicorn 23.0.0`
  - `Listening at: http://0.0.0.0:8080`
  - worker boot with no immediate exit loop
- Production interpretation:
  - the service-backed ingress correction has cleared bootstrap/runtime-shell defects,
  - the next boundary is the bounded canonical `PR3-S1` replay to measure true throughput/latency/error posture on the corrected architecture.
### 21:25 UTC - PR3-S1 measurement drift corrected before the bounded replay
- I inspected the dispatcher before rerunning `PR3-S1` and found an evidence defect:
  - the replay traffic had been repointed to the internal ALB path,
  - but the measurement code still read `AWS/ApiGateway` metrics only.
- That would have made the next bounded run non-authoritative because it would score the old edge while exercising the new one.
- Correction implemented:
  - auto-resolve the active ingress surface from the ingest URL,
  - use `AWS/ApiGateway` metrics only when the active host is `execute-api`,
  - use `AWS/ApplicationELB` metrics (`RequestCount`, `4xx/5xx`, `TargetResponseTime`) when the active host is the internal ALB.
- Production interpretation:
  - PR3 evidence is now bound to the real ingress edge under test,
  - the next bounded replay can be trusted as throughput/latency/error evidence for the service-backed architecture.
### 21:35 UTC - First truthful bounded replay isolated a bundle-root defect in the service container
- The corrected bounded `PR3-S1` replay measured the internal ALB path truthfully and failed fast at `15.3 admitted eps`.
- `WSP` logs showed repeated `http_503` and `IG_PUSH_RETRY_EXHAUSTED`.
- `IG` service logs exposed the actual root cause:
  - `FileNotFoundError: '/app/src/config/platform/ig/schema_policy_v0.yaml'`
  - raised while `_gate_for(...)` loaded the schema policy.
- Interpretation:
  - the service is preserving the right ingress logic,
  - but the Lambda-derived `_bundle_root()` assumption is wrong for the container image layout (`/app` root vs `/app/src` package root).
- Correction pinned:
  - make bundle-root resolution runtime-aware,
  - set `PLATFORM_BUNDLE_ROOT=/app` explicitly in the ECS task definition,
  - rebuild the image, redeploy the service, and rerun bounded `PR3-S1` immediately.
### 22:05 UTC - Active IG revision verification cleared the stale-log ambiguity
- I verified the live ingress service against the current ECS deployment only.
- Active truth now is:
  - task definition raud-platform-dev-full-ig-service:3
  - image digest sha256:28deae6b0752b116ab44f0c286805df8be35761059d06df939069e95501a18cb
  - PLATFORM_BUNDLE_ROOT=/app present in the live task definition
  - 6/6 ECS tasks healthy and 6/6 ALB targets healthy.
- I then pulled only the current task log streams for those six revision-:3 tasks.
- Result:
  - clean gunicorn startup only,
  - no FileNotFoundError,
  - no schema-path bootstrap error,
  - no boot-loop.
- Interpretation:
  - the earlier /app/src/config/... failure was a stale-stream read from drained tasks and is not the active service truth anymore,
  - the next bounded PR3-S1 replay can now be trusted to measure the real service under load.
### 22:15 UTC - PR3-S1 bounded rerun hit Fargate quota because WSP lanes are oversized for the service-backed architecture
- The rerun failed at WSP launch, not inside IG:
  - PR3.S1.WSP.B01_RUN_TASK_FAILED:wsp_lane_128:Youve reached the limit on the number of vCPUs you can run concurrently.
- Capacity facts:
  - regional Fargate on-demand quota is 140 vCPU
  - WSP task definition is 1 vCPU / 2 GiB
  - live IG service consumes 12 vCPU (6 x 2 vCPU).
- Meaning:
  - the old 138-lane S1 topology was valid when IG was not consuming steady Fargate capacity,
  - it now over-requests 150 vCPU total and fails before the fleet fully launches.
- Decision:
  - keep the 3000 eps target fixed,
  - keep 138 lanes for the next replay,
  - right-size WSP lane overrides to 512 CPU / 1024 MiB because the emitter is an HTTP replay client and the old 1 vCPU lane shape is wasteful.
- Additional fix:
  - pr3_s1_executor.py now reads latency from the runtime summary instead of incorrectly re-querying API Gateway after the active edge moved to the ALB-backed service.
- Next action: rerun bounded canonical PR3-S1 immediately with the right-sized WSP lane shape.
### 22:30 UTC - Right-sized WSP lanes exposed the real service-backed IG bottleneck
- Bounded canonical PR3-S1 rerun used:
  - 138 lanes
  - stream_speedup=95
  - 	arget_request_rate_eps=3005
  - 	ask_cpu=512
  - 	ask_memory=1024
- Result:
  - equest_count_total=66642
  - dmitted_request_count=65029
  - observed_admitted_eps=361.272
  - rror_rate_ratio=0.024204
  - 4xx_total=1219
  - 5xx_total=394
- Supporting runtime evidence:
  - ALB TargetResponseTime p95=0.769s..3.480s
  - ALB TargetResponseTime p99=2.382s..4.335s
  - ECS CPU 36-60%
  - ECS memory 10-12%
- Interpretation:
  - the quota blocker is gone,
  - the service-backed ingress hot path itself is too slow under steady load,
  - this is not a raw CPU or memory saturation problem.
- WSP lane logs show repeated 	imeout and http_502 retries during the window, which explains the collapse in per-lane emission.
- Next correction pinned:
  - surface IG phase timings through the service logging path,
  - rebuild and redeploy the image,
  - run a smaller diagnostic window before the next full S1 replay.
### 22:45 UTC - PR3-S1 evidence was contaminated by duplicate-path replay because the dispatcher reused a stale platform_run_id
- I checked the new revision-4 diagnostic window before doing more scaling.
- Root cause found:
  - the PR3-S1 dispatcher still injected `platform_run_id=platform_20260223T184232Z`
  - IG metrics for the same window were dominated by `decision.DUPLICATE`
  - so the measured S1 throughput was mostly duplicate-path throughput, not fresh admits.
- Meaning:
  - the current S1 evidence is not claimable for steady fresh-ingest certification,
  - duplicate-path behavior is still useful realism evidence, but it cannot stand in for admit-path proof.
- Decision:
  - remove the stale hardcoded default from the dispatcher
  - mint a fresh attempt-scoped `platform_run_id` by default for each PR3-S1 replay
  - keep explicit override only for deliberate same-run replay cases.
- Next action:
  - patch the dispatcher now
  - rerun the bounded S1 window with a fresh run namespace
  - separate admit-path metrics from duplicate-path metrics before choosing the next remediation.
### 23:05 UTC - PR3-S1 remediation is now targeting the real synchronous ingress hot path, not the easiest rerun path
- Fresh-admit evidence is trustworthy now: the stale `platform_run_id` contamination is gone, and IG metrics are dominated by `decision.ADMIT` rather than `decision.DUPLICATE`.
- The measured limiter is the synchronous ingress request path itself:
  - EB publish is durable but non-trivial,
  - receipt persistence is also materially expensive,
  - the managed response path additionally performs a redundant idempotency lookup and returns the full receipt body inline.
- Production interpretation:
  - this is not a reason to lower the target or declare the current path "good enough";
  - it is a reason to remove avoidable synchronous response work before paying for more fleet scale.
- Chosen remediation direction:
  - keep `ADMIT` tied to durable EB append,
  - reduce hot-path response overhead,
  - add finer-grained latency instrumentation for DDB/object-store substeps,
  - rerun bounded PR3-S1 on the same strict boundary after deployment.
### 23:20 UTC - First managed IG hot-path fix set is implemented and locally validated
- Hot-path code changes now in place:
  - no redundant post-admit idempotency lookup just to echo `receipt_ref`,
  - receipt refs are returned directly from the admission result,
  - AWS clients used by the managed edge now use explicit larger connection pools instead of the small botocore defaults,
  - finer-grained DDB/object-store latency metrics are emitted from the admission path.
- Local validation status:
  - `py_compile` passed for all changed ingress modules,
  - targeted ingress tests passed: `test_managed_service.py`, `test_phase4_service.py`, `test_admission.py`.
- Remaining local gap:
  - `test_phase510_efficiency.py` could not run because local `psycopg` is missing; that is an environment dependency issue, not a regression signal from this change.
- Next action:
  - commit/push this boundary,
  - rebuild and redeploy the managed ingress service image,
  - rerun bounded fresh PR3-S1 against the same strict root.
### 23:34 UTC - Verified live rollout of the hot-path ingress build before the next bounded PR3-S1 rerun
- ECS service `fraud-platform-dev-full-ig-service` is now on task definition `:5` with image digest `sha256:0803c98e947e8b2dfd243d085b860fbdcdca603785e7362a14522766b897a341`.
- Runtime posture confirmed:
  - private subnets only
  - desired/running count = 6
  - task shape = `2048 CPU / 4096 MiB`
  - rate-limit env = `3000 rps / 6000 burst`
  - gunicorn = `4 workers x 32 threads`
- Interpretation:
  - the next PR3-S1 window will exercise the new ingress code path rather than the stale revision,
  - service rollout alone is not evidence of production readiness; the next meaningful proof remains a fresh bounded canonical WSP->IG run on a fresh namespace.
- Next action:
  - execute bounded canonical PR3-S1 immediately,
  - evaluate admitted EPS, 5xx rate, and latency against the production threshold without waivers.
### 21:48 UTC - PR3-S1 red split into an under-driven replay envelope and a latency tooling bug
- Fresh bounded result on the new ingress image:
  - admitted EPS = `465.75`
  - admitted requests = `27,945`
  - `4xx=0`, `5xx=0`
- Interpretation:
  - the ingress hot-path change improved stability,
  - but the replay harness still did not drive anything close to the required `3000 EPS`, so this window cannot certify or falsify the true 3k edge.
- Root causes found:
  - replay side: `stream_speedup=95` with `24` lanes underfeeds the oracle-backed replay for a 3k target;
  - measurement side: the dispatcher strips `targetgroup/` from the ALB dimension, so `TargetResponseTime` p95/p99 comes back empty even though CloudWatch has the datapoint.
- Production conclusion:
  - do not lower the PR3 target,
  - fix the latency collector,
  - then rerun on a materially stronger replay multiplier (`~650-700` class) so the platform is tested at a real certification load boundary instead of a toy driver envelope.
### 21:46 UTC - PR3-S1 throughput ceiling is now pinned on WSP's synchronous push model
- Rerun at `stream_speedup=700` produced:
  - admitted EPS = `430.97`
  - `4xx=18`, `5xx=0`
  - ALB latency now readable once the target-group dimension bug was fixed.
- Interpretation:
  - replay speedup is no longer the main limiter;
  - WSP is structurally capped because each output pushes to IG synchronously and a lane only has four output threads.
- Production consequence:
  - adding hundreds of lanes to compensate would be wasteful and still the wrong design;
  - the correct fix is bounded intra-output HTTP concurrency with safe checkpoint advancement.
- Additional tooling correction needed:
  - ALB `TargetResponseTime` came back in seconds and is currently mislabeled as milliseconds by the dispatcher; this must be corrected before the next score can be trusted.
### 21:55 UTC - WSP concurrency boundary passed local smoke; pytest collection still blocked by missing `psycopg`
- Validation completed:
  - `py_compile` passed for the changed WSP runner and PR3 dispatcher
  - direct smoke with `WSP_IG_PUSH_CONCURRENCY=4` produced `STREAMED`, `emitted=8`, `max_concurrency=4`
- Interpretation:
  - the new path is materially concurrent and not just a dormant config change.
- Remaining local validation gap:
  - pytest collection for WSP tests still fails because this workstation lacks `psycopg`; that is an environment dependency gap, not a signal that the concurrency change is broken.
- Next action:
  - commit/push this boundary,
  - repack the remote image,
  - rerun canonical PR3-S1 on the updated WSP image.
### 21:53 UTC - Remote WSP runtime is now materially refreshed to the concurrency-capable image
- Remote packaging run `22783424960` built commit `fba6d353de6389183caafb7d7f01899595edc984` into immutable digest `sha256:8f70c1f862fe28f14398b292f271f6fa7085a0c25eead2d1574c8a5e846c0143`.
- Registered `fraud-platform-dev-full-wsp-ephemeral:30` pinned to that digest.
- Meaning:
  - the next PR3-S1 run will finally execute the new WSP intra-output concurrency path remotely instead of the old serialized runner.
- Evidence path:
  - `runs/dev_substrate/dev_full/road_to_prod/run_control/pr3_20260306T021900Z/wsp_taskdef_refresh_20260306T215258Z/`

### 22:05 UTC - PR3-S1 ingress collapse is traced to request-path gate/health stampedes plus runaway post-timeout work
- I reviewed the live red window (`21:54Z` to `21:57Z`) from the service-backed `PR3-S1` rerun.
- Impact metrics:
  - ALB request count by minute: `2512`, `7529`, `9699`
  - target `2xx`: `2782`, `4193`
  - ALB `5xx`: `119`, `1`
  - ALB latency: worst-minute `p95=24.428s`, `p99=29.034s`
  - ECS CPU average: `84.18%`, `65.65%`, `93.73%` with `max=100%`
  - ECS memory average stayed low at `16%` to `22%`
- The ECS service history shows the practical consequence: tasks were repeatedly marked unhealthy because the container health check timed out, so the service was starving `/healthz` and replacing itself during the certification window.
- Root causes now pinned from raw ingress logs:
  - gate construction is not single-flight; the window emitted `313` `IG gate initialized` events with `p50 init=6.234s`, `p95=12.052s`, `max=15.8s`
  - health checks are running inline on request threads with `IG_HEALTH_BUS_PROBE_MODE=describe`, which means stale/cold cache refresh can hit object-store probes and Kafka metadata calls on the hot path
  - request threads continue working far past the client budget: managed request logs reached `p9595.45s`, `p99110.38s`, `max134.82s`
- Phase-level signal:
  - `phase.validate_seconds` was the exploding stage (`20s` to `95s` class)
  - `phase.publish_seconds` stayed roughly `1s` to `4.5s`
  - `phase.receipt_seconds` stayed roughly `0.8s` to `6.4s`
- Production interpretation:
  - this is not just a matter of adding more tasks
  - scaling the current shape would scale an internal stampede and burn more money for bad throughput
  - the correct next move is to eliminate hot-path gate/health stampedes and prevent requests from chewing CPU long after the response budget is already lost
- Next implementation sequence:
  - single-flight gate initialization
  - background/cached health evaluation instead of request-thread probe execution
  - remove `describe` bus-health mode from the ECS ingress hot path
  - then retune/scale the service envelope and rerun canonical `PR3-S1`

### 22:14 UTC - Implemented the anti-stampede ingress fix set and repinned the ECS service health posture
- Runtime code hardening completed:
  - `_gate_for(platform_run_id)` is now single-flight under lock
  - gate initialization now pre-warms health once instead of letting first-request concurrency stampede cold health evaluation
  - stale health now refreshes in the background while request threads use the last known health state
- IaC pins updated for the service-backed ingress path:
  - `IG_HEALTH_BUS_PROBE_MODE` is now variable-driven and pinned to `none` for the ECS service hot path
  - ingress service desired count pin moved from `6` to `12` for the next bounded capacity rerun
- Local validation:
  - `py_compile` passed for the changed ingress modules
  - `terraform fmt -check infra/terraform/dev_full/runtime` passed
  - targeted tests passed via `py -3.12 -m pytest` for:
    - `test_health_governance.py`
    - `test_managed_service.py`
    - `test_admission.py`
- Expected effect on the next PR3-S1 rerun:
  - fewer cold gate initializations
  - no Kafka metadata probe storm on request threads
  - lower `phase.validate_seconds` tail
  - less task churn from ALB health-check starvation
- Next action:
  - materialize the new image and live ECS service posture
  - then rerun bounded canonical `PR3-S1` to see the post-stampede throughput/latency frontier

### 22:24 UTC - Live runtime rollout is confirmed on the corrected image and corrected ingress posture
- Verified live ECS state instead of assuming the rollout succeeded:
  - ingress service `fraud-platform-dev-full-ig-service` is on task definition `:6`
  - image digest is `sha256:1f33883cf7c53e8b6f5aaae3e79e62a880048ff97ec35142fb73b3da7e3be80c`
  - `IG_HEALTH_BUS_PROBE_MODE=none`
  - desired/running count is `12/12`
  - private-subnet posture remains intact with `assignPublicIp=DISABLED`
- Verified remote WSP replay task definition `fraud-platform-dev-full-wsp-ephemeral:31` is pinned to the same digest.
- Meaning:
  - the next canonical `PR3-S1` rerun is no longer contaminated by stale image drift
  - if the state remains red, the remaining limiter is now a real live-platform behavior issue rather than a packaging/materialization miss
- Next action:
  - rerun bounded canonical `PR3-S1` from the same strict execution root and measure the new throughput/latency/error posture

### 22:31 UTC - Post-remediation PR3-S1 moved the frontier materially, but retry overlap is still being mishandled as quarantine truth
- New bounded canonical `PR3-S1` result:
  - admitted throughput `621.800 eps` vs previous `46.367 eps`
  - error ratio `3.986%`
  - ALB `5xx=0`
  - ALB `p95=7.138s`
  - ALB `p99=10.966s`
- Interpretation:
  - the anti-stampede ingress fix worked; this is no longer a general collapse
  - the platform is still not production ready because it is far below `3000 eps steady` and still well outside the latency envelope
- Root cause found from the new hot slice:
  - WSP retries timed-out pushes with the same `event_id`
  - IG currently turns `PUBLISH_IN_FLIGHT` overlap into `decision=QUARANTINE`
  - WSP then treats that as terminal `IG_PUSH_REJECTED`, killing lanes for what is actually a transient retry race
- Why this matters:
  - under real at-least-once ingress pressure, retry overlap is normal transport behavior, not bad data
  - persisting it as quarantine truth is semantically wrong and inflates the error posture
- Next implementation sequence:
  - coalesce in-flight duplicates instead of quarantining them
  - surface unresolved in-flight/ambiguous states as retryable edge failures, not terminal 4xx-style rejection
  - then retune the ingress concurrency envelope and rerun the same PR3-S1 boundary

### 22:42 UTC - Implemented retry-safe in-flight dedupe handling and repinned the service concurrency envelope
- Code changes:
  - `admission.py` now waits briefly for `PUBLISH_IN_FLIGHT` / `PUBLISH_AMBIGUOUS` rows to resolve instead of turning retry overlap into quarantine truth
  - unresolved transport overlap now raises retryable `PUBLISH_IN_FLIGHT_RETRY` / `PUBLISH_AMBIGUOUS_RETRY`
  - `aws_lambda_handler.py` maps those retryable conditions to `503 retry_required` and does not DLQ them
- Service posture repin:
  - desired count `12 -> 16`
  - gunicorn threads `32 -> 8`
  - workers remain `4`
- Why:
  - retry overlap is a transport condition in an at-least-once edge, not bad data
  - the previous `4 x 32` thread posture was oversubscribed for a Python admission service on `2 vCPU` tasks and was contributing to queueing/tail latency
- Validation:
  - `py_compile` passed for ingress modules
  - ingress tests passed: `24 passed`
  - `terraform fmt -check infra/terraform/dev_full/runtime` passed
- Next action:
  - commit/push this boundary, rebuild the shared image, roll the updated runtime live, and rerun bounded canonical `PR3-S1`

### 22:36 UTC - The retry-safe ingress boundary is now live on dev_full
- Packaging:
  - workflow run `22784762559`
  - source commit `5d656d5384ed165b551e1ec2e476fa8799b4b40a`
  - new immutable digest `sha256:aa9cc0b2ff129d93e1082ab74fbe1dd2b406c3a43fd08dfab6d6ebc4f24e38a0`
- Live rollout result:
  - WSP task definition `fraud-platform-dev-full-wsp-ephemeral:32`
  - IG task definition `fraud-platform-dev-full-ig-service:7`
  - IG desired/running count `16/16`
  - IG gunicorn posture `workers=4`, `threads=8`
  - IG hot-path health probe mode still `none`
- Evidence path:
  - `runs/dev_substrate/dev_full/road_to_prod/run_control/pr3_20260306T021900Z/runtime_refresh_20260306T223644Z/`
- Next action:
  - rerun the same bounded canonical `PR3-S1` window and measure the post-retry-fix frontier

### 22:46 UTC - Latest PR3-S1 evidence says the remaining bottleneck is front-door queueing, not admission logic
- Latest bounded rerun result:
  - admitted throughput `1186.5 eps`
  - error ratio `0.972%`
  - `PUBLISH_IN_FLIGHT` count in the hot slice: `0`
- Critical interpretation from the hot minute:
  - ALB `p95=13.862s`, `p99=23.672s`
  - in-app `admission_seconds p95` is only about `0.35s` to `0.40s`
  - `phase.validate`, `phase.publish`, and `phase.receipt` p95 values are all sub-second
- Meaning:
  - the service is mostly queueing before useful app work begins
  - the next limiter is request-handling slot capacity at the service edge
- Chosen next repin:
  - desired count `16 -> 24`
  - gunicorn threads `8 -> 16`
  - workers stay `4`
- Why:
  - we need materially more concurrent request slots to collapse ALB queueing
  - but we avoid jumping straight back to the old `4 x 32` oversubscription extreme

### 22:45 UTC - Front-door concurrency expansion is now live on the ingress service
- Live capacity repin result:
  - IG task definition `fraud-platform-dev-full-ig-service:8`
  - desired/running count `24/24`
  - gunicorn posture `workers=4`, `threads=16`
  - image digest unchanged: `sha256:aa9cc0b2ff129d93e1082ab74fbe1dd2b406c3a43fd08dfab6d6ebc4f24e38a0`
- Evidence path:
  - `runs/dev_substrate/dev_full/road_to_prod/run_control/pr3_20260306T021900Z/ig_capacity_repin_20260306T224522Z/`
- Next action:
  - rerun bounded canonical `PR3-S1` and see whether ALB queueing materially collapses under the widened front door

### 22:53 UTC - Latest PR3-S1 suggests the next limiter is producer fan-out shape
- Latest bounded rerun result:
  - admitted throughput `1799.183 eps`
  - blockers left: throughput, tiny `5xx`, latency `p95/p99`
- Interpretation:
  - inside-app latency is now much lower than ALB latency
  - retry noise is materially down
  - the remaining gap likely includes producer-side fan-out that is still too narrow for the widened ingress fleet
- Chosen next move:
  - keep target rate `3200 eps`
  - widen canonical WSP lane count `24 -> 48`
  - reduce lane launch stagger for the wider fleet so the window stays honest
  - keep the current ingress service posture fixed to isolate producer-shape effects

### 23:12 UTC - 48-lane PR3-S1 rerun proved the producer is no longer the main limiter; ingress task churn is now the production-critical defect
- Latest 48-lane impact metrics:
  - observed request throughput `1812.800 eps`
  - observed admitted throughput `1799.783 eps`
  - error ratio `0.7180%`
  - `4xx` ratio `0.7088%`
  - `5xx` ratio `0.0092%`
  - ALB `p95=11.259s`, `p99=16.762s`
- Interpretation:
  - doubling producer lanes from `24` to `48` did not move admitted throughput in any meaningful way
  - the remaining gap to `3000 eps` is therefore not primarily a producer fan-out problem
- Live service events during the same hot minute showed repeated ECS replacements caused by target-group health-check timeouts (`Request timed out`)
- Meaning:
  - the ingress fleet is shedding capacity during the exact certification window
  - this self-inflicted churn depresses throughput and inflates tail latency
- Next action:
  - repin the ingress service capacity and health envelope before any further PR3-S1 rerun

### 23:18 UTC - I repinned the managed ingress capacity and health envelope to stop health-check self-sabotage and give the service a production-shaped headroom test
- Terraform pins updated:
  - task CPU `2048 -> 4096`
  - task memory `4096 -> 8192`
  - desired count `24 -> 32`
  - gunicorn workers `4 -> 8`
  - gunicorn threads `16 -> 8`
- Health policy pins added:
  - interval `30s`
  - timeout `10s`
  - healthy threshold `2`
  - unhealthy threshold `5`
  - container start period `60s`
  - ECS service grace `180s`
- Also corrected the container probe itself so it no longer uses a hidden hardcoded `2s` timeout while ALB/ECS are using a wider health window
- Why:
  - the previous posture was too aggressive for a hot bounded window and was actively killing useful tasks
  - the new posture still fails closed on dead tasks, but should stop removing healthy capacity during pressure
- Next action:
  - validate the Terraform delta, apply the ingress-only repin live, then rerun canonical `PR3-S1` and measure the new impact metrics

### 23:22 UTC - Runtime control defect found and corrected during ingress repin recovery
- The managed ingress Terraform module still defaults `ig_service_enabled=false`
- Consequence:
  - a target-only apply without explicit canonical service pins will destroy the live managed ingress service instead of updating it
- I recovered the service immediately with explicit pins:
  - `ig_service_enabled=true`
  - image digest `sha256:aa9cc0b2ff129d93e1082ab74fbe1dd2b406c3a43fd08dfab6d6ebc4f24e38a0`
- Recovery result:
  - task definition `fraud-platform-dev-full-ig-service:9`
  - fleet `32/32`
  - task shape `4096 CPU / 8192 MiB`
  - gunicorn `8 workers x 8 threads`
  - health envelope `30s interval / 10s timeout / 5 failures / 180s grace`
- Meaning:
  - the service was restored cleanly and the repin is live
  - future Terraform service applies for this lane must carry explicit enable/image pins until the control default is fixed structurally

### 23:26 UTC - PR3-S1 is no longer blocked by health churn or CPU; the synchronous receipt object write is now the dominant tail-latency source
- New frontier after the recovered/stabilized fleet:
  - observed request throughput `1935.183 eps`
  - observed admitted throughput `1934.633 eps`
  - error ratio `0.0284%`
  - `4xx` ratio `0.0138%`
  - `5xx` ratio `0.0146%`
  - ALB `p95=13.774s`, `p99=21.662s`
- Fleet health and compute posture during the run:
  - ECS stayed stable at `32/32`
  - CPU average `30.6%`, max `53.6%`
  - memory average `9.3%`
- Meaning:
  - raw compute saturation is not the current limiter
  - task churn is no longer depressing the frontier
- App-level latency diagnosis:
  - `phase.publish_seconds` remained comparatively small
  - `phase.dedupe_*` remained small
  - `phase.receipt_object_seconds` repeatedly spiked into multi-second territory with `p95` around `1.9s` to `2.5s` and maxima roughly `5s` to `11s`
- Request-log correlation:
  - many successful `202` admits still spent `15s` to `26s` on the request path
- Production conclusion:
  - the synchronous S3 receipt write is now the dominant tail-latency source on the ingress hot path
  - more fleet scale would be a poor next move compared with removing this storage penalty from the request/ack path
- Next action:
  - redesign hot-path receipt persistence so durable provenance remains intact but cold object archival leaves the synchronous admit path

### 23:34 UTC - Hot-path receipt persistence is now being corrected at the design level, not papered over with more fleet scale
- Root cause confirmed:
  - the ingress fleet is healthy and not CPU-bound
  - the multi-second tail is dominated by synchronous receipt object writes to S3
- Chosen correction:
  - move durable hot receipts onto the DDB admission record for the managed ingress path
  - stop blocking the admit response on cold object archival
  - use deterministic DDB-backed `receipt_ref` on the hot path
- Why this is the better production move:
  - DDB is already the trusted operational state boundary for ingress idempotency
  - synchronous S3 archival is the wrong latency profile for a `3000 eps` admit edge
  - in-process async writes were rejected because they weaken durability on task death
- Local validation passed:
  - `py_compile`
  - targeted ingress tests `15 passed`
  - `terraform fmt -check`
  - `terraform validate`
- Next action:
  - commit/push this boundary, rebuild the image, roll the service, rerun canonical `PR3-S1`
### 23:46 UTC - Fresh image + live redeploy is now the only valid boundary before another PR3-S1 judgment
- The DDB hot-receipt fix is implemented and locally validated, but the live ingress ECS tasks are still on the old immutable image digest
- Meaning:
  - another PR3-S1 run right now would measure stale code and produce invalid evidence
- Chosen boundary:
  - rebuild/publish the shared runtime image through `dev_full_m1_packaging.yml`
  - capture the new digest
  - redeploy only the managed ingress resources with explicit `ig_service_enabled=true` and explicit `ig_service_image_uri=<new digest>`
  - verify ECS is steady on the new task definition before rerunning
- Safety rule retained:
  - no target-only Terraform apply for this lane without the explicit enable/image pins, because the module default still disables the managed ingress service
### 23:29 UTC - Fresh immutable image is published for the ingress hot-receipt fix
- Packaging lane `dev_full_m1_packaging.yml` completed successfully
- Artifact boundary:
  - workflow run `22786239105`
  - source commit `5bb042bb86f400c43aed0213bafa37e212d650fd`
  - image digest `sha256:e3e6ee322b81039c0c2f9a349c96eb884b8c47a01a83f7279c414945c0e69813`
- Meaning:
  - the next valid PR3-S1 rerun must use this digest, not the older `sha256:aa9cc0b2...` image still on the live service
- Next action:
  - redeploy the managed ingress resources with explicit `ig_service_enabled=true` and this new image digest, then verify ECS steady state before measuring again
### 23:36 UTC - The new image rollout failed because the rollout policy assumed overlap capacity the account does not have
- ECS did create task definition `:10` with the new image and `IG_RECEIPT_STORAGE_MODE=ddb_hot`
- But the service rolled back before adoption because deployment placement exceeded the Fargate on-demand quota envelope
- Measured constraint:
  - steady fleet `32 x 4 vCPU = 128 vCPU`
  - account quota `140 vCPU`
  - existing rollout policy `minimum healthy 50 / maximum 200`
- Meaning:
  - this is a deployment-capacity policy defect, not proof that the new image is unhealthy
  - with only `12 vCPU` headroom, a `200%` overlap rollout is structurally impossible
- Next action:
  - repin the ingress ECS deployment policy to a quota-aware max/min pair in Terraform, then redeploy the new digest and verify the running tasks actually move to it
### 23:41 UTC - ECS rejected `maxPercent=100`, so the ingress rollout ceiling is repinned to a narrow overlap band
- AWS rejected the first quota-aware rollout with:
  - `Availability Zone Rebalancing does not support maximumPercent <= 100`
- Interpretation:
  - exact zero-overlap is not allowed on this ECS posture
  - the right fix is the smallest quota-safe value above `100`, not disabling the feature
- New pin:
  - ingress deployment `maximumPercent = 109`
- Why:
  - steady fleet `32 tasks`
  - narrow overlap remains inside the `140 vCPU` account envelope while still permitting rolling replacement
- Next action:
  - reapply the redeploy under this narrow-overlap policy and confirm the running tasks actually move to the new digest
### 23:42 UTC - Live ingress is now on the corrected digest and the rollout policy is fixed
- Deployment completed on task definition `fraud-platform-dev-full-ig-service:10`
- Verified live task image digest:
  - `sha256:e3e6ee322b81039c0c2f9a349c96eb884b8c47a01a83f7279c414945c0e69813`
- Verified live ingress env:
  - `IG_RECEIPT_STORAGE_MODE=ddb_hot`
  - `IG_GUNICORN_WORKERS=8`
  - `IG_GUNICORN_THREADS=8`
- Rollout policy now pinned live as:
  - `minimumHealthyPercent=50`
  - `maximumPercent=109`
- Meaning:
  - the service is finally executing the corrected hot-receipt path
  - the next canonical `PR3-S1` rerun will be measuring the real new design, not stale code
### 23:52 UTC - PR3-S1 is now throughput-green but still failed on `5xx`; the remaining defect is DDB write amplification on the hot receipt path
- Latest steady impact metrics:
  - admitted throughput `3171.35 eps`
  - request throughput `3174.42 eps`
  - `p95=154.98 ms`
  - `p99=353.33 ms`
- Meaning:
  - the former S3 receipt bottleneck is gone
  - the platform is now operating in the intended throughput/latency range for steady-state PR3-S1
- Remaining blocker:
  - `32` target `5xx` responses (`0.0168%`)
- Root cause:
  - DDB idempotency table produced `3117` `WriteThrottleEvents` in the measurement minute
  - ingress logs show `PutItem` / `UpdateItem` `ThrottlingException` on the admit path
- Chosen fix:
  - keep DDB hot receipts
  - collapse the post-publish admitted-state write and receipt write into one DDB update
  - harden AWS retry posture to `adaptive / 8 attempts`
- Local validation:
  - ingress tests `15 passed`
  - wider Postgres efficiency test not runnable locally because `psycopg` is absent
- Next action:
  - publish new image, redeploy, rerun PR3-S1 until the remaining `5xx` blocker is cleared

### 00:25 UTC - PR3-S1 regression is now traced to two production defects: MSK publish timeout posture and governance append contention under quarantine spikes
- Latest strict rerun is not failing because WSP cannot launch; WSP reaches IG and then receives fail-closed quarantine responses after downstream publish ambiguity.
- Root cause 1:
  - live IG logs show `Timed out ApiVersionRequest in flight (after 2002ms...)` against MSK Serverless;
  - publish path raises `KAFKA_PUBLISH_TIMEOUT` and IG correctly maps that to `PUBLISH_AMBIGUOUS`.
- Interpretation:
  - the fail-closed behavior is correct;
  - the timeout pin is not. A ~2s publish/metadata deadline is too tight for real MSK Serverless + IAM/OAUTH under hot ingress load.
- Root cause 2:
  - quarantine/anomaly emission still writes governance truth through shared S3 JSONL append;
  - under concurrent quarantine spikes this hits `S3_APPEND_CONFLICT` / `PreconditionFailed`.
- Interpretation:
  - shared-object append is not a production-grade authoritative persistence shape for concurrent governance writers;
  - authoritative governance truth needs conflict-free per-event persistence, with projections derived secondarily.
- Rejected shortcut:
  - not repinning WSP back to an easier low-rate path just to clear `PR3-S1`.
- Chosen remediation:
  - keep the real `WSP -> IG -> DDB -> MSK` path;
  - repin ingress Kafka timeout posture for real MSK latency;
  - separate ECS IG timeout control from the Lambda timeout variable;
  - move governance durability to conflict-free event objects while retaining the readable/queryable governance surface.
- Next action:
  - implement the ingress Kafka + governance fixes, test them locally, rebuild the runtime image, redeploy ingress, then rerun strict `PR3-S1` from the same run-control root.

### 00:34 UTC - Kafka/governance hardening is implemented locally and validated; the next valid boundary is a new immutable image and live ingress redeploy
- Governance durability fix:
  - platform governance now writes one event object per event as the durable truth;
  - `events.jsonl` is treated as a projection, not the sole authoritative write target.
- Why this matters:
  - concurrent quarantine/anomaly spikes no longer depend on shared S3 JSONL append succeeding in order for governance truth to exist.
- Kafka timeout fix:
  - publisher now uses a realistic request-timeout floor plus a wider delivery/socket deadline;
  - managed ingress ECS service now gets its own Kafka timeout pin instead of inheriting the Lambda timeout variable.
- Repins chosen:
  - Lambda Kafka timeout default `5000 ms`
  - managed ingress service Kafka timeout default `10000 ms`
- Local validation results:
  - targeted tests `26 passed`
  - `py_compile` passed
  - `terraform fmt -check` + `terraform validate` passed
- Interpretation:
  - the code/config correction boundary is now clean enough to justify a fresh immutable runtime image;
  - further analysis without redeploy would just be stale-runtime speculation.
- Next action:
  - commit/push this correction boundary,
  - build/publish a fresh image,
  - redeploy ingress onto it,
  - rerun strict `PR3-S1` and judge the impact metrics only.

### 00:44 UTC - Managed ingress is live on the new immutable digest and corrected Kafka timeout pin
- Immutable build:
  - GitHub Actions run `22787785477`
  - source commit `c6481c1fbd89987676e89f032af3ed205a18acf4`
  - image digest `sha256:b4e132e49e14d7c4dff921001227defe1b416c0901967c29e92e53c55b90deda`
- Live ingress rollout:
  - task definition advanced to `fraud-platform-dev-full-ig-service:12`
  - service reached `32/32` running with rollout state `COMPLETED`
  - live env confirms `KAFKA_REQUEST_TIMEOUT_MS=10000`
  - live env confirms `IG_RECEIPT_STORAGE_MODE=ddb_hot`
- Meaning:
  - PR3-S1 can now be rerun against a runtime that materially includes the governance durability fix and the corrected Kafka timeout posture.
- Next action:
  - execute strict PR3-S1 rerun from `pr3_20260306T021900Z` and judge the impact metrics from that live boundary only.


### 01:08 UTC - PR3-S1 rerun failed in the harness, not in the platform: missing ELB read permission and over-broad evidence bootstrap are the next production defects to remove
- GitHub Actions rerun `22788053348` did not produce a fresh authoritative `PR3-S1` runtime summary.
- The hard failure was:
  - `AccessDenied` on `elasticloadbalancing:DescribeTargetGroups` for role `GitHubAction-AssumeRoleWithAction`.
- Meaning:
  - the canonical ALB-backed ingress path cannot currently resolve its live target-group metric surface under GitHub OIDC;
  - this is a certification-tooling/observability defect, not proof that the ingress platform regressed.
- A second defect in the same rerun:
  - bootstrap synced the full `run_control/<pr3_id>/` evidence root and spent significant time downloading irrelevant historical artifacts, including recursive `ig_edge_materialize/.../bin/X11/...` paths.
- Meaning:
  - the PR3 harness is still too wasteful and too fragile for a production-grade certification lane.
- Chosen fix:
  - add least-privilege `DescribeTargetGroups` access for the GitHub OIDC role,
  - shrink bootstrap to the minimal authoritative receipts needed for strict upstream lock,
  - add dispatcher fallback so previously known ALB dimensions can still be used if direct discovery is temporarily unreadable.
- Next action:
  - implement these tooling corrections, apply the IAM remediation live, then rerun strict `PR3-S1` immediately.


### 01:16 UTC - PR3-S1 harness authority/read-path correction is live
- Implemented harness fixes:
  - dispatcher can reuse previously resolved ALB metric dimensions from prior `g3a_s1_wsp_runtime_manifest.json` when host matches;
  - workflow bootstrap now copies only `pr3_s0_execution_receipt.json` plus prior runtime manifest instead of syncing the full run-control tree;
  - workflow evidence rollup now records `ALB` vs `APIGW` correctly instead of hardcoding API Gateway labels.
- IAM remediation applied live:
  - policy `GitHubActionsPR3RuntimeDevFull` updated to allow:
    - `elasticloadbalancing:DescribeLoadBalancers`
    - `elasticloadbalancing:DescribeTargetGroups`
- Validation before apply:
  - `py_compile` passed for `pr3_s1_wsp_replay_dispatch.py`
  - workflow YAML parse passed
  - `terraform fmt -check` + `terraform validate` passed for `infra/terraform/dev_full/ops`
- Meaning:
  - the next strict `PR3-S1` rerun should return to measuring the live ALB-backed ingress boundary instead of failing inside the harness on ELB discovery or wasting time on oversized evidence bootstrap.
- Next action:
  - commit/push this correction boundary and rerun `PR3-S1` immediately.


### 01:24 UTC - PR3-S1 rerun cleared the harness defects and exposed a real Fargate headroom issue: replay lanes are overpinned
- Strict rerun `22788481700` got past bootstrap and ELB discovery, then failed launching replay lane `12` with:
  - `PR3.S1.WSP.B01_RUN_TASK_FAILED ... reached the limit on the number of vCPUs you can run concurrently`
- Measured capacity posture:
  - account Fargate quota: `140 vCPU`
  - live ingress fleet: `32` tasks at `4096 CPU / 8192 MiB` each = `128 vCPU`
  - replay task default: `1024 CPU / 2048 MiB` per lane
- Meaning:
  - the replay harness is overprovisioned for a shared dev account and is exhausting the remaining `~12 vCPU` headroom before all lanes launch.
- Chosen fix:
  - keep the ingress fleet unchanged,
  - repin canonical replay lanes to a leaner explicit override (`256 CPU / 1024 MiB`) so the certification harness fits beside the live service.
- Next action:
  - patch the workflow/task overrides and rerun `PR3-S1` immediately.


### 01:31 UTC - PR3-S1 replay lanes are now right-sized, but the canonical dispatcher had regressed to public-subnet defaults and timed out against the internal ALB
- Fresh rerun `22788594376` launched all 24 replay lanes successfully with lean task overrides.
- Fresh evidence still showed `0 eps` because every lane failed with `IG_PUSH_RETRY_EXHAUSTED`.
- Lane logs confirm the failure mode is repeated `timeout`, not semantic rejection.
- Root cause from the manifest:
  - replay tasks ran on `subnet-005205ea65a9027fc,subnet-01fd5f1585bfcca47`
  - `assignPublicIp=ENABLED`
  - canonical ingress URL is the internal ALB `internal-fp-dev-full-ig-svc-...elb.amazonaws.com`
- Meaning:
  - the dispatcher silently reverted to the old public-subnet default instead of the private-VPC posture required for the internal ingress service.
- Chosen fix:
  - repin dispatcher defaults to private subnets `subnet-0a7a35898d0ca31a8,subnet-0e9647425f02e2f27`
  - repin `assignPublicIp=DISABLED`
  - rerun strict `PR3-S1` immediately.


### 01:36 UTC - PR3-S1 is now clean on latency/error and only source-limited on volume; the replay harness is still too serialized
- Fresh clean evidence from `22788814078`:
  - admitted throughput `1475.1625 eps`
  - error rate `0%`
  - `p95=62.21 ms`
  - `p99=90.65 ms`
- Remaining blockers:
  - early throughput floor miss (`1476 < 2100 eps`)
  - final throughput miss (`1475 < 3000 eps`)
- Meaning:
  - ingress is healthy;
  - the harness is under-driving volume rather than the platform rejecting or slowing it.
- Root-cause interpretation:
  - current canonical pin still uses `IG_PUSH_CONCURRENCY=1`, which serializes each lane and caps usable per-lane EPS.
- Chosen fix:
  - repin replay push concurrency to `4` and widen the HTTP pool so the source can actually drive the platform toward the 3000-eps target.
- Next action:
  - patch the workflow/dispatcher concurrency posture and rerun strict `PR3-S1` immediately.


### 02:20 UTC - PR3-S1 is now near-green, but the remaining throughput and 5xx misses are real production-hardening defects
- Authoritative strict rerun `22789024407` closed the previous harness underdrive gap substantially:
  - admitted throughput `2610.900 eps`
  - request count `4,699,646`
  - admitted count `4,699,620`
  - `4xx=0`
  - `5xx=26`
  - `p95=106.9998 ms`
  - `p99=141.5351 ms`
  - window `1800 s`
- Remaining strict blockers:
  - throughput shortfall: `2610.900 < 3000.000 eps`
  - zero-5xx breach: `0.000006 > 0.000000`
- Measured ingress-fleet headroom proves this is not an IG capacity ceiling:
  - ECS service stayed `32/32` healthy
  - CPU roughly `22%..25%` average, `~31%` max
  - memory roughly `7.2%..7.9%`
  - ALB healthy hosts stayed `32`, unhealthy hosts stayed `0`
- Meaning:
  - the throughput miss is still on the source replay width, not the ingress fleet;
  - the residual `5xx` leak is a resilience defect in the hot path because a tiny number of transient downstream failures are still escaping as client-visible `503`s.
- Important implementation defect discovered:
  - runtime pins `IG_INTERNAL_RETRY_MAX_ATTEMPTS` and `IG_INTERNAL_RETRY_BACKOFF_MS` exist in env/Terraform,
  - but the active admission path does not actually use them to harden idempotent post-publish receipt/update operations.
- Chosen production fix:
  - wire the retry contract into the real admission path,
  - strengthen the ingress service publish/receipt envelope so transients are absorbed internally,
  - widen canonical PR3-S1 replay horizontally by lane count instead of masking the issue with oversized per-lane compute.
- Next action:
  - patch code + Terraform/workflows,
  - build and deploy a fresh immutable image,
  - rerun strict `PR3-S1` from the same upstream boundary.


### 02:28 UTC - Implemented the PR3-S1 resilience + replay-width correction boundary
- Code changes:
  - `admission.py` now retries idempotent post-publish receipt/admitted-state operations through a bounded internal retry helper;
  - `aws_lambda_handler.py` now actually passes `IG_INTERNAL_RETRY_*` into the managed-edge gate wiring;
  - `config.py` now carries the same retry fields in `WiringProfile`.
- IaC/runtime changes:
  - added shared Terraform variable `ig_kafka_publish_retries` with default `5`;
  - both Lambda IG and ECS-service IG now consume that retry count;
  - service Kafka timeout default repinned from `10000 ms` to `15000 ms`.
- Workflow changes:
  - `dev_full_pr3_s1_managed.yml` default lane count repinned from `24` to `32`;
  - `dev_full_pr3_ig_edge_materialize.yml` now accepts immutable service image URI plus the retry/timeout pins and rolls the ECS ingress service as part of the same targeted apply.
- Validation:
  - `py_compile` passed for the touched ingress modules;
  - workflow YAML parse passed;
  - `terraform fmt -check` passed for `infra/terraform/dev_full/runtime`.
- Meaning:
  - the next boundary is remote realization, not more local theory:
    - build/push fresh immutable image,
    - materialize live ingress with the new image + retry envelope,
    - rerun strict `PR3-S1` at the widened `32`-lane canonical width.

### 02:34 UTC - Ingress materialization failed because the workflow left the managed ingress enablement gate implicit
- Failed rollout `22790080603` was not an AWS/runtime rejection. Terraform tried to destroy the live ingress ECS/ALB surface because the workflow targeted `ig_service[0]` resources while leaving `ig_service_enabled=false` by default.
- This produced a false-destructive plan (`0 add / 1 change / 9 destroy`) on the managed ingress surface.
- Interpretation:
  - the platform path is still correct;
  - the rollout tooling was wrong because a production rollout cannot rely on a hidden default for a live service enablement gate.
- Chosen fix:
  - repin the rollout workflow to pass `ig_service_enabled=true` explicitly when materializing the ingress ECS service.
- Acceptance for rerun:
  - no count-based destroy plan on ingress,
  - live image/env readback matches the fresh immutable image + retry pins,
  - then resume strict `PR3-S1`.

### 02:38 UTC - Ingress materialization is now blocked by an incomplete ELBv2 read envelope on the GitHub OIDC role
- Rerun 22790176136 proved the workflow enablement defect is fixed: Terraform reached a real bounded ingress update plan instead of the earlier false-destroy posture.
- The apply then failed on missing ELBv2 read actions during provider refresh:
  - lasticloadbalancing:DescribeLoadBalancerAttributes
  - lasticloadbalancing:DescribeTargetGroupAttributes
- Interpretation:
  - this is an IAM tooling defect on the GitHub OIDC role, not a platform-capacity issue.
- Chosen fix:
  - widen policy GitHubActionsPR3RuntimeDevFull to the full ELBv2 describe surface needed for Terraform-managed ALB/TG rollout (DescribeLoadBalancers, DescribeLoadBalancerAttributes, DescribeTargetGroups, DescribeTargetGroupAttributes, DescribeListeners, DescribeRules, DescribeTags).
- Next action:
  - validate and apply the IAM delta live from infra/terraform/dev_full/ops, then rerun ingress materialization immediately.

### 02:40 UTC - Applied the ELBv2 read-envelope remediation live on the GitHub OIDC role
- Validated infra/terraform/dev_full/ops and applied a targeted live update to managed policy GitHubActionsPR3RuntimeDevFull.
- Added ELBv2 read actions:
  - DescribeLoadBalancerAttributes
  - DescribeTargetGroupAttributes
  - DescribeListeners
  - DescribeRules
  - DescribeTags
- Meaning:
  - the GitHub OIDC role should now be able to refresh the managed ingress ALB/TG family during Terraform apply.
- Next action:
  - rerun ingress materialization immediately and continue to the live verification boundary.

### 02:42 UTC - Repinning PR3 ELB runtime read to the full read-only Describe* family
- Rerun 22790256328 advanced further and then failed on lasticloadbalancing:DescribeListenerAttributes.
- Interpretation:
  - the correct durable fix is the full read-only ELBv2 describe family, not continued piecemeal action additions.
- Chosen fix:
  - repin PR3ElbRuntimeRead to lasticloadbalancing:Describe* on *.
- Next action:
  - apply the IAM delta live and rerun ingress materialization immediately.

### 02:44 UTC - Ingress rollout is now blocked only by Lambda concurrency authority
- Rerun 22790311956 got materially further:
  - ECS task-definition replacement completed,
  - ECS service update completed.
- Remaining failure:
  - lambda:PutFunctionConcurrency denied on raud-platform-dev-full-ig-handler while lifting reserved concurrency toward the pinned envelope.
- Chosen fix:
  - widen PR3IngressLambdaControl with lambda:PutFunctionConcurrency and lambda:DeleteFunctionConcurrency.
- Next action:
  - apply the IAM delta live and rerun ingress materialization immediately.

### 02:48 UTC - Repinning Lambda IG reserved concurrency from 1000 to 600
- AWS account posture is real and binding here:
  - total Lambda concurrency = 1000
  - unreserved currently = 640
  - current IG reserved concurrency = 360
- The old 1000 pin is therefore not deployable in this account and is an overspecified authority for this environment.
- Production sizing rationale:
  - PR3-S1 already reached 2610.9 eps at 360 reserved concurrency,
  - measured latency band implies 3000 eps does not require anything close to 1000 reserved concurrency,
  - 600 gives meaningful headroom while preserving account flexibility.
- Chosen fix:
  - repin LAMBDA_IG_RESERVED_CONCURRENCY to 600 in runtime IaC and authority docs.
- Next action:
  - rerun ingress materialization and verify Lambda lands at 600.

### 02:52 UTC - Ingress materialization is now green and live-verified
- Workflow 22790437771 completed successfully.
- Verified live posture:
  - Lambda IG reserved concurrency = 600
  - Lambda handler = raud_detection.ingestion_gate.aws_lambda_handler.lambda_handler
  - ECS IG service steady on task definition :13
  - ECS image digest = sha256:843750a949a94a5a0eaf984ce231c0e91e3ced0032b1f3b0bfa4af81514aeb64
  - ECS retry envelope = IG_INTERNAL_RETRY_MAX_ATTEMPTS=5, IG_INTERNAL_RETRY_BACKOFF_MS=400, KAFKA_REQUEST_TIMEOUT_MS=15000, KAFKA_PUBLISH_RETRIES=5
  - health mode = pigw_lambda_ddb_kafka
  - ingress limits = 3000 rps / 6000 burst
- Meaning:
  - ingress remediation is complete enough to resume strict PR3-S1 on the corrected live boundary.

### 03:46 UTC - PR3-S1 latest red run is dominated by stale run identity, so it is benchmarking duplicate handling rather than fresh admission
- Strict rerun `22790499579` failed with:
  - throughput `2242.198 eps < 3000`,
  - `43` total `5xx`,
  - latency `p95=803 ms`, `p99=1113 ms`.
- Live infra evidence does **not** support a simple capacity-exhaustion story:
  - ECS ingress remained `32/32` healthy,
  - ALB healthy hosts remained `32`,
  - ECS CPU mostly `14%..26%`,
  - ECS memory about `7%..8%`.
- Error shape is edge-generated, not mostly app-generated:
  - ALB `ELB_5XX` accounts for almost all failures,
  - target `5xx` is only `5`,
  - no target connection error signal.
- The decisive clue came from ingress task logs:
  - repeated worker summaries like `admit=12 duplicate=898 quarantine=0`,
  - duplicate-heavy request mix,
  - duplicate receipt/admission p95 roughly `1.37s`.
- Root cause:
  - IG dedupe key is `sha256(platform_run_id:event_class:event_id)`,
  - `event_id` is intentionally stable across oracle-store reruns,
  - PR3-S1 kept reusing `platform_run_id=platform_20260223T184232Z`,
  - so reruns after the earlier successful attempts are naturally duplicate storms.
- Production decision:
  - do **not** wipe dedupe state,
  - do **not** weaken IG semantics,
  - instead give each certification attempt a fresh runtime `platform_run_id`/`scenario_run_id` while keeping the oracle input world fixed.
- Next action:
  - patch PR3 tooling/workflow defaults for fresh runtime identity per attempt,
  - rerun strict PR3-S1 on that corrected boundary before judging the remaining capacity posture.

### 04:02 UTC - Converting the PR3-S1 stale-identity diagnosis into an explicit execution guard
- The stale-identity issue is now being fixed as tooling, not just remembered as operator guidance.
- Production decision:
  - blank `platform_run_id` and `scenario_run_id` should mint fresh attempt identities by default,
  - runtime-identity reuse must remain explicit and default-off,
  - dispatcher must still probe admissions/receipts and fail closed if reuse slips through.
- Why this matters:
  - without this guard, PR3-S1 can silently degrade from first-seen admission proof into duplicate-receipt benchmarking,
  - that would produce misleading impact metrics while looking superficially valid.
- Immediate next action:
  - finish the workflow correction, validate it locally, and rerun strict PR3-S1 on a fresh runtime identity boundary.

### 04:08 UTC - Fresh-identity PR3-S1 guard implemented and locally validated
- Dispatcher change:
  - probes Aurora `admissions` and `receipts` for the chosen `platform_run_id`,
  - raises `PR3.S1.WSP.B26_RUNTIME_ID_REUSED` if prior state exists and reuse is not explicitly allowed.
- Workflow change:
  - blank runtime IDs now mint fresh UTC-derived identifiers,
  - those resolved values are used for both runtime materialization and the canonical WSP replay lane,
  - identity reuse is explicit and default-off.
- Local validation completed:
  - `python -m py_compile scripts/dev_substrate/pr3_s1_wsp_replay_dispatch.py`
  - YAML parse for `.github/workflows/dev_full_pr3_s1_managed.yml`
- Next action:
  - push the correction and rerun strict PR3-S1 so the next evidence reflects first-seen admission behavior.

### 04:11 UTC - GitHub dispatch input ceiling blocked the corrected PR3-S1 rerun
- First remote rerun attempt failed at dispatch with GitHub API `HTTP 422`.
- Cause:
  - `.github/workflows/dev_full_pr3_s1_managed.yml` now exposes `26` `workflow_dispatch` inputs,
  - GitHub hard-limits this surface to `25`.
- Interpretation:
  - this is a workflow control-plane issue, not a runtime or throughput issue.
- Chosen remediation:
  - remove dead/non-operator inputs from the dispatch surface,
  - keep the fresh-identity guard,
  - preserve the pinned runtime contract inside the workflow.
- Immediate next action:
  - trim the workflow inputs, push, and rerun materialization plus strict PR3-S1 on the same fresh identity pair.

### 04:20 UTC - PR3 runtime readiness failure traced to stale image lineage, not missing Kafka secrets
- Fresh-identity `PR3-S1` failed before the steady window because PR3 runtime pods were not actually healthy.
- Live cluster inspection showed:
  - all PR3 deployments at `0/1` available,
  - `AL/DF/DLA/archive_writer` crashing on `KAFKA_SASL_CREDENTIALS_MISSING`,
  - `IEG/OFP` crashing on `*_EVENT_BUS_KIND_UNSUPPORTED`.
- Important verification:
  - Kubernetes secret `fp-pr3-runtime-secrets` already contains `KAFKA_SASL_MECHANISM=OAUTHBEARER` and the expected DSN/run-id values,
  - local source on this branch already supports Kafka/OAUTH and Kafka event-bus mode for these components.
- Conclusion:
  - the PR3 EKS materialization path is pinned to a stale repo image digest via the workflow default `worker_image_uri`,
  - while the canonical ECS WSP task family is already on a newer lineage.
- Chosen fix:
  - remove the stale hardcoded EKS image default,
  - build a fresh immutable image,
  - repin the canonical WSP task family to that digest so both ECS replay and EKS runtime consume the same code lineage.

### 04:24 UTC - PR3-S1 launcher blocked by missing psycopg on the CI runner
- After the image-lineage correction, PR3 runtime materialization is genuinely healthy again:
  - all six deployments reached `1/1` available on digest `sha256:8ec634853ae03cfa624633ddde8ccb2108a2915973cee7eeaaa7c653b875c873`.
- The next strict `PR3-S1` rerun failed before remote dispatch with:
  - `ModuleNotFoundError: No module named 'psycopg'`
- Cause:
  - the launcher now probes Aurora for runtime-identity reuse,
  - but the workflow dependency step still installs only `boto3` and `botocore`.
- Chosen fix:
  - add `psycopg[binary]` to the steady-harness runner dependency surface,
  - rerun on the same fresh identity pair because no replay traffic was launched before the import failure.

### 04:28 UTC - First psycopg fix hit the wrong workflow job
- The repeated import failure showed the first dependency patch landed on `materialize_runtime`, not `steady_harness`.
- Interpretation:
  - platform runtime is still healthy,
  - the defect remains a CI job completeness issue.
- Immediate fix:
  - add `psycopg[binary]` to the `steady_harness` dependency step itself,
  - push and rerun on the same fresh identity pair.

### 04:31 UTC - Aurora identity probe must move inside the VPC
- After the import issue was fixed, the next rerun failed with:
  - `psycopg.errors.ConnectionTimeout: connection timeout expired`
- Interpretation:
  - the guard is correct,
  - the execution location is wrong because the GitHub-hosted runner cannot reach private Aurora.
- Chosen fix:
  - run the identity-reuse probe from an in-cluster PR3 pod via `kubectl exec`,
  - keep Aurora private,
  - have the dispatcher skip the runner-local probe once the in-VPC preflight passes.

### 04:52 UTC - PR3-S1 canonical root is mixed-state, so I relaunched S1 on the last proven calibration instead of trusting the stale pass receipt
- Control-root defect found:
  - `pr3_s1_execution_receipt.json` still says `PR3_S1_READY`,
  - but the active root summary was overwritten later by a failed strict attempt,
  - so the root currently mixes green receipt and red summary truth.
- Production decision:
  - do not advance to `PR3-S2` off that ambiguous root,
  - re-establish `PR3-S1` with a fresh authoritative rerun.
- Proven calibration selected from attempt history:
  - `138` lanes,
  - `stream_speedup=95.0`,
  - `180s` settled window,
  - `540000` sample minima,
  - prior clean impact metrics were `3003.4222 eps`, `0` errors, `p95=20.98 ms`, `p99=26.74 ms`.
- Fresh rerun launched:
  - workflow `dev-full-pr3-s1-managed`,
  - run `22792386082`,
  - branch `cert-platform`,
  - OIDC role `arn:aws:iam::230372904534:role/GitHubAction-AssumeRoleWithAction`.
