# 2026-03-12

## 01:12:40 +00:00
- Re-ran the stored Phase 4 rollup on `phase4_case_label_coupled_20260312T003302Z` after the refreshed post snapshot had matured.
- Confirmed the coupled run closes green:
  - `phase4_coupled_readiness_receipt.json -> PHASE4_READY`
  - `steady = 3060.177777777778 eps`
  - `burst = 7118.0 eps`
  - `recovery = 3018.4333333333334 eps`
  - `4xx = 0`
  - `5xx = 0`
- Accepted judgment: the old `PHASE4.B24_LABEL_COMMIT_UNDERCOUNT` red was a stale scoring artifact, not a live Case + Label commit failure.

## 01:13:12 +00:00
- Promoted the enlarged working platform to:
  - `Control + Ingress + RTDL + Case + Label`
- Opened `Phase 5` on the managed learning corridor.
- Re-pinned the active managed learning surfaces from the dev-full handles:
  - Databricks OFS workspace / build job / quality job / serverless-jobs-only compute policy
  - SageMaker execution role / training prefix / batch prefix / model package group / online endpoint
  - MLflow tracking URI SSM path
- Updated the production-readiness notebook, phase docs, plan state, and readiness graphs to match the Phase 4 closure and Phase 5 handoff.

## 01:22:53 +00:00
- Added `scripts/dev_substrate/phase5_learning_mlops_readiness.py` as the dedicated Phase 5 telemetry-gate probe for the managed learning corridor.
- First run exposed a real harness blindspot:
  - MLflow tracking URI resolves to the symbolic `databricks` backend rather than a raw HTTP URL.
- Patched the probe to treat that as a Databricks-managed MLflow hosting mode.
- Re-ran the same telemetry gate on source scope `phase4_case_label_coupled_20260312T003302Z`.
- Accepted result:
  - `execution_id = phase5_learning_mlops_20260312T012219Z`
  - `verdict = PHASE5A_READY`
  - `open_blockers = 0`
- Managed-surface facts pinned:
  - Databricks user `eorumwese@gmail.com`
  - OFS build job id `736420749736071`
  - OFS quality job id `37768192213816`
  - SageMaker execution role trust valid
  - model package group `fraud-platform-dev-full-models` present
  - MLflow tracking mode `databricks`

## 03:37:17 +00:00
- Re-read the allowed Data Engine semantic authorities for the learning corridor:
  - interface pack
  - `5B` build / `S5`
  - `6B` build / `S5`
- Accepted the posture correction that the main readiness plan was still too platform-heavy for Phase 5 / Phase 6.
- Updated `platform.production_readiness.plan.md` so Phase 5 now explicitly requires:
  - interface-pack-authorized dataset admission,
  - `6B.S5`-gated world admission for learning/eval bases,
  - label as-of / maturity correctness,
  - supervision coverage and cohort/regime visibility,
  - offline feature admissibility,
  - model-fit reasoning on admitted data rather than job-success-only proof.
- Updated Phase 6 in the same plan so the coupled proof now explicitly requires semantic stability and explainability across:
  - runtime -> case/label -> dataset -> bundle -> runtime.

## 03:58:12 +00:00
- Re-read the retained bounded learning runners against the strengthened Phase 5 / 6 standard.
- Accepted a real harness-method blocker:
  - `pr3_s4_learning_bound.py` falls back to local OFS / MF / MPR workers.
  - `pr3_s4_learning_bound_remote.py` packages current-run inputs with old managed M10 / M11 / M12 evidence but does not yet enforce the new interface-pack + `6B.S5` semantic admission posture explicitly.
- Verified the current world-side learning gate directly at the pinned oracle root for manifest fingerprint `76ec81ce37897b0837f5f1b242a3fa557532067d416e5177efb8fc27c4865460`:
  - `6B` `_passed.flag` exists,
  - `s5_validation_report_6B.json` is readable,
  - `s4_flow_truth_labels_6B` and `s4_case_timeline_6B` are materially present.
- Replanned the next execution step narrowly:
  - build a new Phase 5.B bounded proof on current-run semantic admission + managed-corridor continuity,
  - then build the coupled Phase 6 proof on runtime registry resolution against the same governed bundle truth.

## 03:53:55 +00:00
- Ran the first new bounded Phase 5.B proof on current promoted scope `phase4_case_label_coupled_20260312T003302Z`.
- Result went red for proving-harness shape reasons rather than a clean live learning-corridor defect:
  - run-facts `output_roles` parsed too narrowly,
  - `6B` validation read assumed a non-material sealed-inputs object,
  - retained managed-artifact discovery was too strict on execution-id path matching.
- Accepted action:
  - fix the new gate narrowly and rerun the same bounded proof immediately.

## 03:55:51 +00:00
- Corrected the bounded Phase 5.B harness and reran on `execution_id = phase5_learning_mlops_bound_20260312T035531Z`.
- Accepted result:
  - `verdict = PHASE5_READY`
  - `open_blockers = 0`
- Current semantic learning basis proved clean:
  - intended outputs limited to `s2_event_stream_baseline_6B` and `s3_event_stream_with_fraud_6B`
  - both carry `business_traffic`
  - current `6B` gate is admitted with `overall_status = WARN` but all required rails `PASS`
  - current label truth remains clean with `accepted > 0`, `pending = 0`, `rejected = 0`
- Retained managed corridor continuity also remained green through `m10b/m10d/m11f/m11g/m12b/m12c/m12d/m12e/m12f/m12g`.

## 03:56:08 +00:00
- Executed the coupled Phase 6 proof on `execution_id = phase6_learning_coupled_20260312T035601Z`.
- Accepted result:
  - `verdict = PHASE6_READY`
  - `open_blockers = 0`
- Decision Fabric runtime resolution stayed aligned with the governed active bundle truth:
  - `dev_full|fraud|primary| -> RESOLVED`
  - `dev_full|baseline|primary| -> RESOLVED`
  - both resolve to `s3://fraud-platform-dev-full-evidence/evidence/runs/platform_20260223T184232Z/learning/mf/candidate_bundle.json`
- Working platform is now:
  - `Control + Ingress + RTDL + Case + Label + Learning + Evolution / MLOps`
- Next active phase shifts to:
  - `Phase 7 - Operations / Governance / Meta readiness`

## 04:09:47 +00:00
- Reopened Learning after reviewing the quality of the rushed Phase 5 / 6 closeout against the standard used in earlier phases.
- Accepted judgment:
  - the recent Phase 5 / 6 receipts are still useful exploratory evidence,
  - but the phase planning, proof depth, and promotion story are not yet at the same production-readiness standard as Phase 0 to Phase 4.
- New execution posture:
  - demote Learning from the promoted working-platform claim,
  - scrap the current rushed Phase 5 / 6 phase-plan surfaces,
  - rebuild `Phase 5` from the main-plan expansion method,
  - focus only on `Phase 5` until it is honestly planned and proven.

## 04:09:47 +00:00
- Re-read the retained managed learning executors to ground the rebuilt Phase 5 in real execution truth.
- Accepted first execution split:
  - `m10d_ofs_build_execution.py` still has a direct Databricks API path and can support a CLI-first managed OFS proof,
  - `m11d_train_eval_execution.py` still defaults to GitHub workflow dispatch,
  - `m12d_promotion_commit.py` still defaults to GitHub workflow dispatch.
- Planning impact:
  - rebuild `Phase 5` around distinct subphase boundaries instead of one homogeneous learning proof,
  - move first on semantic admission and OFS dataset-basis proof,
  - hold train/eval and promotion/rollback behind an explicit method judgment on workflow dependence.

## 04:17:41 +00:00
- Executed the rebuilt `Phase 5.A` semantic-admission and telemetry gate on:
  - `execution_id = phase5_learning_mlops_20260312T041559Z`
  - `source_execution_id = phase4_case_label_coupled_20260312T003302Z`
- Accepted result:
  - `verdict = PHASE5A_READY`
  - `open_blockers = 0`
- The gate materially pinned:
  - current upstream label truth is present and clean,
  - intended outputs stay limited to `s2_event_stream_baseline_6B` and `s3_event_stream_with_fraud_6B`,
  - both intended outputs carry `business_traffic`,
  - `6B.S5` is readable with all required machine rails `PASS`,
  - Databricks / SageMaker / MLflow managed surfaces are readable on the current source scope.

## 04:17:41 +00:00
- Read the retained Databricks OFS source files directly after `Phase 5.A` went green:
  - `platform/databricks/dev_full/ofs_build_v0.py`
  - `platform/databricks/dev_full/ofs_quality_v0.py`
- Accepted the first honest `Phase 5.B` blocker:
  - both managed OFS sources are still bootstrap stubs,
  - they only prove control-plane liveness,
  - they do not yet prove current-world dataset-basis correctness, leakage / as-of / maturity enforcement, supervision coverage, or manifest completeness.
- New active posture:
  - treat the stubbed Databricks OFS build surface as the real blocker,
  - repin the managed OFS source to a bounded current-world dataset-basis proof before claiming `Phase 5.B` progress.

## 04:56:29 +00:00
- Replaced the stubbed Databricks OFS build / quality sources with a real bounded current-world `Phase 5.B` probe:
  - `platform/databricks/dev_full/ofs_build_v0.py`
  - `platform/databricks/dev_full/ofs_quality_v0.py`
  - orchestrated by `scripts/dev_substrate/phase5_ofs_dataset_basis.py`
- Ran the rebuilt managed OFS boundary three times while narrowing failures:
  - `phase5_ofs_dataset_basis_20260312T043900Z`
    - red because serverless Databricks had no raw `boto3` S3 credentials
  - `phase5_ofs_dataset_basis_20260312T045330Z`
    - red because Spark Connect does not implement `toJSON()`
  - `phase5_ofs_dataset_basis_20260312T045500Z`
    - red on the first real managed-storage access question

## 04:56:29 +00:00
- Accepted the current real `Phase 5.B` blocker:
  - Databricks serverless cannot read the authoritative object-store basis for the current world
  - the first failing read is:
    - `s3://fraud-platform-dev-full-object-store/platform_20260312T003302Z/sr/run_facts_view/76488594c9b8a02bd5c8b2d4c28b71ff.json`
  - the managed notebook runtime is hitting S3 with anonymous credentials and gets `403 Forbidden`
- Judgment:
  - `Phase 5.A` remains green
  - stubbed OFS source is no longer the blocker
  - the active `Phase 5.B` blocker is managed Databricks object-store authorization

## 05:24:36 +00:00
- Fixed the managed Databricks object-store authorization boundary instead of working around it locally.
- Live managed surfaces materialized:
  - storage credential:
    - `fraud_platform_dev_full_object_store_ro_v0`
  - external location:
    - `fraud_platform_dev_full_object_store_v0`
- Live AWS role remediation:
  - `fraud-platform-dev-full-databricks-cross-account-access`
  - trust now binds to the UC master role + external ID
  - read-only S3 + KMS access for `fraud-platform-dev-full-object-store` attached

## 05:24:36 +00:00
- Fixed two narrow notebook-side defects exposed after the managed read path came alive:
  - Databricks serverless path no longer depends on raw `boto3` S3 access
  - `_passed.flag` is now read through `dbutils.fs.head(...)` instead of being treated as a Spark text source
- Validation:
  - `python -m py_compile scripts/dev_substrate/phase5_ofs_dataset_basis.py platform/databricks/dev_full/ofs_build_v0.py platform/databricks/dev_full/ofs_quality_v0.py` -> pass

## 05:24:36 +00:00
- Re-ran rebuilt `Phase 5.B` through the updated orchestrator:
  - `phase5_ofs_dataset_basis_20260312T051729Z`
    - build `SUCCESS`
    - quality `SUCCESS`
    - red on `PHASE5.B53_EVENT_HORIZON_EXCEEDS_LABEL_ASOF`
  - `phase5_ofs_dataset_basis_20260312T052250Z`
    - build `SUCCESS`
    - quality `SUCCESS`
    - red again on the same blocker with managed-access metadata captured in the summary
- Accepted current truthful blocker:
  - this is no longer a Databricks storage issue
  - this is no longer a notebook runtime issue
  - the active blocker is now semantic:
    - the admitted world still has `event_max_ts_utc = 2026-04-01T00:01:41.104298Z`
    - but `label_asof_utc = 2026-03-12T01:10:38.932670+00:00`
    - so the bounded OFS basis is correctly failing closed on future-leakage risk

## 05:41:04 +00:00
- Reframed the active `Phase 5.B` failure class more narrowly:
  - the defect is not "Databricks still broken"
  - the defect is not "label as-of generally wrong"
  - the defect is that the rebuilt proof was still scoring the full retained `6B` horizon against a bounded promoted Phase 4 run window
- Accepted correction:
  - derive the learning temporal law from the promoted Phase 4 mission binding
  - carry explicit `feature_asof_utc`, `label_asof_utc`, and `label_maturity_lag`
  - trim the raw `6B` partitions to the bounded learning slice before scoring OFS admissibility
- Patched:
  - `scripts/dev_substrate/phase5_learning_mlops_readiness.py`
  - `scripts/dev_substrate/phase5_ofs_dataset_basis.py`
  - `platform/databricks/dev_full/ofs_build_v0.py`
  - `platform/databricks/dev_full/ofs_quality_v0.py`

## 05:47:58 +00:00
- Re-ran `Phase 5.A` on the corrected temporal law:
  - `execution_id = phase5_learning_mlops_20260312T054200Z`
  - `verdict = PHASE5A_READY`
  - `feature_asof_utc = 2026-03-05T00:00:00Z`
  - `label_asof_utc = 2026-03-05T00:00:00Z`
  - `label_maturity_lag = 3d`

## 05:52:27 +00:00
- Re-ran `Phase 5.B` on the corrected bounded-time law:
  - `execution_id = phase5_ofs_dataset_basis_20260312T054900Z`
  - `verdict = PHASE5B_READY`
  - `open_blockers = 0`
- Narrow transient blocker found and fixed during the rerun:
  - Databricks quality dispatch hit `HTTP 400` because the build snapshot was still being passed through notebook params
  - corrected by writing the build snapshot to object-store evidence and handing the quality notebook a ref instead of the full payload
- Accepted bounded OFS slice metrics:
  - `event_rows = 331,506,996`
  - `event_label_rows = 331,506,996`
  - `flow_label_rows = 175,830`
  - `case_timeline_rows = 23,681`
  - `fraud_event_count = 9,806`
  - `fraud_truth_event_count = 8,315,296`
  - `fraud_truth_flow_count = 4,110`
  - `distinct_campaign_count = 6`
  - `mature_event_rows = 315,700,696`
  - `mature_fraud_event_count = 9,340`
- Accepted time-bound judgment:
  - raw event horizon still extends to `2026-04-01T00:01:41.104298Z`
  - bounded event horizon is trimmed to `2026-03-05T00:00:00.983649Z`
  - bounded case horizon is trimmed to `2026-03-04T23:57:50.567244Z`
  - the current promoted Phase 4 world is therefore learning-admissible on the bounded OFS slice

## 06:02:48 +00:00
- Re-read the retained `M11.D` managed train/eval lane directly from `.github/workflows/dev_full_m11_managed.yml`.
- Accepted `Phase 5.C` blocker:
  - the current retained train/eval lane does not consume the admitted Phase 5.B OFS basis
  - it synthesizes convenience CSV rows from a fingerprint-derived seed inside the workflow itself
  - that makes it a managed-surface operability reference only, not valid `Phase 5.C` closure authority
- New active posture:
  - do not reuse the earlier rushed learning closure
  - repin `Phase 5.C` onto the actual bounded OFS basis first
  - only then decide whether workflow dispatch itself is still acceptable or must also be repinned

## 06:36:25 +00:00
- Narrowed the live `Phase 5` problem again before new code:
  - no further OFS rebuild needed
  - no further `Phase 5.A/B` reruns needed
  - the missing proof is a bounded managed train/eval + lineage + governed publish/rollback chain on the already-admitted basis
- Accepted current working judgment:
  - the admitted `Phase 5.B` slice has enough real signal to support bounded supervised learning
  - the next work is to build that managed proof path directly instead of reusing either:
    - the fingerprint-seeded convenience workflow lane
    - the local deterministic MF surrogate

## 06:49:39 +00:00
- Ran the first rebuilt bounded managed `Phase 5.C/D` execution:
  - `execution_id = phase5_learning_managed_20260312T064500Z`
- Fail-fast result:
  - SageMaker `CreateTrainingJob` rejected before training started
  - blocker:
    - execution role `fraud-platform-dev-full-sagemaker-execution` lacked `s3:ListBucket` on `fraud-platform-dev-full-object-store`
- Accepted correction:
  - do not move the sample back to the evidence bucket
  - keep the same admitted object-store basis
  - fix the SageMaker role for the bounded Phase 5 learning prefixes instead
- Live remediation applied:
  - updated inline IAM policy `fraud-platform-dev-full-sagemaker-execution-data-access`
  - added object-store list / read / write access for `platform_*/learning/phase5/*`
  - patched Terraform authority in:
    - `infra/terraform/dev_full/data_ml/main.tf`
    - `infra/terraform/dev_full/data_ml/variables.tf`

## 06:54:21 +00:00
- Ran the second bounded managed `Phase 5.C/D` execution:
  - `execution_id = phase5_learning_managed_20260312T065100Z`
- Accepted result:
  - the SageMaker role/object-store IAM blocker is closed
  - SageMaker training job was created successfully
  - the new failure is runner-local:
    - both `train` and `validation` channels were pointed at the same mixed dataset prefix
- Narrow correction applied:
  - separated Phase 5 sample artefacts into explicit train / validation / test subpaths in:
    - `scripts/dev_substrate/phase5_learning_managed_train_eval.py`
  - preserved managed SageMaker failure reasons in the blocker path for future red runs

## 07:04:26 +00:00
- Ran the first full end-to-end managed green candidate:
  - `execution_id = phase5_learning_managed_20260312T065500Z`
- Result:
  - SageMaker training: success
  - batch transform: success
  - MLflow lineage: success
  - gate / publish / rollback drill: success
- But accepted judgment:
  - this is diagnostic-only, not closure authority
  - the sampled event horizon still exceeded the admitted `feature_asof_utc`
  - so the run is semantically red even though the managed chain itself is operationally green
- Correction applied:
  - over-collect labels
  - join to events
  - hard-trim to `ts_utc <= feature_asof_utc`
  - fail closed if the selected sample still exceeds the admitted time law

## 07:32:48 +00:00
- Accepted the rebuilt managed `Phase 5` closure authority:
  - `execution_id = phase5_learning_managed_20260312T071600Z`
  - `verdict = PHASE5_READY`
  - `next_phase = PHASE6`
- Accepted why this run is different from the earlier green candidate:
  - the bounded sample now stays inside the admitted time law
  - `feature_asof_utc = 2026-03-05T00:00:00Z`
  - `event_scan.ts_max_utc = 2026-03-04T22:25:01.492086Z`
- Accepted managed learning proof:
  - SageMaker train job `fraud-platform-dev-full-mtrain-33d02bc931`: `Completed`
  - SageMaker transform job `fraud-platform-dev-full-mbatch-33d02bc931`: `Completed`
  - MLflow lineage run `4c5b014fd4c1405493f61a25ce6704c7`: `FINISHED`
  - governed publication: `PASS` / `ELIGIBLE` / `PUBLISHED`
  - rollback drill: `VALIDATED`
- Accepted bounded eval metrics:
  - `auc_roc = 0.9104674176699058`
  - `precision_at_50 = 1.0`
  - `log_loss = 0.21071451840777855`
- Carried forward one non-blocking caution:
  - the bounded sample preserved the rare campaign regime instead of dropping it
  - but campaign visibility is still sparse:
    - `campaign_present_rows = 2`
  - so `Phase 6` should widen runtime-facing cohort visibility rather than rely on aggregate-only optics

## 08:41:09 +00:00
- Ran a bounded cost-hygiene pass before opening `Phase 6`.
- Deleted rejected rebuilt `Phase 5` learning prefixes under `platform_20260312T003302Z`:
  - `064500Z`
  - `065100Z`
  - `065500Z`
  - `070500Z`
- Deleted rejected MF train-run prefixes:
  - `tr_291591bab04d1b11d0d5ab73051e2ba7`
  - `tr_a3d73e355d7722597313983f57131237`
- Deleted stale SageMaker model resources for the rejected current-run candidates:
  - `fraud-platform-dev-full-mtrain-model-658803aa7d`
  - `fraud-platform-dev-full-mtrain-model-b8cada27d7`
- Pruned the ingress Lambda zip artefact pile in `fraud-platform-dev-full-artifacts/artifacts/lambda/ig_handler/` and kept only:
  - latest retained Git package
  - latest retained local Phase 0 package
  - latest retained manual package
- Measured storage reduction:
  - ingress Lambda artefacts:
    - `737.8 MiB -> 64.4 MiB`
  - current promoted-run `learning/phase5` prefix:
    - `3.8 MiB -> 790.5 KiB`
  - current promoted-run `mf/train_runs` prefix:
    - `33.9 KiB -> 11.4 KiB`
- Added prevention in Terraform:
  - lifecycle rule for `artifacts/lambda/ig_handler/`
  - current-object expiry `14` days
  - noncurrent-version expiry `7` days
- Deliberately did not bulk-delete old top-level `platform_*` prefixes yet:
  - there is likely more storage there
  - but a safe wider sweep needs a preserve ledger for promoted green runs and rollback references first

## 08:58:40 +00:00
- Widened the cost review from `Phase 5` artefacts to the full live platform using Cost Explorer for `2026-03-07` through `2026-03-11`.
- Accepted the main whole-platform cost findings:
  - `DynamoDB` cost is dominated by write units, but the ingress table had also accumulated stale transient state:
    - `97,745,446` items
    - `142,509,236,122` bytes
  - `S3` recent spend is mostly request-driven, not GB-storage-driven
  - `RDS` spend is mainly Aurora ACU + IO, not storage
  - runtime compute spend included an idle EKS worker floor, not just ECS/Fargate
- Verified there was no active ingress pressure before touching transient runtime state:
  - Lambda invocations in the previous hour: none
  - DDB consumed writes in the previous hour: `0`
- Recycled the ingress idempotency ledger:
  - deleted and recreated `fraud-platform-dev-full-ig-idempotency`
  - preserved the same runtime contract:
    - key `dedupe_key`
    - `PAY_PER_REQUEST`
    - TTL `ttl_epoch`
  - post-reset table state:
    - `ItemCount = 0`
    - `TableSizeBytes = 0`
- Put the live EKS runtime into explicit standby:
  - nodegroup `fraud-platform-dev-full-m6f-workers`:
    - from `desired=4`, `min=2`
    - to `desired=0`, `min=0`
  - scaled all deployments in:
    - `fraud-platform-rtdl`
    - `fraud-platform-case-labels`
    - to `0`
  - scaled `kube-system/coredns` to `0` so the last worker would not be held by the DNS floor alone
- Measured effect:
  - worker drain is in progress toward zero
  - Aurora CPU dropped from the prior `~100%` plateau to roughly `~17-18%` in the latest samples
  - Aurora capacity metric was still reading `4.0 ACU` in the immediate post-standby samples, so that still needs a later recheck
- Recorded the resulting operational posture:
  - live runtime is now intentionally in standby for cost control between phases
  - before the next active proving run, EKS nodegroup size, `coredns`, and the RTDL / Case+Label deployments must be restored deliberately

## 09:18:40 +00:00
- Started the rebuilt `Phase 6` boundary audit instead of spending immediately on traffic.
- Captured the first real coupled-runtime blocker:
  - `pr3_rtdl_materialize.py` was only mounting the DF registry snapshot
  - DF registry policy was still coming from the image
  - so promoted active-bundle truth was not yet a trustworthy live runtime boundary
- Corrected that boundary in code:
  - `pr3_rtdl_materialize.py` now mounts both the DF registry policy and DF snapshot into the run-scoped runtime profile
  - `pr3_runtime_warm_gate.py` now reports the live DF policy ref / revision and explicit fallback bundle refs
- Recreated the missing `Phase 6` planning surface:
  - added `platform.production_readiness.phase6.md`
- Rebuilt the Phase 6 proving tooling around the actual goal:
  - new live DLA bundle-attribution probe
  - rebuilt coupled rollup
  - rebuilt Phase 6 executor for candidate adoption -> rollback -> restore

## 09:25:28 +00:00
- Ran the first rebuilt `Phase 6` executor:
  - `execution_id = phase6_learning_coupled_20260312T092007Z`
- Useful live outcome before failure:
  - runtime restore from standby succeeded
  - promoted registry-surface rematerialization succeeded
  - all RTDL and Case + Label deployments rolled out healthy
- Narrow failure class:
  - shared `pr3_control_plane_bootstrap.py` still required `g3a_run_charter.active.json`
  - rebuilt Phase 6 runner had only written `g6a_run_charter.active.json`
- Accepted judgment:
  - this is a harness compatibility defect
  - not a runtime or learning-coupling defect
- Applied the narrow fix:
  - Phase 6 runner now writes both:
    - `g6a_run_charter.active.json`
    - `g3a_run_charter.active.json`

## 10:14:11 +00:00
- Ran the second rebuilt `Phase 6` executor:
  - `execution_id = phase6_learning_coupled_20260312T092549Z`
- Candidate path result:
  - control bootstrap passed
  - candidate runtime warm gate passed on the promoted registry surfaces
  - DF warm-gate telemetry confirmed promoted active truth:
    - mounted promoted snapshot
    - mounted promoted policy
    - `policy_revision = r3`
    - promoted bundle refs for `fraud` and `baseline`
- New blocker class:
  - rollback rematerialization was healthy
  - but the reused pre-traffic warm gate failed on:
    - `PR3.S4.WARM.B12K_OFP_NOT_OPERATIONALLY_READY`
- Accepted judgment:
  - candidate adoption is not the blocker
  - the reused rollback / restore warm gate is checking the wrong transition boundary
  - rollback / restore authority should sit on post-activation bundle attribution, not on the pre-traffic OFP transition posture
- Applied the narrow fix:
  - kept candidate warm gate hard
  - treated rollback / restore warm-gate `B12K` as advisory-only when it is the sole blocker
  - kept rollback / restore truth anchored on post-activation DLA bundle attribution
