
## 2026-03-10 04:43:05 +00:00
- Updated docs/model_spec/platform/implementation_maps/dev_substrate/dev_full/proving_plane/production_readiness.md so first appearance of major platform abbreviations now uses full component names, including World Streamer Producer (WSP), Ingestion Gate (IG), Real-Time Decision Loop (RTDL), Context Store Flow Binding (CSFB), Identity Entity Graph (IEG), Online Feature Plane (OFP), Degrade Ladder (DL), Decision Fabric (DF), Action Layer (AL), Decision Log Audit (DLA), and the managed learning lane names.


## 2026-03-10 04:53:43 +00:00
- Expanded docs/design/platform/dev_full/graph/dev_full_platform_network_current_v0.mermaid.mmd to make production paths explicit in the system graph rather than implied by topology alone. Added hot path, admission path, case-and-label path, learning path, recovery path, degrade path, replay path, rollback path, observability path, and cost-control path overlays, then rerendered the PNG companion.


## 2026-03-10 05:00:43 +00:00
- Added a path-centric companion graph at docs/design/platform/dev_full/graph/dev_full_platform_production_paths_current_v0.mermaid.mmd with explicit traversals for the hot, admission, case-label, learning, recovery, degrade, replay, rollback, observability, and cost-control paths across the real implemented dev_full components.


## 2026-03-10 05:05:14 +00:00
- Refined the graph split under docs/design/platform/dev_full/graph/: removed the production-path subgraph and overlay edges from dev_full_platform_network_current_v0.mermaid.mmd so the main system graph stays structural, and expanded the legend in dev_full_platform_production_paths_current_v0.mermaid.mmd so path prefixes (HP, AP, CL, LP, RC, DG, RP, RB, OP, CP) are readable without external explanation. Rerendered both PNG companions.


## 2026-03-10 05:15:01 +00:00
- Clarified retained and handle-only graph labels across the dev_full graph set. Managed Flink is now explicitly labeled as a retained surface rather than an active output path; the SageMaker serving endpoint is labeled as a pinned reference only; Redis is labeled as an SSM-backed handle with no live resource shown; and the MWAA IAM role is labeled as role-only with no materialized environment. Rerendered the affected PNG companions.


## 2026-03-10 05:21:37 +00:00
- Added a causal readiness example graph for the Control + Ingress plane at docs/design/platform/dev_full/graph/readiness/dev_full_control_ingress_readiness_delta_current_v0.mermaid.mmd. The graph is structured as component -> defect -> remediation -> measured impact -> closure so the plane's green status is explainable rather than just asserted.


## 2026-03-10 05:32:11 +00:00
- Tightened docs/model_spec/platform/implementation_maps/dev_substrate/dev_full/proving_plane/platform.production_readiness.md in place. Rewrote the opening production-readiness definition and the Control + Ingress section to remove vague language and replace it with explicit platform properties, failure modes, ownership boundaries, and production consequences while keeping the document in discussion form rather than converting it into a gate checklist.

## 05:38
- Continued the platform.production_readiness.md precision pass.
- Tightened the Operations / Governance / Meta section so observability, auditability, governance, and cost discipline are described in concrete production terms rather than broad adjectives.
- Cleaned up rough headings and sharpened the whole-platform and production-path sections so the discussion stays readable but no longer feels like a loose transcript.
## 05:39
- Completed the final polish pass on platform.production_readiness.md.
- Normalized lower-half naming and headings so the cross-plane and production-path sections read consistently with the earlier plane sections.
- Removed one duplicated whole-platform heading, promoted the production paths into real subsection headings, and aligned terms like Archive Writer, CaseTrigger, Decision Fabric, Databricks (Offline Feature Plane / OFS), and MLflow (Model Promotion and Registry / MPR).
## 06:07
- Replaced the raw discussion in platform.production_readiness.plan.md with an execution-grade plan.
- The new plan is organized around proving a growing production-ready network: plane readiness first, then coupled-network readiness, then promotion into the working platform.
- Pinned bounded proof slices, retained the current 3000 steady / 6000 burst ingress envelope, and defined the phase sequence from Control + Ingress revalidation through full-platform bounded stress authorization.
## 06:15
- Tightened platform.production_readiness.plan.md so each phase explicitly anchors back to platform.production_readiness.md.
- Added per-phase Production-readiness anchor, Quantitative proof focus, and Qualitative proof focus sections so execution remains tied to the actual meaning of production readiness rather than just phase movement.
## 06:29
- Added a readiness-network graph that shows only the currently confirmed production-ready platform network.
- New artifact: docs/design/platform/dev_full/graph/readiness/dev_full_platform_network_production_ready_current_v0.mermaid.mmd (+ PNG).
- The graph is derived from the wired system graph but intentionally omits every plane, component, and path that has not yet been promoted into the working platform. Current scope shown: Control + Ingress only.
## 06:39
- Added current-state readiness-delta graphs for the non-closed planes and the whole platform in docs/design/platform/dev_full/graph/readiness/.
- New explanatory artifacts:
  - dev_full_rtdl_readiness_delta_current_v0.*
  - dev_full_case_label_readiness_delta_current_v0.*
  - dev_full_learning_mlops_readiness_delta_current_v0.*
  - dev_full_ops_gov_meta_readiness_delta_current_v0.*
  - dev_full_full_platform_readiness_delta_current_v0.*
- These are derived explanatory graphs that show the current amber/red posture based on work done so far; they are not authority or proof by themselves.
## 08:07
- Removed the temporary amber/red readiness-delta graphs from docs/design/platform/dev_full/graph/readiness/.
- Kept only the control+ingress green readiness-delta graph and the current confirmed production-ready network graph.
- Rationale: the non-green graphs are more useful when created during active plane remediation rather than as stale summary artifacts.
## 08:28
- Added a detailed resource graph for the currently confirmed production-ready network.
- New artifact: docs/design/platform/dev_full/graph/readiness/dev_full_platform_resources_production_ready_current_v0.mermaid.mmd (+ PNG).
- The graph shows only the current working-platform resources (Control + Ingress and its supporting transport/state/identity/network/observability surfaces) but with concrete AWS names and key resource posture such as API ID/stage, Lambda memory/concurrency/timeout, ECS task CPU/memory/desired count, DDB key/TTL, and the relevant VPC endpoints and IAM roles.

## 09:49
- Updated docs/model_spec/platform/implementation_maps/dev_substrate/dev_full/proving_plane/platform.production_readiness.plan.md so telemetry is now a mandatory planning surface rather than an implied execution detail.
- Added telemetry-first hardening doctrine near the top of the plan and a new per-phase operating rule that blocks execution until log tails, live counters, boundary-health checks, and fail-fast conditions are explicitly pinned.
- Expanded every phase with a concrete Telemetry plan section covering live logs, progress counters, boundary health, fail-fast triggers, and the minimal hardening artifact set.
- Added allowed Data Engine semantic-reference guidance into the plan so RTDL, Case + Label, and Learning / MLOps phases now point back to the exact engine docs we may inspect for data semantics while still preserving the interface-pack black-box rule.
- The semantic anchor set now explicitly includes:
  - docs/model_spec/data-engine/interface_pack/data_engine_interface.md
  - docs/model_spec/data-engine/interface_pack/engine_gates.map.yaml
  - docs/model_spec/data-engine/implementation_maps/segment_5B.build_plan.md
  - docs/model_spec/data-engine/implementation_maps/segment_6B.build_plan.md
  - docs/model_spec/data-engine/layer-2/specs/state-flow/5B/state.5B.s5.expanded.md
  - docs/model_spec/data-engine/layer-3/specs/state-flow/6B/state.6B.s5.expanded.md
- Tightened the Immediate next action so Phase 0 now starts with telemetry pinning before any bounded Control + Ingress revalidation run is authorized.
## 10:02
- Added an explicit readiness metric ledger to docs/model_spec/platform/implementation_maps/dev_substrate/dev_full/proving_plane/platform.production_readiness.plan.md.
- The ledger now pins working target families for:
  - Control + Ingress
  - RTDL
  - Case + Label Management
  - Learning + Evolution / MLOps
  - Operations / Governance / Meta
  - full-platform cross-plane continuity
- This change removes "eyeball it" ambiguity from the plan by naming, for each plane, the metric, the current target posture, the evidence surface, and the owning phase.
- The ledger is still a working engineering reference rather than a rigid certification checklist: targets may be tightened by evidence, but are not to be loosened casually just to get a green verdict.
## 10:08
- Updated the readiness metric ledger rules in docs/model_spec/platform/implementation_maps/dev_substrate/dev_full/proving_plane/platform.production_readiness.plan.md so the ledger is now explicitly living.
- New rule: as phases progress and new production-readiness-defining metrics are discovered, they must be added to the ledger in place rather than left implicit in notes, summaries, or memory.
- New closure rule: when a phase expansion reveals a materially necessary missing metric family, the ledger must be updated before that phase can be treated as honestly closed.
## 10:24
- Added a road map for phase expansion to docs/model_spec/platform/implementation_maps/dev_substrate/dev_full/proving_plane/platform.production_readiness.plan.md using the USER's progression method directly.
- The plan now explicitly says: start from the phase goal, ask how that goal can actually be accomplished, derive the necessary steps from that, and let those real required steps define the subphases.
- It also now says telemetry and metrics are to be derived after the subphases are defined from the real goal of the phase, and that phase closure depends on the actual goal being accomplished rather than on subphases merely being run.
## 10:42
- Expanded `Phase 0 - Control + Ingress revalidation` into its own execution-grade phase document at `docs/model_spec/platform/implementation_maps/dev_substrate/dev_full/proving_plane/platform.production_readiness.phase0.md`.
- The expansion follows the agreed method directly: start from the parent phase goal, ask what must be true for that goal to be genuinely accomplished, derive only the necessary subphases from that, then define telemetry and metrics per subphase.
- `Phase 0` is now broken into four derived subphases:
  - `Phase 0.A` live operator surface and preflight truth
  - `Phase 0.B` fresh-run correctness revalidation
  - `Phase 0.C` envelope and recovery revalidation
  - `Phase 0.D` working-platform baseline reaffirmation
- Each subphase now carries its own telemetry sub-ledger and concrete definition of done so the phase can be executed toward its real goal rather than through arbitrary states.
- Updated the parent proving plan to point `Phase 0` to the new expansion document.
## 10:49
- Reverted the temporary `Phase 0` expansion doc and removed the parent-plan pointer to it.
- Reason: the expansion incorrectly pulled historical `M6` Control + Ingress build-phase authority directly into the proving-plane phase expansion. That was the wrong planning posture for the current proving method and introduced old build-track assumptions into the new production-readiness planning path.
## 11:00
- Re-expanded `Phase 0 - Control + Ingress revalidation` into `docs/model_spec/platform/implementation_maps/dev_substrate/dev_full/proving_plane/platform.production_readiness.phase0.md`.
- This time the expansion was derived strictly from current proving-plane authority only:
  - `platform.production_readiness.md`
  - `platform.production_readiness.plan.md`
- Historical `M*` build-phase decomposition was explicitly excluded from authority for this step.
- The resulting subphases are derived from the actual parent phase goal and its contributors:
  - `Phase 0.A` telemetry and preflight truth
  - `Phase 0.B` fresh-run correctness proof
  - `Phase 0.C` envelope and recovery proof
  - `Phase 0.D` working-platform base reaffirmation
- Each subphase now carries its own telemetry sub-ledger and closure logic without importing older build-track state structure.
## 2026-03-10 11:27:54 +00:00
- Continued `Phase 0.A` preflight for Control + Ingress using only the proving-plane authority and live AWS inspection.
- Pinned the actual active external ingress path as `API Gateway v2 -> Lambda`, not the internal ECS ingress service:
  - API `fraud-platform-dev-full-ig-edge` (`pd7rtjze95`)
  - stage `v1`
  - route `POST /ingest/push`
  - integration `sm9ahw3` -> Lambda `fraud-platform-dev-full-ig-handler`
- Recorded the main live operator surfaces now available for Phase 0:
  - WSP logs: `/ecs/fraud-platform-dev-full-wsp-ephemeral`
  - Lambda logs: `/aws/lambda/fraud-platform-dev-full-ig-handler`
  - internal ECS ingress logs: `/ecs/fraud-platform-dev-full-ig-service`
  - ECS container insights: `/aws/ecs/containerinsights/fraud-platform-dev-full-ingress/performance`
  - DynamoDB idempotency table: `fraud-platform-dev-full-ig-idempotency`
  - SQS DLQ: `fraud-platform-dev-full-ig-dlq`
- Recorded the first real telemetry gap for active hardening:
  - API Gateway stage `v1` has `DetailedMetricsEnabled=false`
  - no API Gateway access-log group is currently visible
  - so Phase 0 must initially rely on Lambda + DDB + SQS + WSP + publish continuity as the primary live truth surface.
- Updated `platform.production_readiness.phase0.md` and `platform.production_readiness.impl_actual.md` so Phase 0.A is now grounded in actual AWS state rather than generic telemetry placeholders.
## 2026-03-10 11:39:18 +00:00
- Extended `Phase 0.A` from raw resource inventory into an actual operator loop for the first bounded Control + Ingress revalidation.
- Confirmed that CloudWatch metric surfaces for the active ingress path do exist:
  - `AWS/ApiGateway` metrics for `ApiId=pd7rtjze95`, `Stage=v1`
  - `AWS/Lambda` metrics for `fraud-platform-dev-full-ig-handler`
  - `AWS/SQS` metrics for `fraud-platform-dev-full-ig-dlq`
  - `AWS/DynamoDB` throttle metrics for `fraud-platform-dev-full-ig-idempotency`
- Important nuance: API Gateway and Lambda currently show sparse/no recent datapoints because the ingress plane is not actively running right now, while the SQS DLQ depth metric is live and readable.
- Updated `platform.production_readiness.phase0.md` so `Phase 0.A` now includes:
  - the active ingress path truth,
  - the telemetry gap around API Gateway detailed metrics / access logs,
  - the live telemetry posture,
  - the default CLI operator loop for `Phase 0.B` and `Phase 0.C`.
- Updated `platform.production_readiness.impl_actual.md` with the same operational conclusion: the next bounded run is required not just to test ingress, but to warm the primary metric and log surfaces that Phase 0 will rely on.
## 2026-03-10 11:46:22 +00:00
- Verified the active front-door health surface for `Phase 0.A` with the current API key.
- `GET https://pd7rtjze95.execute-api.eu-west-2.amazonaws.com/v1/ops/health` returned `200` and explicitly identified the active ingress mode as `apigw_lambda_ddb_kafka` with the expected declared envelope `3000 / 6000`.
- This closes an important remaining ambiguity in `Phase 0.A`: the active external ingress path is not only inferred from configuration, it is now directly confirmed by the live runtime.
- Updated the Phase 0 doc and proving-plane implementation notes so this health probe is part of the pinned preflight truth and the boundary-health checks.
## 2026-03-10 11:35:42 +00:00
- Added `scripts/dev_substrate/phase0_control_ingress_revalidate.py` as the proving-plane CLI entrypoint for bounded Control + Ingress correctness runs.
- This wrapper keeps the execution core shared by reusing `pr3_wsp_replay_dispatch.py`, but removes the old `PR3` naming from the operator surface by:
  - generating fresh run ids,
  - writing under `runs/dev_substrate/dev_full/proving_plane/run_control/`,
  - pinning `Phase 0` execution/blocker naming.
- Dry-run validation passed and printed the exact remote command that would be launched for `Phase 0.B`.
- Updated the Phase 0 doc and proving-plane implementation notes so the default bounded correctness entrypoint is now explicit.
## 2026-03-10 11:55:49 +00:00
- First bounded `Phase 0.B` launch failed immediately at the proving-loop wrapper boundary, before any AWS traffic was generated.
- Cause: the shared remote WSP replay dispatcher requires the execution root to already exist, and the new Phase 0 wrapper had not pre-created `runs/dev_substrate/dev_full/proving_plane/run_control/<execution_id>/`.
- Treated this as a hardening-loop defect, not a platform-runtime defect.
- Updated `platform.production_readiness.impl_actual.md` with the observed failure and reasoning, then patched `scripts/dev_substrate/phase0_control_ingress_revalidate.py` so it now creates the execution root before invoking the shared remote dispatcher.
## 2026-03-10 11:59:41 +00:00
- Second bounded `Phase 0.B` launch failed on the next legacy dependency inside `pr3_wsp_replay_dispatch.py`.
- Cause: the shared dispatcher hard-requires `pr3_s0_execution_receipt.json` and refuses to start unless it reads `PR3_S0_READY` from that file.
- Treated this as another proving-loop compatibility defect rather than a Control + Ingress runtime defect.
- Recorded the reasoning in `platform.production_readiness.impl_actual.md` and patched the Phase 0 wrapper so it now seeds a minimal compatibility `pr3_s0_execution_receipt.json` in the execution root before invoking the shared dispatcher.
## 2026-03-10 12:13:17 +00:00
- The first real `Phase 0.B` runtime failure was traced to an ingress-target drift in the shared dispatcher, not yet to a true ingress defect.
- The generated manifest showed the bounded run was hitting the retained internal ALB surface:
  - `http://internal-fp-dev-full-ig-svc-1208840173.eu-west-2.elb.amazonaws.com/v1/ingest/push`
  - `metric_surface.mode = ALB`
- This contradicted the `Phase 0.A` pinned active front door `API Gateway -> Lambda`, which explained why WSP saw `http_503` while the Lambda log stream stayed dark.
- Recorded the reasoning in `platform.production_readiness.impl_actual.md` and patched `scripts/dev_substrate/phase0_control_ingress_revalidate.py` so the wrapper now explicitly passes:
  - `https://pd7rtjze95.execute-api.eu-west-2.amazonaws.com/v1/ingest/push`
- Updated the Phase 0 doc to make the execute-api target part of the default bounded correctness posture.

## 2026-03-10 12:31:00 +00:00
- Restarted `Phase 0` from fresh proving authority only and rebuilt the implementation-note trail instead of continuing the stale note history.
- Re-pinned the live Control + Ingress boundary on AWS:
  - API `fraud-platform-dev-full-ig-edge` (`pd7rtjze95`)
  - stage `v1`
  - route `POST /ingest/push`
  - integration `sm9ahw3` -> Lambda `fraud-platform-dev-full-ig-handler`
- Verified the live health surface with the current API key and confirmed the runtime still reports:
  - mode `apigw_lambda_ddb_kafka`
  - envelope `3000 / 6000`
- Fresh preflight surfaced two live nuances that now govern the phase:
  - `/ops/health` requires the application API key even though API Gateway route auth is `NONE`
  - `/fraud-platform/dev_full/ig/service_url` still points to the retained internal ALB and remains a drift hazard if not overridden explicitly
- Updated `platform.production_readiness.phase0.md` and the implementation notes so those facts are now part of the pinned `Phase 0.A` truth.

## 2026-03-10 12:39:59 +00:00
- Ran the first fresh bounded `Phase 0.B` correctness revalidation on the corrected execute-api target:
  - execution `phase0_20260310T123239Z`
  - platform run `platform_20260310T123239Z`
- The run materially exercised the intended ingress boundary and stayed clean on the failure signals:
  - `4xx = 0`
  - `5xx = 0`
  - Lambda `Errors = 0`
  - Lambda `Throttles = 0`
  - DLQ depth stayed flat at the pre-run baseline (`92`)
- The verdict still returned red because admitted throughput only reached `1734.125 eps` against the declared `3000 eps` target.
- Narrow diagnosis from live API Gateway/Lambda telemetry plus WSP lane logs:
  - ingress admitted what it received cleanly
  - the current `Phase 0` WSP shape (`24` lanes, `stream_speedup 19.7`) did not materially drive the declared ingress envelope
  - sampled lane logs showed roughly `60-65 eps` real per-lane pace despite a configured `125 eps` target and also reported `pack manifest missing` / `pack not sealed`
- Compared against prior proven run evidence under `runs/dev_substrate/dev_full/road_to_prod/run_control/pr3_20260306T021900Z/`, which previously reached `3025.36 eps` on the same substrate with a stronger WSP posture (`stream_speedup 51.2`, `lane_count 40`).
- Decision: treat the current red as a proving-shape calibration defect, not yet an ingress-runtime defect, and rerun `Phase 0.B` narrowly with a stronger evidence-backed WSP launch shape before reopening ingress remediation.

## 2026-03-10 13:10:58 +00:00
- Continued fresh `Phase 0.B` on the stronger evidence-backed WSP posture (`40` lanes, `stream_speedup 51.2`) and separated three distinct runtime truths:
  - `phase0_20260310T124200Z` hit intermittent `5xx` / lane exits because the Lambda ingress hot path was still running `IG_HEALTH_BUS_PROBE_MODE=describe`; Lambda logs pinned the real cause as `IG_UNHEALTHY:BUS_UNHEALTHY`.
  - repinned the live Lambda env to `IG_HEALTH_BUS_PROBE_MODE=none`, then reran as `phase0_20260310T130200Z`; this produced the first clean truthful runtime red:
    - `4xx = 0`
    - `5xx = 0`
    - admitted throughput `1584.525 eps`
    - `p95 = 1479.95 ms`
    - `p99 = 1990.17 ms`
    - Lambda errors/throttles stayed `0`
    - Lambda concurrency peaked at `599/600`
  - increased reserved concurrency to `900` as a narrow remediation probe and reran as `phase0_20260310T130900Z`; this was materially worse, not better:
    - all `40` lanes exited non-zero before the measurement window stabilized,
    - lane/Lambda evidence pinned `KAFKA_PUBLISH_TIMEOUT -> PUBLISH_AMBIGUOUS`,
    - run-scoped quarantine artifacts were created for valid traffic.
- Production judgment from those runs:
  - after the WSP calibration fix, `Phase 0.B` is exposing real ingress-runtime defects rather than harness defects,
  - the truthful baseline red is now the `600`-concurrency run,
  - the `900` uplift is a failed remediation probe, not a new baseline.
- Reverted live Lambda reserved concurrency back to `600` and reverted the Terraform default from `900` back to `600` so the platform is not left in the known-worse `PUBLISH_AMBIGUOUS` posture while Phase 0 continues.
- Removed another telemetry blind spot in the shared dispatcher:
  - when lane count is high enough to use metadata-only capture, bounded-run summaries now still keep short abnormal-lane log tails plus extracted failure markers for non-zero-exit lanes.
  - this preserves small hardening artifacts while keeping lane-collapse reasons attributable inside `runs/`.

## 2026-03-10 13:10:58 +00:00
- While pulling hot-path timing from Lambda logs, found a code-level observability defect in the live ingress entrypoint:
  - `MetricsRecorder` already flushes `phase.publish_seconds`, `phase.receipt_seconds`, `phase.dedupe_*`, and `admission_seconds`,
  - but `aws_lambda_handler.py` only set its own module logger to `INFO`, not the root / `fraud_detection` logger hierarchy,
  - so those metrics flush lines were not materially appearing in CloudWatch during the fresh Phase 0 windows.
- Patched `src/fraud_detection/ingestion_gate/aws_lambda_handler.py` locally so Lambda logging now raises:
  - root logger level,
  - `fraud_detection`,
  - `fraud_detection.platform_narrative`,
  - handler module logger,
  all from `IG_LOG_LEVEL` with default `INFO`.
- Verified local syntax with `python -m py_compile` on the touched ingress and proving scripts.
- This is a local telemetry remediation only for now; it still needs deployment before the next bounded rerun can use the phase-level timing signals in CloudWatch.

## 2026-03-10 14:07:26 +00:00
- Deployed the first request-timing Lambda build for deeper `Phase 0.B` visibility:
  - deployment artifact `runs/dev_substrate/dev_full/proving_plane/deploy/phase0_ig_lambda_20260310T135006Z.*`
- Immediately reran bounded `Phase 0.B` as `phase0_20260310T135126Z`.
- That run went red for a proving-agent reason, not a platform-runtime reason:
  - API Gateway `5xx = 2206`
  - Lambda `Errors` matched the `5xx`
  - root cause in logs: `NameError: name '_prune_none' is not defined`
  - all `40` WSP lanes eventually failed through retry exhaustion
- Repaired the telemetry helper in `src/fraud_detection/ingestion_gate/aws_lambda_handler.py` so timing emission is compacted correctly and cannot break admission if the logging path faults.
- Deployed the repaired Lambda build:
  - deployment artifact `runs/dev_substrate/dev_full/proving_plane/deploy/phase0_ig_lambda_20260310T135741Z.*`
  - verified live Lambda code hash `oXhYuOAnn1QR3kFu8hoEuGdWPBwCic52eGscCDDQ47g=`
- Reran bounded `Phase 0.B` again as `phase0_20260310T135855Z` on the restored truthful runtime.
- Current truthful result from that rerun:
  - valid-traffic `4xx = 0`
  - valid-traffic `5xx = 0`
  - admitted throughput `1324.508 eps`
  - `p95 = 553.894 ms`
  - `p99 = 1472.352 ms`
  - Lambda `Throttles = 0`
  - three WSP lanes failed non-zero because Lambda returned `400` with `decision = QUARANTINE`
- New live telemetry now materially pins the remaining ingress blockers:
  - `IG gate initialized` fired `525` times during the run with `init_seconds p50 ≈ 1.38 s`, `p95 ≈ 1.414 s`
  - sampled successful `IG request timing` logs show `gate_ms ≈ 1.37-1.41 s` and `admit_ms ≈ 0.8-0.9 s`
  - sampled abnormal `400 QUARANTINE` requests show `admit_ms ≈ 11-15 s`, indicating long duplicate/in-flight resolution waits rather than edge instability
- Operational judgment after the truthful rerun:
  - the current `Phase 0.B` red is no longer a throttling problem
  - it is also not the earlier `KAFKA_PUBLISH_TIMEOUT` family
  - the remaining blockers are cold-start / gate-init churn, slow admit-path work, and long duplicate-resolution quarantine paths

## 2026-03-10 14:30:49 +00:00
- Continued `Phase 0.B` with the newly visible Lambda timings and found the next real blind spot at the object-store health boundary rather than the admit hot path itself.
- Fresh bounded rerun `phase0_20260310T142125Z` showed that the ingress was still being poisoned by benign governance append contention:
  - Lambda logs carried both `Governance projection append deferred ... reason=S3_APPEND_CONFLICT`
  - and `IG object store observed unhealthy ... ConditionalRequestConflict`
- Narrow conclusion: the earlier object-store hardening only treated the in-process `RuntimeError("S3_APPEND_CONFLICT")` form as benign, not the live S3 `ClientError` equivalent.
- Patched `src/fraud_detection/ingestion_gate/store.py` so append contention is now treated as benign for:
  - `S3_APPEND_CONFLICT`
  - `ConditionalRequestConflict`
  - `PreconditionFailed` / `412`
- Rebuilt and redeployed the ingress Lambda:
  - deployment artifact `runs/dev_substrate/dev_full/proving_plane/deploy/phase0_ig_lambda_20260310T143049Z.*`
  - live code hash after deploy: `Nnc6Es87IZewxrfoAoDMks1vlO+h+c6mNsmul8RgRSA=`
- Reran bounded `Phase 0.B` twice on the restored live boundary:
  - `phase0_20260310T143110Z`
  - `phase0_20260310T143801Z`
- Those reruns materially changed the platform judgment:
  - `4xx = 0`
  - `5xx = 0`
  - latency clean (`p95 ≈ 49-50 ms`, `p99 ≈ 56-57 ms`)
  - admitted throughput only missed the gate by a tiny amount (`2999.925 eps`, then `2999.650 eps`)
- Tried one narrow probe with `target_request_rate_eps = 3000.5` (`phase0_20260310T144447Z`) and found it did not create a clean green; it only shifted which measured minute underfilled.
- Operational judgment moved again:
  - ingress runtime semantics are now clean on the bounded correctness shape
  - the active blocker moved back to the proof boundary, not the ingress runtime

## 2026-03-10 15:25:52 +00:00
- Spent the next bounded runs attributing that last proof-boundary miss instead of patching by instinct.
- Ran `phase0_20260310T145355Z` with full WSP lane logs.
- That run stayed semantically clean:
  - admitted throughput `2999.375 eps`
  - `4xx = 0`
  - `5xx = 0`
  - clean latency
- The full-log probe established three useful truths:
  - CloudWatch metric counts were not settling late; later direct metric queries matched the original totals exactly
  - the miss was tiny in absolute terms, on the order of `75` requests across the full measured `120 s`
  - the second measured minute was effectively on target while the first measured minute carried almost all of the deficit
- Tested a forced synchronized `campaign_start_utc` shape as `phase0_20260310T150019Z`:
  - result was materially worse and invalid as a baseline
  - widespread `http_503`
  - `IG_PUSH_RETRY_EXHAUSTED`
  - `IG_PUSH_REJECTED`
  - valid-traffic quarantine and tail-latency blowout
- Tested a warmup-shaped one-minute probe as `phase0_20260310T150824Z`:
  - this also failed and is not a valid replacement proof shape
  - widespread non-zero lane exits
  - early cutoff
  - `IG_PUSH_REJECTED`
- Current truthful state at this checkpoint:
  - the unsynchronized bounded run shape remains the only semantically trustworthy `Phase 0.B` baseline
  - ingress runtime defects uncovered earlier in the day are no longer the active blocker
  - `Phase 0.B` remains open because the bounded correctness proof gate itself still needs reshaping so it measures a genuinely stable steady-state minute without introducing synchronized-arrival artifacts

## 2026-03-10 15:45:36 +00:00
- Patched the shared dispatcher start-confirmation logic in `scripts/dev_substrate/pr3_wsp_replay_dispatch.py`.
- Reason:
  - the warmup-shaped probe had exposed a harness bug where `active_confirmed_at` stayed pinned to `submitted_at` unless every lane was `RUNNING` at the same instant
  - on large `40`-lane fleets that can be too strict and can contaminate `measurement_start_utc`
- The dispatcher now confirms fleet start once all lanes have at least started and records `fleet_confirmation_mode` in the summary.
- Recompiled the shared dispatcher and Phase 0 wrapper with `python -m py_compile`.
- Reran the warmed one-bin probe after the fix:
  - `phase0_20260310T153432Z`
  - `warmup_seconds = 60`
  - `duration_seconds = 60`
  - `fleet_confirmation_mode = all_running`
  - admitted throughput `2999.617 eps`
  - `4xx = 0`
  - `5xx = 0`
  - clean latency
- Immediately repeated the same warmed one-bin probe to test repeatability:
  - `phase0_20260310T154004Z`
  - this repeat failed badly with widespread `IG_PUSH_REJECTED` lane exits and admitted throughput collapse to `630.433 eps`
- Current judgment after those two runs:
  - the dispatcher timing fix is real and should stay
  - the warmed single-bin proof posture is not yet repeatable enough to adopt as the new `Phase 0.B` baseline
  - `Phase 0.B` remains open on proof-shape instability rather than on a newly rediscovered ingress-runtime defect

## 2026-03-10 15:58:47 +00:00
- Updated the authoritative readiness docs to reflect the current `Phase 0` closeout posture rather than leaving it implicit in the run notes.
- Main planning correction:
  - `Phase 0` is now explicitly treated as a proof-boundary closure problem, not an ingress rediscovery problem
  - the semantically trustworthy unsynchronized bounded run stays frozen as the active `Phase 0.B` baseline
  - synchronized-start and unstable warmed single-bin probes are now documented as diagnostic-only, not promotion baselines
- Added explicit doctrine to the parent plan for:
  - truthful steady-state measurement
  - narrow rerun discipline once a trustworthy baseline exists
- Updated the `Phase 0` expansion so `0.B` now emphasizes:
  - measurement-start attribution from confirmed fleet participation
  - repeatability on the same truthful run shape
  - no `Phase 0.C` escalation until `0.B` is green on a repeatable baseline
- Operational impact:
  - future `Phase 0` spend should now go toward telemetry lock, repeatability proof, and narrow defect attribution instead of more ad hoc traffic-shape experiments

## 2026-03-10 16:06:49 +00:00
- Updated the three readiness graphs under `docs/design/platform/dev_full/graph/readiness/` to reflect the current `Phase 0` truth.
- Network and resource graph corrections:
  - removed the retained internal ALB / ECS ingress path from the currently promoted ready-network claim
  - pinned the active external front door as `API Gateway -> Lambda`
  - updated the resource view to the current API identity and ingress runtime posture
- Readiness-delta graph correction:
  - replaced the older green closure story with the current open `Phase 0.B` story
  - current visual verdict now shows:
    - ingress semantics clean on the truthful unsynchronized baseline
    - active blocker = repeatable steady-state proof boundary
    - `Phase 0.C` blocked until `Phase 0.B` closes on a repeatable truthful baseline
- Regenerated all three Mermaid PNG artifacts with the local Mermaid CLI so the rendered graphs match the source files.

## 2026-03-10 16:34:55 +00:00
- Tightened `Phase 0` telemetry on the real external ingress boundary:
  - updated live API Gateway stage `pd7rtjze95 / v1` to enable detailed metrics
  - created APIGW access-log group `/aws/apigateway/fraud-platform-dev-full-ig-edge-v1-access`
  - added the missing CloudWatch Logs resource policy so APIGW could write access logs
  - verified access-log delivery with a real authenticated `GET /ops/health` probe
- Patched the proving dispatcher summary so it now records:
  - fleet confirmation mode
  - measurement-alignment metadata
  - minute-bin shortfall distribution
- Ran the frozen unsynchronized `Phase 0.B` baseline three more times:
  - `phase0_20260310T161236Z`
  - `phase0_20260310T161814Z`
  - `phase0_20260310T162638Z`
- All three reruns stayed semantically clean:
  - valid-traffic `4xx = 0`
  - valid-traffic `5xx = 0`
  - clean latency
  - old minute-aligned APIGW gate still rendered them at `2999.100`, `2999.700`, and `2999.600 eps`
- New decisive evidence:
  - exact APIGW access-log windows anchored on `active_confirmed_utc` counted:
    - `362874` successful requests over `120 s` for `phase0_20260310T161814Z` (`3023.950 eps`)
    - `362957` successful requests over `120 s` for `phase0_20260310T162638Z` (`3024.642 eps`)
  - both exact windows also kept tail latency inside budget (`p95 ≈ 49.951 ms`, `p99 ≈ 58.966 / 56.996 ms`)
- Additional attribution:
  - WSP progress logs on the full-log rerun implied about `3003.85 eps` at the source side over the steady minute, so the source is not materially under-driving the boundary
- Current judgment:
  - ingress runtime is now repeatably clean
  - the truthful steady-state gate is the exact APIGW access-log window from confirmed fleet participation
  - the remaining red belongs to the older minute-aligned APIGW metric-bin proof gate
  - next narrow action is to codify that exact-window gate into the proving harness, then rerun once more and close `Phase 0.B` if consistent

## 2026-03-10 16:59:41 +00:00
- Investigated the user's DynamoDB cost concern as part of the active `Phase 0` ingress hot path.
- Live posture confirmed:
  - table `fraud-platform-dev-full-ig-idempotency`
  - `PAY_PER_REQUEST`
  - cost dominated by `EUW2-WriteRequestUnits`, not storage
  - live Lambda env `IG_RECEIPT_STORAGE_MODE = ddb_hot`
  - idempotency rows were carrying large inline `receipt_payload_json` values
- Ran a live diagnostic to test whether `object_store` receipt mode could replace the expensive hot-row write shape:
  - updated Lambda env and Terraform defaults temporarily to `object_store`
  - bounded rerun `phase0_20260310T164435Z` failed badly
  - admitted throughput collapsed to about `65.6 eps`
  - valid traffic saw `IG_PUSH_REJECTED`
  - Lambda logs showed `KAFKA_PUBLISH_TIMEOUT`
  - quarantine receipts captured `PUBLISH_AMBIGUOUS`
- Rejected that posture for `Phase 0` because it reduced DynamoDB spend by breaking the declared production shape.
- Reverted runtime and Terraform defaults back to `ddb_hot`.
- Patched `src/fraud_detection/ingestion_gate/aws_lambda_handler.py` so DynamoDB hot rows now store a compact receipt summary instead of the full inline receipt body.
- Added targeted test coverage in `tests/services/ingestion_gate/test_managed_service.py` for the compact serializer.
- Verification:
  - `python -m pytest tests/services/ingestion_gate/test_managed_service.py -q` -> passed
  - `python -m py_compile src/fraud_detection/ingestion_gate/aws_lambda_handler.py` -> passed
- Rebuilt and redeployed the live Lambda bundle, then reran the frozen bounded baseline as `phase0_20260310T165309Z`.
- Successful rerun posture after the compact-hot patch:
  - `4xx = 0`
  - `5xx = 0`
  - `p95 ≈ 52.0 ms`
  - `p99 ≈ 60.4 ms`
  - no lane collapse
  - no publish ambiguity
  - old minute-bin gate still rendered `2999.575 eps`, so the known `Phase 0.B` proof-gate blocker remains
- Live cost evidence:
  - fresh row from `platform_20260310T165309Z` shows `receipt_payload_json` reduced to `392` bytes
  - successful run `ConsumedWriteCapacityUnits` fell to roughly `488k` to `540k WRU/min`, down from the earlier successful baseline around `~720k WRU/min`
- Updated the active readiness-delta graph so the user-facing `Phase 0` picture now includes the DynamoDB write-amplification defect, the rejected `object_store` branch, and the accepted compact-`ddb_hot` remediation.
- Current judgment:
  - DynamoDB write amplification was a real production-cost defect
  - compact `ddb_hot` receipts are the accepted remediation for now
  - `object_store` receipts on Lambda remain unfit for the declared `Phase 0` envelope
  - the active `Phase 0.B` blocker is still the stale proof gate, not DynamoDB runtime failure

## 2026-03-10 17:58:54 +00:00
- Codified the APIGW-side truthful boundary into `scripts/dev_substrate/pr3_wsp_replay_dispatch.py`:
  - exact APIGW access-log window support
  - APIGW stage access-log group discovery
  - lag-guard polling before finalizing exact log counts
- Learned during that work:
  - APIGW detailed metrics expose route-level `Count` for `POST /ingest/push`
  - exact APIGW logs are route-truthful but arrive with variable lag
  - the exact count window should stay aligned to the steady-state proof window, not float directly on `active_confirmed_utc`
- Bounded `Phase 0.B` runs on the updated proof shape:
  - `phase0_20260310T170545Z` -> green under codified exact-window gate
  - `phase0_20260310T171145Z` -> red and confirmed that `warmup_seconds = 0` still leaves the proof dependent on accidental pre-window settle time
  - `phase0_20260310T171930Z` with `warmup_seconds = 60` -> near-threshold red at `2999.55 eps`
  - `phase0_20260310T172719Z` with `warmup_seconds = 60` and `target_request_rate_eps = 3010` initially appeared red because APIGW access logs were still catching up; later re-query showed the exact aligned route count had risen materially
  - `phase0_20260310T173526Z` on the same candidate shape -> green (`3011.94 eps`, `4xx = 0`, `5xx = 0`, clean latency)
  - `phase0_20260310T174232Z` repeat on the same candidate shape -> red again
- Ran one full-log diagnostic repeat on the candidate shape:
  - `phase0_20260310T175034Z`
  - one lane (`wsp_lane_06`) exited early with `IG_PUSH_REJECTED`
  - ECS marked that task `EssentialContainerExited`
  - lane emitted `0`
  - aggregate WSP progress-derived emitted count still showed fleet activity (`612877`)
  - admitted throughput collapsed to `2401.22 eps`
  - APIGW `4xx = 0`, `5xx = 0`
- Current judgment:
  - `Phase 0.B` is no longer mainly blocked by the old proof gate
  - it is no longer mainly blocked by the DynamoDB cost posture
  - the active blocker is unstable WSP-side lane behavior under the candidate steady-state proof shape, specifically observed as lane-level `IG_PUSH_REJECTED`

## 2026-03-10 18:07:03 +00:00
- Added a live implementation-note entry before continuing further work.
- Current active pivot:
  - `Phase 0.B` blocker remains WSP-side lane instability (`IG_PUSH_REJECTED`)
  - before resuming that remediation, inspect the next user-called cost accumulators: `ECS`, `MSK`, and relational database surfaces
  - also restore branch hygiene by committing and pushing the current state on `cert-platform`

## 2026-03-10 18:12:41 +00:00
- Continued the live cost pass before returning to the remaining `Phase 0.B` WSP instability.
- Confirmed the retained internal ingress ECS path is live drift relative to the current proving boundary:
  - active external boundary remains `API Gateway -> Lambda`
  - ECS service `fraud-platform-dev-full-ig-service` is still provisioned but `desiredCount = 0`
  - internal ALB `fp-dev-full-ig-svc` is still active
  - stale SSM parameter `/fraud-platform/dev_full/ig/service_url` still points to that internal ALB
  - current Terraform defaults already want `ig_service_enabled = false`
- Ran runtime Terraform planning and confirmed the desired reconciliation shape is:
  - create codified APIGW access-log resources for truthful Phase 0 telemetry
  - destroy the retained internal ingress ECS / ALB / SSM stack
- Apply did not proceed yet because the runtime workspace now requires the authoritative Lambda remote bundle triplet and the live Lambda has moved ahead of Terraform state through recent direct Phase 0 deploys.
- Narrowed Aurora / MSK posture:
  - Aurora cost is tied to live write I/O and connection churn during hardening windows, not to an obviously unused retained surface
  - MSK cost is a mix of fixed serverless cluster-hours plus traffic-driven bytes in/out
  - therefore neither should be blunted yet without better runtime attribution
- Next action pinned:
  - build and upload a fresh authoritative ingress Lambda bundle from current repo state
  - rerun runtime apply so APIGW telemetry is codified and the unused internal ingress stack is removed safely

## 2026-03-10 18:17:03 +00:00
- Built fresh authoritative ingress Lambda bundle:
  - local artifact `runs/dev_substrate/dev_full/proving_plane/deploy/phase0_ig_lambda_20260310T181500Z.zip`
  - uploaded to `s3://fraud-platform-dev-full-artifacts/artifacts/lambda/ig_handler/manual-20260310T181500Z.zip`
  - `sha256_base64 = 3DxIaUwPXkI7syppAvP03Ut+c9gOOsNw+bFA4PHmqn8=`
- Verification while preparing apply caught a local proving-harness defect:
  - `scripts/dev_substrate/pr3_wsp_replay_dispatch.py` had an invalid f-string expression in the APIGW access-log query builder
  - patched by escaping the route key before formatting the query
  - `python -m py_compile src/fraud_detection/ingestion_gate/aws_lambda_handler.py scripts/dev_substrate/pr3_wsp_replay_dispatch.py` now passes
- Runtime Terraform reconciliation:
  - first apply refreshed the live Lambda and destroyed the retained internal ingress resources
  - same apply stopped on `ResourceAlreadyExistsException` because the APIGW access-log group already existed from the earlier manual telemetry bootstrap
  - imported `/aws/apigateway/fraud-platform-dev-full-ig-edge-v1-access` into Terraform state
  - reran plan/apply successfully
- Resulting live posture after reconcile:
  - internal ALB `fp-dev-full-ig-svc` removed
  - SSM `/fraud-platform/dev_full/ig/service_url` removed
  - ingress ECS cluster now `INACTIVE`
  - APIGW stage `v1` shows:
    - `DetailedMetricsEnabled = true`
    - access-log destination `/aws/apigateway/fraud-platform-dev-full-ig-edge-v1-access`
  - live Lambda `fraud-platform-dev-full-ig-handler` now pinned to:
    - `CodeSha256 = 3DxIaUwPXkI7syppAvP03Ut+c9gOOsNw+bFA4PHmqn8=`
    - `LastModified = 2026-03-10T18:14:47.000+0000`
    - `IG_HEALTH_BUS_PROBE_MODE = none`
    - `IG_RECEIPT_STORAGE_MODE = ddb_hot`
    - reserved concurrency `600`
- Removed the older duplicate manual APIGW log resource policy and confirmed only the Terraform-managed policy remains:
  - `fraud-platform-dev-full-ig-api-access`
- Cost/remediation judgment:
  - obsolete retained ingress ECS / ALB / SSM drift is now removed
  - APIGW telemetry is now codified under Terraform
  - Aurora and MSK remain open cost investigations of active runtime behavior, not yet topology-removal candidates
- Next active blocker returns to `Phase 0.B` repeatability:
  - WSP-side lane instability
  - observed as lane-level `IG_PUSH_REJECTED`

## 2026-03-10 18:24:00 +00:00
- Started the next explicit cost-discipline pass before resuming `Phase 0.B` remediation.
- Scope of this pass: `ECS`, `MSK`, and relational-database surfaces.
- Working question: identify spend caused by retained drift, overprovisioned proving defaults, duplicated evidence paths, or hardening-side write/traffic amplification that does not materially help platform production readiness.
- Constraint kept: no cosmetic cost cuts that weaken the active `Control + Ingress` boundary or lower the declared production target.

## 2026-03-10 18:33:00 +00:00
- Live cost inspection narrowed the remaining spend shape:
  - `ECS`: only live cluster is `fraud-platform-dev-full-wsp-ephemeral`; current spend is not idle ECS drift but Fargate task runtime from WSP replay
  - `MSK`: dominant cost is serverless cluster-hours, then bytes in/out; partition-hours and storage are minor
  - `Aurora`: dominant cost is `StorageIOUsage`, then serverless ACU usage; storage footprint is minor
- Live WSP ECS task definition `fraud-platform-dev-full-wsp-ephemeral:60` confirmed current proving-task sizing is:
  - `cpu = 1024`
  - `memory = 2048`
- WSP ECS log group `/ecs/fraud-platform-dev-full-wsp-ephemeral` inspected:
  - `storedBytes = 94429389`
  - `retentionInDays = null`
- Aurora CloudWatch inspection showed:
  - `VolumeWriteIOPs` spikes into the millions during hardening windows
  - `ACUUtilization` is near-saturated during those same windows
  - `DatabaseConnections` spikes above `200`
- Engineering judgment from the live split:
  - there is no truthful MSK topology cut to make yet without harming the active substrate
  - the immediate cost-remediation candidates are proving-side:
    - WSP task right-sizing
    - WSP checkpoint pressure on Aurora
    - unbounded WSP log retention

## 2026-03-10 18:39:00 +00:00
- Applied WSP log-retention guardrail live:
  - `/ecs/fraud-platform-dev-full-wsp-ephemeral`
  - `retentionInDays = 7`
- Patched WSP config + Phase 0 dispatcher/wrapper so bounded `Phase 0` runs can override:
  - checkpoint backend
  - checkpoint root
  - checkpoint DSN
  - checkpoint flush frequency
- Current accepted `Phase 0` checkpoint posture is now:
  - `WSP_CHECKPOINT_BACKEND=file`
  - `WSP_CHECKPOINT_ROOT=/tmp/wsp-checkpoints`
  - `WSP_CHECKPOINT_FLUSH_EVERY=50000`
- Repinned local Phase 0 wrapper defaults from stale shape to current truthful baseline:
  - lane count `40`
  - stream speedup `51.2`
- Ran two cost calibrations:
  - `phase0_20260310T182532Z`
    - wrong-shape `24`-lane calibration only
    - semantically clean but far below throughput target
  - `phase0_20260310T183102Z`
    - truthful `40`-lane / `51.2x` calibration with `512 CPU / 1024 MiB` overrides and file checkpoints
    - semantically clean
    - aligned APIGW bins nearly on target (`2999.908 eps`)
    - exact APIGW access-log window materially low (`2903.800 eps`)
- Cost decision from calibration:
  - keep file-backed checkpoints and low checkpoint-flush pressure as accepted defaults for bounded `Phase 0`
  - do not keep reduced WSP task CPU/memory as the default yet because the exact-window measurement is still ambiguous at the truthful baseline

## 2026-03-10 18:24:00 +00:00
- Started the next explicit cost-discipline pass before resuming `Phase 0.B` remediation.
- Scope of this pass: `ECS`, `MSK`, and relational database surfaces.
- Working question: identify spend caused by retained drift, overprovisioned runtime defaults, duplicated evidence paths, or hardening-side write/traffic amplification that does not materially help platform production readiness.
- Constraint kept: no cosmetic cost cuts that weaken the active `Control + Ingress` proving boundary or lower the declared production target.

## 2026-03-10 18:46:00 +00:00
- Returned to direct `Phase 0.B` execution after the cost pass.
- Decision: the `30 s` settle cost-calibration runs were useful for attribution only, not for closure.
- Next run pinned to the accepted truthful baseline:
  - `40` lanes
  - `51.2x` stream speedup
  - default WSP task-definition size
  - file-backed WSP checkpoints
  - normal settle window
- Goal of next run: verify that the accepted cost fixes do not reopen `Phase 0.B` on the truthful exact-window gate.

## 2026-03-10 19:03:00 +00:00
- Ran accepted frozen `Phase 0.B` baseline after the cost pass:
  - execution `phase0_20260310T185503Z`
  - file-backed WSP checkpoints
  - `WSP_CHECKPOINT_FLUSH_EVERY = 50000`
  - default WSP task-definition size
- Result:
  - verdict `REMOTE_WSP_WINDOW_READY`
  - `open_blockers = 0`
  - exact APIGW window throughput `3018.458 eps`
  - `4xx = 0`
  - `5xx = 0`
  - `p95 = 46.997 ms`
  - `p99 = 53.947 ms`
- Judgment:
  - accepted cost fixes did not reopen `Phase 0.B`
  - `Phase 0.B` now green on the codified exact-window APIGW access-log gate
  - next active boundary becomes `Phase 0.C` bounded envelope / recovery proof

## 2026-03-10 19:07:00 +00:00
- Re-pinned the `Phase 0.C` proving method before running anything else.
- Decision:
  - do not reuse the generic recovery rollup unchanged because its continuous path still hard-blocks on `ALB`
  - keep the ingress proof surface on the real `API Gateway -> Lambda` edge
  - build/evaluate `Phase 0.C` from one continuous scheduled `WSP` campaign with exact APIGW sub-window evidence
- Reason:
  - `Phase 0.B` is already green, so the active risk is proving the wrong surface or importing later-plane requirements into `Control + Ingress`
  - the dynamic posture here is methodological correction, not a lowering of the `Phase 0.C` target
- Next work:
  - add a narrow APIGW-exact `Phase 0.C` execution/evaluation path
  - keep accepted cost posture from `Phase 0.B`
  - execute bounded burst/recovery proof once the telemetry path is truthful

## 2026-03-10 19:20:00 +00:00
- Ran first truthful `Phase 0.C` continuous APIGW campaign:
  - execution `phase0_20260310T191050Z`
  - `48` lanes
  - `600.0x` stream speedup
  - scheduled windows:
    - `90 s` steady `3000 eps`
    - `30 s` burst `6000 eps`
    - `180 s` recovery target `3000 eps`
  - accepted cheaper proving posture still used:
    - file checkpoints
    - high checkpoint flush interval
    - bounded WSP log retention
- Result:
  - run went red before the burst window became meaningful
  - steady exact APIGW window admitted only about `622.44 eps`
  - steady window carried `404` `4xx`, `2672` `5xx`
  - steady latency breached badly (`p95 ~ 416.49 ms`, `p99 ~ 11675.83 ms`)
  - Lambda metrics for the run window:
    - `Errors = 182`
    - `Throttles = 1634`
  - WSP abnormal lane tails showed repeated `http_503` then `IG_PUSH_REJECTED` / `IG_PUSH_RETRY_EXHAUSTED`
  - DLQ depth did not grow during this run
- Judgment:
  - this is not yet a truthful `6000 burst` verdict
  - the current proof shape is collapsing in the opening steady segment because the entire fleet is being released against the same campaign-start boundary on a fresh run identity
  - next adaptation is to add a non-scored presteady warmup segment and keep the scored proof on the same APIGW surface

## 2026-03-10 19:31:00 +00:00
- Ran warmup-adjusted `Phase 0.C` rerun:
  - execution `phase0_20260310T192032Z`
  - added non-scored presteady warmup:
    - `60 s` at `1500 eps`
  - kept `48` lanes / `600.0x` source posture
- Result:
  - run still went red before the scored windows became useful
  - Lambda infra stress was no longer the story:
    - `Errors = 0`
    - `Throttles = 0`
  - Lambda logs instead showed:
    - `PUBLISH_IN_FLIGHT_RETRY`
    - `PUBLISH_AMBIGUOUS_RETRY`
  - Lambda metrics/timing logs for this run showed `phase.publish_seconds` stretching into multi-second territory, in some cases near `10 s`
  - DLQ depth increased by `36`
- Judgment:
  - the active blocker is now the continuous WSP source posture overdriving the ingress publish path, not API Gateway startup shock
  - this is still not a truthful verdict on whether the platform can hold the declared `6000` burst target
  - next narrow action is to recalibrate `Phase 0.C` from the already-proven `40`-lane / `51.2x` baseline rather than from the larger `48`-lane / `600.0x` stress-oriented source posture

## 2026-03-10 19:41:00 +00:00
- Ran `Phase 0.C` recalibration from the already-proven steady-state source posture:
  - execution `phase0_20260310T193106Z`
  - `40` lanes
  - `51.2x` stream speedup
  - same presteady warmup:
    - `60 s` at `1500 eps`
- Result:
  - run still failed before the scored steady window became useful
  - APIGW exact presteady window from `19:33:30Z` to `19:34:51Z` admitted only about `352.99 eps`
  - `4xx = 411`
  - `5xx = 1367`
  - abnormal WSP lanes again ended on `IG_PUSH_REJECTED`
- Judgment:
  - the remaining common defect is the synchronized segment-edge transition itself, not just the earlier `48`-lane / `600.0x` source posture
  - next narrow adaptation is to keep the single continuous run identity but stagger lane campaign starts and begin scoring only after the last lane has entered the scored segment

## 2026-03-10 19:42:34 +00:00
- Paused the rerun loop to reclassify the active `Phase 0.C` error before spending on another attempt.
- Decision:
  - treat the current blocker as a proving-shape defect caused by synchronized segment-entry shock
  - do not lower the `6000 eps` burst target
  - do not reopen `Phase 0.B`
  - correct the method by staggering per-lane campaign starts and scoring windows only after the full fleet has entered each scored segment
- Next work:
  - finish stagger support in `pr3_wsp_replay_dispatch.py`
  - propagate the same fleet-spread accounting into `phase0_control_ingress_envelope.py`
  - validate locally, then run one more bounded `Phase 0.C` proof on the corrected posture

## 2026-03-10 19:43:08 +00:00
- Finished the staggered-start method correction for `Phase 0.C`.
- Code posture now:
  - dispatcher supports per-lane `WSP_CAMPAIGN_START_UTC` offsets via `--campaign-start-stagger-seconds`
  - envelope wrapper extends total campaign duration by fleet spread
  - steady / burst / recovery APIGW scoring windows begin only after the last lane has entered the scored segment
  - summary artifacts capture `campaign_start_stagger_seconds` and `campaign_start_spread_seconds`
- Local validation:
  - `python -m py_compile scripts/dev_substrate/pr3_wsp_replay_dispatch.py scripts/dev_substrate/phase0_control_ingress_envelope.py`
  - `python scripts/dev_substrate/phase0_control_ingress_envelope.py --dry-run`
- Dry-run evidence:
  - `48` lanes
  - `0.5 s` campaign-start stagger
  - `23.5 s` full-fleet spread
  - total campaign duration automatically expanded to keep the scored windows truthful
- Next work:
  - execute one more bounded `Phase 0.C` run on this corrected proof shape
  - inspect whether steady survives cleanly and whether burst or recovery becomes the real next blocker

## 2026-03-10 19:57:54 +00:00
- Ran corrected staggered `Phase 0.C` proof:
  - execution `phase0_20260310T194344Z`
  - generated by `phase0-envelope-staggered-lanes40-speed51p2`
  - `40` lanes
  - `51.2x` stream speedup
  - presteady `60 s @ 1500 eps`
  - `0.5 s` campaign-start stagger per lane (`19.5 s` fleet spread)
- Result:
  - dispatcher verdict green: `REMOTE_WSP_WINDOW_READY`
  - Lambda `Errors = 0`
  - Lambda `Throttles = 0`
  - DLQ delta `= 0`
  - steady window:
    - admitted `3133.26 eps`
    - `4xx = 4874`
    - `5xx = 0`
  - burst window:
    - admitted `3201.13 eps`
    - `4xx = 10478`
    - `5xx = 0`
  - recovery window:
    - admitted `3029.55 eps`
    - `4xx = 0`
    - `5xx = 0`
    - sustained green from first recovery bin
- Judgment:
  - stagger correction successfully removed the segment-entry collapse
  - `Phase 0.C` is now blocked by a narrower burst/4xx problem, not by fleet synchronization, Lambda infra failure, or DLQ growth
- Next work:
  - classify the exact `4xx` response type(s) from APIGW access logs and Lambda logs
  - determine whether burst is limited by ingress rejection, by `WSP` source realization, or by both

## 2026-03-10 20:00:10 +00:00
- Classified the remaining `Phase 0.C` `4xx` from APIGW access logs.
- Result:
  - all steady-window `4xx` were `429`
  - all burst-window `4xx` were `429`
  - `integration_status = "-"`, so they were API Gateway throttles, not Lambda semantic rejections
  - live stage `v1` posture confirmed:
    - `ThrottlingRateLimit = 3000`
    - `ThrottlingBurstLimit = 6000`
- Additional finding:
  - the current staggered scorer is still wrong
  - it starts each scored segment when the last lane enters, but ends it when the last lane leaves
  - that causes steady scoring to include early burst participation and burst scoring to include early recovery participation
  - per-second APIGW counts showed the "steady" interval staying clean near `3000 eps` until the tail where early lanes had already begun burst
- Judgment:
  - the observed burst average around `3201 eps` over `30 s` matches the APIGW token-bucket math for a `3000 rate / 6000 burst` edge, so the earlier `30 s @ 6000 eps` gate is not a truthful no-throttle proof target
- Next work:
  - patch the scorer to use full-overlap segment windows only
  - reject invalid stagger/segment combinations with no overlap
  - repin the burst proof around the live token-bucket semantics of the current ingress edge

## 2026-03-10 20:01:51 +00:00
- Applied the staggered-segment scorer correction in `phase0_control_ingress_envelope.py`.
- New scorer posture:
  - each scored segment now uses only its full-overlap window
  - invalid segment/spread combinations now fail fast if the overlap window would be `<= 0`
- Dry-run validation for the current `40`-lane / `0.5 s` stagger posture:
  - steady overlap `= 70.5 s`
  - burst overlap `= 10.5 s`
  - recovery overlap `= 160.5 s`
- Also updated the active `Phase 0` map so burst proof is explicitly tied to:
  - live APIGW `3000 rate / 6000 burst` token-bucket semantics
  - full-overlap scoring when staggered lane starts are used
- Next work:
  - checkpoint this correction in git
  - rerun `Phase 0.C` with a burst duration derived from the live edge semantics rather than the earlier arbitrary `30 s` duration

## 2026-03-10 20:03:05 +00:00
- Derived the next `Phase 0.C` burst run shape from the live APIGW token bucket instead of from the earlier arbitrary `30 s` burst assumption.
- New run-shape decision:
  - keep `40` lanes / `51.2x`
  - target a total fleet spread of `1.0 s`
  - with `40` lanes, use `campaign_start_stagger_seconds ≈ 0.025641`
  - set `burst_seconds = 3`
  - with full-overlap scoring, that yields an exact `2.0 s` all-lanes burst window
- Reason:
  - current stage posture is `3000 rate / 6000 burst`
  - a short `2 s` full-overlap burst is truthful to the live token-bucket edge
  - the resulting scored burst window lands on exact-second boundaries, improving APIGW access-log measurement honesty
- Next work:
  - run bounded `Phase 0.C` on this token-bucket-derived burst shape

## 2026-03-10 20:13:45 +00:00
- Ran short-spread token-bucket-derived `Phase 0.C` attempt:
  - execution `phase0_20260310T200312Z`
  - `40` lanes / `51.2x`
  - total fleet spread `= 1.0 s`
  - burst segment `= 3 s` on the old coupled timing model
- Result:
  - startup-collapse defect returned immediately
  - all `40` lanes ended `B18_CONTAINER_EXIT_NONZERO`
  - WSP tails showed repeated `http_503` followed by `IG_PUSH_REJECTED`
  - APIGW exact window admitted only about `260.93 eps`
  - `4xx = 227`
  - `5xx = 1126`
  - `p95 ≈ 2004.87 ms`
  - `p99 ≈ 26653.88 ms`
- Judgment:
  - small total startup spread is not safe for this fresh-run boundary
  - the deeper issue was that one timing control was still trying to handle both:
    - startup staggering
    - common steady/burst/recovery timing
- Accepted correction:
  - keep per-lane `WSP_CAMPAIGN_START_UTC` for safe startup staggering
  - add shared `WSP_RATE_PLAN_START_UTC` so all lanes change rate-plan segments on one common clock
- Next work:
  - rerun `Phase 0.C` with safe `19.5 s` startup spread preserved
  - use a shared `2 s` burst segment on the common rate-plan clock

## 2026-03-10 20:27:50 +00:00
- Ran common-rate-plan `Phase 0.C` proof:
  - execution `phase0_20260310T201418Z`
  - safe startup spread preserved:
    - `0.5 s` per lane
    - `19.5 s` total fleet spread
  - shared rate-plan clock active
  - burst segment `= 2 s`
- Result:
  - dispatcher green
  - `4xx = 0`
  - `5xx = 0`
  - Lambda `Errors = 0`
  - Lambda `Throttles = 0`
  - DLQ delta `= 0`
  - recovery green immediately and sustained
  - remaining reds:
    - steady admitted `= 2875.89 eps`
    - burst admitted `= 4672.00 eps`
- Diagnosis:
  - per-second APIGW counts showed steady ramping from about `1578 eps` up to `3000 eps` over the first ~`20 s`
  - burst per-second counts stayed around `3089` / `3086`, not `6000`
  - this is a `WSP` segment-limiter seeding defect, not an ingress-edge defect
  - each segment is entering with an almost-empty bucket because `initial_tokens` is still pinned to the old tiny fixed value (`0.25`)
- Next work:
  - seed each segment with at least its own bucket capacity
  - rerun the same bounded common-rate-plan `Phase 0.C` shape

## 2026-03-10 20:28:52 +00:00
- Applied the `WSP` segment-limiter seeding correction for `Phase 0.C`.
- New rate-plan behavior:
  - each scheduled segment now seeds with at least its own bucket capacity
  - formula used:
    - `initial_tokens >= target_eps * target_burst_seconds`
- Dry-run for the current `40`-lane / common-rate-plan / `2 s` burst shape now shows:
  - presteady lane bucket seed `= 9.375`
  - steady lane bucket seed `= 18.75`
  - burst lane bucket seed `= 37.5`
  - recovery lane bucket seed `= 18.75`
- Next work:
  - rerun the same bounded common-rate-plan `Phase 0.C` shape
  - verify whether the remaining steady and burst under-realization disappears

## 2026-03-10 20:43:30 +00:00
- Ran seeded-bucket common-rate-plan `Phase 0.C` proof:
  - execution `phase0_20260310T202917Z`
  - same safe startup spread and shared rate-plan clock
  - same `2 s` burst segment
  - per-segment limiter buckets now pre-seeded
- Result:
  - clean semantics held:
    - `4xx = 0`
    - `5xx = 0`
    - Lambda `Errors = 0`
    - Lambda `Throttles = 0`
    - DLQ delta `= 0`
  - but throughput remained red:
    - steady admitted `= 2881.91 eps`
    - burst admitted `= 4718.50 eps`
- Diagnosis:
  - `WSP` still applies oracle timestamp replay delay before the ingress limiter
  - the ingress limiter is not the only pacing surface
  - at `stream_speedup = 51.2`, replay pacing still caps source realization before the limiter can fully express the declared envelope
- Next work:
  - keep the same clean `Phase 0.C` shape
  - raise `stream_speedup` so replay pacing no longer dominates the proof

## 2026-03-10 20:59:49 +00:00
- Paused before another `Phase 0.C` rerun to repin the diagnosis rather than spend on a repeated ambiguous run.
- Working diagnosis after the speed-`100` attempt:
  - ingress edge remains semantically clean
  - scheduled limiter is no longer the active blocker
  - `WSP` is still double-pacing the proof:
    - oracle timestamp replay delay first
    - scheduled ingress limiter second
- Accepted next correction:
  - add an explicit scheduled-rate proof switch
  - when enabled with a live rate plan, bypass replay delay and let the scheduled limiter own pacing
  - keep lane staggering and the shared rate-plan clock unchanged

## 2026-03-10 21:02:16 +00:00
- Implemented the narrow `Phase 0.C` source-path correction:
  - new `WSP_DISABLE_REPLAY_DELAY_WHEN_RATE_PLAN` switch
  - active only when a scheduled rate plan is present
  - `phase0_control_ingress_envelope.py` now enables it for the bounded envelope proof
- Local validation completed before the next AWS rerun:
  - `py_compile` passed on touched Python files
  - `tests/services/world_streamer_producer/test_runner.py` passed
  - `tests/services/world_streamer_producer/test_push_retry.py` passed
- Also cleaned local test drift exposed during validation:
  - removed a stray assertion fragment from `test_runner.py`
  - repinned push-retry tests to patch the runner session object instead of raw `requests.post`

## 2026-03-10 21:18:40 +00:00
- Ran bounded `Phase 0.C` rerun with scheduled-rate replay-delay bypass wired in:
  - execution `phase0_20260310T210242Z`
  - generated by `phase0-envelope-common-rateplan-bypass-replay-burst2s`
- Run result:
  - semantics clean
  - recovery fully green
  - steady still red at `2882.96 eps`
  - burst still red at `4732.00 eps`
- Live ECS log inspection then showed the more important truth:
  - remote WSP tasks were still emitting the old scheduler log shape
  - the new replay-bypass log line never appeared
  - live task family was still `fraud-platform-dev-full-wsp-ephemeral:60`
- Reclassified the blocker:
  - stale live WSP image/task revision, not yet a truthful verdict on the new code path
- Next work:
  - build/push fresh runtime image
  - register new WSP task definition revision
  - rerun the same bounded `Phase 0.C` proof on the updated runtime

## 2026-03-10 21:35:10 +00:00
- Fresh WSP runtime image deployed:
  - built and pushed `230372904534.dkr.ecr.eu-west-2.amazonaws.com/fraud-platform-dev-full:phase0c-20260310T2119Z`
  - digest `sha256:cde0404e6042c821300d73eca0765f5e4478de16e3881a8de33c1b4fbca4f3a3`
  - registered new task definition revision `fraud-platform-dev-full-wsp-ephemeral:61`
- Reran bounded `Phase 0.C` on the fresh runtime:
  - execution `phase0_20260310T212058Z`
  - generated by `phase0-envelope-common-rateplan-bypass-replay-burst2s-r61`
- Live log truth changed as expected:
  - new `rate_plan_start_utc=...` log shape present
  - new `WSP replay delay bypass active ...` log line present
- Runtime result:
  - old under-realization no longer dominant
  - burst admitted `= 7271 eps`
  - but run went red on real overload:
    - burst `4xx`
    - steady `5xx`
    - Lambda `Errors = 39`
    - Lambda `Throttles = 12`
    - `wsp_lane_34` failed after repeated `http_503 -> IG_PUSH_REJECTED`
- Accepted next correction:
  - keep fresh runtime image and replay-bypass posture
  - smooth intra-lane emission by cutting push concurrency
  - rerun the same bounded `Phase 0.C` shape

## 2026-03-10 21:49:50 +00:00
- Ran bounded `Phase 0.C` rerun with fresh runtime image and smoothed push posture:
  - execution `phase0_20260310T213535Z`
  - generated by `phase0-envelope-common-rateplan-bypass-replay-burst2s-r61-igpush1`
  - `ig_push_concurrency = 1`
- Result:
  - dispatcher green
  - steady green at `3005.09 eps`
  - burst still red at `5044.50 eps`
  - recovery green at `3016.65 eps`
  - `4xx = 0`
  - `5xx = 0`
  - Lambda clean
  - no lane failures
- Reclassified `Phase 0.C` blocker narrowly:
  - only burst realization remains under target
- Accepted next correction:
  - keep the smoothed push posture
  - raise source token-bucket `burst_seconds`
  - rerun the same bounded `2 s` burst proof

## 2026-03-10 22:03:15 +00:00
- Ran bounded `Phase 0.C` rerun with widened source bucket window:
  - execution `phase0_20260310T215010Z`
  - generated by `phase0-envelope-common-rateplan-bypass-replay-burst2s-r61-igpush1-bucket0p5`
  - `ig_push_concurrency = 1`
  - `target_burst_seconds = 0.5`
- Result:
  - steady green at `3013.30 eps`
  - recovery green at `3022.73 eps`
  - semantics clean
  - burst worsened to `4847.50 eps`
- Accepted next correction:
  - restore `target_burst_seconds = 0.25`
  - raise `ig_push_concurrency` to `2`
  - rerun the same bounded `Phase 0.C` shape

## 2026-03-10 22:17:25 +00:00
- Ran bounded `Phase 0.C` midpoint-concurrency rerun:
  - execution `phase0_20260310T220334Z`
  - generated by `phase0-envelope-common-rateplan-bypass-replay-burst2s-r61-igpush2`
  - `ig_push_concurrency = 2`
  - `target_burst_seconds = 0.25`
- Result:
  - burst admitted `= 7010.50 eps` but with `646` burst `4xx`
  - steady fell to `2869.00 eps`
  - recovery reopened with `828` `4xx`
- Accepted next correction:
  - keep the clean `ig_push_concurrency = 1` posture
  - change source-lane geometry rather than per-lane aggression
  - rerun with higher lane count so aggregate burst is expressed through lower per-lane pressure

## 2026-03-10 22:32:27 +00:00
- Logged the first higher-lane geometry rerun as the new `Phase 0.C` baseline-to-beat:
  - execution `phase0_20260310T221746Z`
  - generated by `phase0-envelope-common-rateplan-bypass-replay-burst2s-r61-lanes48-igpush1`
  - `lane_count = 48`
  - `ig_push_concurrency = 1`
  - `target_burst_seconds = 0.25`
- Result:
  - steady green at `3038.51 eps`
  - recovery green at `3019.37 eps`
  - `4xx = 0`
  - `5xx = 0`
  - Lambda clean
  - burst `= 5965.50 eps`
  - only blocker was `BURST_THROUGHPUT_SHORTFALL:observed=5965.500:target=6000.000`
- Accepted next correction:
  - keep the clean `48`-lane / `ig_push_concurrency = 1` posture intact
  - keep replay bypass and the fresh runtime revision
  - take only a small lane-count step upward to see whether the remaining `34.5 eps` burst gap closes without reopening overload

## 2026-03-10 22:47:37 +00:00
- Ran the next narrow lane-geometry rerun from the clean `48`-lane posture:
  - execution `phase0_20260310T223332Z`
  - generated by `phase0-envelope-common-rateplan-bypass-replay-burst2s-r61-lanes50-igpush1`
  - `lane_count = 50`
  - `ig_push_concurrency = 1`
  - `target_burst_seconds = 0.25`
- Result:
  - steady green at `3025.30 eps`
  - burst green at `6019.50 eps`
  - recovery green at `3019.21 eps`
  - `4xx = 0`
  - `5xx = 0`
  - Lambda `Errors = 0`
  - Lambda `Throttles = 0`
  - DLQ delta `= 0`
  - dispatcher verdict `= REMOTE_WSP_WINDOW_READY`
- Accepted judgment:
  - the burst shortfall is closed without reopening overload
  - `Phase 0.C` is green on the truthful APIGW access-log gate
  - `Control + Ingress` can now be reaffirmed as the working-platform base once the phase doc and readiness graphs are refreshed

## 2026-03-10 22:49:51 +00:00
- Closed the remaining documentary follow-through for green `Phase 0`:
  - updated `platform.production_readiness.phase0.md` to mark `Phase 0.D` green and `Phase 0` closed
  - updated `platform.production_readiness.plan.md` so the immediate next action now points to `Phase 1 - RTDL plane readiness`
  - refreshed all three readiness graph sources and regenerated their PNGs
- Result:
  - repo authority, notes, and reflected readiness graphs now all agree with the green `phase0_20260310T223332Z` evidence
  - `Control + Ingress` remains the confirmed working-platform base

## 20:42 Wired overview graphs refreshed
- Rebuilt docs/design/platform/dev_full/graph/wired/dev_full_platform_overview_v1.mermaid.mmd as the clean current dev_full wired overview. The old source file did not exist; only a stale PNG remained.
- Rewrote docs/design/platform/dev_full/graph/wired/dev_full_platform_overview_v2.mermaid.mmd as the richer current wired overview.
- Both overviews now align to docs/design/platform/dev_full/graph/wired/dev_full_platform_network_current_v0.mermaid.mmd and current active-vs-retained posture:
  - public ingress is HTTP API Gateway -> Ingress Lambda
  - internal ALB + ECS ingress service is retained, non-primary ingress
  - RTDL active runtime is EKS; Managed Flink is retained/READY only
  - learning lane is Databricks (OFS) -> SageMaker (MF) -> MLflow (MPR) -> active bundle resolution
  - case/label and governance surfaces match current wired topology
- Re-rendered both PNGs.
