
## 02:44 Added Group 1 / Path 1 graphs for A
- Added docs/experience_lake/dev_full/a_production_wiring/assets/wired.g01.p01.run_legitimization.network.mermaid.mmd and matching PNG.
- Added docs/experience_lake/dev_full/a_production_wiring/assets/wired.g01.p01.run_legitimization.resources.mermaid.mmd and matching PNG.
- Both graphs are scoped only to Group 1 Path 1 (Run legitimization path): operator intent -> GitHub Actions -> Step Functions -> run control -> committed run header/config digest under the run evidence root.
- The network graph captures the control/authority and owned-outcome structure.
- The resource graph captures the concrete resources and identities that materially seat the same path.

## 02:47 Added Group 1 / Path 2 graphs for A
- Added docs/experience_lake/dev_full/a_production_wiring/assets/wired.g01.p02.source_realization.network.mermaid.mmd and matching PNG.
- Added docs/experience_lake/dev_full/a_production_wiring/assets/wired.g01.p02.source_realization.resources.mermaid.mmd and matching PNG.
- Both graphs are scoped only to Group 1 Path 2 (Source realization path): external producer-owned oracle world -> oracle-store basis -> managed-sort realization -> stream_view / 	ruth_view outcomes plus manifests, receipts, and parity/readability evidence.
- The network graph stays at the path-owned outcome level where the current wired graph is intentionally abstract.
- The resource graph names only the concrete seats the current wired platform materially exposes for this path.

## 02:48 Added Group 1 / Path 3 graphs for A
- Added docs/experience_lake/dev_full/a_production_wiring/assets/wired.g01.p03.ready_authorization.network.mermaid.mmd and matching PNG.
- Added docs/experience_lake/dev_full/a_production_wiring/assets/wired.g01.p03.ready_authorization.resources.mermaid.mmd and matching PNG.
- Both graphs are scoped only to Group 1 Path 3 (Ready authorization path): pinned run + source-legitimate basis -> Step Functions control closure -> READY on p.bus.control.v1 + READY receipt with Step Functions execution reference.
- Both graphs explicitly mark compute-only READY (Managed Flink / stream compute) as present but non-authoritative for this path.

## 03:00 Added Group 1 synthesis graphs for A
- Added docs/experience_lake/dev_full/a_production_wiring/assets/wired.g01.run_world_source_authority.network.mermaid.mmd and matching PNG.
- Added docs/experience_lake/dev_full/a_production_wiring/assets/wired.g01.run_world_source_authority.resources.mermaid.mmd and matching PNG.
- The synthesis stays bounded to the three real Group 1 paths only and closes the group through the three pinned closure points: `RUN_PINNED`, `ORACLE_READY`, and `READY_PUBLISHED`.
- The network graph expresses the group-level claim from the notebook directly: the platform begins from governed legitimacy.
- The resource graph names the concrete seats that materially close the same authority corridor without dragging in later-group runtime, ingress, or decisioning surfaces.

## 03:07 Added Group 2 / Path 1 graphs for A
- Added docs/experience_lake/dev_full/a_production_wiring/assets/wired.g02.p01.boundary_access.network.mermaid.mmd and matching PNG.
- Added docs/experience_lake/dev_full/a_production_wiring/assets/wired.g02.p01.boundary_access.resources.mermaid.mmd and matching PNG.
- Both graphs are scoped only to Group 2 Path 1 (Boundary access path): canonical behavioural traffic -> execute-api ingress contract -> stage `v1` -> route `POST /ingest/push` -> IG Lambda boundary.
- The network graph keeps the owned outcome deliberately narrow: traffic has reached the correct live ingress boundary and is now eligible for ingress processing.
- The resource graph names the concrete API Gateway, route, Lambda, and API-key handle surfaces and keeps the retained internal ALB present only as an explicitly excluded stale boundary alternative.

## 03:11 Added Group 2 / Path 2 graphs for A
- Added docs/experience_lake/dev_full/a_production_wiring/assets/wired.g02.p02.admission_disposition.network.mermaid.mmd and matching PNG.
- Added docs/experience_lake/dev_full/a_production_wiring/assets/wired.g02.p02.admission_disposition.resources.mermaid.mmd and matching PNG.
- Both graphs are scoped only to Group 2 Path 2 (Admission and disposition path): boundary-valid request -> IG Lambda contract application -> deterministic dedupe/admission basis -> DynamoDB-backed ingress truth plus receipts and quarantine refs.
- The network graph closes at ingress-owned truth only: admitted, duplicate-safe, rejected invalid, ambiguous/retry-governed, or quarantined.
- The resource graph keeps the concrete seats inside ingress ownership only: Lambda, DynamoDB idempotency table, DLQ, evidence/quarantine surfaces, and the IAM roles that materially seat the same truth boundary.

## 03:17 Added Group 2 / Path 3 graphs for A
- Added docs/experience_lake/dev_full/a_production_wiring/assets/wired.g02.p03.authoritative_bus_publication.network.mermaid.mmd and matching PNG.
- Added docs/experience_lake/dev_full/a_production_wiring/assets/wired.g02.p03.authoritative_bus_publication.resources.mermaid.mmd and matching PNG.
- Both graphs are scoped only to Group 2 Path 3 (Authoritative bus publication path): admitted ingress event family -> IG publish logic -> semantically split traffic and context topic family -> bus-visible handoff to RTDL consumers.
- The network graph keeps the owned outcome at the bus handoff boundary itself and does not drift into later ingest-commit evidence closure.
- The resource graph names the concrete publish seats: IG Lambda, the four pinned topics, MSK Serverless, Glue Schema Registry, the pinned continuity rule, and the downstream CSFB consumer seat.

## 03:22 Added Group 2 / Path 4 graphs for A
- Added docs/experience_lake/dev_full/a_production_wiring/assets/wired.g02.p04.ingest_commit_truth.network.mermaid.mmd and matching PNG.
- Added docs/experience_lake/dev_full/a_production_wiring/assets/wired.g02.p04.ingest_commit_truth.resources.mermaid.mmd and matching PNG.
- Both graphs are scoped only to Group 2 Path 4 (Ingest commit truth path): run-scoped ingress activity -> ingest-commit closure reads authoritative ingress evidence surfaces -> commits receipt summary, quarantine summary, offset-proof snapshot, and ingest-commit verdict.
- The network graph keeps the path on committed ingest evidence only and makes the mode-aware proof rule explicit: the current API Gateway -> Lambda -> DynamoDB edge uses admission-index proxy rather than fabricated broker offsets.
- The resource graph stays honest to the current wired seats the notebook exposes for this path: DynamoDB-backed ingress truth, IG receipts/quarantine refs, Run control, the evidence bucket, and the pinned ingest artifact set under `evidence/runs/{platform_run_id}/ingest/`.

## 03:26 Added Group 2 synthesis graphs for A
- Added docs/experience_lake/dev_full/a_production_wiring/assets/wired.g02.canonical_traffic_admission_bus_publication.network.mermaid.mmd and matching PNG.
- Added docs/experience_lake/dev_full/a_production_wiring/assets/wired.g02.canonical_traffic_admission_bus_publication.resources.mermaid.mmd and matching PNG.
- The synthesis stays bounded to the four real Group 2 paths only and closes the group through four explicit boundaries: active ingress boundary, ingress disposition truth, authoritative bus handoff, and committed ingest truth.
- The network graph expresses the notebook's group-level claim directly: the platform turns canonical behavioural traffic into governed runtime entry truth.
- The resource graph names the concrete ingress, bus, and evidence seats that materially realize the same corridor, while keeping the current-wired nuance explicit that ingest proof is mode-aware and uses admission-index proxy for the current API Gateway -> Lambda -> DynamoDB edge.

## 03:34 Added Group 3 / Path 1 graphs for A
- Added docs/experience_lake/dev_full/a_production_wiring/assets/wired.g03.p01.entity_relationship_projection.network.mermaid.mmd and matching PNG.
- Added docs/experience_lake/dev_full/a_production_wiring/assets/wired.g03.p01.entity_relationship_projection.resources.mermaid.mmd and matching PNG.
- Both graphs are scoped only to Group 3 Path 1 (Entity and relationship projection path): admitted traffic and context family -> RTDL intake/binding -> dedicated entity and relationship projection logic -> durable current graph truth.
- The network graph keeps the path bounded to current graph / entity projection truth only and makes the runtime-allowed source-family constraint explicit.
- The resource graph names the concrete seats that materially realize the same path: the four admitted topics, EKS / RTDL namespace, `fp-pr3-csfb`, `fp-pr3-ieg`, Aurora, pinned durable-write commit posture, and the retained Managed Flink parity surface as non-primary.

## 03:40 Added Group 3 / Path 2 graphs for A
- Added docs/experience_lake/dev_full/a_production_wiring/assets/wired.g03.p02.joined_context_formation.network.mermaid.mmd and matching PNG.
- Added docs/experience_lake/dev_full/a_production_wiring/assets/wired.g03.p02.joined_context_formation.resources.mermaid.mmd and matching PNG.
- Both graphs are scoped only to Group 3 Path 2 (Joined context formation path): admitted thin traffic + allowed time-safe join surfaces + current projection state -> context-store / flow-binding surface -> honest joined runtime context with readiness posture.
- The network graph keeps the path bounded to joined, time-safe context truth only and makes the pinned join map plus no-false-ready discipline explicit.
- The resource graph names the concrete seats that materially realize the same path: the four admitted topics, current projection truth as an input, the RTDL namespace, `fp-pr3-csfb`, and the downstream handoff from joined context into `fp-pr3-ieg` and `fp-pr3-ofp`.

## 03:45 Added Group 3 / Path 3 graphs for A
- Added docs/experience_lake/dev_full/a_production_wiring/assets/wired.g03.p03.online_feature_readiness.network.mermaid.mmd and matching PNG.
- Added docs/experience_lake/dev_full/a_production_wiring/assets/wired.g03.p03.online_feature_readiness.resources.mermaid.mmd and matching PNG.
- Both graphs are scoped only to Group 3 Path 3 (Online feature readiness path): joined runtime context + current projection state -> Online Feature Plane -> feature-ready runtime truth.
- The network graph keeps the path bounded to feature-ready truth only and makes availability, freshness, missing-feature posture, and requester/projector compatibility explicit parts of the boundary.
- The resource graph names the concrete seats that materially realize the same path: `fp-pr3-ofp`, its upstream context/projection inputs, Aurora-backed online feature state, the retained Redis handle seam, and the retained Managed Flink parity surface as non-primary.

## 03:51 Added Group 3 / Path 4 graphs for A
- Added docs/experience_lake/dev_full/a_production_wiring/assets/wired.g03.p04.decision_guardrail.network.mermaid.mmd and matching PNG.
- Added docs/experience_lake/dev_full/a_production_wiring/assets/wired.g03.p04.decision_guardrail.resources.mermaid.mmd and matching PNG.
- Both graphs are scoped only to Group 3 Path 4 (Decision guardrail path): upstream RTDL context, feature, projection, and required-signal posture -> Degrade Ladder -> adjudicated decision posture for downstream decision formation.
- The network graph keeps the path bounded to decision posture truth only and makes the semantic classification boundary explicit: advisory vs insufficiency vs fail-closed.
- The resource graph names the concrete seats that materially realize the same path: `fp-pr3-dl`, its upstream RTDL input surfaces, the live telemetry basis that feeds it, and the handoff of adjudication input into `fp-pr3-df`.

## 03:58 Added Group 3 / Path 5 graphs for A
- Added docs/experience_lake/dev_full/a_production_wiring/assets/wired.g03.p05.decision_formation.network.mermaid.mmd and matching PNG.
- Added docs/experience_lake/dev_full/a_production_wiring/assets/wired.g03.p05.decision_formation.resources.mermaid.mmd and matching PNG.
- Both graphs are scoped only to Group 3 Path 5 (Decision formation path): joined context + feature-ready truth + adjudicated guardrail posture + runtime-consumed active bundle/policy authority -> Decision Fabric -> explicit decision truth.
- The network graph keeps the path bounded to decision truth itself and makes decision identity, provenance, explanation, and governed bundle/policy consumption explicit boundary content.
- The resource graph names the concrete seats that materially realize the same path: `fp-pr3-df`, its upstream RTDL inputs, the runtime-consumed active-bundle resolution surface, and the immediate decision publication seats that carry the resulting decision truth onward without collapsing this path into action or audit ownership.

## 04:05 Added Group 3 / Path 6 graphs for A
- Added docs/experience_lake/dev_full/a_production_wiring/assets/wired.g03.p06.action_outcome_emission.network.mermaid.mmd and matching PNG.
- Added docs/experience_lake/dev_full/a_production_wiring/assets/wired.g03.p06.action_outcome_emission.resources.mermaid.mmd and matching PNG.
- Both graphs are scoped only to Group 3 Path 6 (Action and outcome emission path): decision truth -> Action Layer -> deterministic, duplicate-safe, attributable action/outcome truth.
- The network graph keeps the path bounded to owned action and outcome truth only, with ambiguity explicitly recorded rather than leaked, and treats later append-only audit ownership as downstream.
- The resource graph stays honest to the current wired seats: `fp-pr3-al` is the owned action surface, while the only named immediate publication seat currently exposed from it in the wired graph is `fp.bus.audit.v1`; that downstream publication is shown without collapsing this path into Group 4 audit ownership.

## 04:10 Added Group 3 synthesis graphs for A
- Added docs/experience_lake/dev_full/a_production_wiring/assets/wired.g03.runtime_context_decisioning.network.mermaid.mmd and matching PNG.
- Added docs/experience_lake/dev_full/a_production_wiring/assets/wired.g03.runtime_context_decisioning.resources.mermaid.mmd and matching PNG.
- The synthesis stays bounded to the six real Group 3 paths only and closes the group through six explicit runtime truths: projection, joined context, feature readiness, guardrail posture, decision truth, and action/outcome truth.
- The network graph expresses the notebook's group-level claim directly: the platform turns admitted traffic into governed real-time decision truth through a bounded sequence of owned runtime boundaries.
- The resource graph names the concrete RTDL seats that materially realize the same chain and keeps the cross-plane nuance explicit that active bundle / policy authority is consumed inside Group 3 but owned upstream by the learning / registry group.

## 04:16 Added Group 4 / Path 1 graphs for A
- Added docs/experience_lake/dev_full/a_production_wiring/assets/wired.g04.p01.audit_append.network.mermaid.mmd and matching PNG.
- Added docs/experience_lake/dev_full/a_production_wiring/assets/wired.g04.p01.audit_append.resources.mermaid.mmd and matching PNG.
- Both graphs are scoped only to Group 4 Path 1 (Audit append path): decision truth + action/outcome truth -> Decision Log and Audit surface -> authoritative append-only audit and lineage truth.
- The network graph keeps the path bounded to append-only audit truth itself and leaves audit-topic publication explicitly as the next downstream boundary.
- The resource graph names the concrete seats that materially realize the same path: `fp-pr3-dla` as a live RTDL workload, its decision and action inputs, and the current-wired audit topic shown only as the later handoff seat rather than the owned closure of this path.

## 04:21 Added Group 4 / Path 2 graphs for A
- Added docs/experience_lake/dev_full/a_production_wiring/assets/wired.g04.p02.audit_publication_durable_reference.network.mermaid.mmd and matching PNG.
- Added docs/experience_lake/dev_full/a_production_wiring/assets/wired.g04.p02.audit_publication_durable_reference.resources.mermaid.mmd and matching PNG.
- Both graphs are scoped only to Group 4 Path 2 (Audit publication and durable reference path): append-only audit truth -> audit-topic handoff -> downstream operational/evidence consumers -> durable by-reference audit surfaces.
- The network graph keeps the path bounded to audit publication plus durable references and leaves immutable archive preservation explicitly for the next path.
- The resource graph names the concrete seats that materially realize the same boundary: `fp.bus.audit.v1`, MSK transport, the downstream case-trigger consumer seat, and the evidence/object-store substrates that carry the durable by-reference surfaces.

## 2026-03-18 03:05:41 +00:00 - Drew Group 4 / Path 3 graphs: immutable archive preservation

- Added network graph wired.g04.p03.immutable_archive_preservation.network.mermaid.mmd and render.
- Added resource graph wired.g04.p03.immutable_archive_preservation.resources.mermaid.mmd and render.
- Kept the current adjudicated posture explicit: ECS/Kubernetes archive-writer consumer route to object-store archive prefix with object-path-plus-payload-readback continuity, rather than drifting into the target managed-connector wording.


## 2026-03-18 03:06:29 +00:00 - Drew Group 4 / Path 4 graphs: archive closure and replay-reference

- Added network graph wired.g04.p04.archive_closure_replay_reference.network.mermaid.mmd and render.
- Added resource graph wired.g04.p04.archive_closure_replay_reference.resources.mermaid.mmd and render.
- Kept the closure distinction explicit: preserved archive objects are not enough; the current wired posture still requires archive-writer closure evidence and an archive closure marker before later lanes can trust replay references.


## 2026-03-18 03:07:17 +00:00 - Drew Group 4 synthesis graphs: durable runtime audit and archive truth

- Added network graph wired.g04.audit_archive_replay_truth.network.mermaid.mmd and render.
- Added resource graph wired.g04.audit_archive_replay_truth.resources.mermaid.mmd and render.
- Kept the four owned outcomes distinct in the synthesis: append-only audit truth, durable audit refs, immutable archive truth, and replay-referenceable archive closure.


## 2026-03-18 03:08:02 +00:00 - Drew Group 5 / Path 1 graphs: case-intent escalation

- Added network graph wired.g05.p01.case_intent_escalation.network.mermaid.mmd and render.
- Added resource graph wired.g05.p01.case_intent_escalation.resources.mermaid.mmd and render.
- Kept the boundary narrow: RTDL decision/audit outputs become explicit case-intent truth at the case-trigger boundary, then hand off through p.bus.case.triggers.v1; this path does not yet create case records or label truth.


## 2026-03-18 03:08:42 +00:00 - Drew Group 5 / Path 2 graphs: case creation and timeline append

- Added network graph wired.g05.p02.case_creation_timeline_append.network.mermaid.mmd and render.
- Added resource graph wired.g05.p02.case_creation_timeline_append.resources.mermaid.mmd and render.
- Kept the boundary narrow: case-intent truth becomes deterministic case identity and append-only operational case history at the case-management boundary; this path does not yet hand supervision into label truth.


## 2026-03-18 03:09:22 +00:00 - Drew Group 5 / Path 3 graphs: case-to-label handoff

- Added network graph wired.g05.p03.case_to_label_handoff.network.mermaid.mmd and render.
- Added resource graph wired.g05.p03.case_to_label_handoff.resources.mermaid.mmd and render.
- Kept the ownership split explicit: operational case truth stays with case management, while this path closes only at label-request / label-pending truth on the handoff into label store; authoritative label commit remains the next path.


## 2026-03-18 03:10:03 +00:00 - Drew Group 5 / Path 4 graphs: authoritative label commit and visibility

- Added network graph wired.g05.p04.authoritative_label_commit_visibility.network.mermaid.mmd and render.
- Added resource graph wired.g05.p04.authoritative_label_commit_visibility.resources.mermaid.mmd and render.
- Kept the boundary narrow: label-store turns supervision requests into append-only authoritative label truth with readback, as-of, and maturity visibility, then hands off through p.bus.labels.events.v1; learning-input basis remains out of scope for this path.


## 2026-03-18 03:10:50 +00:00 - Drew Group 5 synthesis graphs: case and label operational truth

- Added network graph wired.g05.case_label_operational_truth.network.mermaid.mmd and render.
- Added resource graph wired.g05.case_label_operational_truth.resources.mermaid.mmd and render.
- Kept the four owned outcomes distinct in the synthesis: case-intent truth, operational case truth, supervision-request truth, and authoritative label truth.


## 2026-03-18 03:11:35 +00:00 - Drew Group 6 / Path 1 graphs: learning-input basis

- Added network graph wired.g06.p01.learning_input_basis.network.mermaid.mmd and render.
- Added resource graph wired.g06.p01.learning_input_basis.resources.mermaid.mmd and render.
- Kept the path narrow: archive truth, authoritative label truth, and replay references are turned into a pinned causal learning-input basis through replay-basis, as-of/maturity, and anti-leakage closure before any offline dataset build begins.


## 2026-03-18 03:12:16 +00:00 - Drew Group 6 / Path 2 graphs: offline dataset commitment

- Added network graph wired.g06.p02.offline_dataset_commitment.network.mermaid.mmd and render.
- Added resource graph wired.g06.p02.offline_dataset_commitment.resources.mermaid.mmd and render.
- Kept the path narrow: pinned learning-input basis becomes committed offline dataset truth through the OFS build boundary, with manifest, fingerprint, rollback recipe, and leakage audit closure; train/eval remains the next path.


## 2026-03-18 03:12:59 +00:00 - Drew Group 6 / Path 3 graphs: train/eval and candidate-bundle

- Added network graph wired.g06.p03.train_eval_candidate_bundle.network.mermaid.mmd and render.
- Added resource graph wired.g06.p03.train_eval_candidate_bundle.resources.mermaid.mmd and render.
- Kept the path narrow: authoritative offline dataset truth becomes provenance-complete, operable candidate-bundle truth through managed train/eval, MLflow lineage closure, and safe-disable / rollback closure before promotion begins.


## 2026-03-18 03:13:41 +00:00 - Drew Group 6 / Path 4 graphs: promotion and rollback-governance

- Added network graph wired.g06.p04.promotion_rollback_governance.network.mermaid.mmd and render.
- Added resource graph wired.g06.p04.promotion_rollback_governance.resources.mermaid.mmd and render.
- Kept the path narrow: eligible candidate bundle becomes explicit promotion truth with rollback drill evidence, bounded-restore discipline, and active-resolution / compatibility checks before runtime authority feedback begins.


## 2026-03-18 03:14:26 +00:00 - Drew Group 6 / Path 5 graphs: active-bundle authority feedback

- Added network graph wired.g06.p05.active_bundle_authority_feedback.network.mermaid.mmd and render.
- Added resource graph wired.g06.p05.active_bundle_authority_feedback.resources.mermaid.mmd and render.
- Kept the path narrow: governed promoted state becomes deterministic runtime decision authority through the active-resolution corridor, and only then is that authority read by the decision fabric; decision formation itself remains outside this path.


## 2026-03-18 03:15:20 +00:00 - Drew Group 6 synthesis graphs: learning, evaluation, and governed activation

- Added network graph wired.g06.learning_evaluation_activation.network.mermaid.mmd and render.
- Added resource graph wired.g06.learning_evaluation_activation.resources.mermaid.mmd and render.
- Kept the five owned outcomes distinct in the synthesis: learning-input basis, offline dataset truth, candidate-bundle truth, governed promotion truth, and runtime decision authority.


## 2026-03-18 03:16:08 +00:00 - Drew Group 7 / Path 1 graphs: run reconstruction and receipt closure

- Added network graph wired.g07.p01.run_reconstruction_receipt_closure.network.mermaid.mmd and render.
- Added resource graph wired.g07.p01.run_reconstruction_receipt_closure.resources.mermaid.mmd and render.
- Kept the path narrow: earlier lane receipts and readable closure evidence are turned into an exact run story and reconciliation truth through the reporter corridor before governance append and final verdict layers begin.


## 2026-03-18 03:16:51 +00:00 - Drew Group 7 / Path 2 graphs: governance append and close-marker

- Added network graph wired.g07.p02.governance_append_close_marker.network.mermaid.mmd and render.
- Added resource graph wired.g07.p02.governance_append_close_marker.resources.mermaid.mmd and render.
- Kept the path narrow: reconstructed run truth becomes append-safe governance truth and formal close-marker under run scope, with schema/run-scope/order correctness explicit; final verdict publication remains the next path.


## 2026-03-18 03:17:31 +00:00 - Drew Group 7 / Path 3 graphs: proof-matrix and final-verdict publication

- Added network graph wired.g07.p03.proof_matrix_final_verdict.network.mermaid.mmd and render.
- Added resource graph wired.g07.p03.proof_matrix_final_verdict.resources.mermaid.mmd and render.
- Kept the path narrow: governed closure truth becomes one explicit whole-platform verdict bundle through full-source and six-proof completeness checks, with readable run-scoped publication and readback identity; observability attestation remains the next path.


## 2026-03-18 03:18:13 +00:00 - Drew Group 7 / Path 4 graphs: drift-visible observability attestation

- Added network graph wired.g07.p04.drift_visible_observability_attestation.network.mermaid.mmd and render.
- Added resource graph wired.g07.p04.drift_visible_observability_attestation.resources.mermaid.mmd and render.
- Kept the path narrow: declared active runtime path and live telemetry from those same surfaces are turned into observability attestation truth through freshness, alert-coverage, correlation, handle-resolution, and drift checks before teardown begins.


## 2026-03-18 03:18:56 +00:00 - Drew Group 7 / Path 5 graphs: idle-safe teardown and residual-readability

- Added network graph wired.g07.p05.idle_safe_teardown_residual_readability.network.mermaid.mmd and render.
- Added resource graph wired.g07.p05.idle_safe_teardown_residual_readability.resources.mermaid.mmd and render.
- Kept the path narrow: full-platform closure becomes safe idle posture through teardown execution, residual verification, and post-teardown evidence readability; cost-to-outcome closure remains the next path.


## 2026-03-18 03:19:45 +00:00 - Drew Group 7 / Path 6 graphs: cost guardrail and cost-to-outcome closure

- Added network graph wired.g07.p06.cost_guardrail_cost_to_outcome.network.mermaid.mmd and render.
- Added resource graph wired.g07.p06.cost_guardrail_cost_to_outcome.resources.mermaid.mmd and render.
- Kept the path narrow: safe-idle post-run posture becomes attributable spend truth through cost-guardrail capture, budget-envelope interpretation, and cost-to-outcome receipt closure, with unattributed spend and missing proof outcome treated as fail-closed concerns.


## 2026-03-18 03:20:32 +00:00 - Drew Group 7 synthesis graphs: run governance and observability closure

- Added network graph wired.g07.run_governance_observability_closure.network.mermaid.mmd and render.
- Added resource graph wired.g07.run_governance_observability_closure.resources.mermaid.mmd and render.
- Kept the six owned outcomes distinct in the synthesis: run reconstruction truth, governance close truth, verdict truth, observability attestation truth, idle-safe truth, and cost-to-outcome truth.


## 2026-03-18 03:28:14 +00:00 - Validated full A graph inventory and renderability

- Checked the expected full A graph set under docs/experience_lake/dev_full/a_production_wiring/assets/ against the path and group inventory.
- Confirmed 39 graph sets present, 78 Mermaid source files present, and no missing PNG renders.
- Re-rendered the full set in sequential batches with mmdc to confirm Mermaid syntax and renderability across all path and group network/resource graphs.
- Result: no missing graph files, no render failures, no graph-level fixes required.


## 2026-03-18 06:45:42 +00:00 - Reorganized A graph set into group folders

- Moved all Group 1..7 path and synthesis graph files under dedicated subgroup folders in docs/experience_lake/dev_full/a_production_wiring/assets/.
- Left the two full-platform wired graphs at the asset root: wired.platform_network_current.* and wired.platform_resources_current.*.
- Verified grouped inventory counts after the move:
  - g01 = 8 mmd / 8 png
  - g02 = 10 mmd / 10 png
  - g03 = 14 mmd / 14 png
  - g04 = 10 mmd / 10 png
  - g05 = 10 mmd / 10 png
  - g06 = 12 mmd / 12 png
  - g07 = 14 mmd / 14 png
- Re-rendered sample graphs from new subgroup paths to confirm the reorganization did not break Mermaid renderability.


## 2026-03-18 10:04:15 +00:00 - Drew first interrogation-derived reasoning graph for A: Group 1 / Path 1

- Added wired.g01.p01.run_legitimization.reasoning.mermaid.mmd and render.
- Kept it derived from the interrogation entry rather than the topology: explicit entry basis, control-boundary logic, design pressure, owned closure, and what the path proves for A.
- This establishes the graph family for path-level interrogation summaries if we continue it across the rest of A.


## 2026-03-18 10:17:20 +00:00 - Rebuilt Group 1 / Path 1 reasoning graph from interrogation requirements

Replaced the first rejected reasoning graph for wired.g01.p01.run_legitimization.reasoning with an interrogation-response flow that follows the per-path interrogation contract directly: requirement/question -> derived notebook answer -> extracted A-claim. Kept the content anchored to the existing Run legitimization path notebook entry instead of summarizing it into generic system-design boxes.


## 2026-03-18 10:30:32 +00:00 - Expanded Group 1 / Path 1 reasoning graph into a full path-design chart

Reworked wired.g01.p01.run_legitimization.reasoning from a compressed interrogation-response graph into a dense system-design chart that preserves the full path interrogation structure for Run legitimization: objective, entry, owned closure, carried objects, route logic, logical reading, concrete seating, rationale, external contract pressure, trade-offs, necessity, and extracted A claims.


## 2026-03-18 10:32:55 +00:00 - Restyled Group 1 / Path 1 reasoning graph toward readiness-delta presentation

Rebuilt wired.g01.p01.run_legitimization.reasoning again after the prior chart still carried the interrogation-flow visual language. Kept the content as full path-design reasoning for Run legitimization, but changed the presentation to the readiness-delta-style left-to-right structure: path pressure -> current design response -> carried/seated/contract basis -> owned closure -> A-supporting claims.


## 2026-03-18 10:38:00 +00:00 - Rebuilt Group 1 / Path 1 reasoning graph around the path spine

Reworked wired.g01.p01.run_legitimization.reasoning again so the chart is now organized around the actual Run legitimization route spine: entry basis -> control-plane entry -> run identity lock -> config digest commit -> durable evidence seating -> committed run basis. The surrounding system-design reasoning now hangs off that spine: carried basis, logical reading, concrete seating, rationale, external contract pressure, trade-offs, necessity, and the A claims earned by the path.


## 2026-03-18 10:41:46 +00:00 - Tightened Group 1 / Path 1 reasoning graph before standardizing the pattern

Adjusted wired.g01.p01.run_legitimization.reasoning to make the owned outcome explicit as committed run basis and to restore the exact necessity close from the notebook: this is the kind of weakness A is meant to expose and reject. This keeps the reasoning chart closer to the path entry before using it as the pattern for the remaining paths.


## 2026-03-18 10:49:17 +00:00 - Added notebook section numbering to Group 1 / Path 1 reasoning graph

Updated wired.g01.p01.run_legitimization.reasoning so the subgraph headers now carry the notebook section numbers. This makes the reasoning chart easier to compare directly against the Run legitimization path interrogation entry while preserving the path-spine layout.


## 2026-03-18 10:51:29 +00:00 - Added Group 1 / Path 2 reasoning graph

Created wired.g01.p02.source_realization.reasoning as a path-spine reasoning chart derived from the Source realization path interrogation entry. The chart is organized around the actual realization route: external oracle world -> raw upload -> managed sort -> realized source surfaces -> canonical oracle basis, with the carried basis, logical reading, concrete seating, rationale, contract pressure, trade-offs, necessity, and A claims attached to that spine.


## 2026-03-18 10:53:55 +00:00 - Added Group 1 / Path 3 reasoning graph

Created wired.g01.p03.ready_authorization.reasoning as the path-spine reasoning chart for Ready authorization path. The chart is centered on READY authorization as a control-plane closure route: prerequisites satisfied -> READY on control bus -> Step Functions READY commit authority -> durable READY receipt -> authoritatively ready run, with the carried controls, logical reading, concrete seating, rationale, governance pressure, trade-offs, necessity, and A claims attached to that spine.


## 2026-03-18 10:54:58 +00:00 - Added Group 2 / Path 1 reasoning graph

Created wired.g02.p01.boundary_access.reasoning as the path-spine reasoning chart for Boundary access path. The chart is centered on the ingress-boundary route: canonical behavioural traffic -> execute-api ingress URL -> stage v1 -> POST /ingest/push -> IG Lambda boundary, with the traffic-entry rules, route contract, concrete seating, rationale, platform/engine boundary law, trade-offs, necessity, and A claims attached to that spine.


## 2026-03-18 10:56:02 +00:00 - Added Group 2 / Path 2 reasoning graph

Created wired.g02.p02.admission_disposition.reasoning as the path-spine reasoning chart for Admission and disposition path. The chart is centered on the ingress-truth route: boundary-accepted request -> IG contract application -> canonical dedupe basis -> authoritative idempotency/admission write -> truthful disposition, with the carried identities, logic of ingress-owned truth, concrete API Gateway/Lambda/DynamoDB seating, rationale for the Lambda-side persistence fix, contract pressure, trade-offs, necessity, and A claims attached to that spine.


## 2026-03-18 10:57:04 +00:00 - Added Group 2 / Path 3 reasoning graph

Created wired.g02.p03.authoritative_bus_publication.reasoning as the path-spine reasoning chart for Authoritative bus publication path. The chart is centered on the runtime handoff route: admitted ingress event family -> IG publish logic -> pinned traffic/context topics on MSK -> bus-visible RTDL handoff -> authoritative bus truth, with the carried topic identity, logical separation of admit truth from publish truth, concrete MSK/topic/consumer seating, rationale for the split topic family, continuity/fail-closed contract pressure, trade-offs, necessity, and A claims attached to that spine.


## 2026-03-18 10:58:06 +00:00 - Added Group 2 / Path 4 reasoning graph

Created wired.g02.p04.ingest_commit_truth.reasoning as the path-spine reasoning chart for Ingest commit truth path. The chart is centered on the ingest-evidence closure route: run-scoped ingress activity -> managed ingest-commit lane -> receipt/quarantine/offset-proof artifacts -> ingest-commit verdict -> durable ingest truth, with the artifact set, evidence-closure logic, concrete S3/run-root seating, rationale for mode-aware offset proof, replay-basis contract pressure, trade-offs, necessity, and A claims attached to that spine.


## 2026-03-18 10:59:05 +00:00 - Added Group 3 / Path 1 reasoning graph

Created wired.g03.p01.entity_relationship_projection.reasoning as the path-spine reasoning chart for Entity and relationship projection path. The chart is centered on the RTDL projection route: admitted traffic/context topics -> RTDL runtime -> entity/graph update logic -> current graph/projection state -> downstream-readable projection truth, with the carried identity/relationship basis, logical reading of entity truth as an owned runtime surface, concrete RTDL/Aurora seating, rationale for making projection first-class, contract pressure, trade-offs, necessity, and A claims attached to that spine.


## 2026-03-18 11:00:05 +00:00 - Added Group 3 / Path 2 reasoning graph

Created wired.g03.p02.joined_context_formation.reasoning as the path-spine reasoning chart for Joined context formation path. The chart is centered on the context-join route: admitted event family -> consume traffic plus allowed context surfaces -> bind through the pinned join map and current projection state -> expose joined runtime context -> readiness truth, with the carried join objects, logical reading of context as constructed runtime truth, concrete RTDL context-store seating, rationale for thin-traffic/in-platform-join posture, contract pressure, trade-offs, necessity, and A claims attached to that spine.


## 2026-03-18 11:01:13 +00:00 - Added Group 3 / Path 3 reasoning graph

Created wired.g03.p03.online_feature_readiness.reasoning as the path-spine reasoning chart for Online feature readiness path. The chart is centered on the feature-state route: joined runtime context + current projection state -> online feature materialization / serve surface -> feature-ready truth -> downstream decision use, with the carried key and requester/projector contracts, logical reading of feature truth as distinct from context truth, concrete RTDL/Aurora/Redis seating, rationale for explicit feature state, runtime-separation contract pressure, trade-offs, necessity, and A claims attached to that spine.


## 2026-03-18 11:02:10 +00:00 - Added Group 3 / Path 4 reasoning graph

Created wired.g03.p04.decision_guardrail.reasoning as the path-spine reasoning chart for Decision guardrail path. The chart is centered on the guardrail route: current RTDL truth surfaces -> guardrail classification logic -> decision posture truth -> downstream decision formation, with the carried typed dependency signals, logical reading of degrade judgment as first-class runtime truth, concrete degrade-ladder seating, rationale for advisory-vs-insufficiency separation, RTDL contract pressure, trade-offs, necessity, and A claims attached to that spine.


## 2026-03-18 11:03:08 +00:00 - Added Group 3 / Path 5 reasoning graph

Created wired.g03.p05.decision_formation.reasoning as the path-spine reasoning chart for Decision formation path. The chart is centered on the decision-fabric route: joined context + online features + guardrail posture + active bundle/policy resolution -> decision fabric -> decision truth -> downstream action/audit lanes, with the carried provenance/explainability basis, logical reading of decision truth as its own owned surface, concrete decision-fabric seating, rationale for explicit decision truth, contract pressure, trade-offs, necessity, and A claims attached to that spine.


## 2026-03-18 11:04:06 +00:00 - Added Group 3 / Path 6 reasoning graph

Created wired.g03.p06.action_outcome_emission.reasoning as the path-spine reasoning chart for Action and outcome emission path. The chart is centered on the decision-to-effect route: decision truth -> action layer -> committed/published outcome surfaces -> downstream RTDL and cross-plane consumers, with the carried decision/provenance basis, logical reading of side effects as their own truth boundary, concrete action-layer seating, rationale for separating action truth from decision truth, contract pressure, trade-offs, necessity, and A claims attached to that spine.


## 2026-03-18 11:05:17 +00:00 - Added Group 4 / Path 1 reasoning graph

Created wired.g04.p01.audit_append.reasoning as the path-spine reasoning chart for Audit append path. The chart is centered on the append-only audit route: decision truth + action/outcome truth -> audit append writer -> append-only audit and lineage truth -> later publication/archive consumers, with the carried lineage/provenance basis, logical reading of audit as first-class append-only truth, concrete RTDL audit-surface seating, rationale for keeping audit closure separate, contract pressure, trade-offs, necessity, and A claims attached to that spine.


## 2026-03-18 11:06:14 +00:00 - Added Group 4 / Path 2 reasoning graph

Created wired.g04.p02.audit_publication_durable_reference.reasoning as the path-spine reasoning chart for Audit publication and durable reference path. The chart is centered on the audit-handoff route: append-only audit truth -> audit-topic handoff -> downstream operational/evidence consumers -> durable audit refs and evidence surfaces, with the carried provenance/reference structure, logical reading of consumable audit truth as distinct from append truth, concrete topic/object-store seating, rationale for explicit downstream audit handoff, contract pressure, trade-offs, necessity, and A claims attached to that spine.


## 2026-03-18 11:07:15 +00:00 - Added Group 4 / Path 3 reasoning graph

Created wired.g04.p03.immutable_archive_preservation.reasoning as the path-spine reasoning chart for Immutable archive preservation path. The chart is centered on the archive-preservation route: runtime/probe source records on MSK -> archive-consumer runtime -> object-storage archive prefix -> run-scoped archive objects with payload/readback continuity proof -> immutable archive history, with the carried continuity/proof basis, logical separation of archive history from audit append and replay-ready closure, concrete archive-writer/object-store seating, rationale for the adjudicated ECS batch-consumer route, contract pressure, trade-offs, necessity, and A claims attached to that spine.


## 2026-03-18 11:08:15 +00:00 - Added Group 4 / Path 4 reasoning graph

Created wired.g04.p04.archive_closure_replay_reference.reasoning as the path-spine reasoning chart for Archive closure and replay-reference path. The chart is centered on the closure route: run-scoped archive objects + continuity proof -> archive closure marker -> replay-reference resolution from spine closure -> downstream learning-input trust, with the carried closure/replay semantics, logical separation of archive existence from replay-referenceable archive truth, concrete archive-prefix/connector seating, rationale for explicit closure gating, contract pressure, trade-offs, necessity, and A claims attached to that spine.


## 2026-03-18 11:09:39 +00:00 - Added Group 5 / Path 1 reasoning graph

Created wired.g05.p01.case_intent_escalation.reasoning as the path-spine reasoning chart for Case-intent escalation path. The chart is centered on the escalation route: RTDL decision/audit outputs -> case-trigger selection and suppression -> case-intent truth -> case-trigger topic -> case-management intake, with the carried operational continuity basis, logical reading of trigger selection as its own truth boundary, concrete case-trigger/topic seating, rationale for explicit escalation ownership, contract pressure, trade-offs, necessity, and A claims attached to that spine.


## 2026-03-18 11:10:40 +00:00 - Added Group 5 / Path 2 reasoning graph

Created wired.g05.p02.case_creation_timeline_append.reasoning as the path-spine reasoning chart for Case creation and timeline append path. The chart is centered on the case-creation route: case-intent truth -> case-management intake -> deterministic case creation -> append-only timeline writes -> operational case truth, with the carried case identity/timeline basis, logical reading of operational case truth as its own append-only system, concrete case-management/Aurora seating, rationale for deterministic case ownership, contract pressure, trade-offs, necessity, and A claims attached to that spine.

## 2026-03-18
- 16:40 GMT: Confirmed and rerendered the Group 5 / Path 3 reasoning graph for `Case-to-label handoff` under `docs/experience_lake/dev_full/a_production_wiring/assets/g05_case_label_operational_truth/`. The graph keeps the accepted numbered path-spine structure and centers the path on the explicit case-truth -> label-request handoff boundary, including the corrected timing basis on the case-management handshake write attempt.
- 16:46 GMT: Added the Group 5 / Path 4 reasoning graph for `Authoritative label commit and visibility path` under `docs/experience_lake/dev_full/a_production_wiring/assets/g05_case_label_operational_truth/`. Kept the accepted numbered path-spine pattern and centered the path on the label-store commit boundary, append-only authoritative label truth, temporal visibility (`as-of` / `maturity`), and downstream label-events handoff.
- 16:52 GMT: Added the Group 6 / Path 1 reasoning graph for `Learning-input basis path` under `docs/experience_lake/dev_full/a_production_wiring/assets/g06_learning_evaluation_activation/`. Kept the accepted numbered path-spine pattern and centered the path on replay-basis pinning, as-of and maturity pinning, leakage and runtime-learning separation checks, and committed learning-input readiness basis.
- 16:56 GMT: Added the Group 6 / Path 2 reasoning graph for `Offline dataset commitment path` under `docs/experience_lake/dev_full/a_production_wiring/assets/g06_learning_evaluation_activation/`. Kept the accepted numbered path-spine pattern and centered the path on input binding and immutability checks, Databricks dataset build, quality-gate adjudication, Iceberg/Glue commit verification, manifest/fingerprint publication, and rollback closure.
- 17:02 GMT: Added the Group 6 / Path 3 reasoning graph for `Train / eval and candidate-bundle path` under `docs/experience_lake/dev_full/a_production_wiring/assets/g06_learning_evaluation_activation/`. Kept the accepted numbered path-spine pattern and centered the path on immutable dataset binding, SageMaker train/eval, evaluation gates, MLflow lineage closure, candidate-bundle publication, safe-disable/rollback closure, and deterministic train-and-eval verdict.
- 17:08 GMT: Added the Group 6 / Path 4 reasoning graph for `Promotion and rollback-governance path` under `docs/experience_lake/dev_full/a_production_wiring/assets/g06_learning_evaluation_activation/`. Kept the accepted numbered path-spine pattern and centered the path on the governed promotion corridor, explicit promotion receipt, rollback drill and bounded-restore evidence, active-resolution and compatibility checks, and committed promotion truth.
- 17:12 GMT: Added the Group 6 / Path 5 reasoning graph for `Active-bundle authority feedback path` under `docs/experience_lake/dev_full/a_production_wiring/assets/g06_learning_evaluation_activation/`. Kept the accepted numbered path-spine pattern and centered the path on promoted registry truth, active-resolution logic, deterministic active bundle/policy resolution, runtime-readable authority surface, and later runtime decision consumption.
- 17:18 GMT: Added the Group 7 / Path 1 reasoning graph for `Run reconstruction and receipt closure path` under `docs/experience_lake/dev_full/a_production_wiring/assets/g07_run_governance_observability_closure/`. Kept the accepted numbered path-spine pattern and centered the path on earlier lane receipts and closure artifacts, the reporter/reconciliation corridor, and the committed run report plus reconciliation truth.
- 17:23 GMT: Added the Group 7 / Path 2 reasoning graph for `Governance append and close-marker path` under `docs/experience_lake/dev_full/a_production_wiring/assets/g07_run_governance_observability_closure/`. Kept the accepted numbered path-spine pattern and centered the path on reconciled run-close truth, the governance append corridor, append log and closure-marker materialization under run scope, and later formal-closure trust for verdict/handoff lanes.
- 17:28 GMT: Added the Group 7 / Path 3 reasoning graph for `Proof-matrix and final-verdict publication path` under `docs/experience_lake/dev_full/a_production_wiring/assets/g07_run_governance_observability_closure/`. Kept the accepted numbered path-spine pattern and centered the path on authoritative source summaries, the full source matrix, the six-proof completeness matrix, deterministic verdict assembly, and publication of the full-verdict bundle at the run-scoped truth surface.
- 17:35 GMT: Added the Group 7 / Path 4 reasoning graph for Drift-visible observability attestation path under docs/experience_lake/dev_full/a_production_wiring/assets/g07_run_governance_observability_closure/. Kept the accepted numbered path-spine pattern and centered the path on declared active runtime path plus live telemetry from those same surfaces, freshness/alert/drift/handle checks, and observability attestation truth for safe certification.
- 17:43 GMT: Added the Group 7 / Path 5 reasoning graph for Idle-safe teardown and residual-readability path under docs/experience_lake/dev_full/a_production_wiring/assets/g07_run_governance_observability_closure/. Kept the accepted numbered path-spine pattern and centered the path on final verdict publication, teardown plan, teardown execution, residual and readability verification, post-teardown cost guardrail snapshot, and idle-safe closure truth.
- 17:51 GMT: Added the Group 7 / Path 6 reasoning graph for Cost guardrail and cost-to-outcome closure path under docs/experience_lake/dev_full/a_production_wiring/assets/g07_run_governance_observability_closure/. Kept the accepted numbered path-spine pattern and centered the path on post-teardown posture plus cost capture and budget handles, cost-guardrail snapshot, phase budget envelope and cost-outcome receipt, and blocker-free cost closure truth.
- 17:59 GMT: Added the Group 1 synthesis reasoning graph for Run and world-source authority under docs/experience_lake/dev_full/a_production_wiring/assets/g01_run_world_source_authority/. Derived it directly from the notebook's group-synthesis entry and centered it on why the group exists, why the three paths belong together, what proof categories it closes for A, the ambiguity to carry upward, the Bi spillover to park, and the final group verdict.
- 18:05 GMT: Added the Group 2 synthesis reasoning graph for Canonical traffic admission and bus publication under docs/experience_lake/dev_full/a_production_wiring/assets/g02_canonical_traffic_admission_bus_publication/. Derived it directly from the notebook's group-synthesis entry and centered it on why the group exists, why the four paths belong together, what proof categories it closes for A, the ingest-proof mode nuance to carry upward, the Bi spillover to park, and the final group verdict.
- 18:11 GMT: Added the Group 3 synthesis reasoning graph for Runtime context formation and decisioning under docs/experience_lake/dev_full/a_production_wiring/assets/g03_runtime_context_decisioning/. Derived it directly from the notebook's group-synthesis entry and centered it on why the RTDL group exists, why the six runtime boundaries belong together, what proof categories it closes for A, the seating and cross-plane authority ambiguities to carry upward, the Bi spillover to park, and the final group verdict.
- 18:18 GMT: Added the Group 4 synthesis reasoning graph for Durable runtime audit and archive truth under docs/experience_lake/dev_full/a_production_wiring/assets/g04_audit_archive_replay_truth/. Derived it directly from the notebook's group-synthesis entry and centered it on why the historical-truth group exists, why the four owned outcomes belong together, what proof categories it closes for A, the archive-route and truth-type ambiguities to carry upward, the Bi spillover to park, and the final group verdict.
- 18:24 GMT: Added the Group 5 synthesis reasoning graph for Operational case and label truth under docs/experience_lake/dev_full/a_production_wiring/assets/g05_case_label_operational_truth/. Derived it directly from the notebook's group-synthesis entry and centered it on why the operational-supervision group exists, why the four owned outcomes belong together, what proof categories it closes for A, the truth-type and timing ambiguities to carry upward, the Bi spillover to park, and the final group verdict.
- 18:31 GMT: Added the Group 6 synthesis reasoning graph for Learning, evaluation, and governed activation under docs/experience_lake/dev_full/a_production_wiring/assets/g06_learning_evaluation_activation/. Derived it directly from the notebook's group-synthesis entry and centered it on why the learning-and-activation group exists, why the five owned outcomes belong together, what proof categories it closes for A, the truth-type and managed-history nuances to carry upward, the Bi spillover to park, and the final group verdict.
