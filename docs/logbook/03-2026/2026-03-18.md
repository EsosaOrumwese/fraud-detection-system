## 12:07:50 +00:00
- Re-checked the production-ready network and resource graphs in `docs/design/platform/dev_full/graph/readiness/` against the final accepted readiness authority and the live standby cloud posture.
- Confirmed the network graph still captures the full promoted platform after final `Phase 9` bounded stress closure.
- Found one stale resource detail in the resource graph:
  - Aurora cluster `fraud-platform-dev-full-aurora` was still labeled `review standby: stopping`
  - live AWS state is now `stopped`
- Corrected `dev_full_platform_resources_production_ready_current_v0.mermaid.mmd` and regenerated the corresponding PNG.
- Live posture re-confirmed during this check:
  - EKS nodegroup `fraud-platform-dev-full-m6f-workers` at `min=0 / desired=0 / max=8`
  - all RTDL and Case + Label deployments `0/0`
  - `kube-system/coredns` `0/0`
  - ingress Lambda reserved concurrency `600`
  - MSK serverless `ACTIVE`
- Judgment:
  - the readiness graphs remain materially correct, and after this refresh the resource graph is again exact about the live standby state rather than only approximately right

## 12:09:01 +00:00
- Adjusted the two long-lived readiness graphs to reflect the accepted warm production-ready posture instead of the temporary review-standby cloud state.
- Removed standby wording from:
  - `dev_full_platform_network_production_ready_current_v0.mermaid.mmd`
  - `dev_full_platform_resources_production_ready_current_v0.mermaid.mmd`
- Restored the resource graph to the accepted ready-shape values:
  - nodegroup `fraud-platform-dev-full-m6f-workers` back to `min 2 / desired 4 / max 8`
  - Aurora cluster shown without temporary stopped-state annotation
- Judgment:
  - the readiness graphs now function purely as accepted production-ready views, while temporary cost-control / review standby remains documented in the notebook and logbook rather than inside the graphs themselves

## 12:10:31 +00:00
- Checked repo authority after the user raised a report that there is \"no action layer\".
- Confirmed the proving-plane docs and runtime implementation still treat `AL` as a real RTDL component:
  - `platform.production_readiness.md`
  - `platform.production_readiness.plan.md`
  - `platform.production_readiness.phase1.md`
  - `src/fraud_detection/action_layer/`
  - deployment `fp-pr3-al`
- Found the actual problem was graph inconsistency:
  - resource graph already showed `AL`
  - network graph omitted `AL` from the decision-lane path
- Corrected both readiness graphs to show `AL` explicitly as part of the RTDL lane:
  - `DF -> DL -> AL -> DLA`
  - `AL -> CaseTrigger`
- Judgment:
  - there is no separate Action Layer plane, but there is an accepted Action Layer component inside RTDL, and the graphs now reflect that distinction clearly

## 12:18:31 +00:00
- Started a second graph family in `docs/design/platform/dev_full/graph/readiness/` so the existing readiness network/resource graphs can remain the readable promoted-platform pair while expanded companions carry more of the earned internal boundaries.
- Added new source graphs:
  - `dev_full_platform_network_production_ready_expanded_current_v0.mermaid.mmd`
  - `dev_full_platform_resources_production_ready_expanded_current_v0.mermaid.mmd`
- Expanded the logical network story to expose:
  - ingress invoke / dedupe / fail-closed / publish boundaries
  - RTDL intake, projection, context, feature, bundle-resolution, guardrail, decision, action, append, and archive boundaries
  - case open / timeline / label-admissibility / label-truth boundaries
  - learning dataset-admission / OFS build / quality gate / train-eval / lineage / promotion boundaries
  - ops/governance alert / drift / cost-control / runbook boundaries
- Expanded the resource graph to map more certified meaning onto concrete runtime seats instead of leaving the resource family grouped only by area.
- Judgment:
  - the readiness directory now has a cleaner split between overview graphs and expanded companion graphs rather than trying to force one pair to do both jobs

## 12:27:49 +00:00
- Compared the readiness graphs against the production-wiring reference `wired.g01.p01.run_legitimization.resources.mermaid.mmd` after the user pointed out that the current graphs were still weak on flow labels.
- Refreshed all four promoted-platform graph sources so more of the meaningful edges now say what is happening rather than leaving the reader to infer it from adjacency:
  - compressed network
  - compressed resources
  - expanded network
  - expanded resources
- Added explicit edge labels for key flow actions including:
  - dispatch / authentication / receipt storage
  - replay / invoke / dedupe / fail-closed / publish
  - projection / context binding / feature supply / decision formation / action outcome / continuity append
  - case open / timeline commit / label truth commit
  - dataset admission / OFS build / quality gate / train-eval / promotion / active-bundle resolution
  - alert / drift / cost-control / runbook routing
- Regenerated all four PNGs after the label refresh.
- Judgment:
  - the graphs now communicate the platform flows more explicitly and are materially closer to the reference style the user pointed at

## 13:20:49 +00:00
- Added an explicit standing-cost control for the `dev_full` streaming substrate:
  - script: `scripts/dev_substrate/dev_full_streaming_standby.py`
  - runbook: `docs/runbooks/dev_full_streaming_standby.md`
- The new control exists because runtime standby alone does not stop MSK Serverless cost; `infra/terraform/dev_full/streaming` was still materially alive even after EKS/Aurora were idled.
- Implemented two explicit actions:
  - `teardown`
  - `restore`
- Default teardown posture is conservative and refuses to destroy streaming unless:
  - EKS nodegroup `fraud-platform-dev-full-m6f-workers` is already `min=0 / desired=0`
  - Aurora cluster `fraud-platform-dev-full-aurora` is `stopped` or `stopping`
- The script snapshots Terraform outputs plus live cluster/SSM presence, destroys only the `streaming` Terraform stack, verifies absence after destroy, and writes durable receipts under `runs/`.
- Validated locally with:
  - `python -m py_compile scripts/dev_substrate/dev_full_streaming_standby.py`
  - `python scripts/dev_substrate/dev_full_streaming_standby.py --help`
  - `python scripts/dev_substrate/dev_full_streaming_standby.py teardown --help`
  - `python scripts/dev_substrate/dev_full_streaming_standby.py restore --help`
- Judgment:
  - the repo now has a truthful operator path for eliminating standing MSK cost between review windows instead of relying on memory or ad hoc Terraform destroys
## 2026-03-18 19:35:00 +00:00 - Mirror expanded Bi production-ready graphs so plane/meta-layer readiness can be inspected without losing the primary compressed views

The current `Bi` production-ready network and resource graphs were doing the right primary job: show the promoted full platform by planes and meta layers. The open complaint was not inaccuracy but compression. Several internal earned boundaries were intentionally collapsed so the primary graphs stayed readable, but that also made Group 4 historical-truth and Group 7 closure logic feel flatter than they really are.

Instead of overwriting the primary graphs, I mirrored the already-existing expanded readiness companions into the `Bi` asset set. That gives us an honest side-by-side posture:
- the primary `Bi` graphs remain the compact promoted platform view
- the expanded `Bi` graphs preserve more internal earned boundaries and handoffs without pretending to be a full object/path ledger

Rendered outputs were regenerated at the larger PNG profile used elsewhere so the comparison is readable.
## 2026-03-18 19:55:00 +00:00 - Start Bi path-level network/resource graph set in A group/path terms with Group 1 / Path 1

The user redirected away from bulk generation and asked that the Bi graph family be built one path at a time. I started with `Group 1 / Path 1 - Run legitimization path` under the Bi asset tree, using A-group naming and the promoted production-ready platform posture rather than the earlier wired baseline.

I created a network graph and a resource graph for this path only. The network graph keeps the path narrow: bounded run intent -> dispatch trust -> Step Functions orchestration -> run-identity authority -> run-control commitment -> committed run basis under the evidence root, with control-bus publication kept visible but not allowed to swallow source-realization or ready-authorization. The resource graph names the concrete seats that materially realize the same path on the production-ready platform.

This is the pattern to judge before continuing with Group 1 / Path 2.
## 2026-03-18 20:03:00 +00:00 - Add Bi Group 1 / Path 2 graphs as the production-ready source-realization corridor

Built `Bi / Group 1 / Path 2 - Source realization path` in A-group terms. This path is lighter in the Bi notebook than the primary objects, so I kept it disciplined: producer-owned source world -> durable object-store basis -> promoted replay-basis realization for WSP and promoted learning-basis realization for the OFS build corridor. I did not let it drift into ready authorization or later dataset / model closure.

The network graph captures source-world realization as a production-ready promoted basis. The resource graph names the concrete seats that materially realize that posture on the accepted ready platform.
## 2026-03-18 20:09:00 +00:00 - Add Bi Group 1 / Path 3 graphs as the production-ready ready-authorization corridor

Built `Bi / Group 1 / Path 3 - Ready authorization path` in A-group terms. The path is kept narrow: committed run basis plus realized source basis -> authoritative READY publication on the control topic -> durable ready receipt and evidence. I kept it out of ingress runtime and later working-base discussion so the path remains the formal READY corridor rather than the whole Control + Ingress object.
## 2026-03-18 20:18:00 +00:00 - Add Bi Group 2 / Path 1 graphs as the production-ready external admission boundary

Built `Bi / Group 2 / Path 1 - Boundary access path` in A-group terms. The path is now drawn as the production-ready external admission boundary: one singular APIGW front door, one keyed route contract, one exact edge telemetry surface, and one accepted invoke boundary into the ingress Lambda. I kept the retained ALB / ECS / stale-helper path visible only as explicitly excluded drift, because that exclusion is part of what the Bi notes say this path earned.
## 2026-03-18 20:24:00 +00:00 - Add Bi Group 2 / Path 2 graphs as the production-ready ingress-owned disposition boundary

Built `Bi / Group 2 / Path 2 - Admission and disposition path` in A-group terms. The path is drawn as the production-ready ingress-truth boundary: deterministic run-scoped disposition basis, truthful fail-closed health semantics, durable DDB-backed admission truth, and explicit receipt / quarantine evidence. I kept it closed at ingress-owned truth and did not let it drift into the next bus-publication path.
## 2026-03-18 20:31:00 +00:00 - Add Bi Group 2 / Path 3 graphs as the production-ready transport-to-consumption bus handoff boundary

Built `Bi / Group 2 / Path 3 - Authoritative bus publication path` in A-group terms. The path is drawn as the production-ready handoff boundary where publish truth is kept separate from admission truth, ambiguity is handled honestly, topic movement is explicit, and closure depends on fresh-scope downstream RTDL consumption rather than on producer-side hope.
## 2026-03-18 20:39:00 +00:00 - Add Bi Group 2 / Path 4 graphs as the production-ready mode-aware ingest-evidence boundary

Built `Bi / Group 2 / Path 4 - Ingest commit truth path` in A-group terms. The path is drawn as a managed gate-owning evidence boundary: completed ingress behavior closes only through durable summaries plus edge-matched proof mode, and under the current active edge the truth law is explicit `IG_ADMISSION_INDEX_PROXY`, not fabricated stronger broker proof. I kept the path narrow so it closes at ingest-commit truth and does not drift into later replay or learning use.
## 2026-03-18 12:57:56 +00:00 - Add Bi Group 3 / Path 1 production-ready network and resource graphs for the entity and relationship projection boundary
Created the `Bi` path-bounded graph pair for `Group 3 / Path 1 - Entity and relationship projection path` under `docs/experience_lake/dev_full/bi_production_readiness/assets/g03_runtime_context_decisioning/`.

The network graph stays narrow on the promoted projection boundary rather than drifting into joined context, feature readiness, or later decisioning. It carries the production-ready changes that the `Bi` notes actually earned for this path: current-run projection mutation is visible, checkpoint posture is bounded enough for the accepted RTDL slice, and replay-era watermark age is expressed through replay-aware operator semantics rather than false hard-red posture.

The resource graph names the concrete promoted seats for that same closure: MSK topic family, `fraud-platform-dev-full` EKS / `fraud-platform-rtdl`, `fp-pr3-csfb`, `fp-pr3-ieg`, the pinned RTDL image family, and the Aurora writer path that materially closes current graph and entity projection truth. Rendered both PNGs at the larger export profile already used for the rest of the `A`/`Bi` graph set.
## 2026-03-18 13:07:12 +00:00 - Add Bi Group 3 / Path 2 production-ready network and resource graphs for the joined-context formation boundary
Created the `Bi` path-bounded graph pair for `Group 3 / Path 2 - Joined context formation path` under `docs/experience_lake/dev_full/bi_production_readiness/assets/g03_runtime_context_decisioning/`.

The network graph keeps the path narrow on `CSFB` as the production-ready joined-context boundary. It expresses the promoted `Bi` posture the notes earned: event-specific joined context is materially formed for the active run, hard join defects remain absent, checkpoint freshness is bounded enough for the accepted RTDL slice, and replay-age watermark / checkpoint noise is treated as advisory rather than as true context failure when the underlying context boundary is healthy.

The resource graph names the concrete promoted seats for that same closure: MSK topic family, `fraud-platform-dev-full` EKS / `fraud-platform-rtdl`, `fp-pr3-csfb`, the pinned RTDL image family, and the downstream runtime handoff into `fp-pr3-ieg` and `fp-pr3-ofp`. Rendered both PNGs with the larger export profile used across the rest of the graph set.
## 2026-03-18 13:17:08 +00:00 - Add Bi Group 3 / Path 3 production-ready network and resource graphs for the online feature readiness boundary
Created the `Bi` path-bounded graph pair for `Group 3 / Path 3 - Online feature readiness path` under `docs/experience_lake/dev_full/bi_production_readiness/assets/g03_runtime_context_decisioning/`.

The network graph keeps the path narrow on `OFP` as the production-ready online-feature boundary. It expresses the promoted `Bi` posture the notes earned: usable feature truth is materially available on the active run, freshness and missingness are judged honestly, graph-version and requester/projector seam defects are kept distinct from true feature absence, and replay-era advisory posture is not confused with real stale-graph-version or missing-feature defects.

The resource graph names the concrete promoted seats for that same closure: `fraud-platform-dev-full` EKS / `fraud-platform-rtdl`, `fp-pr3-csfb`, `fp-pr3-ieg`, `fp-pr3-ofp`, the pinned RTDL image family, the Aurora writer path, and the concrete health/contract surfaces that make feature readiness explainable enough to judge. Rendered both PNGs with the larger export profile used across the graph set.
## 2026-03-18 13:25:08 +00:00 - Add Bi Group 3 / Path 4 production-ready network and resource graphs for the decision-guardrail boundary
Created the `Bi` path-bounded graph pair for `Group 3 / Path 4 - Decision guardrail path` under `docs/experience_lake/dev_full/bi_production_readiness/assets/g03_runtime_context_decisioning/`.

The network graph keeps the path narrow on `DL` as the production-ready guardrail boundary. It expresses the promoted `Bi` posture the notes earned: typed upstream dependency truth becomes adjudicated decision posture, replay and stale advisories do not automatically force closure, post-window idle decay is kept separate from active-window insufficiency, and even explicit `FAIL_CLOSED` labels are only accepted when they are produced on the correct proof boundary.

The resource graph names the concrete promoted seats for that same closure: `fraud-platform-dev-full` EKS / `fraud-platform-rtdl`, `fp-pr3-csfb`, `fp-pr3-ieg`, `fp-pr3-ofp`, `fp-pr3-dl`, and downstream `fp-pr3-df`, plus the live dependency and verdict-integrity surfaces that make guardrail posture explainable enough to trust. Rendered both PNGs with the larger export profile used across the graph set.
## 2026-03-18 13:34:12 +00:00 - Add Bi Group 3 / Path 5 production-ready network and resource graphs for the decision-formation boundary
Created the `Bi` path-bounded graph pair for `Group 3 / Path 5 - Decision formation path` under `docs/experience_lake/dev_full/bi_production_readiness/assets/g03_runtime_context_decisioning/`.

The network graph keeps the path narrow on `DF` as the production-ready decision-formation boundary. It expresses the promoted `Bi` posture the notes support for this object: joined context, feature-ready truth, guardrail posture, and runtime-consumed active bundle or policy authority become explicit decision truth with bounded decision-latency semantics, explicit quarantine / fail-closed treatment, and complete decision provenance and explanation fields.

The resource graph names the concrete promoted seats for that same closure: `fraud-platform-dev-full` EKS / `fraud-platform-rtdl`, `fp-pr3-csfb`, `fp-pr3-ofp`, `fp-pr3-dl`, `fp-pr3-df`, the active-bundle registry truth, SSM-backed runtime handles, and the immediate `fp.bus.rtdl.v1` / `fp.bus.audit.v1` publication seats. Rendered both PNGs with the larger export profile used across the graph set.
## 2026-03-18 13:42:31 +00:00 - Add Bi Group 3 / Path 6 production-ready network and resource graphs for the action-and-outcome emission boundary
Created the `Bi` path-bounded graph pair for `Group 3 / Path 6 - Action and outcome emission path` under `docs/experience_lake/dev_full/bi_production_readiness/assets/g03_runtime_context_decisioning/`.

The network graph keeps the path narrow on `AL` as the production-ready action and outcome boundary. It expresses the promoted `Bi` posture the notes earned: explicit decision truth becomes deterministic downstream-usable RTDL output truth, duplicate-safe side effects and ambiguity recording stay explicit, and the output truth is timely and usable enough to support later Case + Label, audit, and replay coupling without collapsing this path into Group 4 ownership.

The resource graph names the concrete promoted seats for that same closure: `fraud-platform-dev-full` EKS / `fraud-platform-rtdl`, `fp-pr3-df`, `fp-pr3-al`, the active-bundle registry truth used for attribution, and the immediate `fp.bus.audit.v1` publication seat, while keeping the owned closure on RTDL output truth itself. Rendered both PNGs with the larger export profile used across the graph set.
## 2026-03-18 13:06:01 +00:00 - Add Bi Group 4 / Path 1 production-ready network and resource graphs for the append-only audit boundary
Created the `Bi` path-bounded graph pair for `Group 4 / Path 1 - Audit append path` under `docs/experience_lake/dev_full/bi_production_readiness/assets/g04_audit_archive_replay_truth/`.

This group carries the `A` path set forward unchanged, so the graph pair stays narrow on `DLA` as the append-only audit boundary while using the `Bi` production-ready posture the platform actually earned: append-only audit evidence is part of the explicit decision-chain closure law, the promoted `fp-pr3-dla` seat is live on the current working platform, and append truth is no longer something the platform is allowed to infer indirectly from later case or archive surfaces.

The resource graph names the concrete promoted seats for that same closure: `fraud-platform-dev-full` EKS / `fraud-platform-rtdl`, `fp-pr3-df`, `fp-pr3-al`, `fp-pr3-dla`, the evidence bucket, and the immediate `fp.bus.audit.v1` handoff seat, while keeping the owned closure on append-only audit and lineage truth itself. Rendered both PNGs with the larger export profile used across the rest of the graph set.
## 2026-03-18 13:52:18 +00:00 - Add Bi Group 4 / Path 2 production-ready network and resource graphs for audit publication and durable reference truth
Created the `Bi` path-bounded graph pair for `Group 4 / Path 2 - Audit publication and durable reference path` under `docs/experience_lake/dev_full/bi_production_readiness/assets/g04_audit_archive_replay_truth/`.

This graph pair stays narrow on the publication and by-reference boundary after append-only audit truth already exists. The production-ready posture here is not just that `fp.bus.audit.v1` exists, but that append-only audit truth is published on the promoted runtime, downstream operational readers can consume it on the named handoff seat, and durable by-reference evidence surfaces in the evidence bucket and object store remain the authoritative reference anchors rather than devolving into shadow payload copies.

The resource graph names the concrete promoted seats for that same closure: `fp-pr3-dla`, `fp.bus.audit.v1`, MSK Serverless, `fp-pr3-case-trigger` as a real downstream consumer, and the `fraud-platform-dev-full-evidence` plus `fraud-platform-dev-full-object-store` anchors that materially realize durable audit refs. Rendered both PNGs with the larger export profile used across the graph set.
## 2026-03-18 14:03:02 +00:00 - Add Bi Group 4 / Path 3 production-ready network and resource graphs for immutable archive preservation
Created the `Bi` path-bounded graph pair for `Group 4 / Path 3 - Immutable archive preservation path` under `docs/experience_lake/dev_full/bi_production_readiness/assets/g04_audit_archive_replay_truth/`.

This graph pair stays narrow on `fp-pr3-archive-writer` as the immutable archive-preservation boundary. The production-ready posture here is that current-run RTDL decision/archive outputs are preserved into run-scoped immutable history with explicit continuity proof, and the accepted RTDL closure posture keeps archive-integrity non-regression in scope rather than treating archive writes as background best effort. The graph deliberately keeps archive closure and replay-reference readiness for the next path.

The resource graph names the concrete promoted seats for that same closure: MSK Serverless, `fp.bus.rtdl.v1`, `fp.bus.audit.v1`, `fp-pr3-archive-writer`, `fraud-platform-dev-full-object-store`, `fraud-platform-dev-full-evidence`, and the run-scoped `archive/{platform_run_id}/events/` prefix that materially realizes immutable preserved runtime history. Rendered both PNGs with the larger export profile used across the rest of the graph set.
## 2026-03-18 14:12:17 +00:00 - Add Bi Group 4 / Path 4 production-ready network and resource graphs for archive closure and replay-reference readiness
Created the `Bi` path-bounded graph pair for `Group 4 / Path 4 - Archive closure and replay-reference path` under `docs/experience_lake/dev_full/bi_production_readiness/assets/g04_audit_archive_replay_truth/`.

This graph pair stays narrow on formal archive closure and replay-referenceability after immutable archive preservation already exists. The production-ready posture here is the closure law itself: RTDL catch-up does not close without archive-writer closure evidence, and learning cannot begin until replay references are resolved from spine closure. That makes this path the formal bridge from preserved archive history into the replay-referenceable archive basis that Group 6 later consumes at `P12 LEARNING_INPUT_READY`.

The resource graph names the concrete seats for that same closure: the run-scoped archive prefix in `fraud-platform-dev-full-object-store`, paired closure evidence in `fraud-platform-dev-full-evidence`, and the run-control / spine-closure authority that resolves replay-referenceable archive basis. Rendered both PNGs with the larger export profile used across the rest of the graph set.
## 2026-03-18 14:23:41 +00:00 - Add Bi Group 5 / Path 1 production-ready network and resource graphs for the case-intent escalation boundary
Created the `Bi` path-bounded graph pair for `Group 5 / Path 1 - Case-intent escalation path` under `docs/experience_lake/dev_full/bi_production_readiness/assets/g05_case_label_operational_truth/`.

This graph pair stays narrow on `CaseTrigger` as the production-ready escalation boundary. The production-ready posture here is not just that RTDL emits outputs and a trigger deployment exists, but that decision-worthy and audit-worthy RTDL outputs become explicit, duplicate-safe, current-run case-intent truth on the real `RTDL -> CaseTrigger` boundary, with trigger counters retained on the boundary itself so the path is judged directly rather than inferred from later case state.

The resource graph names the concrete promoted seats for that same closure: MSK Serverless, `fp.bus.rtdl.v1`, `fp.bus.audit.v1`, `fp-pr3-case-trigger`, `fp.bus.case.triggers.v1`, and downstream `fp-pr3-case-mgmt`, plus the boundary evidence surfaces (`triggers_seen`, `published`, `duplicates`, `quarantine`, `payload_mismatch_total`) that make the path production-judgeable on its own terms. Rendered both PNGs with the larger export profile used across the rest of the graph set.
## 2026-03-18 14:32:29 +00:00 - Add Bi Group 5 / Path 2 production-ready network and resource graphs for deterministic case creation and append-only timeline truth
Created the `Bi` path-bounded graph pair for `Group 5 / Path 2 - Case creation and timeline append path` under `docs/experience_lake/dev_full/bi_production_readiness/assets/g05_case_label_operational_truth/`.

This graph pair stays narrow on `Case Management` as the production-ready case-truth boundary. The production-ready posture here is not just that a case exists later, but that case-intent truth is turned into deterministic case identity and append-only timeline truth at the owned `Case Management` boundary, with duplicate-case prevention, invalid-transition absence, and truthful `decision_to_case` timing on the real processing clocks rather than earlier observed-time artifacts.

The resource graph names the concrete promoted seats for that same closure: `fp.bus.case.triggers.v1`, `fp-pr3-case-mgmt`, `fraud-platform-dev-full-aurora`, and the Aurora writer path that materially realizes deterministic case truth and append-only operational case history. Rendered both PNGs with the larger export profile used across the rest of the graph set.
## 2026-03-18 13:15:44 +00:00 - Add Bi Group 5 / Path 3 production-ready network and resource graphs for the case-to-label supervision-request handoff
Created the `Bi` path-bounded graph pair for `Group 5 / Path 3 - Case-to-label handoff path` under `docs/experience_lake/dev_full/bi_production_readiness/assets/g05_case_label_operational_truth/`.

This graph pair stays narrow on the real `Case Management -> Label Store` handshake boundary. The production-ready posture here is not just that labels later appear, but that operational case truth becomes explicit, timely, duplicate-safe supervision-request truth on the owned handoff seam, with clean truth ownership preserved so case truth remains with `Case Management` and authoritative label truth begins only once `Label Store` takes intake ownership.

The resource graph names the concrete promoted seats for that same closure: `fraud-platform-dev-full` EKS / `fraud-platform-case-labels`, `fp-pr3-case-mgmt`, `fp-pr3-label-store`, `fraud-platform-dev-full-aurora`, and the authoritative handoff clock basis `cm_label_emissions.first_requested_at_utc` that corrected the earlier false-red `LABEL_PENDING` timing story. It also keeps the boundary evidence explicit through label pending / accepted / rejected and duplicate-safe handoff checks, while stopping the owned closure before authoritative label commit begins. Rendered both PNGs with the larger export profile used across the rest of the graph set.
## 2026-03-18 13:17:07 +00:00 - Add Bi Group 5 / Path 4 production-ready network and resource graphs for authoritative label commit and temporal visibility
Created the `Bi` path-bounded graph pair for `Group 5 / Path 4 - Authoritative label commit and visibility path` under `docs/experience_lake/dev_full/bi_production_readiness/assets/g05_case_label_operational_truth/`.

This graph pair stays narrow on `Label Store` as the owned authoritative label boundary. The production-ready posture here is not just that labels later appear, but that supervision-request truth becomes append-only, provenance-complete, conflict-visible, idempotent authoritative label truth at the real `Label Store` boundary, with temporal readback explicit enough to answer `as_of` and maturity questions without letting this path blur into later learning-input basis.

The resource graph names the concrete promoted seats for that same closure: `fraud-platform-dev-full` EKS / `fraud-platform-case-labels`, `fp-pr3-label-store`, `fraud-platform-dev-full-aurora`, and the downstream `fp.bus.labels.events.v1` publication seat. It also keeps the ready boundary evidence explicit through pending / accepted / rejected counts, duplicate and conflict counters, commit and readback health, and a fail-closed temporal-visibility posture around `label_asof_utc`, maturity, and future-boundary interpretation. Rendered both PNGs with the larger export profile used across the rest of the graph set.
## 2026-03-18 13:18:23 +00:00 - Add Bi Group 6 / Path 1 production-ready network and resource graphs for causal learning-input readiness
Created the `Bi` path-bounded graph pair for `Group 6 / Path 1 - Learning-input basis path` under `docs/experience_lake/dev_full/bi_production_readiness/assets/g06_learning_evaluation_activation/`.

This graph pair stays narrow on causal learning admission before OFS dataset build begins. The production-ready posture here is not that Databricks can reach storage, but that replay-referenceable archive truth, authoritative label truth, and replay references become one admitted learning basis with replay-basis pinning, `feature_asof_utc` and `label_asof_utc` pinned to the same causal boundary at this stage, maturity policy pinned, future-boundary interpretation fail-closed, anti-leakage checks explicit, and managed-corridor continuity attributable to the same admitted basis.

The resource graph names the concrete promoted seats for that same closure: `fraud-platform-dev-full-object-store`, `fraud-platform-dev-full-evidence`, `fp-pr3-label-store`, `fp-pr3-archive-writer`, the Databricks workspace, and the Unity Catalog storage credential / external-location surface `fraud_platform_dev_full_object_store_v0`. It also keeps the canonical basis evidence explicit through replay-basis receipt, readiness snapshot, `as_of` / maturity policy snapshot, and runtime-learning separation snapshot, while stopping the owned closure before offline dataset commitment begins at `fraud-platform-dev-full-ofs-build-v0`. Rendered both PNGs with the larger export profile used across the rest of the graph set.
## 2026-03-18 13:19:36 +00:00 - Add Bi Group 6 / Path 2 production-ready network and resource graphs for bounded offline dataset truth
Created the `Bi` path-bounded graph pair for `Group 6 / Path 2 - Offline dataset commitment path` under `docs/experience_lake/dev_full/bi_production_readiness/assets/g06_learning_evaluation_activation/`.

This graph pair stays narrow on the managed OFS dataset boundary. The production-ready posture here is not that Databricks jobs run, but that one already-admitted causal learning basis becomes one bounded offline dataset truth with the promoted `feature_asof_utc`, `label_asof_utc`, and maturity law carried forward unchanged, the raw retained horizon rejected in favor of the promoted mission window, and the stronger dataset-truth contract closed through manifest, fingerprint, rollback recipe, and leakage-safe usefulness under the tighter causal trim.

The resource graph names the concrete promoted seats for that same closure: the Databricks workspace, the Unity Catalog storage credential / external-location surface `fraud_platform_dev_full_object_store_v0`, `fraud-platform-dev-full-ofs-build-v0`, `fraud-platform-dev-full-ofs-quality-v0`, `fraud-platform-dev-full-object-store`, `fraud-platform-dev-full-evidence`, and the later `fraud-platform-dev-full-mtrain` handoff surface. It keeps the owned closure on committed offline dataset truth itself, with manifest / fingerprint / rollback artifacts and time-bound leakage evidence explicit, before train and eval begin. Rendered both PNGs with the larger export profile used across the rest of the graph set.
## 2026-03-18 13:20:44 +00:00 - Add Bi Group 6 / Path 3 production-ready network and resource graphs for candidate-bundle truth on the managed train/eval corridor
Created the `Bi` path-bounded graph pair for `Group 6 / Path 3 - Train / eval and candidate-bundle path` under `docs/experience_lake/dev_full/bi_production_readiness/assets/g06_learning_evaluation_activation/`.

This graph pair stays narrow on the managed train/eval corridor. The production-ready posture here is not that SageMaker and MLflow ran, but that one committed offline dataset basis became one provenance-complete candidate bundle on the same admitted bounded basis, with metrics, leakage, and stability gates closed, candidate provenance carrying the upstream dataset fingerprint plus replay and `as_of` controls, and safe-disable plus rollback readiness closing before promotion begins. The accepted candidate closure also keeps the stronger semantic law explicit by rejecting a superficially green managed run when the selected sample still exceeded the admitted `feature_asof_utc` boundary.

The resource graph names the concrete promoted seats for that same closure: the SageMaker execution role, `fraud-platform-dev-full-mtrain`, `fraud-platform-dev-full-mbatch`, the Databricks-backed MLflow surface, `fraud-platform-dev-full-models`, and the evidence bucket that stores train/eval and operability receipts. It keeps the owned closure on candidate-bundle truth and pre-promotion rollback readiness itself, without drifting into the later promotion corridor. Rendered both PNGs with the larger export profile used across the rest of the graph set.

## 2026-03-18 13:24:02 +00:00 - Added Bi Group 6 / Path 4 production-ready network and resource graphs
Created the `Bi` path-level network/resource pair for `group 6 / path 4` under `docs/experience_lake/dev_full/bi_production_readiness/assets/g06_learning_evaluation_activation/`.

Files:
- `ready.g06.p04.promotion_rollback_governance.network.mermaid.mmd`
- `ready.g06.p04.promotion_rollback_governance.network.mermaid.png`
- `ready.g06.p04.promotion_rollback_governance.resources.mermaid.mmd`
- `ready.g06.p04.promotion_rollback_governance.resources.mermaid.png`

The pair keeps the path on governed promotion truth itself:
- eligible candidate bundle as the bounded entry
- strict fail-closed promotion corridor
- explicit promotion proof beyond registry mutation
- rollback drill plus bounded restore evidence
- governed active-resolution and compatibility checks
- governance append and deterministic `P15` verdict closure
- no drift into runtime authority feedback, which remains `group 6 / path 5`

## 2026-03-18 13:25:24 +00:00 - Added Bi Group 6 / Path 5 production-ready network and resource graphs
Created the `Bi` path-level network/resource pair for `group 6 / path 5` under `docs/experience_lake/dev_full/bi_production_readiness/assets/g06_learning_evaluation_activation/`.

Files:
- `ready.g06.p05.active_bundle_authority_feedback.network.mermaid.mmd`
- `ready.g06.p05.active_bundle_authority_feedback.network.mermaid.png`
- `ready.g06.p05.active_bundle_authority_feedback.resources.mermaid.mmd`
- `ready.g06.p05.active_bundle_authority_feedback.resources.mermaid.png`

The pair keeps the path on runtime-readable active authority itself:
- governed promoted state plus rollback-governance truth as the bounded entry
- deterministic registry-to-runtime active authority resolution
- candidate / rollback / restore attribution remaining live under fresh scopes
- evidence-layer authority pinned to the staged registry surface instead of stale carried-forward fields
- later operator / integrated readback only as strengthening evidence
- no drift into Group 3 decision formation, which remains the later consumer

## 2026-03-18 13:26:58 +00:00 - Added Bi Group 7 / Path 1 production-ready network and resource graphs
Created the `Bi` path-level network/resource pair for `group 7 / path 1` under `docs/experience_lake/dev_full/bi_production_readiness/assets/g07_run_governance_observability_closure/`.

Files:
- `ready.g07.p01.run_reconstruction_receipt_closure.network.mermaid.mmd`
- `ready.g07.p01.run_reconstruction_receipt_closure.network.mermaid.png`
- `ready.g07.p01.run_reconstruction_receipt_closure.resources.mermaid.mmd`
- `ready.g07.p01.run_reconstruction_receipt_closure.resources.mermaid.png`

The pair keeps the path on exact run reconstruction itself:
- earlier lane receipts and readable closure evidence as the bounded entry
- dedicated `M8 / P11` reconstruction corridor
- single-writer contention discipline and managed one-shot reporter execution
- closure-bundle completeness and reconciliation coherence
- deterministic reconstruction judgment and handoff-safe refs
- no drift into governance append / close-marker or later final-verdict closure

## 2026-03-18 13:28:07 +00:00 - Added Bi Group 7 / Path 2 production-ready network and resource graphs
Created the `Bi` path-level network/resource pair for `group 7 / path 2` under `docs/experience_lake/dev_full/bi_production_readiness/assets/g07_run_governance_observability_closure/`.

Files:
- `ready.g07.p02.governance_append_close_marker.network.mermaid.mmd`
- `ready.g07.p02.governance_append_close_marker.network.mermaid.png`
- `ready.g07.p02.governance_append_close_marker.resources.mermaid.mmd`
- `ready.g07.p02.governance_append_close_marker.resources.mermaid.png`

The pair keeps the path on formal governance closure itself:
- committed run report plus reconciliation as the bounded entry
- dedicated `M8.H` governance-close corridor
- source-truth governance surfaces separated from downstream evidence-contract projection
- append-safe history plus explicit close marker under the run scope
- fail-closed schema / run-scope / ordering / marker coverage verification
- no drift into final-verdict publication

## 2026-03-18 13:29:21 +00:00 - Added Bi Group 7 / Path 3 production-ready network and resource graphs
Created the `Bi` path-level network/resource pair for `group 7 / path 3` under `docs/experience_lake/dev_full/bi_production_readiness/assets/g07_run_governance_observability_closure/`.

Files:
- `ready.g07.p03.proof_matrix_final_verdict.network.mermaid.mmd`
- `ready.g07.p03.proof_matrix_final_verdict.network.mermaid.png`
- `ready.g07.p03.proof_matrix_final_verdict.resources.mermaid.mmd`
- `ready.g07.p03.proof_matrix_final_verdict.resources.mermaid.png`

The pair keeps the path on whole-platform judgment itself:
- formal governance closure as the bounded entry basis
- strict `P16 / M13` verdict corridor
- blocker-free source matrix and six-proof completeness as explicit judged inputs
- source-scope-aware verdict logic rather than naive summary-pass handling
- identity-verified publication of one deterministic final-verdict bundle to run-control and the run-scoped truth surface
- no drift into observability attestation, teardown, or cost closure

## 2026-03-18 13:30:34 +00:00 - Added Bi Group 7 / Path 4 production-ready network and resource graphs
Created the `Bi` path-level network/resource pair for `group 7 / path 4` under `docs/experience_lake/dev_full/bi_production_readiness/assets/g07_run_governance_observability_closure/`.

Files:
- `ready.g07.p04.drift_visible_observability_attestation.network.mermaid.mmd`
- `ready.g07.p04.drift_visible_observability_attestation.network.mermaid.png`
- `ready.g07.p04.drift_visible_observability_attestation.resources.mermaid.mmd`
- `ready.g07.p04.drift_visible_observability_attestation.resources.mermaid.png`

The pair keeps the path on truthful live visibility itself:
- declared active runtime path plus live telemetry as the bounded entry
- active-path fidelity rather than generic monitoring presence
- freshness, exercised alert coverage, runbook linkage, handle integrity, and drift visibility as explicit closure conditions
- proof-surface misreads kept separate from real runtime invisibility
- no drift into teardown or post-run cost closure

## 2026-03-18 13:31:48 +00:00 - Added Bi Group 7 / Path 5 production-ready network and resource graphs
Created the `Bi` path-level network/resource pair for `group 7 / path 5` under `docs/experience_lake/dev_full/bi_production_readiness/assets/g07_run_governance_observability_closure/`.

Files:
- `ready.g07.p05.idle_safe_teardown_residual_readability.network.mermaid.mmd`
- `ready.g07.p05.idle_safe_teardown_residual_readability.network.mermaid.png`
- `ready.g07.p05.idle_safe_teardown_residual_readability.resources.mermaid.mmd`
- `ready.g07.p05.idle_safe_teardown_residual_readability.resources.mermaid.png`

The pair keeps the path on safe idle and residual-readability itself:
- published final verdict plus observability attestation as the bounded entry
- managed teardown corridor with verdict-first sequencing
- residual-zero / bounded residual proof and post-teardown readability as explicit closure surfaces
- live idle / restore posture kept explicit
- neighboring writer and cost-parser defects kept out of this path

## 2026-03-18 13:32:58 +00:00 - Added Bi Group 7 / Path 6 production-ready network and resource graphs
Created the `Bi` path-level network/resource pair for `group 7 / path 6` under `docs/experience_lake/dev_full/bi_production_readiness/assets/g07_run_governance_observability_closure/`.

Files:
- `ready.g07.p06.cost_guardrail_cost_to_outcome.network.mermaid.mmd`
- `ready.g07.p06.cost_guardrail_cost_to_outcome.network.mermaid.png`
- `ready.g07.p06.cost_guardrail_cost_to_outcome.resources.mermaid.mmd`
- `ready.g07.p06.cost_guardrail_cost_to_outcome.resources.mermaid.png`

The pair keeps the path on attributable spend truth itself:
- idle-safe teardown truth plus run-scoped cost snapshot and resolved budget handles as the bounded entry
- post-idle cost-accountability corridor rather than teardown execution
- numeric budget-envelope interpretation kept fail-closed
- visible major spend-family attribution with `unattributed spend = 0`
- explicit cost-to-outcome receipt tying spend to artifacts and decision / risk retired

## 2026-03-18 13:34:34 +00:00 - Added Bi Group 1 synthesis production-ready network and resource graphs
Created the `Bi` group-level synthesis pair for `group 1` under `docs/experience_lake/dev_full/bi_production_readiness/assets/g01_run_world_source_authority/`.

Files:
- `ready.g01.run_world_source_authority.network.mermaid.mmd`
- `ready.g01.run_world_source_authority.network.mermaid.png`
- `ready.g01.run_world_source_authority.resources.mermaid.mmd`
- `ready.g01.run_world_source_authority.resources.mermaid.png`

The pair keeps Group 1 on its three promoted truths:
- production-ready committed run basis
- production-ready realized source basis
- production-ready authorized READY state

It closes on the group claim that the ready platform begins from governed run legitimacy plus realized source legitimacy before ingress is allowed to matter, and it stops at the later handoff into `group 2`.

## 2026-03-18 13:35:48 +00:00 - Added Bi Group 2 synthesis production-ready network and resource graphs
Created the `Bi` group-level synthesis pair for `group 2` under `docs/experience_lake/dev_full/bi_production_readiness/assets/g02_canonical_traffic_admission_bus_publication/`.

Files:
- `ready.g02.canonical_traffic_admission_bus_publication.network.mermaid.mmd`
- `ready.g02.canonical_traffic_admission_bus_publication.network.mermaid.png`
- `ready.g02.canonical_traffic_admission_bus_publication.resources.mermaid.mmd`
- `ready.g02.canonical_traffic_admission_bus_publication.resources.mermaid.png`

The pair keeps Group 2 on its four promoted truths:
- active ingress boundary
- production-ready ingress disposition truth
- production-ready authoritative runtime bus handoff
- production-ready committed ingest truth

It closes on the group claim that canonical behavioural traffic becomes governed runtime entry truth before RTDL reasoning begins, and it stops at the later handoff into `group 3`.

## 2026-03-18 13:37:09 +00:00 - Added Bi Group 3 synthesis production-ready network and resource graphs
Created the `Bi` group-level synthesis pair for `group 3` under `docs/experience_lake/dev_full/bi_production_readiness/assets/g03_runtime_context_decisioning/`.

Files:
- `ready.g03.runtime_context_decisioning.network.mermaid.mmd`
- `ready.g03.runtime_context_decisioning.network.mermaid.png`
- `ready.g03.runtime_context_decisioning.resources.mermaid.mmd`
- `ready.g03.runtime_context_decisioning.resources.mermaid.png`

The pair keeps Group 3 on its six promoted runtime truths:
- production-ready projection truth
- production-ready joined context truth
- production-ready feature-ready truth
- production-ready guardrail posture
- production-ready decision truth
- production-ready action and outcome truth

It keeps runtime-consumed active bundle / policy authority explicit as an upstream-owned input from `group 6`, and it stops at the later handoffs into `group 4` and `group 5`.

## 2026-03-18 13:38:28 +00:00 - Added Bi Group 4 synthesis production-ready network and resource graphs
Created the `Bi` group-level synthesis pair for `group 4` under `docs/experience_lake/dev_full/bi_production_readiness/assets/g04_audit_archive_replay_truth/`.

Files:
- `ready.g04.audit_archive_replay_truth.network.mermaid.mmd`
- `ready.g04.audit_archive_replay_truth.network.mermaid.png`
- `ready.g04.audit_archive_replay_truth.resources.mermaid.mmd`
- `ready.g04.audit_archive_replay_truth.resources.mermaid.png`

The pair keeps Group 4 on its four promoted historical truths:
- production-ready append-only audit truth
- production-ready durable audit refs / evidence surfaces
- production-ready immutable archive truth
- production-ready replay-referenceable archive closure

It closes on the group claim that runtime decisions and outcomes become durable historical truth that later replay, learning, case handling, and audit review can trust, and it keeps those later consumers as downstream trust users only.

## 2026-03-18 13:39:47 +00:00 - Added Bi Group 5 synthesis production-ready network and resource graphs
Created the `Bi` group-level synthesis pair for `group 5` under `docs/experience_lake/dev_full/bi_production_readiness/assets/g05_case_label_operational_truth/`.

Files:
- `ready.g05.case_label_operational_truth.network.mermaid.mmd`
- `ready.g05.case_label_operational_truth.network.mermaid.png`
- `ready.g05.case_label_operational_truth.resources.mermaid.mmd`
- `ready.g05.case_label_operational_truth.resources.mermaid.png`

The pair keeps Group 5 on its four promoted operational truths:
- production-ready case-intent truth
- production-ready operational case truth
- production-ready supervision-request truth
- production-ready authoritative label truth

It keeps the ownership split explicit across `case-trigger`, `case-management`, and `label-store`, and it stops at the later handoff into `group 6` learning input.

## 2026-03-18 13:42:35 +00:00 - Added Bi Group 6 synthesis production-ready network and resource graphs
Created the `Bi` group-level synthesis pair for `group 6` under `docs/experience_lake/dev_full/bi_production_readiness/assets/g06_learning_evaluation_activation/`.

Files:
- `ready.g06.learning_evaluation_activation.network.mermaid.mmd`
- `ready.g06.learning_evaluation_activation.network.mermaid.png`
- `ready.g06.learning_evaluation_activation.resources.mermaid.mmd`
- `ready.g06.learning_evaluation_activation.resources.mermaid.png`

The pair keeps Group 6 on its five promoted model-evolution truths:
- production-ready learning-input readiness basis
- production-ready offline dataset truth
- production-ready candidate-bundle truth
- production-ready promotion and rollback-governance truth
- production-ready runtime decision authority

It keeps the managed corridor explicit across Databricks, SageMaker, MLflow, the promoted registry surface, and runtime authority readback, and it stops at the later handoff back into `group 3` runtime decision formation.

## 2026-03-18 13:46:53 +00:00 - Added Bi Group 7 synthesis production-ready network and resource graphs
Created the `Bi` group-level synthesis pair for `group 7` under `docs/experience_lake/dev_full/bi_production_readiness/assets/g07_run_governance_observability_closure/`.

Files:
- `ready.g07.run_governance_observability_closure.network.mermaid.mmd`
- `ready.g07.run_governance_observability_closure.network.mermaid.png`
- `ready.g07.run_governance_observability_closure.resources.mermaid.mmd`
- `ready.g07.run_governance_observability_closure.resources.mermaid.png`

The pair keeps Group 7 on its six promoted closure truths:
- production-ready run reconstruction and reconciliation truth
- production-ready governance append and close-marker truth
- production-ready full-platform verdict truth
- production-ready observability attestation truth
- production-ready idle-safe teardown truth
- production-ready cost-to-outcome truth

It keeps the closeout ladder explicit from governed reconstruction through formal verdict publication, drift-visible observability, verdict-first teardown, and post-idle cost accountability.

## 2026-03-18 13:50:22 +00:00 - Normalized Bi group synthesis truth shapes back to cylinders
Updated the seven `Bi` group-level synthesis graph pairs so durable truths, committed surfaces, storage seats, and authoritative bases use cylinder nodes again.

Scope:
- `ready.g01.run_world_source_authority.*`
- `ready.g02.canonical_traffic_admission_bus_publication.*`
- `ready.g03.runtime_context_decisioning.*`
- `ready.g04.audit_archive_replay_truth.*`
- `ready.g05.case_label_operational_truth.*`
- `ready.g06.learning_evaluation_activation.*`
- `ready.g07.run_governance_observability_closure.*`

Round nodes are now reserved for later handoffs / downstream consumers in the group synthesis family. All 14 synthesis PNGs were rerendered after the Mermaid source update.

## 2026-03-18 14:24:00 +00:00 - Executed full post-certification teardown down to retained storage/core substrate
Moved from review standby to actual teardown.

Actions carried through:
- runtime destroy in `infra/terraform/dev_full/runtime`
- Aurora start / wait / rerun when the first destroy hit `InvalidDBClusterStateFault` because the cluster was stopped
- streaming destroy through `scripts/dev_substrate/dev_full_streaming_standby.py teardown --force`
- `ops` destroy in `infra/terraform/dev_full/ops`
- `data_ml` destroy in `infra/terraform/dev_full/data_ml`
- deletion of remaining `fraud-platform-dev-full` CloudWatch log groups
- deletion of the empty ECS cluster object

Verified afterward:
- no Lambda functions under `fraud-platform-dev-full`
- no EKS cluster under `fraud-platform-dev-full`
- no MSK cluster `fraud-platform-dev-full-msk`
- no bootstrap-brokers SSM parameter
- no Aurora cluster
- no remaining `/aws/*` log groups containing `fraud-platform-dev-full`

Retained intentionally:
- S3 buckets and contents
- core VPC/subnets/routes/security group
- KMS key / alias for retained buckets
- core IAM roles
- Terraform backend bucket / lock table

Judgment:
- standing compute/streaming/database spend removed
- remaining bill should now be dominated by retained storage rather than active runtime

## 2026-03-18 14:24:00 +00:00 - Fixed the force-bypass defect in the streaming standby control before relying on it operationally
The first real teardown attempt exposed that `--force` did not actually bypass the runtime standby probe. Because runtime had already been destroyed, the EKS lookup failed before the flag could matter. Updated `scripts/dev_substrate/dev_full_streaming_standby.py` so:

- `terraform init` uses the working backend-arg form for this workspace
- forced teardown skips the runtime standby probe and records that skip in the receipt

This mattered because without it the new MSK teardown control would still fail precisely in the review posture it was meant to support.

## 2026-03-18 14:40:00 +00:00 - Pruned the remote evidence bucket and kept the certification spine locally
The retained evidence the user actually needs for review is the accepted certification spine, and that authority already lives locally under `runs/dev_substrate/dev_full/proving_plane/run_control/`.

Checked the S3 evidence bucket and found that the accepted late-phase receipts were not mirrored there. What was still present remotely was mostly old hardening history:
- `_scrapped` cert attempts
- pre-green `m*` / `pr3_*` run-control bundles
- legacy `evidence/runs/platform_*` payloads

So I deleted:
- `s3://fraud-platform-dev-full-evidence/evidence/dev_full/`
- `s3://fraud-platform-dev-full-evidence/evidence/runs/`

Verified afterward:
- `s3://fraud-platform-dev-full-evidence/evidence/` is now empty

Residual S3 picture after that prune:
- evidence bucket history removed
- artifacts bucket remains but is small (`~67.6 MiB`)
- remaining meaningful S3 cost is now mainly retained object-store data plus normal versioning / request / KMS overhead

## 2026-03-18 14:48:43 +00:00 - Rerendered full Bi graph family at higher export clarity
Rerendered the full `Bi` Mermaid graph family under `docs/experience_lake/dev_full/bi_production_readiness/assets/` using the pinned export profile:
- width: `3200`
- height: `2400`
- scale: `4`

Scope:
- all path-level network graphs
- all path-level resource graphs
- all group-level synthesis network graphs
- all group-level synthesis resource graphs

This replaces the earlier lower-resolution export profile for the `Bi` PNG family. Spot checks now land around ~`12736px` width, which is materially better for dense label-heavy graphs that need aggressive zoom.
