## 12:07:50 +00:00
- Re-checked the production-ready network and resource graphs in `docs/design/platform/dev_full/graph/readiness/` against the final accepted readiness authority and the live standby cloud posture.
- Confirmed the network graph still captures the full promoted platform after final `Phase 9` bounded stress closure.
- Found one stale resource detail in the resource graph:
  - Aurora cluster `fraud-platform-dev-full-aurora` was still labeled `review standby: stopping`
  - live AWS state is now `stopped`
- Corrected `dev_full_platform_resources_production_ready_current_v0.mermaid.mmd` and regenerated the corresponding PNG.
- Live posture re-confirmed during this check:
  - EKS nodegroup `fraud-platform-dev-full-m6f-workers` at `min=0 / desired=0 / max=8`
  - all RTDL and Case + Label deployments `0/0`
  - `kube-system/coredns` `0/0`
  - ingress Lambda reserved concurrency `600`
  - MSK serverless `ACTIVE`
- Judgment:
  - the readiness graphs remain materially correct, and after this refresh the resource graph is again exact about the live standby state rather than only approximately right

## 12:09:01 +00:00
- Adjusted the two long-lived readiness graphs to reflect the accepted warm production-ready posture instead of the temporary review-standby cloud state.
- Removed standby wording from:
  - `dev_full_platform_network_production_ready_current_v0.mermaid.mmd`
  - `dev_full_platform_resources_production_ready_current_v0.mermaid.mmd`
- Restored the resource graph to the accepted ready-shape values:
  - nodegroup `fraud-platform-dev-full-m6f-workers` back to `min 2 / desired 4 / max 8`
  - Aurora cluster shown without temporary stopped-state annotation
- Judgment:
  - the readiness graphs now function purely as accepted production-ready views, while temporary cost-control / review standby remains documented in the notebook and logbook rather than inside the graphs themselves

## 12:10:31 +00:00
- Checked repo authority after the user raised a report that there is \"no action layer\".
- Confirmed the proving-plane docs and runtime implementation still treat `AL` as a real RTDL component:
  - `platform.production_readiness.md`
  - `platform.production_readiness.plan.md`
  - `platform.production_readiness.phase1.md`
  - `src/fraud_detection/action_layer/`
  - deployment `fp-pr3-al`
- Found the actual problem was graph inconsistency:
  - resource graph already showed `AL`
  - network graph omitted `AL` from the decision-lane path
- Corrected both readiness graphs to show `AL` explicitly as part of the RTDL lane:
  - `DF -> DL -> AL -> DLA`
  - `AL -> CaseTrigger`
- Judgment:
  - there is no separate Action Layer plane, but there is an accepted Action Layer component inside RTDL, and the graphs now reflect that distinction clearly

## 12:18:31 +00:00
- Started a second graph family in `docs/design/platform/dev_full/graph/readiness/` so the existing readiness network/resource graphs can remain the readable promoted-platform pair while expanded companions carry more of the earned internal boundaries.
- Added new source graphs:
  - `dev_full_platform_network_production_ready_expanded_current_v0.mermaid.mmd`
  - `dev_full_platform_resources_production_ready_expanded_current_v0.mermaid.mmd`
- Expanded the logical network story to expose:
  - ingress invoke / dedupe / fail-closed / publish boundaries
  - RTDL intake, projection, context, feature, bundle-resolution, guardrail, decision, action, append, and archive boundaries
  - case open / timeline / label-admissibility / label-truth boundaries
  - learning dataset-admission / OFS build / quality gate / train-eval / lineage / promotion boundaries
  - ops/governance alert / drift / cost-control / runbook boundaries
- Expanded the resource graph to map more certified meaning onto concrete runtime seats instead of leaving the resource family grouped only by area.
- Judgment:
  - the readiness directory now has a cleaner split between overview graphs and expanded companion graphs rather than trying to force one pair to do both jobs
