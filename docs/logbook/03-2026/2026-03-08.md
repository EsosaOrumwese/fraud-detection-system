# 2026-03-08

## 09:20:25 +00:00
- Reopened `PR3-S4` on the live evidence instead of treating the prior `DF/case` red state as generic downstream instability.
- Confirmed the soak envelope itself is green:
  - admitted throughput remains above the pinned `3000 eps` soak target,
  - `4xx=0`, `5xx=0`,
  - latency remains within the `350/700 ms` budget,
  - `OFP`, `IEG`, `DLA`, and `archive_writer` stay operationally healthy on lag/checkpoint surfaces.
- Narrowed the real blocker to a `DF` replay-time contract bug:
  - `decision_fabric/metrics/last_metrics.json` shows latencies in the `1.5M..3.0M ms` range,
  - `decision_fabric/reconciliation/reconciliation.json` shows `DECISION_DEADLINE_EXCEEDED` dominating current-run outcomes,
  - `src/fraud_detection/decision_fabric/worker.py` still anchors `decision_started_at_utc` to historical `published_at_utc` whenever it is parseable.
- Production interpretation recorded in the implementation map:
  - replay/oracle-backed runs must preserve event-time provenance without using historical event time as the online SLA start,
  - otherwise `DF` deterministically fails closed under replay and falsely starves case/label downstream.
- Selected next remediation:
  - patch `DF` timing to use run-local observation / first-seen time for decision budgets,
  - validate with focused tests,
  - rebuild/rematerialize the runtime image,
  - rerun strict `PR3-S4` on the same boundary.

## 09:24:40 +00:00
- Implemented the bounded `DF` replay-timing correction in `src/fraud_detection/decision_fabric/worker.py`.
- New runtime behavior:
  - `decision_deadline_ms` is now anchored to worker first-seen observation time for the current offset,
  - historical `published_at_utc` is preserved for provenance only and no longer drives the online deadline clock.
- Updated worker-runtime tests to reflect the new contract:
  - valid historical publish timestamps no longer override observed time,
  - defer-path still persists first-seen state with parseable publish timestamps.
- Local validation passed:
  - `python -m py_compile src/fraud_detection/decision_fabric/worker.py tests/services/decision_fabric/test_worker_runtime.py`
  - `python -m pytest tests/services/decision_fabric/test_worker_runtime.py -q`
  - result: `9 passed`
- Next step:
  - push this slice,
  - rebuild/publish the remote platform image,
  - rematerialize PR3 runtime,
  - rerun strict `PR3-S4`.

## 10:15:40 +00:00
- Reopened the post-rerun `PR3-S4` runtime state with live EKS evidence instead of treating the remaining `DL/OFP` red path as a generic blocker.
- Confirmed the new failure pattern is narrower than before:
  - `OFP` remains current on checkpoint/lag,
  - `missing_features = 0`,
  - run-scoped `snapshot_failures = 1`,
  - `DL` logs show the state recovered to `NORMAL` and then later fell back to `FAIL_CLOSED` on `ofp_health` alone.
- Production diagnosis recorded in the implementation map:
  - `DL` currently treats any non-zero OFP `snapshot_failures` as a hard error,
  - that keeps the whole RTDL/case path fail-closed even after OFP has recovered and is actively processing current-run traffic,
  - this is too strict for replay certification and misrepresents the actual platform posture.
- Selected next remediation:
  - patch `DL` shared OFP health handling to admit only the narrow recovered-transient posture (fresh checkpoints, no missing features, no stale graph, replay-only health reasons, active event application, no red snapshot-failure reason),
  - preserve fail-closed behavior for real OFP faults,
  - validate with focused tests before rebuilding and rerunning strict `PR3-S4`.

## 10:21:55 +00:00
- Implemented the narrowed `DL/OFP` remediation in `src/fraud_detection/degrade_ladder/worker.py`.
- New behavior:
  - `DL` still fails closed on real OFP faults,
  - but it no longer holds the full RTDL path red on a recovered replay-soak transient when checkpoints are current, events are actively applying, graph/features are clean, and the only health reasons are replay-age advisories.
- Added focused worker observability tests to prove both sides of the contract:
  - recovered transient is allowed,
  - active red snapshot-failure remains blocked.
- Local validation passed before any runtime rebuild:
  - `python -m py_compile src/fraud_detection/degrade_ladder/worker.py tests/services/degrade_ladder/test_phase7_worker_observability.py`
  - `python -m pytest tests/services/degrade_ladder/test_phase7_worker_observability.py -q`
  - result: `9 passed`
- Next sequence:
  - push the slice,
  - rebuild/publish the image,
  - repin runtime source image,
  - rerun strict `PR3-S4`.

## 10:19:20 +00:00
- Pushed commit `7dc6ff55b` to `origin/cert-platform`.
- Rebuilt the immutable platform image from `cert-platform`:
  - workflow `dev_full_m1_packaging`
  - run `22819038500`
  - result `success`
  - new digest `sha256:82a0d26e8c2760d236535cb8c22cb94a693030bae803f433992239b687124bd7`
- Repinned the shared WSP/PR3 source task family used by the canonical remote path:
  - `fraud-platform-dev-full-wsp-ephemeral:57`
  - image now points at the new immutable digest.
- Next step remains strict and unchanged:
  - rerun `PR3-S4` on the same boundary,
  - inspect whether the RTDL and case/label deltas now materialize.

## Entry: 2026-03-08 11:07:22 +00:00 - PR3-S4 narrowed to decision-lane IAM drift at the embedded OFP snapshot boundary
1. I re-read the latest strict PR3-S4 soak artifacts before changing code again because the active blocker signature had shifted from generic RTDL red to a very specific DF/OFP serve-path failure while ingress and soak stability remained green.
2. The decisive runtime facts are now:
   - soak hot-path metrics are still production-grade for this state (~3029 eps, 4xx=0, 5xx=0, latency inside the pinned envelope),
   - OFP projector freshness is healthy enough to keep applying events (vents_applied=142,938, missing_features=0, stale_graph_version=0),
   - yet snapshots_built stays pinned at 0 while snapshot_failures climbs from 0 to 5,
   - DF then records OFP_UNAVAILABLE, FEATURE_GROUP_MISSING:core_features, ACTIVE_BUNDLE_INCOMPATIBLE, and quarantines/fail-closes the few current-run decisions that do appear,
   - downstream case_trigger, case_mgmt, and label_store remain starved rather than independently disproven.
3. The important implementation fact is that DF does **not** call the p-pr3-ofp pod over a network service. decision_fabric.worker builds OfpGetFeaturesService in-process, so snapshot materialization executes under the decision-lane pod identity, not the 
tdl pod identity.
4. I verified the runtime/IAM asymmetry in the current materialization posture:
   - p-pr3-ofp runs under ROLE_EKS_IRSA_RTDL,
   - p-pr3-df runs under ROLE_EKS_IRSA_DECISION_LANE,
   - Terraform currently grants ks_irsa_rtdl_object_store_rw (and 
tdl KMS access) to the 
tdl role only,
   - there is no equivalent object-store/KMS policy attached to the decision_lane IRSA role even though the shared dev_full profile pins ofp.snapshot_store_root = s3://fraud-platform-dev-full-object-store.
5. Production interpretation:
   - this is a real platform defect, not a harmless test quirk,
   - a production decision lane that embeds feature-snapshot materialization must carry the exact object-store and key permissions required by that materialization path,
   - otherwise the system appears healthy at the projector plane while the actual decision path remains unable to serve features under load,
   - that in turn falsely starves the case/label plane and would later contaminate learning/evolution evidence as well.
6. Alternatives considered:
   - externalize OFP serving into a separate RPC surface immediately: valid long-term architecture option, but too broad for the bounded PR3-S4 rerun and would add new failure surfaces before we have closed the current state,
   - relax DF or registry contracts again: rejected because the blocker is now identity/IAM drift, not semantic policy,
   - continue rerunning soak without fixing IAM: rejected as resource waste because the state is deterministically red until the embedded OFP writer can access the object store.
7. Selected remediation for the active boundary:
   - extend ROLE_EKS_IRSA_DECISION_LANE with the same object-store read/write bucket/object permissions already pinned for ROLE_EKS_IRSA_RTDL,
   - extend the decision-lane role with the same core KMS rights needed to read/write encrypted object-store artifacts,
   - apply that change live in a bounded Terraform step,
   - then rerun the exact PR3-S4 boundary and inspect whether DF/case-label deltas materialize or whether a second-order serve-path issue remains.
8. Evidence expectation after remediation:
   - snapshots_built must rise above 0 during the soak,
   - snapshot_failures must stop monotonically climbing,
   - DF should stop emitting OFP_UNAVAILABLE for the corrected path,
   - only after that can case_trigger, case_mgmt, and label_store be judged honestly on the same run scope.

## Entry: 2026-03-08 11:14:30 +00:00 - Applied the bounded live IAM fix for the embedded DF/OFP snapshot path before rerunning the same S4 boundary
1. I validated the Terraform configuration in infra/terraform/dev_full/runtime after adding the decision-lane policies.
2. I then created a targeted plan against exactly two resources only:
   - ws_iam_role_policy.eks_irsa_decision_lane_core_kms
   - ws_iam_role_policy.eks_irsa_decision_lane_object_store_rw
3. The plan was intentionally bounded because the runtime stack still carries unrelated drift and this remediation only needed the live identity/IAM lane corrected for the active PR3-S4 blocker.
4. Apply completed successfully:
   - created raud-platform-dev-full-irsa-decision-lane-core-kms,
   - created raud-platform-dev-full-irsa-decision-lane-object-store-rw.
5. Production effect of that live change:
   - the decision-lane pod identity now has the same object-store bucket/object rights and core KMS rights that the embedded OFP materializer requires to publish snapshots to s3://fraud-platform-dev-full-object-store,
   - this removes the identity drift between the projector lane (
tdl) and the decision-serving lane (decision-lane) for the shared OFP snapshot path.
6. I did not broaden the change beyond the active blocker:
   - no workflow contract changed,
   - no runtime thresholds changed,
   - no state rollup logic changed,
   - no unrelated runtime drift was applied.
7. Exact next step remains fail-closed and bounded:
   - rerun strict PR3-S4 on the same authority boundary,
   - inspect whether snapshots_built becomes non-zero, snapshot_failures stops climbing, DF stops surfacing OFP_UNAVAILABLE, and whether the case/label plane finally receives material current-run participation.
## 12:02:00 +00:00
- Re-read the latest `PR3-S4` production/road-to-prod authorities and the strict rerun artifacts before editing again.
- Confirmed the active red is no longer OFP snapshot IAM or registry-scope drift.
- Live DF reconciliation for `platform_20260308T110940Z` now shows the decision lane is being clamped by degrade posture:
  - `ACTIVE_BUNDLE_INCOMPATIBLE`
  - `CAPABILITY_BLOCK:feature_group=core_features`
  - `CAPABILITY_MISMATCH:allow_model_primary`
  - `FALLBACK_EXPLICIT`
  - `CONTEXT_STATUS:CONTEXT_BLOCKED`
- Live DL surfaces confirm the whole-plane cause:
  - `decision_mode=FAIL_CLOSED`
  - `bad_required_signals=["ofp_health"]`
- Live OFP surfaces contradict that level of severity:
  - `checkpoint_age_seconds ~= 0.01`
  - `snapshot_failures = 0`
  - `events_applied = 148280`
  - `missing_features = 1`
  - `health_reasons = ["WATERMARK_TOO_OLD"]`
- Conclusion recorded in impl map:
  - DL is currently too strict for production because it turns a tiny aggregate OFP feature miss inside an otherwise healthy replay/soak window into a whole-plane `FAIL_CLOSED` posture.
  - Case/label pods are alive, but their workers do not emit run-scoped idle surfaces early enough, so `PR3-S4` sees them as missing instead of healthy-idle.
- Next bounded fix:
  - narrow DL shared OFP health to material failures only,
  - seed case-trigger/case-mgmt scenario identity explicitly and export zero-state surfaces,
  - keep the rerun boundary exact at `PR3-S4`.
## 12:18:00 +00:00
- Completed the bounded PR3-S4 remediation code slice before touching runtime packaging again.
- `degrade_ladder.worker` now treats shared OFP health as whole-plane red only at materially severe thresholds rather than on any single aggregate missing-feature count.
- `case_trigger`, `case_mgmt`, and `label_store` now accept explicit scenario-run seeding so the case/label plane can export run-scoped idle surfaces without waiting for first live trigger traffic.
- `pr3_rtdl_materialize.py` now propagates `ACTIVE_SCENARIO_RUN_ID`, `CASE_TRIGGER_SCENARIO_RUN_ID`, and `CASE_MGMT_SCENARIO_RUN_ID` into the shared runtime env/secret surface.
- Local validation passed before rebuild:
  - `pytest tests/services/degrade_ladder/test_phase7_worker_observability.py -q`
  - `python -m py_compile` over the touched workers/materializer.
- Interpretation:
  - this is a production-hardening correction, not a waiver;
  - the decision plane remains fail-closed for materially bad OFP states,
  - but the platform should no longer self-collapse on a tiny aggregate OFP miss while simultaneously misreporting the case/label plane as absent.
- Next step:
  - rebuild/publish the shared runtime image,
  - rerun the exact strict `PR3-S4` boundary,
  - judge the result by cross-plane impact metrics instead of a spine-only rollup.
## 12:28:00 +00:00
- Packaging run `22820719910` completed successfully for commit `b11cc20979eb2359aeadd824c0576682e4a7b279`.
- New immutable platform image digest resolved: `230372904534.dkr.ecr.eu-west-2.amazonaws.com/fraud-platform-dev-full@sha256:47b559549016138b081f9fac1276f5b37b4574b46aa0acc41d80d981c91c8551`.
- I stopped short of a local ECS task-definition mutation because that would push PR3-S4 back into local orchestration rather than a remote, auditable, production-style execution.
- Diagnosis of the state contract:
  - `dev_full_pr3_s4_soak.yml` already checks out the branch ref for code,
  - but it still materializes RTDL from the live WSP task-family image unless explicitly overridden,
  - and it launches WSP replay lanes from the latest active task-family revision.
- Chosen fix:
  - patch the PR3-S4 workflow itself to accept an explicit immutable image URI,
  - repin the WSP ECS family inside GitHub Actions,
  - pass the same digest into runtime materialization so the whole state stays digest-pinned and fully remote.
## 12:55:00 +00:00
- Strict `PR3-S4` rerun `22820789580` completed with materially strong soak metrics but not state closure.
- Green impact metrics from the rerun:
  - admitted throughput `3028.987 eps` vs `3000 eps`
  - `4xx=0`, `5xx=0`, `error_rate=0`
  - `latency_p95=102.20 ms`, `latency_p99=123.14 ms`
  - OFP/IEG/DLA lag and checkpoint posture remained inside the state thresholds
  - DL stayed `NORMAL` / `GREEN`
  - archive backlog remained `0`
- Cross-plane improvement from the remediation:
  - `case_trigger` now emits run-scoped healthy-idle artifacts
  - `case_mgmt` now emits run-scoped healthy-idle artifacts
- Remaining red:
  - `label_store` still missing run-scoped artifacts
  - `DF` still health-red with `publish_quarantine_total=6`, `fail_closed_total=1`, `decisions_total=6`
  - `case_trigger` still unexercised because DF did not produce usable downstream decisions
  - S4 workflow still lacks required dependency/schema/cost-idlesafe drill materialization
- Next work stays bounded to S4:
  - fix label-store startup export
  - inspect and remediate DF semantic quarantine cause
  - add the missing S4 drill lanes to the workflow before rerunning the exact state.
## 13:35:00 +00:00
- Re-read the active `PR3-S4` runtime code and workflow surfaces before changing anything else.
- Confirmed the remaining red is now a production-topology defect, not just a retry budget issue:
  - `decision_fabric`, `action_layer`, and `case_trigger` still send internal canonical events back through `IG_INGEST_URL`.
  - Those same event classes already have pinned class-map + partitioning profiles for direct native-bus routing.
- Chosen production fix recorded in the impl map:
  - move internal downstream hops (`DF`, `AL`, `CaseTrigger`) onto a shared internal event-bus publisher that reuses the existing IG routing contract,
  - keep WSP/external ingress on IG,
  - add explicit `label_store` startup export so healthy-idle label surfaces are visible at run start,
  - extend the S4 workflow with the required dependency/schema/cost-idlesafe drill lanes before the next strict rerun.
- Rationale:
  - internal plane hops should not spend the public ingress boundary under production load,
  - simply raising timeouts would hide the wrong topology instead of correcting it.
## 14:20:00 +00:00
- Completed the local PR3-S4 remediation slice before any remote packaging/rerun.
- Implemented a shared internal canonical-event publisher (`platform_internal_publish.py`) so `decision_fabric`, `action_layer`, and `case_trigger` can route internal downstream events onto the native bus using the existing IG class-map + partitioning contract.
- `pr3_rtdl_materialize.py` now forces `DF`, `AL`, and `CASE_TRIGGER` into `internal_bus` mode for the PR3 runtime so internal plane hops stop hairpinning through `IG_INGEST_URL`.
- `label_store.worker` now exports run-scoped startup artifacts immediately when run identity is already pinned.
- Added the missing S4 drill implementations:
  - `pr3_s4_dependency_drill.py`
  - `pr3_s4_schema_drill.py`
  - `pr3_s4_cost_guardrail.py`
- Patched `dev_full_pr3_s4_soak.yml` to execute those drills before rollup and patched `pr3_s4_rollup.py` to consume real drill/cost receipts instead of hardcoding those lanes red.
- Local validation results:
  - `python -m py_compile` passed for all touched runtime/drill files
  - `python -m pytest ...` passed: `30 passed`
  - workflow YAML parse passed
- Interpretation:
  - the next S4 rerun will now test the real remaining runtime semantics instead of being blocked by missing certification lanes or the wrong internal publish topology.
## 15:05:00 +00:00
- Re-read the latest strict `PR3-S4` artifacts and current branch code before editing again.
- Confirmed the remaining red is no longer an ingress/backbone problem:
  - S4 throughput/latency/error posture is still green,
  - the active red has moved into decision-plane semantics plus case/label participation.
- Verified current branch state:
  - `case_trigger._extract_platform_run_id()` already handles direct `pins.platform_run_id`,
  - so I will not reapply the earlier suspected run-scope patch blindly.
- Recorded the material next diagnosis in the implementation map:
  - OFP health is still too coarse for production because absolute cumulative `missing_features` and replay watermark age can force whole-plane red even when lag/checkpoint and snapshot integrity are healthy,
  - that posture propagates into DL required-signal failure and DF fail-closed behavior,
  - `case_trigger` and `decision_log_audit` still need bounded diagnosis on their current dark-lane behavior.
- Next bounded work:
  - refit OFP health to materially meaningful severity,
  - finish the live case-trigger/DLA intake diagnosis,
  - rerun the exact strict `PR3-S4` boundary once those concrete defects are corrected.
## 15:32:00 +00:00
- Applied the next bounded PR3-S4 remediation slice locally before any remote rerun.
- `case_trigger.worker` Kafka intake no longer strips canonical RTDL envelopes down to the nested business payload. This keeps event type, schema identity, and top-level pins intact on the case/label boundary.
- `online_feature_plane.observability` now emits derived runtime metrics (`event_basis`, `missing_feature_rate`) and evaluates missing-feature severity by material rate, not only cumulative count.
- Replay-only stale watermark posture is now treated as advisory when checkpoint freshness, snapshot integrity, and miss-rate severity remain healthy.
- `degrade_ladder.worker` now mirrors that OFP severity logic so a tiny miss rate no longer forces whole-plane `FAIL_CLOSED` while materially bad miss rates still do.
- Added regression coverage for:
  - case-trigger Kafka canonical-envelope preservation,
  - OFP low miss-rate replay health,
  - DL fail-closed only on material OFP miss severity.
- Local validation:
  - `python -m py_compile` passed for the touched runtime files and tests,
  - targeted pytest passed: `16 passed`.
- Next step:
  - rebuild/publish the shared runtime image,
  - rerun strict `PR3-S4`,
  - inspect whether the remaining red moves from semantic artifacts to genuine downstream defects.
## 15:12:00 +00:00
- Live PR3-S4 diagnosis moved from symptoms to exact downstream defects.
- `case_trigger` is consuming real `fp.bus.rtdl.v1` records for the active run, and direct trigger adaptation/publish on a sampled `decision_response` succeeds.
- Actual blocker: `required_platform_run_id` is being left as the literal nested token `${CASE_TRIGGER_REQUIRED_PLATFORM_RUN_ID:-${ACTIVE_PLATFORM_RUN_ID:-}}`, so the worker silently run-scope skips every real record before metrics/publish.
- Confirmed adjacent risk: `case_mgmt` and `label_store` use the same single-pass env-token parser with the same nested default syntax, so the same silent starvation pattern can recur downstream.
- `DLA` has a separate authority defect in addition to the same resolver risk:
  - its intake policy is still pinned to `fp.bus.traffic.fraud.v1`,
  - but S4 production closure requires decision/action lineage on the RTDL stream, not upstream traffic ingestion.
- Next bounded remediation slice:
  - patch nested env/default resolution for the downstream case-label/DLA workers,
  - repin DLA intake to `fp.bus.rtdl.v1`,
  - add focused regression tests,
  - then rebuild and rerun strict `PR3-S4` on the same boundary.
## 15:18:00 +00:00
- Cleaned transient artifact clutter before resuming platform work.
- Removed repo-root transient directories: prior `temp_*`, `temp_pr3s4_*`, and `tmp` debug/download folders.
- Removed stale scratch artifact-download directories under `scratch_files/` for prior `m1`, `pr3_s2`, `pr3_s3`, and `rc2r2` runs.
- Removed clearly temporary `runs/tmp*` directories only.
- Preserved authoritative evidence roots under `runs/` such as `dev_substrate`, `fraud-platform`, `github_actions`, and the historical local run roots.
- Repo root is now back to authoritative content plus active source changes, without scattered transient run copies.
## 15:37:00 +00:00
- Repinned the active runtime-cert execution method from soak-first to `correctness -> stress -> soak`.
- Reason: recent PR3 failures were mostly bounded correctness defects that should have been caught cheaply before any long expensive run.
- New rule being written into the active authorities:
  - no soak authorization until a bounded integrated whole-platform correctness window is green,
  - no soak authorization until a short higher-pressure stress window is also green,
  - fail-fast remains mandatory in all three windows.
- Operational consequence for active work:
  - `PR3-S4` will resume as a bounded whole-platform correctness gate first,
  - long soak is blocked until that gate and the following stress window both pass.
## 16:10:00 +00:00
- Validated the bounded downstream fix slice locally before any new remote run:
  - targeted pytest for case-trigger, case-mgmt, label-store, and DLA now passes: `19 passed`.
  - `python -m py_compile` for the touched runtime files/tests also passes.
- Re-read the live `PR3-S4` workflow and rollup against the updated road-to-prod method.
- Confirmed execution drift still exists:
  - plan and PR3 authority now require `correctness -> stress -> soak`,
  - but `dev_full_pr3_s4_soak.yml` still launches the 30-minute soak window first,
  - and `pr3_s4_rollup.py` still scores long-duration soak artifacts instead of a bounded whole-platform correctness gate.
- Chosen correction:
  - repin `S4` execution to a bounded integrated correctness run,
  - emit explicit correctness scorecard / cross-plane report / soak authorization artifacts,
  - block long soak entirely inside `S4`; that expensive work now belongs only after correctness and stress are green.
## 16:36:00 +00:00
- Completed the local PR3-S4 execution refit from soak-first to bounded correctness-first.
- Local validation remains green after the downstream case-label/DLA fixes:
  - targeted pytest: `19 passed`
  - `python -m py_compile` passed for touched workers and new S4 scripts.
- Generalized the S4 drill scripts so they can read bounded correctness artifacts instead of only the old soak artifact family.
- Added `scripts/dev_substrate/pr3_s4_correctness_rollup.py` to emit:
  - `g3a_correctness_scorecard.json`
  - `g3a_correctness_component_snapshot.json`
  - `g3a_correctness_cross_plane_report.json`
  - `g3a_drill_replay_integrity.json`
  - `g3a_soak_authorization.json`
  - `pr3_s4_execution_receipt.json`
- Refitted `dev_full_pr3_s4_soak.yml` so the live state now runs a bounded correctness window (`8` lanes, `600 eps`, `180 s`, `30 s` warmup) instead of launching the old 30-minute soak.
- Workflow validation passed (`yaml_ok`), and the new correctness rollup CLI parses cleanly.
- Next step: refresh the shared platform image so the live runtime actually includes the corrected downstream worker logic before the bounded correctness rerun.
## 16:30:54 +00:00
- Reclassified the first bounded `PR3-S4` correctness run as diagnostic only, not authoritative, because the local chain had ignored a red warm gate and two cheap drill artifacts were missing for harness reasons.
- Corrected the cheap evidence layer first:
  - `pr3_s4_schema_drill.py` now self-loads `src/` without ad hoc `PYTHONPATH`.
  - `pr3_s4_correctness_rollup.py` now treats fresh-run missing pre-counters as zero baselines.
  - `CSFB` replay-only `WATERMARK_TOO_OLD` now scores as advisory when join/conflict/failure posture is otherwise clean.
  - `S4` now synthesizes the lag-recovery drill from the already-bound `PR3-S3` recovery artifacts when needed.
- Result of that correction pass:
  - synthetic blockers dropped away,
  - `AL` no longer looks fundamentally dead once the live runtime is inspected directly,
  - `DLA` remains the real downstream red lane.
- Live runtime findings after reopening the same active `platform_run_id`:
  - `AL` is materially active (`intake_total=3876`, `outcome_executed_total=3876`, `publish_admit_total=3876`, health `GREEN`).
  - `case_trigger`, `case_mgmt`, and `label_store` continued advancing and stayed `GREEN`.
  - `DLA` remained at `append_success_total=0`, `candidate_total=0`, and no lineage state.
- Root cause found for `DLA`:
  - fixed `stream_id=dla.intake.v0` caused checkpoint reuse across runs,
  - fresh required-run consumers were being forced back to `earliest` even though `dev_full` pins `event_bus_start_position=latest`,
  - the worker therefore spent its bounded window burning old RTDL backlog and recording `RUN_SCOPE_MISMATCH` instead of reaching the current run scope.
- Applied code correction:
  - `DLA` stream id is now run-scoped with `::{required_platform_run_id}` when needed.
  - `DLA` fresh required-run consumers now honor pinned `latest` instead of being forced to `earliest`.
  - `AL` got the same fresh-run start-position correction.
- Local validation after the fix stayed green:
  - targeted pytest for `action_layer` and `decision_log_audit`: `3 passed`
  - `python -m py_compile` passed for the touched runtime files.
- Next step: rebuild/publish the shared image, rematerialize PR3, and rerun bounded `PR3-S4` correctness from a truly green warm boundary.
## 18:05:00 +00:00
- Material drift found while extending `PR3-S4` to whole-platform correctness.
- Active bounded-correctness run scope is `platform_20260308T155913Z` from `g3a_correctness_wsp_runtime_manifest.json`.
- Object store listing for `s3://fraud-platform-dev-full-object-store/platform_20260308T155913Z/` shows only:
  - `archive/`
  - `ig/`
  - `online_feature_plane/`
- There is no `sr/` prefix and no `obs/` prefix for that active run.
- Consequence:
  - current synthetic PR3 runtime identity cannot support real `OFS/MF/MPR` + reporter/governance correctness,
  - learning/evolution and ops/gov would remain structurally unprovable even if the hot path stays green.
- Classified as blocker-grade drift because the active PR3 run identity is not control-plane-authored and therefore lacks the artifacts required for whole-platform closure.
- Documented remediation candidates in the implementation note:
  - repin PR3 run creation to a control-plane-authored active run id, or
  - add an explicit pre-correctness control-plane bootstrap that seeds `sr/` and `obs/` artifacts for the active run.
## 16:54:57 +00:00
- Reframed the active `PR3-S4` correction as a whole-platform run-authoring problem, not another hot-path-only runtime fix.
- Confirmed the real production gap:
  - S4 currently fabricates `platform_run_id` / `scenario_run_id` inside the workflow,
  - that lets the runtime spine participate,
  - but it leaves learning/evolution and ops/gov structurally unprovable because the active run has no authentic `sr/` or `obs/` roots.
- Verified the repo already has bounded run surfaces for the missing planes:
  - `scenario_runner` can author a run against the pinned oracle-store engine root,
  - `platform_reporter.worker` and `platform_conformance.worker` can emit run-scoped `obs/` artifacts,
  - `offline_feature_plane.worker` and `model_factory.worker` expose bounded `enqueue-*` and `run --once` paths.
- Chosen correction:
  - bootstrap S4 from a control-plane-authored run id,
  - export that authored run scope to runtime + downstream planes,
  - run bounded reporter/conformance + OFS/MF participation on the same scope,
  - then let S4 score whole-platform correctness directly before any stress/soak authorization.
- Rejected weaker alternatives:
  - synthetic `sr/obs` backfill around a fake run id,
  - or deferring learning/obs-gov proof out of S4.
- Next implementation slice:
  - add the PR3 control-plane bootstrap script,
  - repin the S4 workflow to consume its authored run outputs,
  - expand the correctness rollup to require direct learning and ops/gov proof.
## 19:10:00 +00:00
- Pinned the next `PR3-S4` correction slice to the cheaper correctness ladder instead of another broad expensive run.
- Chosen implementation shape for this slice:
  - author the active run through `scenario_runner`,
  - feed the authored run scope into the existing RTDL/case-label/runtime steps,
  - execute bounded learning (`OFS/MF`) and ops/gov proofs on that same run scope,
  - and only then decide whether any larger stress/soak window is authorized.
- Explicitly chose the remote workflow runner as the bounded downstream execution surface for this correction because it is cheaper than immediately expanding more always-on runtime, while still using real AWS object-store / Aurora / MSK authorities. This is not a return to laptop-local orchestration.
- Also pinned two missing material fixes before execution:
  - add the missing canonical `MF` training/governance profile files that the `dev_full` learning lane already points at,
  - replace synthetic run-id minting in `dev_full_pr3_s4_soak.yml` with a strict Scenario Runner bootstrap step.
- Next code edits from here:
  - add `pr3_control_plane_bootstrap.py`,
  - add bounded `S4` learning/ops-gov scripts,
  - update the workflow + rollup to require their direct receipts.

## 2026-03-08 19:35:00 +00:00
- Continued PR3-S4 under the bounded whole-platform correctness method.
- Recorded the concrete implementation slice before coding: remove synthetic S4 run minting, add Scenario Runner control bootstrap, add direct bounded learning proof, add direct bounded ops/gov proof, and make those receipts mandatory in the S4 rollup.
- Chose the remote workflow runner as the first bounded execution surface for learning/governance because it is cheaper and stricter than broadening the live runtime set before basic whole-platform continuity is proven.

## 2026-03-08 20:10:00 +00:00
- Implemented the PR3-S4 bounded whole-platform correction slice.
- Added `pr3_control_plane_bootstrap.py` so S4 now authors `sr/run_status` and `sr/run_facts_view` on the active run before replay proceeds.
- Added direct bounded learning proof (`pr3_s4_learning_bound.py`) using active-run archive + label truth -> OFS -> MF -> MPR.
- Added direct bounded ops/gov proof (`pr3_s4_ops_gov_bound.py`) using run-scoped conformance + observability root checks + uploaded `obs/` receipts.
- Added missing MF profile pins and updated the S4 workflow and rollup so control, learning, and ops/gov are hard blockers rather than proxy notes.
- Ran local cheap validation only: python compile for the new scripts and YAML parse for the updated workflow/profile files both passed.
## 17:26:24 +00:00
- Ran the first remote bounded `PR3-S4` correctness workflow on `cert-platform` (`22826024606`) with the pinned dev_full OIDC role.
- The run failed fast in 41s, which is acceptable cost posture, but the red surface was still harness-side rather than platform-side.
- Exact failures captured from `gh run view --log-failed`:
  - `Bootstrap active run through Scenario Runner`: `ModuleNotFoundError: No module named 'pydantic'`.
  - `Capture post-correctness runtime snapshot`: kubeconfig absent after bootstrap failure, so `kubectl` hit `localhost:8080`.
  - `Build PR3-S4 correctness cost and idle-safe receipts`: script/ workflow argument skew (`--artifact-prefix`, `--receipt-path` not recognized by the committed branch copy).
  - `Build PR3-S4 correctness rollup`: missing correctness runtime summary file caused `FileNotFoundError` instead of a single blocker receipt.
- Decision for the next correction slice:
  - harden the workflow dependency install set,
  - gate post-failure snapshots on kubeconfig success,
  - commit the newer cost-guardrail contract already expected by the workflow,
  - make the rollup fail closed with blocker receipts when upstream correctness artifacts are missing.
- This is a cheap harness correction, not another platform redesign. The point is to ensure the next rerun surfaces only real control/runtime/learning/case-label/ops blockers.
## 17:31:30 +00:00
- Reran bounded `PR3-S4` after harness hardening as GitHub Actions run `22826105594`.
- Harness stayed clean; only `Bootstrap active run through Scenario Runner` failed.
- Artifact inspection showed the first real blocker:
  - `s3:GetObject AccessDenied` for `s3://fraud-platform-dev-full-object-store/oracle-store/local_full_run-7/a3bd8cac9a4284cd36072c6b9624a0c1/run_receipt.json`
  - principal: `GitHubAction-AssumeRoleWithAction`
- Decision:
  - do not broaden the CI role indiscriminately,
  - extend the existing managed policy `GitHubActionsPR3RuntimeDevFull` with read-only oracle-store bucket/object permissions through Terraform,
  - then rerun the same bounded `PR3-S4` gate.
- This is now a real control-plane/IAM blocker, not harness noise.
## 17:34:30 +00:00
- Applied the real blocker fix live through Terraform in `infra/terraform/dev_full/ops`.
- Change made to managed policy `GitHubActionsPR3RuntimeDevFull`:
  - read-only bucket access on `fraud-platform-dev-full-object-store` limited to `oracle-store` prefixes,
  - `s3:GetObject` on `fraud-platform-dev-full-object-store/oracle-store/*`.
- Validation:
  - `terraform fmt` passed,
  - targeted `terraform plan` showed exactly one in-place policy update,
  - targeted `terraform apply` completed successfully (`0 added, 1 changed, 0 destroyed`).
- Purpose of the fix:
  - allow Scenario Runner control bootstrap to read the canonical oracle run receipt by reference,
  - without broadening CI to arbitrary object-store write power.
- Next step: rerun bounded `PR3-S4` unchanged and continue to the next real blocker.
## 17:35:30 +00:00
- Reran bounded `PR3-S4` after the oracle-store IAM fix (`22826199850`).
- The S3 access denial is resolved.
- New blocker is now a code defect in `pr3_control_plane_bootstrap.py`:
  - `unhashable type: 'list'`
  - caused by the completeness check using `value in {"", [], None}` while `traffic_output_ids` is a list.
- Decision: patch the bootstrap script with a safe missing-value predicate and rerun the same bounded S4 gate.
## 18:01:00 +00:00
- First in-VPC `PR3-S4` bounded rerun (`22826528068`) proved the main control-topology correction:
  - bootstrap no longer fails at GitHub-runner -> private-MSK DNS/reachability,
  - the remote worker emitted real SR events through `PLAN_COMMITTED` before dying.
- Meaning:
  - the runner-edge/network blocker is cleared,
  - the remaining red is now inside the remote `Scenario Runner` execution path after plan commit.
- Problem with the current receipt quality:
  - the worker exited before printing its final JSON summary,
  - the orchestrator therefore only captured a generic `EKS_JOB_FAILED` plus a short log excerpt.
- Follow-up correction being applied now:
  - capture pod/container termination state before cleanup,
  - preserve a longer log/describe excerpt in `g3a_control_plane_bootstrap.json`,
  - widen the temporary bootstrap job to `500m/1Gi request` and `2 CPU/2Gi limit` so the next rerun can either clear a simple container kill or return an exact terminated-state reason.
- This is still the same exact rerun boundary (`PR3-S4` bootstrap only), not a broader runtime rerun.
## 17:47:27 +00:00
- Diagnosed the active PR3-S4 control-bootstrap blocker as a topology defect, not a missing package or IAM read gap.
- The GitHub runner is currently trying to author Scenario Runner truth by talking directly to private MSK brokers; that is not production-valid and the live failure (Failed to resolve ... kafka-serverless) confirms it.
- Checked the live Step Functions authority raud-platform-dev-full-platform-run-v0; it is only a trivial Pass state and cannot materialize sr/run_status / sr/run_facts_view for S4.
- Chosen remediation:
  - keep GitHub Actions as the outer orchestrator,
  - move SR authoring into the VPC as a one-shot EKS Job using the platform image,
  - reuse the existing pinned 
tdl IRSA trust surface for the temporary bootstrap job,
  - collect the worker JSON back into g3a_control_plane_bootstrap.json locally after Job completion.
- Reason for this choice:
  - no new direct GitHub-runner reach into private MSK,
  - no broad new IAM surface,
  - cheaper repeated execution than packaging a temporary Lambda on every bounded rerun,
  - preserves the real control-plane ordering needed by learning and ops/gov.
- Next step is code refit of the bootstrap path plus moving kubeconfig earlier in the S4 workflow so the bootstrap can use the cluster before runtime materialization.
## 18:00:37 +00:00
- First in-VPC PR3-S4 bounded rerun (22826528068) proved the main control-topology correction:
  - bootstrap no longer fails at GitHub-runner -> private-MSK DNS/reachability,
  - the remote worker emitted real SR events through PLAN_COMMITTED before dying.
- Meaning:
  - the runner-edge/network blocker is cleared,
  - the remaining red is now inside the remote Scenario Runner execution path after plan commit.
- Problem with the current receipt quality:
  - the worker exited before printing its final JSON summary,
  - the orchestrator therefore only captured a generic EKS_JOB_FAILED plus a short log excerpt.
- Follow-up correction being applied now:
  - capture pod/container termination state before cleanup,
  - preserve a longer log/describe excerpt in g3a_control_plane_bootstrap.json,
  - widen the temporary bootstrap job to 500m/1Gi request and 2 CPU/2Gi limit so the next rerun can either clear a simple container kill or return an exact terminated-state reason.
- This is still the same exact rerun boundary (PR3-S4 bootstrap only), not a broader runtime rerun.
## 18:09:44 +00:00
- Inspected the bounded `PR3-S4` in-VPC bootstrap failure at the code-path level rather than spending on another broad rerun.
- Confirmed the pod reaches `PLAN_COMMITTED` and then dies with `OOMKilled`, so the remaining blocker is the heavy post-plan `ScenarioRunner` path rather than network, IAM, or GitHub-runner topology.
- Decision for the next correction slice:
  - keep Scenario Runner as the truth author,
  - but switch the in-VPC worker from full `submit_run()` to a lightweight internal path that anchors the run, commits the plan, and commits `READY` directly against the authoritative oracle pack by reference.
- Reason:
  - `PR3-S4` bounded correctness needs a real `sr/run_status` + `sr/run_facts_view` for the active run scope,
  - it does not need a redundant expensive oracle evidence rescan before learning/ops-gov can prove whole-platform continuity,
  - this is the cheaper and more production-aligned boundary before any future stress/soak work.
- Next step is to patch the worker, validate it locally, and rerun the same bounded `PR3-S4` workflow boundary.
## 18:26:30 +00:00
- Bounded rerun `22826852334` materially cleared the previous blocker class:
  - control bootstrap green,
  - runtime materialization green,
  - correctness campaign green at `659.93 eps`, `118,788` admitted, `0` 4xx, `0` 5xx, `p95=34.83ms`, `p99=50.15ms`.
- The next red lane is isolated to `Run PR3-S4 bounded learning proof`.
- The summary receipt for that lane reports `connection timeout expired`, which is analytically consistent with the script trying to reach private Aurora from the GitHub runner.
- Decision:
  - do not widen runner-edge network reach,
  - move bounded learning proof into the VPC as a one-shot EKS job and return only the summary receipt to the runner.
- This is the same production-valid correction pattern already used to fix control bootstrap.
## 18:42:20 +00:00
- Reran bounded `PR3-S4` after moving learning into the VPC (`22827162362`).
- The Aurora timeout class is now closed.
- New precise blocker from the learning pod:
  - missing local authority file `docs/model_spec/platform/migration_to_dev/dev_full_handles.registry.v0.md` inside the runtime image.
- Decision:
  - do not rebuild the whole image yet,
  - inject the required authority file into the temporary learning job and make the script honor an explicit override path.
- This keeps the correction small, cheap, and specific to the now-proven blocker.
## 18:57:40 +00:00
- Reran bounded `PR3-S4` after injecting the learning registry authority file (`22827403598`).
- New learning blocker is not network and not missing docs anymore.
- The pod still assumed the runner-local `g3a_control_plane_bootstrap.json` existed on its filesystem.
- Decision:
  - remove that boundary leak,
  - pass `run_facts_ref` directly into the in-VPC learning job,
  - and make the learning script prefer explicit run-facts pins over local bootstrap-file discovery.
## 20:35:00 +00:00
- Inspected the active `PR3-S4` runtime live instead of rerunning blind.
- Found the real upstream blocker for the learning red:
  - `case_trigger`, `case_mgmt`, and `label_store` are loading literal nested env tokens for `required_platform_run_id` from the mounted runtime profile rather than the active run id.
  - This makes the case/label plane deterministically skip the active run, so the later learning proof sees zero labelled subjects.
- This is a mounted-profile materialization defect plus a warm-gate coverage hole, not a new OFS/MF/MPR semantic failure.
- Chosen remediation for the next slice:
  - materialize concrete run-scoped values into the mounted runtime profile during `pr3_rtdl_materialize.py`,
  - extend the warm gate to interrogate downstream worker config before any traffic window begins,
  - rerun the same bounded `PR3-S4` boundary only after those cheaper checks are green.
## 20:50:00 +00:00
- Found a second upstream runtime-authority defect while inspecting the active `PR3-S4` pods.
- Live `DLA` is loading an older bundled policy with `admitted_topics = fp.bus.traffic.fraud.v1` instead of the repo authority `fp.bus.rtdl.v1`.
- Live DLA checkpoints confirm it is actually advancing on the traffic topic, which explains the current zero-participation on the decision audit lane.
- Remediation added to the current slice:
  - mount the authoritative DLA intake policy into the runtime bundle during materialization,
  - rewrite the active profile to that mounted path,
  - add a warm-gate probe so this drift fails before any future bounded traffic run.
## 21:05:00 +00:00
- Added the next explicit PR3-S4 design decision before code changes.
- Reasoning now pinned in the implementation map:
  - the warm gate must become the fail-fast sentinel for case/label and DLA runtime drift,
  - bounded correctness stays the active boundary,
  - no more paying for traffic windows just to rediscover deterministic pre-traffic config defects.
- Next action in this slice is code-level warm-gate enforcement plus local validation before any rerun.

## 21:18:00 +00:00
- Implemented the cheaper pre-traffic hardening for `PR3-S4`.
- `pr3_rtdl_materialize.py` now emits a concretely materialized active-run profile and mounts the repo-authoritative DLA intake policy into the runtime bundle.
- `pr3_runtime_warm_gate.py` now probes case-trigger, case-mgmt, label-store, and DLA before traffic and fails closed on run-scope/topic drift.
- Local validation passed: `python -m py_compile scripts/dev_substrate/pr3_rtdl_materialize.py scripts/dev_substrate/pr3_runtime_warm_gate.py`.
- Next step is the same bounded `PR3-S4` rerun, now with fail-fast coverage on the previously expensive downstream defects.
## 23:35:00 +00:00
- Diagnosed the active PR3-S4 learning red (REPLAY_EVENTS_SHORTFALL:0<10) as a bounded-proof cut defect, not a missing learning plane.
- Evidence from the active run:
  - recent labels (ORDER BY committed_at_utc DESC) did not intersect archived traffic replay rows,
  - earliest labels (ORDER BY committed_at_utc ASC) intersected immediately; the first 15 archived traffic rows matched labelled subjects.
- Production decision:
  - bounded correctness will use a stable archived-labelled prefix,
  - not the freshest tail of the run,
  - and it will still require real archived traffic replay for OFS -> MF -> MPR.
- Next patch is in scripts/dev_substrate/pr3_s4_learning_bound.py, followed by the same bounded PR3-S4 rerun only.
## 20:44:00 +00:00
- The stable-prefix replay fix moved the red lane forward; the next failure is now inside the bounded learning harness itself.
- Latest rerun reached OFS worker config load and failed with:
  - `OFS_RUN_SCOPE_INVALID:platform_20260308T202443Z:${OFS_REQUIRED_PLATFORM_RUN_ID:-${ACTIVE_PLATFORM_RUN_ID:-}}`
- Source inspection confirms the cause:
  - `scripts/dev_substrate/pr3_s4_learning_bound.py` still loads OFS/MF/MPR from raw `config/platform/profiles/dev_full.yaml`,
  - so the bounded learning lane is bypassing the already-correct PR3 runtime profile materialization path and reintroducing unresolved nested run-scope tokens.
- Decision:
  - do not widen the run,
  - do not rerun until this local harness defect is fixed,
  - patch the bounded learning script to materialize a concrete active-run profile and load OFS/MF/MPR from that file.
- This keeps the rerun boundary unchanged and avoids paying for another deterministic template-resolution failure.

## 23:24 Terraform teardown
- Teardown executed for `infra/terraform/dev_full/ops`, `data_ml`, `runtime`, and `streaming`; all four stacks are now empty.
- `core` was torn down selectively to preserve only the S3 buckets, S3 bucket controls, and KMS key/alias backing those retained buckets.
- Residual live dependencies outside the active Terraform states blocked VPC deletion and were removed directly: `fraud-platform-dev-full-archive-sink-v0` Lambda, its stale Kafka event-source mapping, `fraud-platform-dev-full-aurora` cluster + writer instance, Aurora subnet group/security group, and the orphan execute-api VPCE security group.
- Final `core` reconciliation removed stale Terraform state entries for the already-deleted VPC and MSK client security group. Remaining `core` state is now the retained buckets + KMS only.
## 23:58:00 +00:00
- Re-anchored `PR3-S4` to the actual `dev_full` build authorities before any further code changes.
- Read `M15` and pinned the missing constraint explicitly: `PR3-S4` cannot be treated as green unless learning/evolution uses real point-in-time data semantics and same-run evidence continuity, not bootstrap placeholders.
- Recorded the corrected whole-platform lane map in `platform.road_to_prod.impl_actual.md` against `M7/M8/M9/M10/M11/M12/M15` so the next remediations are tied to the already-built `dev_full` platform rather than to older conceptual/component reasoning.
- Next step is code-path inspection of the active `PR3-S4` runners/orchestrators against that corrected lane map before any bounded rerun is authorized.
## 00:10:00 +00:00
- Finished the first direct code-path inspection pass for the active `PR3-S4` proof surfaces.
- Found a concrete authority defect in `scripts/dev_substrate/pr3_s4_ops_gov_bound.py`:
  - it still proves `environment_conformance` through `local_parity/dev/prod` static profile comparison,
  - and it only checks four run roots (`sr`, `ig`, `archive`, `online_feature_plane`).
- That is too narrow for the corrected `PR3-S4` contract. It does not prove same-run whole-platform continuity for case/label and learning/evolution.
- Recorded the remediation plan in the implementation note: repin the ops/gov proof to `dev_full` same-run whole-platform evidence continuity, then patch the S4 rollup to consume and report that stronger proof before any rerun is authorized.
## 00:22:00 +00:00
- Applied the local `PR3-S4` proof correction for ops/gov and S4 reporting.
- `scripts/dev_substrate/pr3_s4_ops_gov_bound.py` no longer uses the static `local_parity/dev/prod` environment-parity checker for this bounded gate.
- The active S4 ops/gov proof now checks:
  - same-run bootstrap refs (`status_ref`, `facts_view_ref`),
  - required active-run roots (`sr`, `ig`, `archive`, `online_feature_plane`, `ofs`, `mf`, `obs`),
  - case/label snapshot coverage from the S4 runtime snapshots,
  - learning ref readback from the bounded OFS/MF/MPR summary.
- `scripts/dev_substrate/pr3_s4_correctness_rollup.py` now emits plane-level impact assessments and a bounded-goal assessment string so the state summary is not just a blocker dump.
- Local validation passed:
  - `python -m py_compile scripts/dev_substrate/pr3_s4_ops_gov_bound.py scripts/dev_substrate/pr3_s4_correctness_rollup.py`
## 00:28:00 +00:00
- Tightened `scripts/dev_substrate/pr3_rtdl_materialize.py` preflight so the case/label IAM lane is fail-fast.
- The materializer already depended on `ROLE_EKS_IRSA_CASE_LABELS` for the `case-labels` service account, but the required-handle gate did not declare it.
- Added `ROLE_EKS_IRSA_CASE_LABELS` to the required-handle check so the next live run cannot spend cloud time only to hit a deterministic missing case/label handle later in materialization.
- Revalidated:
  - `python -m py_compile scripts/dev_substrate/pr3_rtdl_materialize.py scripts/dev_substrate/pr3_s4_ops_gov_bound.py scripts/dev_substrate/pr3_s4_correctness_rollup.py`
