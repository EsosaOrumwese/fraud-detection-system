## 2026-03-22 00:00:00 +00:00 - Moved Appendix B interpretation out of image footers and into canonical markdown

Refined the Appendix B presentation posture so the analytical visuals stay compact and the explanatory interpretation lives in the canonical portfolio markdown instead of inside the image itself.

What changed:
- removed footer interpretation text from:
  - `appendix_b_panel_temporal_realism`
  - `appendix_b_panel_load_concentration`
  - `appendix_b_panel_supervision_realism`
- rerendered the three visuals with tighter image height
- appended `Appendix B — Governed World Analytical Panels` to `canonical-portfolio.md`
- placed each image inline in the markdown followed by a short interpretation block

Judgment:
- this is a better recruiter-facing posture because the charts now read as cleaner analytical exhibits while the markdown preserves the explanation immediately below each panel in preview mode

## 2026-03-22 03:49:00 +00:00 - Added Appendix G daily cost movement visual for the accepted proving window

The Section 10 ranked cost bar already answered which service families dominated the accepted proving window, but it did not show how spend moved day by day inside that same bounded window. I added an appendix-grade complementary visual instead of replacing the main bar.

What changed:
- created `build_portfolio_cost_appendix_panels.py`
- generated `appendix_g_cost_window_daily_breakdown` as PNG, SVG, and source CSV
- scoped the chart to the accepted proving window only: `2026-03-10` through `2026-03-13`
- used a two-part view:
  - daily total Cost Explorer line
  - daily stacked service-family bars
- wired the image and interpretation into `canonical-portfolio.md` under `Appendix G`

Judgment:
- this is the right complement to the existing Section 10 bar chart because it answers a different question: not just what dominated total spend, but how daily spend moved and which platform families were active on each day of the accepted proof window

## 2026-03-22 04:03:00 +00:00 - Replaced the appendix cost stacked bars with a daily-total plus service matrix view

The user was right to reject the first appendix cost breakdown. A stacked daily service-family bar chart was the wrong fit for four days times many service families: it hid exact values and collapsed the service mix into unreadable layers.

What changed:
- rebuilt `appendix_g_cost_window_daily_breakdown` from `scratch_files/costs.csv`
- kept the daily total line for `2026-03-10` through `2026-03-13`
- replaced the lower stacked bars with a heatmap-style service matrix plus a right-side window-total column
- grouped the long tail into `Remaining low-cost services`
- updated the `Appendix G` interpretation in `canonical-portfolio.md` so it matches the new matrix view

Judgment:
- this is the correct chart form for this data shape because it preserves the daily movement story while keeping the dominant service families and their exact daily dollar values legible

## 2026-03-22 12:02:00 +00:00 - Reframed Appendices D, E, and F as doctrine and evidence tables instead of forced visuals

The user was right to push back on the earlier appendix treatment. Production readiness here is not best represented as a visual-first artifact. The stronger appendix form is explanatory and evidentiary: define the readiness doctrine, show how that doctrine was applied by plane, then show what the accepted integrated and widened authorities achieved after that standard had already been earned.

What changed:
- added `Appendix D` to `canonical-portfolio.md` as the explicit readiness contract
- added `Appendix E` as the plane-by-plane readiness application table
- added `Appendix F` as the accepted integrated versus widened authority comparison table
- removed the unused generated D/E render assets and the supporting script so the appendix stays aligned with the chosen representation

Judgment:
- this is the better appendix posture because it treats D/E/F as evidence surfaces rather than as arbitrary graphics
- the appendix now makes clear that readiness was a multi-family promotion rule, not a shorthand for EPS or ingestion speed

## 2026-03-22 19:10:30 +00:00 - Split the mixed post-cert branch into a clean engineering-only promotion scope before opening the next `dev -> main` PR
The accepted production-ready platform already sits on `main`, but the active `cert-platform` branch had drifted into a mixed surface:

- production-readiness follow-on engineering
- teardown and residual-cost controls
- readiness graph corrections and expansion
- outward-facing portfolio and presentation assets

That is too broad for the next PR. The cleaner move is to preserve the engineering trail and leave the outward-facing pack for a separate promotion.

Actions taken:

- created a fresh branch from the audited `main` tip for engineering-only post-cert work
- restored only the production-readiness plan/notebook updates, readiness graph updates, and the streaming standby control from `cert-platform`
- left the outward-facing portfolio tree out of scope for this branch

Judgment:

- the next PR should describe engineering truth only:
  - post-cert production-readiness documentation refinement
  - readiness graph correction and expansion
  - explicit streaming teardown / restore control for standing-cost shutdown
- the portfolio and presentation pack should travel separately so reviewers do not have to parse two unrelated stories in one PR

## 2026-03-22 19:18:30 +00:00 - Reviewer comments exposed real operator-path defects in the new streaming standby control, so I fixed the tool before merging the engineering PR
The first review pass on the engineering-only `dev -> main` PR found three valid issues in `dev_full_streaming_standby.py` and its runbook:

- the script assumed a local ignored `backend.hcl` file instead of working from the checked-in `backend.hcl.example`
- the runbook said `terraform init -reconfigure`, but the script did not actually pass `-reconfigure`
- failure-path receipts were not durable if `terraform init`, `destroy`, or `apply` aborted early

I fixed the operator surface directly:

- backend config now resolves explicitly, defaults to `infra/terraform/dev_full/streaming/backend.hcl.example`, and can still be overridden with `--backend-config`
- `terraform init` now runs with `-reconfigure`
- teardown and restore now write failure receipts instead of losing the receipt on exception
- standby precheck now fails closed with explicit blocker ids if EKS or Aurora describe calls fail

Judgment:

- these were legitimate operator-handoff defects, not reviewer noise
- fixing them before merge is the right move because this script exists precisely for fresh-checkout / review / handoff situations where hidden local-state assumptions are unacceptable

## 2026-03-22 19:39:50 +00:00 - Split the remaining `cert-platform` outward-facing pack onto its own promotion branch after the engineering PR merged cleanly
After `PR #87` closed, `cert-platform` still carried a second meaningful body of work:

- outward-facing portfolio assets
- canonical storyline material
- speaking-posture documents
- the supporting portfolio appendix build scripts

That remaining scope is real, but it is a different review story from the post-cert engineering follow-up. I therefore did not try to fold it into the already-merged engineering PR or treat the whole residual `cert-platform` diff as one acceptable promotion.

Actions taken:

- created a fresh branch from `dev` after the engineering PR merge
- restored only:
  - `docs/experience_lake/outward-facing-assets/`
  - `scripts/build_portfolio_appendix_b_data_panels.py`
  - `scripts/build_portfolio_cost_appendix_panels.py`
- left the broader non-outward-facing residual branch drift out of scope

Judgment:

- the next PR should review the outward-facing evidence pack on its own terms:
  - canonical portfolio doctrine
  - recruiter-facing visual/evidence assets
  - speaking-posture support sheets
  - supporting appendix-panel build scripts

## 2026-03-22 19:48:34 +00:00 - The outward-facing PR review found reproducibility and portability defects in the supporting scripts and markdown, so I corrected those before merge
The first reviewer pass on the outward-facing PR surfaced four valid issues:

- `build_portfolio_appendix_b_data_panels.py` was pinned to one local run root and one local `phase5` summary path
- `build_portfolio_cost_appendix_panels.py` read from untracked `scratch_files/costs.csv` without an override path and wrote a CSV before ensuring the asset directory existed
- `portfolio_deck_evidence_matrix.md` used absolute Windows file links that are not portable in GitHub
- `canonical-storyline.md` opened with a chat-style lead-in instead of a clean standalone canonical reference

I corrected the outward-facing support surface directly:

- both build scripts now accept explicit input overrides and fail with clearer path-validation errors instead of assuming one local checkout state
- the cost appendix builder now creates the output asset directory before exporting its source CSV
- the evidence matrix now uses repo-relative links where the source is tracked and plain text where the source is intentionally local-only
- the canonical storyline now opens as a standalone reference instead of an AI-style exchange

Judgment:

- these are real handoff/readability defects for a shareable outward-facing pack
- fixing them keeps the PR honest without changing the substance of the portfolio claims
