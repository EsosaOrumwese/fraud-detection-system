## 2026-03-22 19:10:30 +00:00 - Split the mixed post-cert branch into a clean engineering-only promotion scope before opening the next `dev -> main` PR
The accepted production-ready platform already sits on `main`, but the active `cert-platform` branch had drifted into a mixed surface:

- production-readiness follow-on engineering
- teardown and residual-cost controls
- readiness graph corrections and expansion
- outward-facing portfolio and presentation assets

That is too broad for the next PR. The cleaner move is to preserve the engineering trail and leave the outward-facing pack for a separate promotion.

Actions taken:

- created a fresh branch from the audited `main` tip for engineering-only post-cert work
- restored only the production-readiness plan/notebook updates, readiness graph updates, and the streaming standby control from `cert-platform`
- left the outward-facing portfolio tree out of scope for this branch

Judgment:

- the next PR should describe engineering truth only:
  - post-cert production-readiness documentation refinement
  - readiness graph correction and expansion
  - explicit streaming teardown / restore control for standing-cost shutdown
- the portfolio and presentation pack should travel separately so reviewers do not have to parse two unrelated stories in one PR

## 2026-03-22 19:18:30 +00:00 - Reviewer comments exposed real operator-path defects in the new streaming standby control, so I fixed the tool before merging the engineering PR
The first review pass on the engineering-only `dev -> main` PR found three valid issues in `dev_full_streaming_standby.py` and its runbook:

- the script assumed a local ignored `backend.hcl` file instead of working from the checked-in `backend.hcl.example`
- the runbook said `terraform init -reconfigure`, but the script did not actually pass `-reconfigure`
- failure-path receipts were not durable if `terraform init`, `destroy`, or `apply` aborted early

I fixed the operator surface directly:

- backend config now resolves explicitly, defaults to `infra/terraform/dev_full/streaming/backend.hcl.example`, and can still be overridden with `--backend-config`
- `terraform init` now runs with `-reconfigure`
- teardown and restore now write failure receipts instead of losing the receipt on exception
- standby precheck now fails closed with explicit blocker ids if EKS or Aurora describe calls fail

Judgment:

- these were legitimate operator-handoff defects, not reviewer noise
- fixing them before merge is the right move because this script exists precisely for fresh-checkout / review / handoff situations where hidden local-state assumptions are unacceptable

## 2026-03-22 19:39:50 +00:00 - Split the remaining `cert-platform` outward-facing pack onto its own promotion branch after the engineering PR merged cleanly
After `PR #87` closed, `cert-platform` still carried a second meaningful body of work:

- outward-facing portfolio assets
- canonical storyline material
- speaking-posture documents
- the supporting portfolio appendix build scripts

That remaining scope is real, but it is a different review story from the post-cert engineering follow-up. I therefore did not try to fold it into the already-merged engineering PR or treat the whole residual `cert-platform` diff as one acceptable promotion.

Actions taken:

- created a fresh branch from `dev` after the engineering PR merge
- restored only:
  - `docs/experience_lake/outward-facing-assets/`
  - `scripts/build_portfolio_appendix_b_data_panels.py`
  - `scripts/build_portfolio_cost_appendix_panels.py`
- left the broader non-outward-facing residual branch drift out of scope

Judgment:

- the next PR should review the outward-facing evidence pack on its own terms:
  - canonical portfolio doctrine
  - recruiter-facing visual/evidence assets
  - speaking-posture support sheets
  - supporting appendix-panel build scripts
