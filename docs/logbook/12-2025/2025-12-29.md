# Logbook
### Date: 29th December 2025
### Project: Fraud Detection System
### Reference commits: Check commits on this date (if any)

* 4:47am
   - Added `graph_linkage_rules_6A` as a sealed policy input in the 6A artefact registry, dictionary, and schemas.
   - Wired `graph_linkage_rules_6A` into S4 link outputs (`s4_device_links_6A`, `s4_ip_links_6A`) dependencies in the 6A artefact registry.

* 6:09pm
   - Added `device_linkage_rules_6A` as a sealed policy input in the 6A artefact registry, dictionary, and schemas.
   - Wired `device_linkage_rules_6A` into S4 device links, entity neighbourhoods, and network summary dependencies alongside `graph_linkage_rules_6A`.

* 6:11pm
   - Updated `graph_linkage_rules_6A_authoring-guide.md` and `device_linkage_rules_6A_authoring-guide.md` to use `mlr.6A.policy.*` manifest keys and the `config/layer3/6A/policy/` paths + schema anchors.

* 7:26pm
   - Updated `prior.merchant_roles_authoring-guide.md` to require sealed `mlr.input.transaction_schema.merchant_ids` (MCC/channel/home_country_iso) and fail closed if missing.
   - Updated linkage rules guides to align with 6A v2 policy wiring (`mlr.6A.policy.*`, `config/layer3/6A/policy/`, and `schemas.6A.yaml#/policy/*`).

