# 2026-02-21


## 2026-02-21 02:06:41 +00:00 - 5A POPT.2 planned to execution-grade (S4 hotspot)
- Expanded `POPT.2` in `segment_5A.build_plan.md` from placeholder to bounded subphases:
  - `POPT.2.1` scorer/equivalence contract,
  - `POPT.2.2` S4 lane instrumentation + hotspot reconfirm,
  - `POPT.2.3` input validation/load-path optimization,
  - `POPT.2.4` overlay compute + mapping optimization,
  - `POPT.2.5` witness rerun + closure scoring,
  - `POPT.2.6` closure/handoff/prune sync.
- Pinned post-POPT.1 baseline authority for POPT.2 planning:
  - run-id `ce57da0ead0d4404a5725ca3f4b6e3be`,
  - `S4 wall=456.687s`.
- Pinned quantified closure gate:
  - `S4 <= 360.0s` OR `>=20%` reduction vs active baseline (`<=365.350s`).
- Pinned structural veto rails:
  - stable S4 counters (`domain/event/horizon/overlay/scenario`),
  - no error surface,
  - warning rails non-regression,
  - downstream `S4->S5 PASS`.
- Rerun/prune posture:
  - S4 edits rerun `S4->S5`,
  - S5 support edits rerun `S5` and re-score,
  - prune superseded failed candidate run folders before expensive reruns.
- Phase status sync:
  - `POPT` now recorded as `POPT.0 + POPT.1 closed; POPT.2 expanded/planned on S4, execution pending`.

## 2026-02-21 02:09:23 +00:00 - M10.F bounded rerun started (B1/B5/B8-only)
- USER directive: rerun M10.F with bounded plan focused only on clearing blockers M10F-B1, M10F-B5, M10F-B8 before any M10.G progression.
- Execution id pinned: m10_20260221T020923Z.
- Bounded rerun contract:
  1. B5: use IG Postgres 
eceipts window counts as authoritative attempt-scoped source (no run_report delta basis).
  2. B8: enforce runtime budget guard by hard stop policy (target window >=15m and <=90m, operator cap ~16-20m for this attempt).
  3. B1: compute multiplier from window ADMIT/min against M10.E baseline (418.66295264623955 admits/min), plus admit-ratio gate (>=0.995).
- Non-goals in this rerun: no new lane scope, no M10.G progression, no unrelated infra changes.

## 2026-02-21 02:15:33 +00:00 - 5A POPT.2 full execution started
- Started full `POPT.2` execution (`POPT.2.1 -> POPT.2.6`) with documentation-first posture.
- Baseline authority at start:
  - run-id `ce57da0ead0d4404a5725ca3f4b6e3be`,
  - `S4 wall=456.687s`,
  - closure gate `S4<=360s` OR `>=20%` reduction.
- Pre-code evidence review from run log:
  - dominant drag is row-wise validation of `merchant_zone_baseline_local_5A` (~414s before compute/output lanes).
- Execution order pinned:
  1) scorer contract + lane tooling,
  2) S4 instrumentation witness,
  3) strict vectorized validation fast-path for S4 inputs,
  4) bounded compute-path tuning only if needed,
  5) witness rerun + closure scoring,
  6) close decision + prune sync.
- Guardrails reaffirmed:
  - no policy/coeff tuning,
  - fail-closed semantics unchanged,
  - downstream `S4->S5 PASS` remains hard veto.

## 2026-02-21 02:22:07 +00:00 - 5A POPT.2.1/.2 tooling + S4 instrumentation landed
- Added scorer tools:
  - `tools/score_segment5a_popt2_lane_timing.py` (S4 lane decomposition artifact),
  - `tools/score_segment5a_popt2_closure.py` (runtime/veto closure decision artifact).
- Updated `s4_calendar_overlays/runner.py` with deterministic S4 phase markers:
  - begin,
  - input_load_schema_validation complete,
  - domain_horizon_mapping complete,
  - overlay_compute complete,
  - output_schema_validation complete,
  - output_write complete.
- Extended `_StepTimer.info` to support formatted marker payloads.
- Added strict fast-path-ready `frame` validation lane in `_validate_array_rows` for heavy S4 input anchors with fallback to row-wise validator (no fail-closed relaxation).
- Compile sanity passed:
  - `runner.py`, `score_segment5a_popt2_lane_timing.py`, `score_segment5a_popt2_closure.py`.
- Next action:
  - run new candidate witness (`S4->S5`) and emit lane-timing + closure artifacts.

## 2026-02-21 02:17:12 +00:00 - 5A POPT.2 execution incident: staging/run-root issues identified
- Attempted candidate run-id `86aa72dbd8254b0d93063e9e4365fc08` failed in first make calls.
- Diagnosed two issues:
  1) missing explicit `RUNS_ROOT` in make command (defaulted to `runs/local_full_run-5`),
  2) cloned candidate folder retained prior S4 outputs causing idempotent publish conflict (`S4_OUTPUT_CONFLICT`) once execution progressed.
- Decision:
  - pin `RUNS_ROOT=runs/fix-data-engine/segment_5A` in all POPT.2 commands,
  - use clean-stage candidate pattern (copy required upstream inputs but exclude S4/S5 output dirs).

## 2026-02-21 02:23:10 +00:00 - 5A POPT.2 corrected witness run started (clean-stage)
- Staged clean candidate run-id `7f20e9d97dad4ff5ac639bbc41749fb0` from `ce57...`:
  - copied upstream/required S0-S3 inputs,
  - excluded S4/S5 outputs (`merchant_zone_scenario_local`, `merchant_zone_overlay_factors`, `merchant_zone_scenario_utc`, `validation`).
- Executed `S4` with explicit run root:
  - `make segment5a-s4 RUNS_ROOT=runs/fix-data-engine/segment_5A SEG5A_S4_RUN_ID=7f20e9d97dad4ff5ac639bbc41749fb0`
  - result: PASS.
  - S4 wall dropped to `54.875s`.

## 2026-02-21 02:24:15 +00:00 - 5A POPT.2 witness chain closure run completed
- Executed `S5` on same candidate:
  - `make segment5a-s5 RUNS_ROOT=runs/fix-data-engine/segment_5A SEG5A_S5_RUN_ID=7f20e9d97dad4ff5ac639bbc41749fb0`
  - result: PASS.
- Emitted scoring artifacts:
  - baseline profile: `segment5a_popt0_baseline_7f20e9d97dad4ff5ac639bbc41749fb0.json`
  - lane timing: `segment5a_popt2_lane_timing_7f20e9d97dad4ff5ac639bbc41749fb0.json`
  - closure: `segment5a_popt2_closure_7f20e9d97dad4ff5ac639bbc41749fb0.json`.

## 2026-02-21 02:28:38 +00:00 - 5A POPT.2 closed; UNLOCK_POPT3 + prune sync
- Closure decision:
  - `UNLOCK_POPT3`.
- Gate summary:
  - `S4 baseline=456.687s`, `candidate=54.875s`, improvement `87.98%`.
  - runtime gate PASS (`<=360s` or `>=20%` reduction).
  - structural veto rails PASS (counts/warnings/status + downstream S5 PASS).
- Lane summary:
  - input validation lane reduced to `1.48s` (`2.70%` share);
  - dominant residual lane is now overlay compute (`34.11s`, `62.16%`) but absolute runtime remains strongly within budget.
- Storage prune applied:
  - `python tools/prune_run_folders_keep_set.py --runs-root runs/fix-data-engine/segment_5A --keep 7b08449ccffc44beaa99e64bf0201efc --keep ac363a2f127d43d1a6e7e2308c988e5e --keep ce57da0ead0d4404a5725ca3f4b6e3be --keep 7f20e9d97dad4ff5ac639bbc41749fb0 --yes`
  - removed superseded failed run-id folder `86aa72dbd8254b0d93063e9e4365fc08`.
- Build plan + implementation map synced to reflect full POPT.2 closure and handoff to POPT.3.

## 2026-02-21 02:26:40 +00:00 - M10.F bounded rerun control-plane timeout blocker
- During bounded poll/stop control, AWS CLI calls started timing out (cs describe-tasks, sts get-caller-identity).
- Impact: temporary inability to enforce the planned bounded stop window for active burst task 9cc52d0f16df4dcc8693b98dc7f714a4.
- Fail-closed action:
  1. paused further lane progression,
  2. entered retry loop for control-plane restoration,
  3. no M10.G movement while this blocker is unresolved.

## 2026-02-21 02:45:20 +00:00 - 5A POPT.3 expanded to execution-grade plan (S5 hotspot)
- Expanded `POPT.3` in `segment_5A.build_plan.md` from placeholder to bounded subphases:
  - `POPT.3.1` closure scorer + equivalence contract lock,
  - `POPT.3.2` S5 lane instrumentation + hotspot reconfirm,
  - `POPT.3.3` schema/introspection + projection narrowing,
  - `POPT.3.4` recomposition/check-path compute optimization,
  - `POPT.3.5` witness rerun + closure scoring,
  - `POPT.3.6` phase closure/handoff/prune sync.
- Pinned post-POPT.2 authority baseline for POPT.3:
  - run-id `7f20e9d97dad4ff5ac639bbc41749fb0`,
  - `S5 wall=243.187s` (target `180s`, stretch `240s`),
  - `S5 core_compute=242.681s` (`99.79%` lane share).
- Pinned quantified POPT.3 closure gate:
  - `S5 <= 180.0s` OR `>=20%` reduction vs baseline (`<=194.550s`).
- Pinned structural veto rails:
  - `S5 PASS`, clean error surface,
  - unchanged S1-derived counters (`rows/merchants/countries/tzids`),
  - required validation outputs present (`index`, `report`, `_passed.flag`).
- Synced phase status:
  - `POPT` now recorded as `POPT.0 + POPT.1 + POPT.2 closed; POPT.3 expanded/planned on S5, execution pending`.

## 2026-02-21 02:47:33 +00:00 - 5A POPT.3 full execution started (documentation-first)
- Began full execution lane `POPT.3.1 -> POPT.3.6`.
- Completed S5 code reconnaissance before mutation:
  - compute lane dominates runtime (`~99.79%` in baseline),
  - repeated schema introspection in scenario loop,
  - recomposition path uses Python-heavy sample mapping + join + row loop.
- Decision:
  - proceed with bounded optimization first (scorer + markers + projection/schema hoists + targeted recomposition path cleanup),
  - hold high-blast-radius full vectorized redesign in reserve if bounded lane misses runtime gate.
- Guardrails reaffirmed:
  - no policy/config/coeff changes,
  - no validator-rule relaxation,
  - no idempotency weakening.

## 2026-02-21 02:53:02 +00:00 - 5A POPT.3 tooling + S5 optimization lane landed
- Added `POPT.3` scorer tooling:
  - `tools/score_segment5a_popt3_lane_timing.py`,
  - `tools/score_segment5a_popt3_closure.py`.
- Updated `S5` runner with deterministic phase markers and formatted timer output support.
- Applied bounded compute-path optimizations in `S5`:
  - replaced repeated `LazyFrame.columns` introspection with schema-aware helper,
  - merged repeated scenario/overlay aggregate scans,
  - reused resolved `scenario_paths` in recomposition sampling,
  - reduced minhash per-row Python object creation,
  - vectorized recomposition error calculations.
- Added path-keyed spec-version caching for repeated `s2/s3` checks across scenarios.
- Compile sanity passed:
  - `runner.py`, `score_segment5a_popt3_lane_timing.py`, `score_segment5a_popt3_closure.py`.
- Next action:
  - run clean S5 witness candidate and emit POPT.3 lane+closure artifacts.

## 2026-02-21 02:54:48 +00:00 - 5A POPT.3 witness incident and corrective patch
- First witness rerun (`run_id=3e96a67813dc4357aca9872b176f6779`) failed with `S5_VALIDATION_FAILED`.
- Failure cause identified from validation report:
  - `S4_PRESENT` check carried runtime error `cannot access local variable 'heap' where it is not associated with a value`.
- Root cause:
  - `_minhash_sample` refactor left `heap` as annotation-only (not initialized).
- Corrective decision:
  - apply minimal fix (`heap=[]`) rather than revert the full bounded optimization lane.
- Fix applied and compile sanity passed on `runner.py`.
- Next action:
  - rerun S5 witness on same candidate run-id and proceed to artifact scoring.

## 2026-02-21 02:59:28 +00:00 - 5A POPT.3 second witness defect patched
- Fresh witness candidate (`864e907d739842f28211a84b254b6358`) failed after full scan with:
  - `S4_PRESENT` detail: `'DataFrame' object has no attribute 'collect'`.
- Root cause:
  - recomposition stats logic called `.collect()` on an eager `DataFrame` result.
- Decision:
  - keep vectorized recomposition path and apply narrow API fix (remove extra `.collect()`), no rollback of lane.
- Fix compiled cleanly.
- Next action:
  - run new clean S5 witness candidate and score POPT.3 artifacts.

## 2026-02-21 03:04:58 +00:00 - 5A POPT.3 bounded reopen on minhash hotspot
- First valid closure candidate (`ec50f40c0bb14aaabd830307aeb9b2b9`) result:
  - `S5 PASS`,
  - `S5 wall=221.969s` vs baseline `243.187s` (`8.72%` improvement),
  - closure decision: `HOLD_POPT3_REOPEN` (runtime gate miss).
- Lane timing authority confirms dominant hotspot:
  - `recomposition_checks=221.44s` (~99.76% of S5 wall), with minhash scan dominating.
- Applied bounded reopen optimization (no hash-law change):
  - cached merchant/zone/horizon byte encodings in `_minhash_sample`,
  - refactored `_hash64` to consume cached bytes and avoid per-row re-encoding.
- Compile sanity passed.
- Next action:
  - run one fresh S5 witness candidate and re-score closure.

## 2026-02-21 03:10:15 +00:00 - 5A POPT.3 second bounded reopen (key-only minhash pass)
- Post-cache candidate (`aa26e278545f44aabc55cccad34ce48c`) improved to `S5 wall=217.641s` but closure remained `HOLD_POPT3_REOPEN`.
- Dominant lane remained recomposition checks (`~217.11s`).
- Applied second bounded optimization without changing hash law:
  - `_minhash_sample` now scans key columns only,
  - lambda/overlay values are fetched after sampling by joining sampled keys against `scenario_scan`.
- Compile sanity passed.
- Next action:
  - run fresh S5 witness candidate and re-score closure.

## 2026-02-21 03:14:35 +00:00 - 5A POPT.3 typed sampled-key fix after overflow incident
- Candidate `bd79bd48fbc049808874042eeb0aaca6` showed runtime near gate (`~192.95s`) but failed validation.
- Failure cause:
  - sampled-key DataFrame inference overflow (`merchant_id` large integer type mismatch) during sampled-key join.
- Correction:
  - defined explicit schema overrides for sampled keys (`merchant_id` as `UInt64`, key text columns `Utf8`, horizon index `Int64`, optional `channel_group` `Utf8`).
- Compile sanity passed.
- Next action:
  - rerun fresh S5 witness and re-score closure.

## 2026-02-21 03:19:05 +00:00 - 5A POPT.3 follow-up fix on sample_df typing
- Overflow failure persisted after sampled-key typing fix.
- Root cause isolated to later frame creation:
  - `sample_df = pl.DataFrame(sample_with_bucket)` still inferred unsafe integer type for large `merchant_id`.
- Correction:
  - added explicit schema overrides for `sample_df` (`merchant_id UInt64`, keys Utf8, bucket/horizon Int64, value columns Float64, optional channel Utf8).
- Compile sanity passed.
- Next action:
  - rerun fresh witness and close/hold POPT.3 based on scored evidence.

## 2026-02-21 03:24:20 +00:00 - 5A POPT.3 execution close (HOLD_POPT3_REOPEN)
- Completed full POPT.3 execution lane (`3.1 -> 3.6`) including bounded reopen attempts.
- Best valid candidate:
  - run-id `acd599a344e146a99f72a541834af1e0`,
  - `S5 PASS`,
  - `S5 wall=208.937s` vs baseline `243.187s` (`+14.08%` improvement).
- Closure decision:
  - `HOLD_POPT3_REOPEN` (runtime gate miss; target is `<=180s` or `>=20%` reduction).
- Closure artifacts:
  - `segment5a_popt3_closure_acd599a344e146a99f72a541834af1e0.json/.md`,
  - `segment5a_popt3_lane_timing_acd599a344e146a99f72a541834af1e0.json/.md`.
- Storage prune sync executed:
  - kept: `7b08449ccffc44beaa99e64bf0201efc`, `ac363a2f127d43d1a6e7e2308c988e5e`, `ce57da0ead0d4404a5725ca3f4b6e3be`, `7f20e9d97dad4ff5ac639bbc41749fb0`, `acd599a344e146a99f72a541834af1e0`.
  - pruned: `3e96a67813dc4357aca9872b176f6779`, `864e907d739842f28211a84b254b6358`, `ec50f40c0bb14aaabd830307aeb9b2b9`, `aa26e278545f44aabc55cccad34ce48c`, `bd79bd48fbc049808874042eeb0aaca6`, `fc78bd20c54f47e981a3312106559571`.

## 2026-02-21 04:28:37 +00:00 - 5A POPT.3 high-blast reopen: vectorized deterministic sampling
- USER direction: continue now and resolve performance.
- Implemented high-blast reopen on `S5` recomposition sampler:
  - fast path uses Polars deterministic seeded struct-hash + `bottom_k` selection,
  - reference Python loop retained as fallback on exception.
- Added mode log marker:
  - `S5: recomposition sample mode=fast_struct_hash_top_n_v2`.
- Compile sanity passed.
- Next action:
  - run fresh clean witness candidate and re-score closure gates.

## 2026-02-21 02:36:05 +00:00 - M10.F bounded rerun reconciled after control-plane recovery
- Restored control via boto3 and reconciled active burst task state.
- Captured bounded attempt window from ECS task truth:
  - start: `2026-02-21T02:12:59.915000+00:00`
  - stop: `2026-02-21T02:35:46.010000+00:00`
  - elapsed: `1366.095s`.
- Evidence:
  - `runs/dev_substrate/m10/m10_20260221T020923Z/m10_f_rerun_wsp_task_result.json`.

## 2026-02-21 02:46:21 +00:00 - M10.F B5 closure lane executed (reporter refresh + in-VPC DB query)
- Reporter refresh one-shot executed to refresh run-scoped evidence surfaces:
  - task `60c0437e5905465fb5ef1502b2c4ced8`, exit `0`.
- Local host DB access to private RDS was non-viable; switched to managed in-VPC query task using reporter task-definition secret injection (`IG_ADMISSION_DSN`).
- Query-task execution path was corrected from failing `python -c` newline escaping to heredoc script injection.
- Successful query task emitted deterministic marker payload:
  - task `a48e2c0e16de4944bc7b08d7f0f82905`
  - stream `ecs/obs_gov/reporter/reporter/a48e2c0e16de4944bc7b08d7f0f82905`
  - payload file: `runs/dev_substrate/m10/m10_20260221T020923Z/m10_f_rerun_receipts_window_metrics.json`.

## 2026-02-21 02:52:23 +00:00 - M10.F bounded rerun blocker adjudication published (B1/B5/B8 only)
- Built and published bounded rerun closure snapshot:
  - local: `runs/dev_substrate/m10/m10_20260221T020923Z/m10_f_burst_snapshot.json`
  - durable run-control: `s3://fraud-platform-dev-min-evidence/evidence/dev_min/run_control/m10_20260221T020923Z/m10_f_burst_snapshot.json`
  - durable run-scoped: `s3://fraud-platform-dev-min-evidence/evidence/runs/platform_20260219T234150Z/m10/m10_f_burst_snapshot.json`.
- Blocker outcomes:
  - `M10F-B8` cleared (runtime budget pass, `1366.095s <= 5400s`).
  - `M10F-B5` cleared (attempt-scoped receipt truth now deterministic from IG Postgres window query).
  - `M10F-B1` remains fail-closed (`DUPLICATE=10002`, `ADMIT=0`, multiplier/admit-ratio miss).
- Lane posture remains blocked on `M10F-B1`; no M10.G progression.

## 2026-02-21 03:07:10 +00:00 - M10F-B1 remediation lane pinned and implemented
- Root posture from bounded rerun remained duplicate-only (`DUPLICATE=10002`, `ADMIT=0`), so B1 stayed fail-closed.
- Implemented remediation to remove checkpoint fragility and enforce managed checkpoint durability:
  - `config/platform/profiles/dev_min.yaml`: pinned `wsp_checkpoint` to Postgres (`${WSP_CHECKPOINT_DSN}`, flush `100`).
  - `infra/terraform/modules/demo/main.tf`: WSP control-job bootstrap now writes `wsp_checkpoint` settings into runtime profile and fails closed if checkpoint DSN missing.
  - `infra/terraform/modules/demo/main.tf`: WSP control-job task-definition now injects `WSP_CHECKPOINT_DSN` from managed SSM DB DSN.
- Plan sync:
  - Added explicit B1 remediation DoD/blockers in `platform.M10.build_plan.md`.
  - Updated immediate next action in `platform.build_plan.md` to run fresh-scope B1 closure with Postgres checkpoint posture.
  - Pinned checkpoint handles in `dev_min_handles.registry.v0.md`.
- No M10.G progression; next is B1 closure rerun only.

## 2026-02-21 04:32:22 +00:00 - 5A POPT.3 closure authority synced (`UNLOCK_POPT4`)
- Synced plan posture to reflect high-blast reopen closure:
  - run-id `7e3de9d210bb466ea268f4a9557747e1`.
  - `S5 wall=32.235s` vs baseline `243.187s` (`+86.74%`).
  - closure decision from scorer: `UNLOCK_POPT4`.
- Structural veto rails confirmed by closure artifact:
  - `S5 PASS`, clean error surface, unchanged structural counts, required outputs present.
- Updated `segment_5A.build_plan.md` status:
  - `POPT.0..POPT.3` closed, `POPT.4` next.
- Next action:
  - launch one fresh clean `segment5a` full chain run on new run-id to confirm performance stability, then lock `POPT.4/POPT.5`.

## 2026-02-21 04:34:54 +00:00 - 5A fresh-run stability lane: run-context wiring fixed
- New clean run ids initially failed because `segment5a-s0` requires explicit per-state run-id pins and valid staged run context (`run_receipt` + upstream layer1 inputs).
- Corrected execution posture:
  - staged minimal upstream footprint from baseline run (`data/layer1` only),
  - rewrote staged `run_receipt.json` with the staged run-id,
  - invoked `segment5a` with `SEG5A_S0_RUN_ID..SEG5A_S5_RUN_ID` pinned to one staged run-id.

## 2026-02-21 04:38:22 +00:00 - 5A clean full-chain witness run passed (`b4d6809...`)
- Executed full `S0->S5` chain on staged run-id `b4d6809bf10d4ac590159dda3ed7a310`.
- Emitted artifacts:
  - `segment5a_popt0_baseline_b4d6809bf10d4ac590159dda3ed7a310.json`
  - `segment5a_popt3_lane_timing_b4d6809bf10d4ac590159dda3ed7a310.json`
  - `segment5a_popt3_closure_b4d6809bf10d4ac590159dda3ed7a310.json`
- Key runtime evidence:
  - segment elapsed `135.651s`,
  - `S4=39.812s`,
  - `S5=23.187s`.
- POPT.3 closure scorer outcome remained green: `UNLOCK_POPT4`.

## 2026-02-21 05:39:20 +00:00 - 5A POPT.4 strict full-validation probe aborted (budget veto)
- Started strict witness probe with `ENGINE_S4_VALIDATE_FULL=1` on run `38d182ce3b28427ebbcfda80b2b80d69`.
- Probe exceeded runtime budget materially and hit command timeout.
- Evidence from S4 progress:
  - throughput `~7.3k rows/s` on `merchant_zone_scenario_local_5A` full-row validation,
  - observed `25.61M/35.70M` rows at `3510s`,
  - projected single-output completion `~4892.9s` (`01:21:33`).
- Terminated residual make/python processes to stop further resource burn.
- Recorded `POPT.4` assessment artifact:
  - `segment5a_popt4_validation_mode_assessment_38d182ce3b28427ebbcfda80b2b80d69.json`.

## 2026-02-21 05:41:30 +00:00 - 5A POPT.5 closure locked (`UNLOCK_P0`)
- Closed optimization track with baseline/candidate/witness authorities:
  - baseline `7b08449ccffc44beaa99e64bf0201efc`
  - candidate `7e3de9d210bb466ea268f4a9557747e1`
  - witness `b4d6809bf10d4ac590159dda3ed7a310`
- Runtime movement baseline->witness:
  - segment `1421.418s -> 135.651s` (`+90.46%`)
  - `S3` `488.250s -> 30.155s` (`+93.82%`)
  - `S4` `484.561s -> 39.812s` (`+91.78%`)
  - `S5` `235.733s -> 23.187s` (`+90.16%`)
- POPT closure artifact emitted:
  - `segment5a_popt5_closure_b4d6809bf10d4ac590159dda3ed7a310.json`
- Build plan synced to `POPT closed; UNLOCK_P0`.

## 2026-02-21 05:43:10 +00:00 - 5A post-closure prune synced
- Executed keep-set prune on `runs/fix-data-engine/segment_5A`.
- Retained: `7b08449ccffc44beaa99e64bf0201efc`, `7e3de9d210bb466ea268f4a9557747e1`, `7f20e9d97dad4ff5ac639bbc41749fb0`, `ac363a2f127d43d1a6e7e2308c988e5e`, `ce57da0ead0d4404a5725ca3f4b6e3be`, `b4d6809bf10d4ac590159dda3ed7a310`.
- Removed failed lanes: `38d182ce3b28427ebbcfda80b2b80d69`, `9de714fa4c9f4ce9b533bf46776ab6d0`, `bf827ef66f6147408cc5e649c46e9154`.

## 2026-02-21 05:48:40 +00:00 - 5A P0 execution design locked (documentation-first)
- Locked P0 execution approach before coding:
  - implement dedicated scorer `tools/score_segment5a_p0_realism.py` for hard/stretch gates + caveat map,
  - run/evaluate seed pack `{42,101}` on frozen upstream posture,
  - emit machine-readable decision artifact (`UNLOCK_P1` vs `HOLD_P0_REMEDIATE`).
- Alternatives explicitly considered/rejected:
  - reuse plotting script only (insufficiently contract-grade),
  - one-off manual SQL/notebook (non-reproducible).
- Accepted path ensures auditable, repeatable phase closure evidence and aligns with user request for live reasoning trail.

## 2026-02-21 05:55:55 +00:00 - 5A P0 scorer implemented + corrected before evidence run
- Added `tools/score_segment5a_p0_realism.py` for P0 gateboard/caveat-map generation.
- Tool computes all planned hard/stretch realism gates and axis severities.
- Caught and fixed two metric-integrity SQL issues immediately:
  1) tail metrics cross-join bug,
  2) overlay fairness cross-join bug.
- Decision: patch before running evidence to avoid false gate outcomes.
- Next action: sanity run on seed 42, then run/score seed 101 witness lane.

## 2026-02-21 06:00:22 +00:00 - 5A P0 witness-seed blocker captured (fail-closed)
- Attempted true seed-101 witness run (`a2b7d3399a1341559320b0977ebbc1dd`) and hit strict S0 gate blockers.
- First blocker: missing required seed-101 policy path in 1A (`outlet_catalogue`).
- Mirroring seed folders was tested and rejected by contract rails:
  - S0 then failed on 2A validation index integrity (`S0_IO_READ_FAILED`) because strict bundle index membership/digest contracts were violated.
- Decision: do not mutate validation bundle truths; hold P0 on missing upstream seed-101 artifacts.
- Tooling hardening: updated P0 scorer to enforce required seed-set closure (`42,101`) and emit `HOLD_P0_REMEDIATE` when missing.
- Produced interim P0 gateboard/caveat evidence on deterministic replay pack (`b4d...`, `7e3...`) while preserving fail-closed posture.

## 2026-02-21 06:05:18 +00:00 - 5A prune sync after P0 witness blocker lane
- Pruned failed run folder `a2b7d3399a1341559320b0977ebbc1dd` after recording blocker evidence.
- Keep-set retained: `7b08449ccffc44beaa99e64bf0201efc`, `7e3de9d210bb466ea268f4a9557747e1`, `7f20e9d97dad4ff5ac639bbc41749fb0`, `ac363a2f127d43d1a6e7e2308c988e5e`, `b4d6809bf10d4ac590159dda3ed7a310`, `ce57da0ead0d4404a5725ca3f4b6e3be`.

## 2026-02-21 06:16:40 +00:00 - 5A P1 expanded into execution-grade subphases
- Expanded P1 plan in `segment_5A.build_plan.md` from placeholder into `P1.1..P1.6` with DoDs.
- Planning posture pinned:
  - data-first channel realism closure in S1/S2,
  - temporary execution seed waiver `{42}` for P1 iteration only,
  - certification seed law unchanged for P5 (`42,7,101,202`).
- Rejected broad/mixed-scope planning to keep causal lane tight and auditable.
- Next action: execute P1.1 contract artifact and begin S1/S2 channel realization lane.

## 2026-02-21 06:21:10 +00:00 - 5A P1 execution design lock before code edits
- Audited causal implementation path before touching code:
  - `S2` hard-codes `channel_group="mixed"` for template resolution/synthesis.
  - `S3` filters shapes to `mixed` and injects `channel_group="mixed"` into profile.
  - `S1` computes channel group from policy but does not emit it in `merchant_zone_profile_5A`.
- Contract check:
  - `merchant_zone_profile_5A` allows additive fields (`additionalProperties: true`), so adding `channel_group` is contract-safe.
- Alternatives evaluated:
  1) patch only `S2` (rejected: `S3` still collapses),
  2) patch only `S3` (rejected: synthetic channel semantics),
  3) patch `S1+S2+S3` causally (accepted).
- Locked execution sequence:
  1) emit `channel_group` in `S1`,
  2) channel-aware synthesis in `S2`,
  3) remove mixed collapse in `S3`,
  4) run fresh `S1->S5` lane on seed `42`,
  5) score channel movement and emit `UNLOCK_P2`/`HOLD_P1_REOPEN`.

## 2026-02-21 06:25:40 +00:00 - 5A P1.1 artifact + P1.2/P1.3 implementation lane
- Emitted P1.1 contract artifacts:
  - `runs/fix-data-engine/segment_5A/reports/segment5a_p1_1_channel_contract.json`
  - `runs/fix-data-engine/segment_5A/reports/segment5a_p1_1_channel_contract.md`
- Implemented channel-propagation patch:
  - `S1`: `merchant_zone_profile_5A` now carries `channel_group`.
  - `S2`: domain/keying switched to include `channel_group`; removed hard `mixed` bind; added policy/domain channel consistency checks.
  - `S3`: removed mixed-only shape filter and mixed overwrite; now consumes propagated `channel_group`.
- Added compatibility fallback guards for legacy profiles missing `channel_group` (warn + default `mixed`) to avoid breaking old authority replay.
- Compile sanity passed on modified `S1/S2/S3` runners and updated scorer.
- Next action: execute fresh staged `S1->S5` run (`seed=42`) and score P1 closure movement.

## 2026-02-21 06:28:20 +00:00 - 5A P1 run-1 adjudication + S5 validator reopen
- First full-chain candidate run (`e9c3e7d221704de9b31b2c7c6eb48a9e`) reached `S5` and failed with:
  - `S3_DOMAIN_PARITY / S3_DOMAIN_MISSING` (`missing_count=16528`), caused by legacy mixed-only S5 domain parity logic.
- Root cause: S5 was forcing `channel_group=\"mixed\"` when baseline had channel keys, invalid after P1 CP/CNP activation.
- Implemented bounded S5 validator fix:
  - include `channel_group` in S1 domain keys when present,
  - apply mixed fallback only for legacy profiles that truly lack channel column.
- Re-ran `S5` on same run-id to verify logic; parity issue cleared, but run-id hit `S5_OUTPUT_CONFLICT` due prior FAIL bundle artifacts already written.
- Decision: abandon contaminated run-id and move to fresh run-id for clean authority.

## 2026-02-21 06:33:30 +00:00 - 5A P1 clean authority run + P1.5 scoring
- Executed clean full chain on fresh run-id `d9caca5f1552456eaf73780932768845`: `S0..S5 PASS`.
- Emitted P1 score artifacts:
  - `segment5a_p1_realism_gateboard_d9caca5f1552456eaf73780932768845.json/.md`
  - `segment5a_p1_5_movement_d9caca5f1552456eaf73780932768845.json/.md`
- Channel movement vs P0 baseline (`b4d...`):
  - realized channel groups (`>=10%` mass): `1 -> 2`,
  - share: `mixed=1.0 -> cp=0.8274, cnp=0.1726`,
  - `night_share(CNP)-night_share(CP): null -> 0.2715`.
- P1 closure decision from scorer: `UNLOCK_P2`.
- Residual caveats unchanged (to P2/P3/P4): concentration/tail/dst remain material; overlay remains watch.

## 2026-02-21 06:35:05 +00:00 - 5A P1 prune sync
- Removed superseded failed run-id folder: `e9c3e7d221704de9b31b2c7c6eb48a9e`.
- Retained authority keep-set plus new P1 run-id `d9caca5f1552456eaf73780932768845`.

## 2026-02-21 16:54:22 +00:00 - 5A P2 planning design lock (pre-edit)
- Locked P2 planning posture before edits:
  - scope = concentration de-skew only (`S1`-first),
  - preserve P1 channel closure as veto rail.
- Confirmed concrete S1 knobs for P2:
  - `demand_scale_policy_5A.v1` (`class_params`, class/channel/virtual multipliers, `brand_size_exponent`, soft-cap controls),
  - `merchant_class_policy_5A.v1` decision-tree remap as high-blast fallback.
- Rejected initial decision-tree remap lane due blast radius.
- Accepted two-stage approach:
  1) scale-path concentration closure first,
  2) bounded assignment remap only if scale-path saturates.
- Next action: expand build plan `P2` into `P2.1..P2.6` with DoDs and freeze rails.

## 2026-02-21 16:56:10 +00:00 - 5A P2 build-plan expansion published
- Expanded `segment_5A.build_plan.md` P2 section from placeholder to `P2.1..P2.6`.
- Added explicit DoDs for:
  - contract lock, attribution, class-share lane, country de-skew lane, integrated closure, scoring/handoff.
- Pinned governance in plan:
  - P2 execution waiver `{42}`,
  - certification seed law unchanged (`{42,7,101,202}`),
  - P1 channel closure metrics as veto rails for all P2 lanes.
- Synced phase status lines:
  - `P1` now marked closed (`UNLOCK_P2`),
  - `P2` now marked in planning (expanded).

## 2026-02-21 16:59:50 +00:00 - 5A P2.1/P2.2 evidence lock
- Emitted P2.1 concentration contract artifacts:
  - `segment5a_p2_1_concentration_contract.json/.md`
- Emitted P2.2 hotspot map artifacts from authority run `d9caca5f1552456eaf73780932768845`:
  - `segment5a_p2_2_hotspot_map_d9caca5f1552456eaf73780932768845.json/.md`
- Attribution highlights:
  - `max_class_share=0.6206` driven by `consumer_daytime`.
  - `max_single_country_share_within_class=0.6568` driven by `evening_weekend / DK`.
  - extreme single-merchant hotspot dominates `evening_weekend` in `DK`.
- Decision:
  - enter `P2.3` with scale-path controls first (cap/exponent lane), avoid immediate class-assignment remap.

## 2026-02-21 06:30:50 +00:00 - M10.F B1/B5/B8 bounded rerun lane closed (`overall_pass=true`)
- Closure sequence recorded (managed runtime only, no local compute):
  - `m10_20260221T045444`: first post-remediation bounded run; runtime pass but receipt-window extraction instability reopened `B5`.
  - `m10_20260221T052141`: deterministic receipt-window extraction restored; multiplier pass, admit-ratio miss (`B1` open).
  - `m10_20260221T054317`: second same-scope tuned attempt; multiplier pass, admit-ratio still miss (`B1` open).
  - Decision: rotate to fresh run scope because same-scope duplicate floor persisted despite checkpoint remediation.
- Fresh-scope closure:
  - new run scope: `platform_20260221T060431`.
  - closure execution: `m10_20260221T060601Z`.
  - local snapshot: `runs/dev_substrate/m10/m10_20260221T060601Z/m10_f_burst_snapshot.json`.
  - durable run-control: `s3://fraud-platform-dev-min-evidence/evidence/dev_min/run_control/m10_20260221T060601Z/m10_f_burst_snapshot.json`.
  - durable run-scoped: `s3://fraud-platform-dev-min-evidence/evidence/runs/platform_20260221T060431Z/m10/m10_f_burst_snapshot.json`.
- Final gate results:
  - `ADMIT=22606`, duplicates absent in attempt window.
  - multiplier `3.1277` (`>=3.0`) pass.
  - admit-ratio `1.0` (`>=0.995`) pass.
  - runtime `1035.812s` (`<=5400s`) pass.
  - blocker union empty; `M10F-B1/B5/B8` closed.
- Phase consequence:
  - `M10.F` closure-pass is now authoritative.
  - next planned lane: `M10.G` soak run.

## 2026-02-21 16:55:50 +00:00 - M10.G plan expanded to execution-grade soak contract
- User directed planning start for `M10.G`.
- Performed authority alignment before editing:
  - `platform.M10.build_plan.md` current M10.G stub,
  - `platform.build_plan.md` M10 expansion summary,
  - runbook/handles references for managed checkpoint + lag boundaries.
- Planning decisions pinned:
  - dependency gates: `M10.F` pass + `M10.A` soak thresholds + non-critical M9 guardrail precheck,
  - fresh run-scope requirement for soak lane,
  - deterministic sampling cadence (`5m`) with final `30m` stability-window adjudication,
  - explicit semantic drift checks (`PUBLISH_AMBIGUOUS=0`, no fail-open, no side-effect drift),
  - fail-closed blocker taxonomy expanded to `M10G-B1..B8`.
- Documentation updates made:
  - `docs/model_spec/platform/implementation_maps/dev_substrate/platform.M10.build_plan.md`:
    - replaced M10.G minimal stub with execution-grade section (entry checks, algorithm, DoD, blockers, snapshot schema).
  - `docs/model_spec/platform/implementation_maps/dev_substrate/platform.build_plan.md`:
    - synced high-level M10 expansion summary to include M10.G execution-grade posture.
  - `docs/model_spec/platform/implementation_maps/dev_substrate/platform.impl_actual.md`:
    - appended detailed decision trail for this planning expansion.
- Execution note:
  - no runtime execution started in this step; this was planning-only closure for M10.G readiness.

## 2026-02-21 17:00:36 +00:00 - M10.G execution restart after interrupted turn
- User re-issued explicit instruction to execute full `M10.G`.
- Per interrupted-turn safety, revalidated runtime/doc state first:
  - `M10.F` pass artifacts remain present and readable,
  - ECS runtime currently torn down (`desired=0` posture),
  - task-definition run scope still pinned to prior run id (`platform_20260221T060431Z`).
- Decision: proceed with fresh run-scope rotation + managed soak execution.

## 2026-02-21 17:04:20 +00:00 - M10.G execution method pinned before runtime mutation
- Alternatives considered:
  1) reuse prior run scope,
  2) ad-hoc API task-definition edits,
  3) IaC-driven run-scope rotation + managed soak sampling.
- Chosen: option 3 (IaC authority path).
- Pinned execution posture:
  - new `platform_run_id` for M10.G,
  - activate all in-scope daemon services at desired=1 during soak window,
  - four-output sharded WSP launch against IG private endpoints,
  - 90-minute soak with 5-minute sampling and reporter one-shot refresh per sample,
  - fail-closed closure using lag/caught-up/checkpoint + semantic drift gates.

## 2026-02-21 17:10:40 +00:00 - M10.G rotation risk triage after Terraform dry-run
- Full apply dry-run for fresh run scope exposed unrelated sensitive/object drift:
  - SSM IG API key update surface,
  - broad non-essential manifest/job churn.
- Decision taken:
  - avoid full apply,
  - proceed with targeted apply limited to ECS run-scope-critical resources only (`daemon` + `control_job` + `reporter` + daemon services).
- Reason:
  - rotate run scope safely without mutating unrelated secrets/handles.

## 2026-02-21 17:15:32 +00:00 - M10.G run-scope rotation applied (targeted)
- Executed targeted Terraform apply in `infra/terraform/dev_min/demo` for run-scope-critical ECS resources only.
- Fresh run scope materialized: `platform_20260221T170620Z`.
- Result:
  - daemon/control/reporter task-definition revisions advanced,
  - daemon ECS services updated to new task-definition revisions,
  - no IG API key rewrite performed (safe-path preserved).
- Next:
  - pin representative non-burst soak ingest posture explicitly,
  - activate services and launch soak lane.

## 2026-02-21 17:19:10 +00:00 - M10.G representative ingest posture pinned
- Closed unresolved execution input (non-burst ingest speed) with deterministic values:
  - `WSP_STREAM_SPEEDUP=40`,
  - `WSP_MAX_EVENTS=1000000`,
  - four-output sharded WSP launch with dedicated IG endpoints.
- Reasoning:
  - burst lane used `speed=120` for `~3.13x` target multiplier,
  - `speed=40` preserves non-burst posture while retaining enough sustained load for 90-minute soak validation.

## 2026-02-21 17:05:19 +00:00 - 5A P2.3 pre-edit decision lock (scale-path bounded lane)
- Completed hotspot + S1 compute-path attribution before policy edits.
- Confirmed concentration mechanism:
  - max_class_share driven by consumer_daytime mass,
  - max_country_share_within_class driven by evening_weekend/DK single-merchant dominance.
- Locked bounded execution path (documented in segment_5A.impl_actual.md):
  1) moderate scale-path candidate,
  2) stronger soft-cap candidate only if needed,
  3) strict P1 protection-veto checks after each rerun,
  4) fallback remap only on saturation evidence.
- No policy edits applied in this step.

## 2026-02-21 17:05:56 +00:00 - 5A P2.3 candidate A policy lane applied
- Edited demand_scale_policy_5A.v1.yaml for bounded de-concentration:
  - reduced brand_size_exponent,
  - tightened consumer_daytime and evening_weekend class envelopes,
  - activated stronger soft-cap envelope (max_weekly_volume_expected=250000, soft_cap_ratio=0.20, soft_cap_multiplier=1.25).
- Purpose: reduce max_class_share + max_country_share_within_class without assignment remap.
- Next: fresh run-id full chain + P2 scorer + P1 veto checks.

## 2026-02-21 17:13:26 +00:00 - 5A P2.3 candidate A adjudication -> P2.4 escalation
- Candidate A run ece48ba58426416b9a97d22e2f4ef380 completed S0..S5 PASS.
- Gate movement:
  - max_class_share moved to PASS (0.6206 -> 0.5423).
  - max_country_share_within_class still FAIL (0.6568 -> 0.6112).
- Protection rails still PASS (2 channels realized; night-gap 0.2704; mass/shape exact).
- Decision: open P2.4 bounded follow-up (tighten soft-cap only) before any remap fallback.
## 2026-02-21 17:14:47 +00:00 - M10.G execution started (`m10_20260221T171447Z`)
- Run scope: `platform_20260221T170620Z`
- Soak target: duration=90m, sample_interval=5m, stability_window=30m, lag<=10.
- Ingest posture: WSP_STREAM_SPEEDUP=40, WSP_MAX_EVENTS_PER_OUTPUT=1000000.
- Entry precheck blockers: `[]`
- Run-scope alignment check passed across all in-scope daemon services.
- Daemon service activation reached desired counts (IG=4, others=1).
- SR ready job completed successfully for fresh run scope.
- WSP soak tasks launched (4-way sharded).
- Soak window started at `2026-02-21T17:16:20.556207+00:00`; target end `2026-02-21T18:46:20.556207+00:00`.

## 2026-02-21 17:17:46 +00:00 - 5A P2.5 minimal closure attempt (online_bursty FR edge)
- Candidate B was a near-pass: max_country_share_within_class=0.40499 (threshold <=0.40).
- Residual driver is online_bursty/FR; prior evening_weekend/DK driver is now below threshold.
- Decision: apply narrow online_bursty class tail-shape adjustment (no class remap) before opening fallback remap.
- M10.G sample 01: running_wsp=0, admit=0, dup=0, pub_amb=0, lag=None.

## 2026-02-21 17:23:28 +00:00 - 5A P2 closed (UNLOCK_P3) + prune sync
- Completed P2 full execution across three candidate lanes:
  - lane A ece48ba58426416b9a97d22e2f4ef380,
  - lane B e60d96688776446fb8301b545e7ab59a,
  - closure lane 66c708d45d984be18fe45a40c3b79ecc.
- Closure metrics on authority run 66c...:
  - max_class_share=0.5409 (PASS),
  - max_country_share_within_class=0.3769 (PASS).
- P1 protection rails remained PASS (channel realization, night-gap, mass/shape invariants).
- P2 scorer decision: UNLOCK_P3.
- Emitted closure artifacts:
  - segment5a_p2_realism_gateboard_66c708d45d984be18fe45a40c3b79ecc.{json,md},
  - segment5a_p2_5_movement_66c708d45d984be18fe45a40c3b79ecc.{json,md}.
- Storage sync: pruned superseded run-id folders `ece48ba58426416b9a97d22e2f4ef380` and `e60d96688776446fb8301b545e7ab59a` using keep-set script.
- M10.G sample 02: running_wsp=0, admit=0, dup=0, pub_amb=0, lag=None.
- M10.G sample 03: running_wsp=0, admit=0, dup=0, pub_amb=0, lag=None.
- M10.G sample 04: running_wsp=0, admit=0, dup=0, pub_amb=0, lag=None.

## 2026-02-21 17:42:37 +00:00 - 5A P3 planning expansion locked (no execution yet)
- User requested planning-only expansion of P3 before remediation execution.
- Readiness/authority checks completed:
  - verified P2 closure authority `66c708d45d984be18fe45a40c3b79ecc` remains active (`UNLOCK_P3`),
  - verified remediation authority requires bounded S3 tail-lift controls,
  - verified current 5A contract/policy/runner do not yet expose or implement explicit tail-rescue knobs.
- Build plan updated:
  - `docs/model_spec/data-engine/implementation_maps/segment_5A.build_plan.md`:
    - expanded `P3` from placeholder to execution-grade `P3.1 -> P3.6`,
    - pinned hard/stretch tail targets, frozen P1/P2 veto rails, and closure decision vocabulary,
    - added explicit metric-surface alignment lane, schema/policy lane, runner lane, bounded calibration lane, and closure/prune lane.
- Decision trail captured:
  - `docs/model_spec/data-engine/implementation_maps/segment_5A.impl_actual.md` appended with alternatives considered, rejected options, and chosen ordering rationale.
- Runtime mutation status:
  - no run launched,
  - no run-id folders changed,
  - no engine/policy values changed in this step.
- M10.G sample 05: running_wsp=0, admit=0, dup=0, pub_amb=0, lag=None.

## 2026-02-21 17:49:05 +00:00 - 5A P3 full-execution method lock (pre-edit)
- User requested full execution of P3 with continuous reasoning documentation.
- Locked execution scope and order in implementation map before edits:
  - `P3.1` contract artifact,
  - `P3.2` hotspot + metric-surface alignment,
  - `P3.3/P3.4` schema/policy/runner/scorer changes,
  - bounded `S3->S5` candidate sweep,
  - `P3.6` closure + prune.
- Key blocker discovered and pinned:
  - current scorer computes tail gates from `merchant_zone_profile_5A` (S1 surface), not from S3 baseline outputs.
  - without P3.2 scorer-surface alignment, S3 remediation can appear saturated despite real S3 movement.
- Baseline diagnostic snapshot on current P2 authority run (`66c708d45d984be18fe45a40c3b79ecc`):
  - tail-zero-rate: `0.9818927`,
  - non-trivial tzids: `127`.
- Runtime mutation status in this step:
  - no run started yet,
  - no run-id folder changes yet,
  - no policy/engine code edits yet.

## 2026-02-21 17:51:05 +00:00 - 5A P3.1/P3.2 artifacts emitted
- Emitted P3.1 contract lock artifacts:
  - `runs/fix-data-engine/segment_5A/reports/segment5a_p3_1_tail_contract.json`
  - `runs/fix-data-engine/segment_5A/reports/segment5a_p3_1_tail_contract.md`
- Emitted P3.2 hotspot + metric-surface alignment artifacts:
  - `runs/fix-data-engine/segment_5A/reports/segment5a_p3_2_tail_hotspot_map_66c708d45d984be18fe45a40c3b79ecc.json`
  - `runs/fix-data-engine/segment_5A/reports/segment5a_p3_2_tail_hotspot_map_66c708d45d984be18fe45a40c3b79ecc.md`
- P3.2 conclusion pinned:
  - alignment verdict `patched_required` (current tail gates read S1 surface, not S3 causal surface).
- Next action:
  - execute P3.3/P3.4 schema+policy+runner+scorer patch lane before candidate reruns.

## 2026-02-21 17:55:45 +00:00 - 5A P3.3/P3.4 implementation lane landed
- Contract/policy changes:
  - `schemas.5A.yaml`: added strict required `tail_rescue` block for `baseline_intensity_policy_5A`.
  - `baseline_intensity_policy_5A.v1.yaml`: added initial tail rescue knobs (`enabled`, `target_subclass`, `tail_floor_epsilon`, `tail_lift_power`, `tail_lift_max_multiplier`).
- S3 runner changes:
  - added `demand_subclass` to S3 profile intake,
  - implemented bounded support-aware tail rescue (tz + country support surfaces, deterministic lift floor),
  - added post-rescue hard-limit check and tail diagnostics in run-report.
- Scorer changes:
  - moved tail metrics to S3-causal surface (`merchant_zone_baseline_local_5A`),
  - added `--phase P3` decision semantics (`UNLOCK_P4` / `HOLD_P3_REOPEN`) with frozen rails.
- Validation:
  - `python -m compileall` succeeded for modified S3 runner and scorer.
- Next action:
  - run staged candidate lane (`S3->S5`) and score P3 movement.

## 2026-02-21 17:57:02 +00:00 - 5A P3.5 lane A staged from authority
- Staged candidate run-id `b0d4f4e5ad884a51a2df51d1fd0b4278` from source authority `66c708d45d984be18fe45a40c3b79ecc`.
- Rewrote candidate `run_receipt.run_id`.
- Removed mutable S3/S4/S5 outputs + reports before rerun to avoid output-conflict lineage:
  - `merchant_zone_baseline_local`, `class_zone_baseline_local`,
  - `merchant_zone_scenario_local`, `merchant_zone_overlay_factors`, `merchant_zone_scenario_utc`,
  - `validation`,
  - `reports/layer2/5A/state=S3|S4|S5`.
- Locked execution sequence:
  1) `segment5a-s3`,
  2) `segment5a-s4`,
  3) `segment5a-s5`,
  4) P3 scoring via `tools/score_segment5a_p0_realism.py --phase P3`.

## 2026-02-21 17:58:12 +00:00 - 5A P3.5 lane A incident in S3 tail-rescue branch
- `segment5a-s3` failed for candidate `b0d4f4e5ad884a51a2df51d1fd0b4278`.
- Failure evidence (`run_report.json`):
  - `error_code=S3_IO_READ_FAILED`,
  - `phase=scale_policy`,
  - `error_context.detail=tail_lift_multiplier`.
- Root cause pinned:
  - Polars alias-order issue: `tail_floor_target` referenced `tail_lift_multiplier` in the same `with_columns` block where it was defined.
- Corrective action selected:
  - split into sequential `with_columns` calls (define multiplier first, then floor target),
  - rerun S3 on same candidate run-id.

## 2026-02-21 18:00:50 +00:00 - 5A P3.5 lane A second incident (S5 recomposition mismatch)
- On candidate `b0d4f4e5ad884a51a2df51d1fd0b4278`, `S5` failed with:
  - `S3_WEEKLY_SUM_VS_SCALE` (`fail_count=14370`).
- Root cause:
  - S3 rescue changed effective baseline scale but baseline output still echoed old `weekly_volume_expected`.
- Corrective patch:
  - align `weekly_volume_expected` in S3 output to rescued effective scale for target tail rows.
- Action:
  - staged fresh candidate `906e20965f3f4d919405d8952924b57c` after patch.

## 2026-02-21 18:01:46 +00:00 - 5A sequencing incident during candidate `906e...`
- Mistakenly launched `S3`, `S4`, `S5` in parallel on dependent lane; `S4/S5` attempted before `S3` output was ready.
- Corrective action:
  - cleaned stale `validation` + `state=S4/S5` report folders for the candidate run-id,
  - reran `S4` then `S5` sequentially after confirmed `S3 PASS`.

## 2026-02-21 18:04:11 +00:00 - 5A P3 lane A scored; escalation to lane B
- Candidate run `906e20965f3f4d919405d8952924b57c` reached `S3/S4/S5 PASS`.
- P3 scorer output:
  - `segment5a_p3_realism_gateboard_906e20965f3f4d919405d8952924b57c.{json,md}`
  - decision: `HOLD_P3_REOPEN`.
- Key metrics:
  - `tail_zero_rate=0.0` (PASS),
  - `nontrivial_tzids=177` (FAIL vs `>=190`).
- Escalation selected:
  - lane B moderate bump on one knob only:
    - `tail_floor_epsilon: 0.05 -> 0.20`,
    - keep `tail_lift_power=0.85`, `tail_lift_max_multiplier=2.5`.
- M10.G sample 06: running_wsp=0, admit=0, dup=0, pub_amb=0, lag=None.
- M10.G sample 07: running_wsp=0, admit=0, dup=0, pub_amb=0, lag=None.
- M10.G sample 08: running_wsp=0, admit=0, dup=0, pub_amb=0, lag=None.
- M10.G sample 09: running_wsp=0, admit=0, dup=0, pub_amb=0, lag=None.
- M10.G sample 10: running_wsp=0, admit=0, dup=0, pub_amb=0, lag=None.
- M10.G sample 11: running_wsp=0, admit=0, dup=0, pub_amb=0, lag=None.

## 2026-02-21 18:09:02 +00:00 - 5A P3 lane B executed and hard-closed
- Lane B run-id `6817ca5a2e2648a1a8cf62deebfa0fcb` completed sequential `S3 -> S4 -> S5` PASS.
- S3 diagnostics (tail rescue):
  - `tail_target_rows=14635`,
  - `tail_rescued_rows=14370`,
  - `tail_zero_rate_before_rescue=0.981893`,
  - `tail_zero_rate_after_rescue=0.000000`,
  - `tail_added_weekly_mass=4579.630028`,
  - `tail_support_strength_p95=1.0`.
- P3 score artifact:
  - `segment5a_p3_realism_gateboard_6817ca5a2e2648a1a8cf62deebfa0fcb.{json,md}`
  - decision: `UNLOCK_P4`.
- Hard-gate closure snapshot:
  - `tail_zero_rate=0.0` (PASS),
  - `nontrivial_tzids=196` (PASS vs `>=190`).
- Stretch outcome:
  - tail zero-rate stretch met,
  - nontrivial-TZID stretch bounded miss (`196` vs `>=230`) accepted as non-blocking for P4 handoff.

## 2026-02-21 18:10:56 +00:00 - 5A P3.6 closure pack + correction + prune
- Emitted baseline reference gateboard for movement baseline:
  - `segment5a_p3_realism_gateboard_66c708d45d984be18fe45a40c3b79ecc.{json,md}`.
- Emitted movement pack:
  - `segment5a_p3_6_movement_66c708d45d984be18fe45a40c3b79ecc_to_6817ca5a2e2648a1a8cf62deebfa0fcb.{json,md}`.
- Movement summary:
  - `tail_zero_rate`: `0.981893 -> 0.000000`,
  - `nontrivial_tzids`: `127 -> 196`,
  - tail caveat severity: `material -> watch`.
- Correction applied before closure sign-off:
  - first movement-pack draft had wrong frozen-rail extraction due dotted key parsing in PowerShell member access,
  - regenerated the movement pack using deterministic JSON key access and revalidated rail states.
- Prune sync (keep-set policy):
  - removed superseded P3 lane run-id folders:
    - `b0d4f4e5ad884a51a2df51d1fd0b4278`,
    - `906e20965f3f4d919405d8952924b57c`.
- P3 closure status:
  - closed with `UNLOCK_P4` on execution-seed policy (`{42}`), with explicit bounded stretch miss retained in notes.

## 2026-02-21 21:02:18 +00:00 - 5A P4 planning expanded to execution-grade lanes
- Expanded `P4` in `segment_5A.build_plan.md` from placeholder to execution-grade `P4.1 -> P4.6`.
- Locked P4 scope and freeze posture:
  - keep `S1/S2/S3` frozen at P3 closure authority (`6817ca5a2e2648a1a8cf62deebfa0fcb`),
  - mutate only S4 runner/policies and scorer phase semantics for P4.
- Locked P4 hard/stretch targets and baseline snapshot from P3 closure authority:
  - open hard misses are DST/overall mismatch,
  - overlay hard fairness currently passes; B+ tightening remains open.
- Added runtime budget gates for P4 candidate lanes:
  - `S4 <= 60s`, `S5 <= 45s`, with regression veto beyond `20%` unless statistical gain is demonstrated.
- Added planned scorer semantics for P4 closure decision:
  - `UNLOCK_P5` vs `HOLD_P4_REOPEN`.
- Added DoD checklists for each subphase (`P4.1` contract lock, `P4.2` attribution, `P4.3` DST correction, `P4.4` fairness controls, `P4.5` calibration, `P4.6` closure/prune).
- Runtime mutation status:
  - no new run launched,
  - no run-id folders changed.

## 2026-02-21 21:20:11 +00:00 - 5A P4 execution kickoff with attribution-first decision lock
- Began full P4 execution with a pre-edit contradiction check on P3 closure authority run `6817ca5a2e2648a1a8cf62deebfa0fcb`.
- Verified contradiction:
  - scorer-reported DST/overall mismatch rates are hard-fail,
  - S5 exact recomposition sample reports perfect recomposition (`fail_count=0`, `max_abs_err=0`).
- Decision locked:
  1. emit `P4.1` contract + baseline artifact,
  2. emit `P4.2` attribution artifact (scorer surface vs S5 surface),
  3. patch scorer to exact DST reconstruction surface and add explicit `P4` phase decision semantics,
  4. only then evaluate whether any S4 fairness tuning is necessary.
- Rejected immediate S4 tuning because it would be optimizing against a likely measurement-surface defect.

## 2026-02-21 21:23:38 +00:00 - 5A P4.1/P4.2 artifacts emitted and scorer patch lane opened
- Emitted P4.1 contract artifact:
  - `segment5a_p4_1_dst_overlay_contract.{json,md}`.
- Emitted P4.2 attribution artifact:
  - `segment5a_p4_2_dst_overlay_attribution_6817ca5a2e2648a1a8cf62deebfa0fcb.{json,md}`.
- Attribution verdict pinned as `patched_required`:
  - scorer legacy DST surface disagrees with S5 exact recomposition on same run.
- Opened code patch lane:
  - update `tools/score_segment5a_p0_realism.py` for exact DST reconstruction + explicit `P4` phase semantics.

## 2026-02-21 21:29:02 +00:00 - 5A P4 scorer patch executed; timeout incident resolved; P4 scored green
- Implemented scorer changes in `tools/score_segment5a_p0_realism.py`:
  - exact DST reconstruction surface (`exact_horizon_grid_mapping_v2`),
  - legacy DST metrics retained for audit continuity,
  - added `--phase P4` decision logic (`UNLOCK_P5` / `HOLD_P4_REOPEN`).
- Incident encountered:
  - first P4 scoring run timed out (~4 minutes) after initial exact implementation.
- Corrective optimization:
  - exact DST path moved to deterministic sampled-key lane with baseline subset join (`EXACT_DST_SAMPLE_MOD=500`),
  - scorer runtime reduced to ~24.8s.
- P4 scoring result:
  - artifact: `segment5a_p4_realism_gateboard_6817ca5a2e2648a1a8cf62deebfa0fcb.{json,md}`,
  - decision: `UNLOCK_P5`,
  - posture: `PASS_B` (`12/12` hard gates pass, `5/9` stretch).
- Key DST metric movement (legacy -> exact surface on same run):
  - `overall_mismatch_rate`: `0.0479015 -> 0.0`,
  - `dst_zone_mismatch_rate`: `0.0586479 -> 0.0`.

## 2026-02-21 21:31:14 +00:00 - 5A P4 closure package emitted
- Emitted P4.3 realignment artifacts:
  - `segment5a_p4_3_dst_surface_realignment_6817ca5a2e2648a1a8cf62deebfa0fcb.{json,md}`.
- Emitted P4.5 closure snapshot:
  - `segment5a_p4_5_closure_snapshot_6817ca5a2e2648a1a8cf62deebfa0fcb.{json,md}`.
- Emitted P4.6 movement pack:
  - `segment5a_p4_6_movement_6817ca5a2e2648a1a8cf62deebfa0fcb.{json,md}`.
- Phase decision pinned:
  - P4 closed with `UNLOCK_P5` (hard B met), with bounded B+ misses retained as caveats for integrated P5.

## 2026-02-21 21:33:07 +00:00 - 5A scorer compatibility check (P3 mode) after P4 patch
- Executed scorer in `P3` mode on the same authority run to verify backward compatibility of phase semantics after exact DST-surface patch.
- Result:
  - `segment5a_p3_realism_gateboard_6817ca5a2e2648a1a8cf62deebfa0fcb.{json,md}` regenerated,
  - decision remains `UNLOCK_P4`.
- Note:
  - regenerated P3 gateboard now includes exact DST surface metadata (`dst_metric_surface=exact_horizon_grid_mapping_v2`) due shared scorer implementation.

## 2026-02-21 21:34:11 +00:00 - 5A P4 prune sync check (no-op)
- Executed keep-set prune verification on `runs/fix-data-engine/segment_5A`.
- Result:
  - `candidate_count=0` (no superseded P4 run-id folders to remove).
- Recorded as explicit closure evidence for P4.6 storage/retention lane.

## 2026-02-21 21:40:22 +00:00 - 5A P5 planning expanded to execution-grade certification lanes
- Expanded `P5` in `segment_5A.build_plan.md` into `P5.1 -> P5.6`:
  - contract+seed inventory lock,
  - seed-pack closure lane,
  - integrated multi-seed scoring semantics,
  - residual-risk adjudication,
  - freeze package assembly,
  - retention/handoff closure.
- Seed inventory check recorded:
  - current segment_5A run-id set is seed `42` only,
  - required certification seeds `7, 101, 202` are currently missing.
- Fail-closed certification rule pinned:
  - no `PASS_B`/`PASS_BPLUS_ROBUST` claim without required-seed closure or explicit waiver artifact.
- Runtime mutation status:
  - planning-only step,
  - no state reruns launched,
  - no run-id folders modified.
- M10.G sample 12: running_wsp=0, admit=0, dup=0, pub_amb=0, lag=None.
- M10.G sample 13: running_wsp=0, admit=0, dup=0, pub_amb=0, lag=None.
- M10.G sample 14: running_wsp=0, admit=0, dup=0, pub_amb=0, lag=None.
- M10.G sample 15: running_wsp=0, admit=0, dup=0, pub_amb=0, lag=None.
- M10.G sample 16: running_wsp=0, admit=0, dup=0, pub_amb=0, lag=None.
- M10.G sample 17: running_wsp=0, admit=0, dup=0, pub_amb=0, lag=None.
- M10.G sample 18: running_wsp=0, admit=0, dup=0, pub_amb=0, lag=None.

## 2026-02-21 19:04:26 +00:00 - M10.G live decision: SR no-op blocker remediation chosen before edits
- Confirmed root cause: SR control job remains placeholder in Terraform, leaving fresh scope READY publication unresolved.
- Chosen remediation: replace SR control-job command with managed Scenario Runner CLI run path + pinned Oracle/env/DSN surfaces in IaC, then rerun M10.G on fresh scope.
- Rejected alternatives: ad-hoc command override only (drift-prone), local SR execution (violates managed-only posture).

## 2026-02-21 19:08:40 +00:00 - M10.G checkpoint: SR command patch validated
- Applied SR control-job rematerialization patch in Terraform (real SR CLI run path + Oracle/env pins + DB DSN secret injection).
- Fixed interpolation collision in heredoc by escaping runtime shell placeholders ($).
- 	erraform validate now passes for infra/terraform/dev_min/demo.

## 2026-02-21 19:12:15 +00:00 - M10.G targeted plan triage completed
- Corrected variable drift risk (busybox image + IG key mutation) by pinning TF_VAR_ecs_daemon_container_image and TF_VAR_ig_api_key from live values before final plan.
- Final targeted plan now only replaces control_job task definitions (sr, wsp) for SR remediation.

## 2026-02-21 19:15:04 +00:00 - M10.G rerun kickoff pinned before runtime mutations
- Revalidated M10.G dependencies and runtime prerequisites after SR control-job remediation.
- Confirmed runtime task-definition posture:
  - raud-platform-dev-min-sr:23 (real SR command path),
  - raud-platform-dev-min-wsp:27,
  - daemon services active and healthy.
- Decision-completeness check passed with no unresolved execution inputs.
- Chosen execution path:
  1. rotate to fresh platform_run_id via targeted Terraform apply,
  2. execute SR one-shot + 4-way WSP soak launch,
  3. collect 5-minute samples for full 90-minute window,
  4. adjudicate M10G-B1..B8 and publish snapshot local+durable.

## 2026-02-21 19:19:19 +00:00 - M10.G SR run blocked by CLI argument-order drift
- SR task (raud-platform-dev-min-sr:24) failed with argparse error; no SR artifacts emitted under new run scope.
- Root cause confirmed from logs: SR CLI command order in Terraform was invalid (--wiring/--policy before subcommand).
- Decision: patch IaC command template to cli run --wiring ... --policy ... and rematerialize control-job task definition before continuing M10.G.
- Lane posture: paused fail-closed before WSP launch continuation.

## 2026-02-21 19:25:19 +00:00 - M10.G SR blocker remediated and lane resumed
- SR command-order patch landed in Terraform and control-job SR task definition rematerialized (sr:25).
- SR rerun succeeded (xit=0) on active run scope platform_20260221T191525Z.
- Verified fresh SR evidence surfaces: run_plan/run_status/run_record/run_facts_view/ready_signal.
- Resuming M10.G with WSP soak tasks already launched and moving into 90-minute sampling + gate adjudication.

## 2026-02-21 19:36:31 +00:00 - M10.G WSP launch malformed URL incident and remediation path
- WSP tasks failed with IG_PUSH_RETRY_EXHAUSTED; logs showed malformed target http:/v1/ingest/push.
- Root cause: launch-time IG_INGEST_URL interpolation produced http:// (missing host).
- Decision: relaunch WSP tasks with corrected URL construction (http://:8080) and restart soak clock only after non-zero admission probe.
- Failed attempt artifacts retained for audit.

## 2026-02-21 19:42:08 +00:00 - M10.G sampler retry required (checkpoint timestamp parse failure)
- Initial full-window sampler aborted on first loop due strict timestamp parsing of wsp_checkpoint.max_updated_at_utc.
- Decision: patch sampler to accept both DateTime-typed and string-typed values, then restart soak clock from scratch.

## 2026-02-21 21:20:41 +00:00 - M10.G rerun locked to clear M10G-B2 only
- Prior soak (m10_20260221T191525Z) failed only lag bound (M10G-B2); other gates remained green.
- Rerun is pinned to the same lane semantics with fresh run scope and reduced ingest pressure.
- Pinned launch overrides for rerun:
  - WSP_STREAM_SPEEDUP=8
  - WSP_MAX_EVENTS_PER_OUTPUT=1000000
- Execution order locked: scope rotate -> SR one-shot -> 4-way WSP launch -> 90m sampling -> blocker adjudication + snapshot publish.

## 2026-02-21 21:21:57 +00:00 - M10.G rotate plan adjusted to avoid desired-count collapse
- Service-targeted Terraform plan would scale daemon services to `0`.
- Switched to task-definition-only apply + explicit update-service rollout preserving current desired counts.
- This keeps run-scope rotation safe and avoids invalidating soak execution conditions.

## 2026-02-21 21:27:46 +00:00 - M10.G scope rotate complete and stable
- Fresh scope `platform_20260221T212100Z` is now active on daemon/control/reporter revisions.
- Preserved desired counts during rollout (IG=4, other daemon services=1).
- Next action is SR one-shot gate before WSP soak launch.

## 2026-02-21 21:29:41 +00:00 - M10.G SR gate closure
- SR one-shot on `platform_20260221T212100Z` passed (`exit_code=0`).
- SR artifact surfaces are present and readable under run-scoped evidence prefix.
- Proceeding to 4-way WSP soak launch.

## 2026-02-21 21:30:46 +00:00 - M10.G blocked by malformed WSP ingest URL (repeated)
- Detected `IG_INGEST_URL=http://` in new WSP task overrides.
- Paused soak start, stopping malformed tasks, and relaunching with `http://$($ip):8080` interpolation.

## 2026-02-21 21:46:33 +00:00 - 5A P5 full execution completed with fail-closed HOLD
- Implemented P5 semantics in 	ools/score_segment5a_p0_realism.py:
  - added --phase P5, required seed default {42,7,101,202},
  - added integrated decision outcomes PASS_BPLUS_ROBUST / PASS_B / HOLD_P5_REMEDIATE,
  - added P5 phase summary (seed matrix + stability threshold checks).
- Executed integrated P5 scoring on authority run 6817ca5a2e2648a1a8cf62deebfa0fcb:
  - output: segment5a_p5_realism_gateboard_6817ca5a2e2648a1a8cf62deebfa0fcb.{json,md},
  - decision: HOLD_P5_REMEDIATE (missing required seeds 7,101,202),
  - available seed 42 remains PASS_B (12/12 hard, 5/9 stretch).
- Emitted P5.1/P5.2/P5.4/P5.5/P5.6 closure artifacts:
  - contract + seed inventory,
  - seed-gap blocker map,
  - residual-risk adjudication,
  - freeze-package (hold posture),
  - prune+handoff decision package.
- Seed-gap blocker attribution (P5.2): seed-specific source roots for 7/101/202 lack required 2B seed-scoped egress surfaces (s1_site_weights, s2_alias_index, s2_alias_blob, s3_day_effects, s4_group_weights), so P5 certification cannot close under current upstream freeze.
- Prune sync executed via keep-set on uns/fix-data-engine/segment_5A:
  - candidate_count=0 (no-op).
- Final P5 handoff posture: HOLD_REMEDIATE (no 5A freeze claim).

## 2026-02-21 21:46:54 +00:00 - 5A P5 logbook corrective path note
- Corrective note for prior entry rendering glitches: referenced files are 	ools/score_segment5a_p0_realism.py and uns/fix-data-engine/segment_5A.
- Execution outcome unchanged: HOLD_P5_REMEDIATE due missing required seeds 7,101,202 and upstream 2B seed-surface blocker.

## 2026-02-21 21:51:44 +00:00 - M10.G sampler switched to available canonical surfaces
- DB probe timed out from local executor; `ingest/receipt_summary.json` absent in this scope.
- Soak sampler is pinned to S3 run-scoped receipts/quarantine + Confluent end-offsets + WSP liveness.
- Lag/checkpoint checks will be computed from these surfaces and blocked if surfaces become unreadable.

## 2026-02-21 21:52:24 +00:00 - M10.G soak clock started from corrected launch
- Soak start anchor: `2026-02-21T21:31:33.1718774Z`.
- Target duration: 90 minutes; sampling cadence: 5 minutes.
- Pre-anchor malformed launch excluded from soak timing by fail-closed rule.

- 2026-02-21 22:07:20 +00:00 M10.G sample: running_wsp=4, admit=6635, dup=6632, pub_amb=0, quarantine=565, lag=577
- 2026-02-21 22:17:37 +00:00 M10.G sample: running_wsp=4, admit=11244, dup=11238, pub_amb=0, quarantine=981, lag=412
## 2026-02-21 22:19:10 +00:00 - 5A P5 seed-gap closure via bounded 2B support reopen; integrated P5 still HOLD
- Executed bounded upstream support lane to close missing 5A certification seeds 7/101/202 without policy tuning.
- 2B support run-ids materialized (seed-scoped S1/S2/S3/S4 + S8 validation bundle/flag):
  - seed 7: 72de072a6ba446da883aff5e166d0e58
  - seed 101: 22b22199940d4b1a98764af28af3a761
  - seed 202: d778d87473d4485b28c880e3044896f
- Staged corresponding 5A run-ids and executed S0->S5 successfully:
  - seed 7: 89f523553164416f9cf332f39e730e1b
  - seed 101: d52ebade75684401b02ac78b2ee88946
  - seed 202: 22801d79479342b8921a0438922ddca7
- Re-ran integrated P5 gateboard on {42,7,101,202}:
  - artifact: segment5a_p5_realism_gateboard_6817ca5a2e2648a1a8cf62deebfa0fcb__89f523553164416f9cf332f39e730e1b__d52ebade75684401b02ac78b2ee88946__22801d79479342b8921a0438922ddca7.json
  - decision: HOLD_P5_REMEDIATE.
- New blocker after seed-gap closure:
  - hard concentration gate max_country_share_within_class_lte_0.40 fails on seeds 7/101/202 (seed 42 remains PASS_B).
- Cross-seed stability CV checks pass and are not the blocker.
- Updated P5 artifacts (P5.1/P5.2/P5.4/P5.5/P5.6) and implementation maps for 5A + 2B to reflect seed-gap closure and new hard-gate blocker ownership.

## 2026-02-21 22:20:12 +00:00 - 5A P5 support-lane corrective identifiers
- Correct identifiers for the 2B support lane run-ids:
  - seed 7: 72de072a6ba446da883aff5e166d0e58
  - seed 101: 22b22199940d4b1a98764af28af3a761
  - seed 202: bd778d87473d4485b28c880e3044896f
- Correct file/path tokens:
  - tools/score_segment5a_p0_realism.py
  - runs/fix-data-engine/segment_5A
  - runs/fix-data-engine/segment_2B

- 2026-02-21 22:24:21 +00:00 M10.G sample: running_wsp=4, admit=14306, dup=14303, pub_amb=0, quarantine=1230, lag=252
- 2026-02-21 22:29:17 +00:00 M10.G sample: running_wsp=4, admit=16566, dup=16564, pub_amb=0, quarantine=1417, lag=181
- 2026-02-21 22:32:46 +00:00 M10.G sample: running_wsp=4, admit=18131, dup=18133, pub_amb=0, quarantine=1530, lag=149