# S4 Sample Evidence (Demo)

- **Scope:** Demonstrates the Segment 1B S4 allocation plan using a minimal synthetic dataset.
- **Generated by:** `temp_s4_sample.py` helper (see log 2025-10-23) leveraging `S4AllocPlanRunner`.
- **Contents:**
  - `data/layer1/1B/` — deterministic inputs (`s3_requirements`, `tile_weights`, `tile_index`) and resulting `s4_alloc_plan` partition.
  - `control/s4_alloc_plan/.../s4_run_report.json` — run report with determinism receipt, PAT counters, and merchant summaries.
  - `reference/iso/iso3166_canonical.parquet` — minimal ISO surface used for FK checks.

Use this bundle during governance review or as a template for local smoke testing. Re-generate by re-running the helper script if fingerprints or hashes change.
