# path: .github/workflows/README.md
# CI/CD workflows (conceptual)
Status: LOCKED.
Purpose: Linting, typing, tests, schema compiles, and release automation.
Owns: minimal pipelines that run on each change; artifact signing policy.
Boundaries: no environment secrets baked into YAML.
When we unlock this: add determinism checks and contract tests.