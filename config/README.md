# Runtime config
Status: LOCKED (conceptual).
Purpose: Non-secret settings.
Owns: env descriptors, policies, profiles
Boundaries: no credentials; no schema authority
When we unlock this: define minimal schema + validation
