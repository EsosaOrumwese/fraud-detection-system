version: "0.1.0"
classes:
  traffic:
    required_pins:
      - manifest_fingerprint
      - parameter_hash
      - seed
      - scenario_id
      - run_id
  control:
    required_pins:
      - manifest_fingerprint
      - run_id
  audit:
    required_pins:
      - manifest_fingerprint
event_types:
  s2_event_stream_baseline_6B: traffic
  s3_event_stream_with_fraud_6B: traffic
  ig.policy.activation: audit
  ig.quarantine.spike: audit
  ig.pull.run: audit
  ig.audit.verify: audit
