# Demand scale policy (5A.S1/S3) - v1
version: v1
defaults:
  weekly_volume_expected: 150.0
  weekly_volume_unit: transactions
  scale_factor: 1.0
classes:
  digital:
    weekly_volume_expected: 400.0
    scale_factor: 1.3
    high_variability_flag: true
  travel:
    weekly_volume_expected: 90.0
    scale_factor: 0.9
    high_variability_flag: true
  lodging:
    weekly_volume_expected: 110.0
    scale_factor: 1.0
    high_variability_flag: true
  food_bev:
    weekly_volume_expected: 220.0
    scale_factor: 1.15
  transport:
    weekly_volume_expected: 140.0
    scale_factor: 1.05
  services:
    weekly_volume_expected: 130.0
    scale_factor: 1.0
  retail_daytime:
    weekly_volume_expected: 160.0
    scale_factor: 1.0
  grocery:
    weekly_volume_expected: 350.0
    scale_factor: 1.1
    low_volume_flag: false
  fuel:
    weekly_volume_expected: 180.0
    scale_factor: 1.05
  entertainment:
    weekly_volume_expected: 110.0
    scale_factor: 1.1
    high_variability_flag: true
  utilities:
    weekly_volume_expected: 80.0
    scale_factor: 0.95
notes: |
  TODO: Replace with governed scale policy once empirical baselines are available.
